# struct_ArrayT

This directory contains comprehensive API reference documentation for the `Array` struct in the Cangjie programming language. It covers the core structure, including constructors, properties, and cloning mechanisms, alongside a wide range of manipulation functions such as concatenation, mapping, and slicing. The documentation details operator overloading for element access and range operations, as well as interface extensions for `ToString` and `Equatable` functionalities. Designed for Cangjie developers, this resource provides the necessary technical details for efficient generic array handling, data transformation, and standard library implementation.

## Quick Navigation

**What do you want to do?**

*   Initialize arrays, access basic properties, or clone data → struct_ArrayT_8more.md
*   Access or modify elements using subscripts and ranges → operator_func_Int64_3more.md, operator_func_RangeInt64_A_3more.md
*   Perform data manipulation like concatenation, mapping, or reversing → func_concatArrayT_6more.md, func_repeatInt64_5more.md
*   Convert arrays to strings or compare elements → extendT_ArrayT_where_T_ToString.md, [Subdirectory]

## Detailed Description

### extendT_ArrayT_where_T_ToString.md extendT_ArrayT_where_T_ToString.md
This document serves as API reference documentation for extending the generic Array type in the Cangjie language to support string conversion via the ToString interface. It covers the extension declaration `extend<T> Array<T> where T <: ToString` and details the implementation of the `toString()` function. The key information provided is that arrays containing elements implementing ToString can be converted into a readable string representation, formatted like "[1, 2, 3]". The function returns a standard String type. This is intended for Cangjie developers working with generic arrays who need to serialize or log array contents. Prerequisites include understanding generics and interfaces. Keywords: Cangjie, Array, ToString, interface extension, generic programming, API reference.

### func_concatArrayT_6more.md func_concatArrayT_6more.md
This document serves as API reference documentation for specific manipulation methods of the Array structure in the Cangjie programming language. It details six functions: concat for joining arrays, two overloads of copyTo for full or partial element copying, fill for replacing values, get for safe element retrieval, and map for transforming array elements. The documentation specifies parameters, return types, and potential exceptions like IllegalArgumentException and IndexOutOfBoundsException. It highlights the use of Option for safe access in the get method and generic type mapping in map. This reference is intended for Cangjie developers seeking to perform array operations such as concatenation, copying, filling, and mapping.

### func_repeatInt64_5more.md func_repeatInt64_5more.md
This document serves as API reference documentation for array manipulation functions in the Cangjie programming language. It details five specific methods: repeat, reverse, slice, splitAt, and swap, providing syntax, parameters, return values, and exceptions for each. Key takeaways include that slice creates a reference to the original data rather than a copy, meaning modifications affect the source array, while repeat requires a positive integer argument. The target audience is Cangjie developers seeking to perform structural modifications or data extraction on array instances.

### operator_func_Int64_3more.md operator_func_Int64_3more.md
This API reference document details the overloaded subscript operator functions for arrays using Int64 indices in the Cangjie language. It covers three main overloads: retrieving a single element, modifying a single element, and extracting a sub-array slice using a Range. Key information includes that the getter throws an exception on out-of-bounds access, unlike the get function which returns None. The setter modifies values at a specific index, while the range operator requires a step size of 1 and handles open-ended ranges by extending to the array's end. All functions throw IndexOutOfBoundsException for invalid indices. This guide is intended for Cangjie developers implementing array manipulation logic who need precise syntax and behavior details for subscript operations.

### operator_func_RangeInt64_A_3more.md operator_func_RangeInt64_A_3more.md
This document serves as API reference documentation for the Cangjie language, detailing specific array operations and interface implementations. It covers three main topics: a custom operator for assigning array values to a specified range, an extension for flattening two-dimensional arrays, and the implementation of the Collection interface for standard arrays. Key information includes constraints for the range operator, such as requiring a step size of 1 and matching lengths, alongside the functionality of the flatten method to convert nested arrays into a single dimension. The document also lists standard collection properties like size and iterator. This reference is intended for Cangjie developers seeking to manipulate array data structures and understand standard library implementations.

### struct_ArrayT_8more.md struct_ArrayT_8more.md
This document provides API reference documentation for the Array struct in the Cangjie programming language, focusing on its constructors, properties, and cloning functionalities. It details initialization methods for creating empty arrays, arrays with repeated values, or arrays generated by functions. The text explains properties like first and last which return Option for safe access, and methods for deep cloning entire arrays or specific ranges. Key information includes exception handling for negative sizes or out-of-bounds ranges, specific behaviors regarding reference types during initialization, and constraints on range step sizes. This technical reference is intended for Cangjie developers seeking to understand array creation and manipulation, requiring familiarity with generic types and exception handling.

### extendT_ArrayT_EquatableArrayT_where_T_EquatableT/
This directory contains API reference documentation for the Cangjie programming language, specifically focusing on the extension of the `Array` type where elements implement the `Equatable` interface. It covers essential functionalities for array comparison, searching for elements or sub-arrays, and structural manipulation like trimming prefixes and suffixes. Key topics include the equality operator (`==`), `indexOf` and `lastIndexOf` functions, and methods for modifying array boundaries. This documentation is suitable for Cangjie developers looking to implement robust search logic, perform equality checks, or manipulate array data structures efficiently.