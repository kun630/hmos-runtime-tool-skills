func setSessionInfo() {
        // 假设已经创建了一个session，如何创建session可以参考之前的案例。
        let session = createAVSession(context, 'SESSION_NAME', AVSessionType.SESSION_TYPE_AUDIO)
        // 播放器逻辑··· 引发媒体信息与播放状态的变更。
        // 设置必要的媒体信息。
        let metadata: AVMetadata = AVMetadata("0") // assetId由应用指定，用于标识应用媒体库里的媒体。
        metadata.title = "TITLE"
        metadata.mediaImage = ValueType.STRING("IMAGE")
        metadata.artist = "ARTIST"
        try {
            session.setAVMetadata(metadata)
            AppLog.info("SetAVMetadata successfully");
        } catch (err: BusinessException) {
            AppLog.error("Failed to set AVMetadata. Code: ${err.code}, message: ${err.message}")
        }
        // 简单设置一个播放状态 - 暂停 未收藏。
        let playbackState: AVPlaybackState = AVPlaybackState()
        playbackState.state = PlaybackState.PLAYBACK_STATE_PAUSE
        playbackState.isFavorite = false
        try {
            session.setAVPlaybackState(playbackState)
            AppLog.info("SetAVPlaybackState successfully");
        } catch (err: BusinessException) {
            AppLog.error("Failed to set AVPlaybackState. Code: ${err.code}, message: ${err.message}")
        }
        // 设置一个播放列表。
        let queueItemDescription_1: AVMediaDescription = AVMediaDescription("001")
        let extras1 = HashMap<String, ValueType>()
        extras1.add("extras", ValueType.STRING("any"))
        queueItemDescription_1.title = "music_name"
        queueItemDescription_1.subtitle = "music_sub_name"
        queueItemDescription_1.description = "music_description"
        queueItemDescription_1.mediaImage = ValueType.STRING("PIXELMAP_OBJECT")
        queueItemDescription_1.extras = extras1
        let queueItem_1: AVQueueItem = AVQueueItem(1)
        queueItem_1.description = queueItemDescription_1
        let queueItemDescription_2: AVMediaDescription = AVMediaDescription("002")
        let extras2 = HashMap<String, ValueType>()
        extras2.add("extras", ValueType.STRING("any"))
        queueItemDescription_2.title = "music_name"
        queueItemDescription_2.subtitle = "music_sub_name"
        queueItemDescription_2.description = "music_description"
        queueItemDescription_2.mediaImage = ValueType.STRING("PIXELMAP_OBJECT")
        queueItemDescription_2.extras = extras2
        let queueItem_2: AVQueueItem = AVQueueItem(2)
        queueItem_2.description = queueItemDescription_2
        let queueItemsArray = [queueItem_1, queueItem_2]
        try {
            session.setAVQueueItems(queueItemsArray)
            AppLog.info("SetAVQueueItems successfully")
        } catch (err: BusinessException) {
            AppLog.error("Failed to set AVQueueItem, error code: ${err.code}, error message: ${err.message}")
        }
        // 设置媒体播放列表名称。
        let queueTitle = "QUEUE_TITLE"
        try {
            session.setAVQueueTitle(queueTitle)
            AppLog.info("SetAVQueueTitle successfully")
        } catch (err: BusinessException) {
            AppLog.error("Failed to set AVQueueTitle, error code: ${err.code}, error message: ${err.message}")
        }
    }
    ```

3. 设置用于被媒体会话控制方拉起的Ability。当用户操作媒体会话控制方的界面时，例如点击播控中心的卡片，可以拉起此处配置的Ability。

    设置Ability时通过WantAgent接口实现，更多关于WantAgent的信息请参考[WantAgent](../../../API_Reference/source_zh_cn/apis/AbilityKit/cj-apis-ability.md)。

    ```cangjie
    import kit.AVSessionKit.*
    import kit.AbilityKit.*

    let context = Global.getStageContext() // 获取Context应用上下文请参见 [API 参考] -> [API参考概述] -> [开发说明] -> [仓颉示例代码说明]