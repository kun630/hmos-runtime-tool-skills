```cangjie
    import kit.AudioKit.*
    import ohos.base.BusinessException

    try {
        audioRenderer.start()
        AppLog.info("Renderer start success.")
    } catch (e: BusinessException) {
        AppLog.error("Renderer start failed, code is ${e.code}, message is ${e.message}")
    }
    ```

4. 调用stop()方法停止渲染。

    ```cangjie
    import kit.AudioKit.*
    import ohos.base.BusinessException

    try {
        audioRenderer.stop()
        AppLog.info("Renderer stop success.")
    } catch (e: BusinessException) {
        AppLog.error("Renderer stop failed, code is ${e.code}, message is ${e.message}")
    }
    ```

5. 调用release()方法销毁实例，释放资源。

    ```cangjie
    import kit.AudioKit.*
    import ohos.base.BusinessException

    try {
        audioRenderer.release()
        AppLog.info("Renderer released.")
    } catch (e: BusinessException) {
        AppLog.error("Renderer release failed, code is ${e.code}, message is ${e.message}")
    }
    ```