# 使用AudioCapturer录制本端的通话声音

This directory serves as a comprehensive technical guide for HarmonyOS developers aiming to implement local call audio recording using the AudioCapturer module within the Cangjie language environment. It details the end-to-end process, starting from obtaining the UIAbility context and initializing the AudioCapturer with critical configurations, specifically setting the source type to `SOURCE_TYPE_VOICE_COMMUNICATION`. The content covers essential steps such as setting up audio stream parameters, registering callbacks for data reading and event monitoring, and writing captured data to a file. Furthermore, it addresses the management of the recording lifecycle, including starting, stopping, and releasing resources, with a strong emphasis on state validation and error handling. This resource is ideal for developers seeking to build robust call recording features that require microphone permissions and precise audio state management. By following these tutorials, developers can ensure their applications handle audio streams efficiently while adhering to system state requirements and business exception protocols.

## Quick Navigation

- **What do you want to learn?**
    - Initialize AudioCapturer and configure call recording settings → 使用AudioCapturer录制本端的通话声音_1.md
    - Manage the recording lifecycle (start, stop, release) → 使用AudioCapturer录制本端的通话声音_2.md

## Detailed Description

### 使用AudioCapturer录制本端的通话声音_1.md 使用AudioCapturer录制本端的通话声音_1.md
This tutorial guides developers on recording local call audio using the AudioCapturer module in the Cangjie language. It covers obtaining the UIAbility context and initializing the AudioCapturer with specific configurations. The key distinction is setting the source type to SOURCE_TYPE_VOICE_COMMUNICATION in audioCapturerInfo. The code demonstrates setting up audio stream parameters, registering callbacks for data reading and event monitoring, and writing captured data to a file. Microphone permission is required. This guide targets HarmonyOS developers implementing call recording features.

### 使用AudioCapturer录制本端的通话声音_2.md 使用AudioCapturer录制本端的通话声音_2.md
This document provides code examples for managing the lifecycle of an AudioCapturer to record local call audio. It details three core operations: starting audio capture, stopping the process, and releasing system resources. Key information highlights the importance of state validation checks before executing actions, such as ensuring the capturer is in the correct state (e.g., PREPARED, RUNNING) before starting or stopping, alongside robust error handling for business exceptions. Intended for developers implementing audio recording features, this technical guide requires familiarity with audio state machines and exception management.