# 使用geometryTransition共享元素转场

This directory provides a comprehensive guide to implementing shared element transitions using the `geometryTransition` interface within the ArkUI framework. It covers the core concepts of binding components with shared IDs to ensure smooth context inheritance during view changes. The content ranges from basic state-switching animations to complex implementations involving modal page transitions. Key topics include `geometryTransition`, shared element transitions, ArkUI, Cangjie, ArkTS, and UI animation. This resource is specifically designed for UI developers and engineers who aim to create polished, fluid user experiences and high-performance navigation animations in their applications.

## Quick Navigation

**What do you want to learn?**

*   Learn the basics of implicit shared element transitions and state switching → 使用geometryTransition共享元素转场_2more.md
*   Implement advanced transitions combining geometryTransition with modal pages → geometryTransition结合模态转场使用/

## Detailed Description

### 使用geometryTransition共享元素转场_2more.md 使用geometryTransition共享元素转场_2more.md
This document serves as a tutorial for implementing implicit shared element transitions using the geometryTransition interface in ArkUI. It covers the core concept of binding two components with a shared ID to create seamless animations and provides a practical code example demonstrating state switching between an image and a container. Key takeaways include understanding that geometryTransition links two different objects to ensure smooth context inheritance during view changes, requiring the same ID and transition effects. This guide is intended for UI developers using the Cangjie language who need to create polished, fluid animations between view states. Keywords: geometryTransition, shared element transition, ArkUI, Cangjie, animation, UI development.

### geometryTransition结合模态转场使用/
This directory serves as a comprehensive resource for developers aiming to implement advanced shared element transitions by combining geometryTransition with modal page transitions. The core objective is to achieve fluid, continuous "one-shot" animations where a specific component—typically an avatar or list item—expands seamlessly into a detailed modal view. The content provides detailed implementation strategies for two distinct frameworks: Cangjie and ArkUI (ArkTS). Readers will learn critical technical steps such as disabling default modal transitions to enable custom effects, binding unique geometry IDs for visual tracking, and managing state for modal visibility. This collection is specifically designed for developers seeking to elevate user experience through sophisticated, high-performance navigation animations in their applications. By following these examples, developers can ensure smooth visual continuity that bridges the gap between list views and detailed content pages.