# 指定密钥参数生成非对称密钥对仓颉

This directory provides technical resources and code examples for generating asymmetric key pairs using specified parameters within the Cangjie programming language and the CryptoArchitectureKit framework. It covers essential algorithms including RSA, ECC, and SM2, detailing how to construct parameter specifications like `ECCCommonParamsSpec` and `RSACommonParamsSpec`. The guides emphasize handling BigInt inputs in big-endian mode and retrieving key attributes for secure storage. These materials are suitable for Cangjie developers and security engineers implementing custom cryptographic operations, key generation workflows, and parameter validation in the HarmonyOS ecosystem.

## Quick Navigation

What do you want to learn?

*   Learn general asymmetric key generation in Cangjie (RSA, ECC, SM2) → 指定密钥参数生成非对称密钥对仓颉.md
*   Generate ECC key pairs using specified common parameters → 指定密钥参数生成ECC密钥对.md
*   Generate SM2 key pairs based on elliptic curve names → 根据椭圆曲线名生成SM2密钥对.md
*   Explore detailed ECC code examples (secp224r1) → 以根据密钥参数生成ECC密钥为例/
*   Generate RSA public keys from specific parameters → 指定密钥参数生成RSA公钥/

## Detailed Description

### 指定密钥参数生成ECC密钥对.md 指定密钥参数生成ECC密钥对.md
This technical tutorial guides developers on generating Elliptic Curve Cryptography (ECC) key pairs using specified common parameters within a cryptographic framework. The document outlines a four-step process involving the construction of an ECCCommonParamsSpec object, creating an asymmetric key generator via createAsyKeyGeneratorBySpec, generating the random key pair, and retrieving key parameters using getAsyKeySpec. It emphasizes that ECCCommonParamsSpec requires the algorithm name 'ECC' and the type COMMON_PARAMS_SPEC. Crucially, it specifies that BigInt inputs must be positive and in big-endian mode. This guide targets software developers implementing cryptographic operations, specifically those working with the CryptoArchitectureKit API who need to manage ECC key generation with custom parameters.

### 指定密钥参数生成非对称密钥对仓颉.md 指定密钥参数生成非对称密钥对仓颉.md
This technical guide demonstrates how to generate asymmetric key pairs using specified parameters within the Cangjie programming language. It covers the implementation for RSA, ECC, and SM2 algorithms, detailing the creation of KeyPair objects and the retrieval of their parameter attributes. The document highlights that generated key pairs facilitate subsequent cryptographic operations like encryption and decryption, while retrieved attributes are essential for secure storage and transport. Intended for Cangjie developers requiring cryptographic functionality, specifically those needing to implement or manage asymmetric encryption standards.

### 根据椭圆曲线名生成SM2密钥对.md 根据椭圆曲线名生成SM2密钥对.md
This technical guide details the procedure for generating SM2 asymmetric key pairs using specific elliptic curve names within the CryptoArchitectureKit. It covers constructing ECCCommonParamsSpec, creating ECCKeyPairSpec, invoking key generation functions, and retrieving curve parameters. The document emphasizes that BigInt inputs must be big-endian and positive. It provides a Cangjie code example demonstrating the instantiation of specs, key generation via createAsyKeyGeneratorBySpec and generateKeyPair, and error handling. This tutorial targets software developers implementing SM2 cryptography who need to programmatically generate key pairs from predefined curve parameters.

### 以根据密钥参数生成ECC密钥为例/
This directory provides comprehensive Cangjie code examples focused on generating Elliptic Curve Cryptography (ECC) keys based on specific key parameters within the HarmonyOS ecosystem. It covers the manual construction of `ECCCommonParamsSpec`, defining prime fields, curve coefficients, and generator points, as well as retrieving detailed cryptographic attributes from generated keys. Key topics include secp224r1 curve implementation, key pair generation workflows, and parameter inspection. These resources are suitable for Cangjie developers and security engineers implementing custom cryptographic operations or validating ECC key properties programmatically. The examples demonstrate both the setup of curve parameters and the subsequent extraction of key details for verification.

### 指定密钥参数生成RSA公钥/
This directory serves as a technical resource for generating RSA public keys using specified parameters within the CryptoArchitectureKit framework. It thoroughly covers the implementation details required to construct key specifications such as `RSACommonParamsSpec` and `RSAPubKeySpec`, initialize asymmetric key generators, and handle BigInt inputs in big-endian mode. The documentation includes practical Cangjie code examples demonstrating how to parse hexadecimal strings for modulus and exponent, alongside validation logic to ensure key consistency. This content is specifically tailored for software developers and security engineers who need to instantiate RSA public keys from existing parameters for cryptographic operations, offering a robust alternative to generating new key pairs.