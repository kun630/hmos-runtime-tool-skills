# class_AVTranscoder

This directory contains API reference documentation for the `AVTranscoder` class in the Cangjie programming language, designed for managing video transcoding operations within the HarmonyOS MediaKit. It covers the complete lifecycle of transcoding tasks, including configuration, execution, state management (pause, resume, cancel), and resource release. The documentation details essential properties like file descriptors for input/output and provides comprehensive error handling and callback mechanisms for progress, completion, and error events. This resource is suitable for multimedia developers and software engineers building applications that require video processing capabilities on the HarmonyOS platform.

## Quick Navigation

**What do you want to do?**

*   **Understand the class structure and properties** → class_AVTranscoder_3more.md
*   **Configure and prepare the transcoder** → func_prepareAVTranscoderConfig.md
*   **Control the transcoding process**:
    *   Start transcoding → func_start.md
    *   Pause transcoding → func_pause.md
    *   Resume transcoding → func_resume.md
    *   Cancel transcoding → func_cancel.md
*   **Handle events and callbacks**:
    *   Monitor progress → func_release_2more.md
    *   Handle completion → func_onAVTranscoderCallbac_2more.md
    *   Handle errors → func_onAVTranscoderCallbackType_Callback1_32d26bfd.md
*   **Clean up resources** → func_release_2more.md

## Detailed Description

### class_AVTranscoder_3more.md
This document serves as API reference documentation for the AVTranscoder class, a video transcoding management component in the Cangjie language. It covers essential properties for data handling, specifically fdSrc for source input and fdDst for destination output, both requiring configuration prior to the prepare method. Key information includes strict warnings against concurrent file descriptor access to prevent data race conditions and specific error codes for handling exceptions. Targeted at multimedia developers, this guide necessitates knowledge of file descriptors and system capabilities to implement video transcoding effectively.

### func_cancel.md
This document provides API reference documentation for the `cancel()` function in the Cangjie language, designed to stop an ongoing video transcoding process within the AVTranscoder framework. The content covers the function signature, functional description, system capabilities, version requirements, and a detailed list of potential exceptions with specific error codes. It also includes a practical code example demonstrating implementation. Key takeaways include the prerequisite that `cancel()` must only be invoked after successful `prepare()`, `start()`, or `pause()` events. The document outlines specific error scenarios like memory issues or IO errors and provides a complete Cangjie code snippet showing context initialization and file handling.

### func_onAVTranscoderCallbac_2more.md
This API reference document details how to manage event callbacks for the AVTranscoder module in the Cangjie programming language. It covers two primary functions: `on` for registering a transcoding completion callback and `off` for deregistering callback events. Key information includes that the `on` function allows only one callback instance, overwriting previous registrations, while both functions handle specific parameters and throw exceptions for memory or argument errors. Code examples demonstrate practical implementation. This guide targets multimedia application developers using the MediaKit who need to handle asynchronous transcoding events.

### func_onAVTranscoderCallbackType_Callback1_32d26bfd.md
This document provides API reference details for the `on` function used to register error event callbacks for the AVTranscoder in the Cangjie language. It covers the function signature, functionality description, system capabilities, parameters, exceptions including BusinessException and IllegalArgumentException, and a usage example. Key information indicates that this function subscribes to error events, requiring users to call `release()` upon error, and supports only one callback subscription at a time. The callback handles `BusinessException` objects. This is intended for multimedia application developers working with AV transcoding who need to implement robust error handling logic.

### func_pause.md
This document provides API reference documentation for the pause() function in the Cangjie programming language, designed to pause video transcoding processes. The content details the function signature, system capabilities, version requirements, and potential BusinessException error codes, including specific IDs for memory or IO errors. It includes a comprehensive code example demonstrating the prerequisite setup, such as configuring AVTranscoderConfig, managing file descriptors, and ensuring the start() method is called before pause(). Key takeaways are that pause() requires a successful start event and can be reversed using resume(). The target audience is software developers working with multimedia applications who need to control transcoding states, requiring knowledge of Cangjie syntax and the HarmonyOS MediaKit.

### func_prepareAVTranscoderConfig.md
This API reference document details the prepare function used to configure video transcoding parameters in the Cangjie programming language. The documentation covers the function signature, system capabilities, mandatory input parameters, and specific error codes including BusinessException and IllegalArgumentException. Key information includes the requirement for an AVTranscoderConfig object containing settings like audio and video bitrates, codecs, and resolution, as well as handling potential errors such as memory shortages or unsupported formats. A practical code example demonstrates how to initialize the configuration and file descriptors. This guide targets developers working with multimedia applications who need to implement video transcoding features using the MediaKit.

### func_release_2more.md
This API reference document details methods for managing video transcoding resources and events in the Cangjie programming language. It covers the release() function for freeing resources and the on() function for registering progress update callbacks. Key information includes that release() renders the AVTranscoder instance unusable, while on() allows monitoring transcoding progress via an Int32 callback, with re-registration overwriting previous handlers. Both functions include specific error codes like No memory and Operation not allowed. This guide targets developers using the MediaKit to implement video transcoding features, requiring knowledge of Cangjie syntax and exception handling.

### func_resume.md
This document provides API reference documentation for the resume() function in the Cangjie language, designed to resume paused video transcoding operations within the AVTranscoder framework. The content details the function signature, system capabilities, version requirements, and a comprehensive list of potential business exceptions with specific error codes. Key information includes the prerequisite that resume() must only be called after a successful pause() event. A practical code example demonstrates the implementation context, covering configuration, file handling, and the sequence of start, pause, and resume calls. Targeted at developers building multimedia applications on HarmonyOS, this guide assumes familiarity with file system operations and error handling.

### func_start.md
This document serves as API reference documentation for the Cangjie start() function, which initiates video transcoding operations. Key sections include the function signature, system capabilities, version history, and a detailed list of potential BusinessException error codes such as memory or IO errors. The document emphasizes that the prepare() method must be successfully triggered before calling start(). A comprehensive code example demonstrates how to configure AVTranscoderConfig, manage file descriptors, and handle exceptions within an application context. Intended for software developers using the MediaKit, this guide requires knowledge of Cangjie syntax and application context management.