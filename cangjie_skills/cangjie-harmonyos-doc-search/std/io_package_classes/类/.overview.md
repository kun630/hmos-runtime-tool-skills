# 类

This directory contains API reference documentation for core input/output (IO) classes within the Cangjie standard library. It covers a range of utility classes designed for handling data streams, including sequential reading, multi-stream writing, and in-memory byte manipulation. Key topics include buffered input and output streams for performance enhancement, byte buffers for data management, and specialized readers and writers for string and text processing. This documentation is suitable for Cangjie developers seeking to implement efficient, safe, and high-performance I/O operations in their applications.

## Quick Navigation

**What do you want to do?**

- Handle multiple input or output streams simultaneously → class_ChainedInputStreamT__2more.md
- Improve I/O performance using buffering:
  - For input streams → [2]
  - For output streams → [3]
- Manage in-memory byte stream operations → [4]
- Process text and strings:
  - Read and convert streams to strings → [5]
  - Write data types to output streams → [6]

## Detailed Description

### class_ChainedInputStreamT__2more.md class_ChainedInputStreamT__2more.md
This document serves as API reference documentation for two utility classes in the Cangjie standard IO library designed for handling multiple streams simultaneously. It details the ChainedInputStream class, which enables sequential reading from an array of input streams, and the MultiOutputStream class, which allows writing data to multiple output streams at once. Key sections include class definitions, constructors that accept stream arrays, and core methods like read, write, and flush. The documentation specifies parameters, return values, and potential IllegalArgumentException for empty inputs, providing a usage example for the input stream. This technical reference targets Cangjie developers seeking to implement complex stream operations, requiring knowledge of basic IO interfaces and generic types.

### [2] class_BufferedInputStreamT_where_T_InputStream/
This directory provides comprehensive API documentation for the `BufferedInputStream` class within the Cangjie standard library. It details a generic wrapper designed to enhance input stream performance through buffering. The content covers class initialization, including various constructor overloads for memory optimization, and extends functionality for resource management and seekable operations. Additionally, it documents essential reading functions and stream state management. This resource is intended for Cangjie developers seeking to implement efficient, safe, and high-performance I/O operations in their applications.

### [3] class_BufferedOutputStreamT_where_T_OutputStream/
This directory contains API documentation for the Cangjie `BufferedOutputStream` class, a generic wrapper designed to enhance I/O performance by buffering data before writing to an underlying output stream. It covers class definitions, constructors with various buffer configurations, and extensions for resource management and seekable operations. Key topics include memory optimization, automatic cleanup via the Resource interface, random access capabilities, and low-level byte writing functions. This documentation is suitable for Cangjie developers looking to implement efficient, safe, and flexible data output operations in their applications.

### [4] class_ByteBuffer/
This directory contains API reference documentation for the Cangjie `ByteBuffer` class, which manages in-memory byte stream read and write operations. It covers class structure, initialization, capacity management, and various methods for data manipulation like reading, writing, seeking, and cloning. Key topics include IOStream inheritance, Seekable interface, and exception handling. This resource is suitable for Cangjie developers implementing data buffering, stream manipulation, and low-level I/O operations.

### [5] class_StringReaderT_where_T_InputStream/
This directory contains comprehensive API reference documentation for the Cangjie `StringReader` class, specifically designed to read and convert data from `InputStream` objects into characters or strings. It covers the class initialization, core methods like `read` and `readToEnd`, internal buffering, and UTF-8 encoding support. Additionally, it details extensions for resource management and seekable stream navigation, alongside specific parsing functions. This resource is suitable for Cangjie developers implementing text processing, I/O operations, and stream parsing who require efficient data handling, error management, and random access capabilities.

### [6] class_StringWriterT_where_T_OutputStream/
This directory provides comprehensive API reference documentation for the `StringWriter` generic class and its associated output functions within the Cangjie programming language. It details the class structure, which utilizes a 4096-byte internal buffer and is currently limited to UTF-8 encoding. The content covers methods for writing various primitive data types, including integers, floats, and booleans, to an `OutputStream`. Additionally, it documents extensions for resource management and stream seeking. This documentation is essential for Cangjie developers implementing I/O operations, buffered writing, and console output who require specific syntax and interface implementation details.