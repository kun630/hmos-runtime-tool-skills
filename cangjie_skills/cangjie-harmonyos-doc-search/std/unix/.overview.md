# unix

This directory contains essential documentation and code examples for implementing Unix domain socket communication within the Cangjie programming language. It serves as a practical guide for developers looking to establish robust inter-process communication (IPC) mechanisms on local systems. The content delves into the specifics of using the standard library to create server and client architectures, highlighting the use of `UnixServerSocket` for binding and accepting incoming connections, and `UnixSocket` for initiating connections and data transfer. Beyond basic socket operations, the directory covers advanced topics such as concurrency management using synchronization primitives like `Barrier` to coordinate threads effectively. It also addresses the operational lifecycle, including file system cleanup via `remove` calls. This resource is highly suitable for Cangjie developers who are building system-level applications or services that require efficient local data exchange and need a clear reference for handling low-level socket APIs and synchronization logic.

## Quick Navigation

- What do you want to learn?
    - How to implement a Unix domain socket server in Cangjie? → unix.md
    - How to implement a Unix domain socket client in Cangjie? → unix.md
    - How to use synchronization primitives like Barrier for concurrency? → unix.md
    - How to perform specific socket operations (bind, accept, connect, read, write)? → unix.md
    - How to manage file system cleanup after socket operations? → unix.md
    - Understanding inter-process communication (IPC) in Cangjie? → unix.md

## Detailed Description

### unix.md unix.md
This document provides a practical code example demonstrating how to implement Unix domain socket communication in the Cangjie programming language. The content covers the implementation of a server function using UnixServerSocket for binding and accepting connections, and a client function using UnixSocket for connecting and reading data. It also utilizes synchronization primitives like Barrier to coordinate concurrent execution between the server and client threads. Key takeaways include the specific API calls for socket operations such as bind, accept, connect, read, and write, as well as file system cleanup using remove. This tutorial targets Cangjie developers seeking to understand inter-process communication mechanisms within the standard library.