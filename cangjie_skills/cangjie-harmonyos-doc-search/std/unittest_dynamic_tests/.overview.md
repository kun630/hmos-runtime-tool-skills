# unittest_dynamic_tests

This directory provides a specialized guide for developers working within the Cangjie framework who need to implement advanced dynamic testing strategies. Unlike standard static testing, this resource focuses on scenarios where test data is generated or discovered at runtime rather than compile time. It introduces the `@TestBuilder` annotation as the primary mechanism for handling these dynamic requirements, guiding users through the construction of flexible test suites using `TestSuite` and `UnitTestCase` classes. The content clearly distinguishes these dynamic approaches from traditional static `@Test` cases, highlighting the benefits of programmatic test execution via the `runTests()` method. Additionally, it addresses the critical need for diverse result dissemination by explaining how to utilize various `Reporter` classes to export test outcomes to console, text, XML, or CSV formats. This documentation is particularly suitable for software engineers and QA professionals who require a highly adaptable testing infrastructure capable of integrating with external data sources and complex configuration environments. By mastering these dynamic testing techniques, users can significantly enhance the flexibility and coverage of their testing workflows within the Cangjie ecosystem.

## Quick Navigation

What do you want to learn?

*   **Implement dynamic testing**: Learn how to handle test data unknown at compile time using the Cangjie framework → unittest_dynamic_tests.md
*   **Use annotations and classes**: Understand the application of `@TestBuilder`, `TestSuite`, and `UnitTestCase` for building test suites → unittest_dynamic_tests.md
*   **Execute tests programmatically**: Discover how to run tests dynamically using `runTests()` instead of static execution → unittest_dynamic_tests.md
*   **Manage test reporting**: Find out how to configure `Reporter` classes to output results in console, text, XML, or CSV formats → unittest_dynamic_tests.md
*   **Understand constraints**: Review the specific limitations and requirements for `@TestBuilder` functions → unittest_dynamic_tests.md

## Detailed Description

### unittest_dynamic_tests.md unittest_dynamic_tests.md

This guide explains how to implement dynamic testing in the Cangjie framework using the `@TestBuilder` annotation to handle test data unknown at compile time. It covers constructing dynamic test suites with `TestSuite` and `UnitTestCase`, contrasting them with static `@Test` cases, and programmatically executing tests via `runTests()`. Key takeaways include the specific constraints on `@TestBuilder` functions and the ability to output test results to various formats like console, text, XML, or CSV using different `Reporter` classes. This tutorial targets developers who need to generate tests based on external data or configurations and require flexible execution and reporting mechanisms.