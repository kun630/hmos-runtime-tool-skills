# 使用AVPlayer播放音频

This directory provides a comprehensive guide and code examples for implementing audio playback using the AVPlayer component in HarmonyOS development. It covers the complete lifecycle of media streaming, including player creation, resource configuration (local and network), playback controls, state monitoring, and memory management. Key topics include handling audio interruptions, setting parameters like volume and speed, and managing player states effectively. This resource is suitable for application developers looking to integrate robust audio features into their applications using the Cangjie language, offering both theoretical workflows and practical implementation details.

## Quick Navigation

**What do you want to do?**

*   Follow the standard 7-step development workflow and lifecycle → 开发步骤及注意事项.md
*   Implement end-to-end audio streaming with detailed parameter settings → 使用AVPlayer播放音频_2more.md
*   Access practical code examples for local files, live streams, and callbacks → 完整示例/

## Detailed Description

### 使用AVPlayer播放音频_2more.md 使用AVPlayer播放音频_2more.md
This development guide explains how to implement audio playback using the AVPlayer component for end-to-end media resource streaming. The document details the complete playback workflow, including player creation, resource configuration, parameter settings like volume and speed, playback controls, and resource management. It also covers state monitoring and development best practices. Key information includes managing player states to avoid errors, utilizing reset or release methods for memory optimization, and handling audio interruptions or device changes. The guide notes that background playback is currently unsupported and requires internet permissions for online resources. This tutorial targets application developers seeking to integrate audio playback features, requiring an understanding of system audio management strategies.

### 开发步骤及注意事项.md 开发步骤及注意事项.md
This document outlines the standard workflow and precautions for developing media playback applications using the AVPlayer API. It details the seven-step lifecycle: creating an instance, registering essential listeners (such as stateChange and error), setting local or network resources, preparing the player, controlling playback, switching resources, and releasing the instance. Key takeaways include the necessity of declaring internet permissions for network streams, using application sandbox paths for local files, and configuring audio rendering info before preparation. This guide is intended for developers implementing multimedia features who need to manage player states and handle resource configurations effectively.

### 完整示例/
This directory provides a comprehensive collection of complete code examples designed to help developers master media playback using the AVPlayer framework in the Cangjie language for HarmonyOS. It covers a wide range of scenarios, including local file playback via URLs, file descriptors, and data sources, as well as live streaming implementations. The examples detail the setup of AVPlayer callbacks for state changes and error handling, illustrating how to manage the player lifecycle effectively. Key topics include AVPlayer initialization, StateChangeCallback usage, and UIAbility integration. These resources are specifically suitable for HarmonyOS developers who need to integrate robust audio and video playback features into their applications and require practical, ready-to-use code references.