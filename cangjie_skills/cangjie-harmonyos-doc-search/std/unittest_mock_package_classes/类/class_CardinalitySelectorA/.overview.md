# class_CardinalitySelectorA

This directory provides comprehensive API documentation for the `CardinalitySelector<A>` class and associated functions within the Cangjie `unittest.mock` framework. It is dedicated to the mechanisms of managing and verifying the execution frequency of stub behaviors, a core requirement for robust unit testing. The documentation details specific methods for setting call constraints, including `anyTimes`, `once`, `times`, `atLeastOnce`, and `atLeastTimes`, while also explaining exception handling for scenarios like verification failures. It is highly suitable for software developers and testers utilizing the Cangjie language who need to enforce precise behavior expectations and validate interaction counts. By using these tools, developers can ensure their mock objects behave exactly as required, leading to more reliable and maintainable test suites. This reference is essential for anyone looking to master the mocking capabilities of the Cangjie standard library.

## Quick Navigation

**What do you want to learn?**

*   Understand the main class for managing execution frequency verification and setting call constraints → class_CardinalitySelectorA_6more.md
*   Learn how to define execution frequency within a specific numeric range using the times function → func_timesInt64_Int64.md

## Detailed Description

The documentation below outlines the specific classes and functions available for configuring these constraints.

### class_CardinalitySelectorA_6more.md class_CardinalitySelectorA_6more.md
This API documentation defines the Cangjie class CardinalitySelector<A>, which manages execution frequency verification for stub behaviors in unit testing. The document details methods for setting call constraints, including anyTimes, once, times, atLeastOnce, and atLeastTimes. Key information includes that these functions validate stub signature call counts, throwing ExpectationFailedException or IllegalArgumentException upon verification failure. Methods like once and times return a Continuation<A> object to allow further configuration. This reference guide targets developers using the unittest mock package to define precise behavior expectations and validate interaction counts in their test cases.

### func_timesInt64_Int64.md func_timesInt64_Int64.md
This document provides API reference documentation for the times function within the Cangjie unittest mock framework, designed to define and verify the execution frequency of stub behaviors within a specific numeric range. The content details the function signature, describes the purpose of the min and max Int64 parameters for setting expected execution limits, and outlines exception handling for verification failures and invalid negative inputs. Key information includes the specific exceptions thrown, such as ExpectationFailedException and IllegalArgumentException. This reference is intended for developers writing unit tests in Cangjie who require precise control over mock interaction verification, assuming familiarity with testing concepts and the core package structure.