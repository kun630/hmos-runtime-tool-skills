# cj-create-custom-components

This directory serves as a comprehensive guide for developers looking to create custom components within the Cangjie UI framework (ArkUI). It covers essential structural rules, syntax constraints, and parameter initialization required to build reusable UI elements. Key topics include the `build()` function specifications, the `@Component` decorator, and component communication mechanisms. This resource is suitable for HarmonyOS developers using the Cangjie language who want to encapsulate UI logic, improve code reusability, and ensure compliance with framework constraints.

## Quick Navigation

- What do you want to learn?
    - How to define structural rules and syntax for custom components? → Go to subdirectory: **创建自定义组件/**
    - How to implement the `@Component` decorator and `build()` function? → Go to subdirectory: **创建自定义组件/**
    - How to handle parameter initialization and component communication? → Go to subdirectory: **创建自定义组件/**

## Detailed Description

### 创建自定义组件/
This directory focuses on creating custom components within the Cangjie UI framework (ArkUI). It covers the structural rules, syntax constraints, and parameter initialization required to build reusable UI elements. Key topics include the `build()` function specifications, the `@Component` decorator, and component communication mechanisms. This resource is suitable for HarmonyOS developers using the Cangjie language who want to encapsulate UI logic, improve code reusability, and ensure compliance with framework constraints.