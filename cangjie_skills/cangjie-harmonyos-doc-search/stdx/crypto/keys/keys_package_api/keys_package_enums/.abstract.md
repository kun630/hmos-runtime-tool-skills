本目录 `keys_package_enums` 专门用于存储密钥包相关的枚举类型API参考文档，核心目标是定义和规范加密算法实现中所需的各种配置选项。文档内容主要聚焦于两大核心组件：一是用于ECDSA（椭圆曲线数字签名算法）密钥生成的 `Curve` 枚举，详细列举了包括NIST P系列和Brainpool BP系列在内的八种主流椭圆曲线，并阐述了它们各自的安全特性；二是用于RSA加密操作的 `PadOption` 枚举，涵盖了OAEP、PSS和PKCS1等多种填充模式，并明确建议开发者优先采用安全...