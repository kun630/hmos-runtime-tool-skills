# class_LogWriter

本目录包含仓颉语言中 `LogWriter` 抽象类及相关函数的 API 参考文档，主要涉及将对象序列化至日志输出目标的核心能力。内容涵盖了用于构建日志结构的 `start`/`end` 方法，以及写入 `Bool`、`Int64`、`Float64`、`String` 等多种数据类型的 `write` 方法。文档详细说明了 `LogWriter` 与 `LogValue` 接口的配合使用，以及在状态异常时抛出 `IllegalStateException` 的错误处理机制。核心关键词包括仓颉语言、日志序列化、LogValue 接口、API 参考。该文档适合需要实现自定义日志序列化或处理结构化日志数据的开发者阅读。

## Quick Navigation

- 你想了解 LogWriter 抽象类的核心定义与结构化方法？
  → 查看 class_LogWriter_13more_fcdf1c15.md LogWriter 类 API 参考
- 你想了解如何向日志写入 String 或通过 LogValue 写入复杂数据？
  → 查看 func_writeStringS_2more_1eb0e830.md 写入函数 API 参考

## Detailed Description

### class_LogWriter_13more_fcdf1c15.md class_LogWriter_13more_fcdf1c15.md

本文档是仓颉语言中抽象类 LogWriter 的 API 参考文档，定义了将对象序列化至日志输出目标的能力。文档涵盖了用于构建日志结构的 start/end 方法，以及写入 Bool、Int64、Float64、DateTime 等类型的 write 方法。关键信息指出 LogWriter 需与 LogValue 接口配合，且操作受状态约束，错误时抛出 IllegalStateException。该文档面向需要实现自定义日志序列化的开发者，提供了处理结构化日志数据的完整接口规范。

### func_writeStringS_2more_1eb0e830.md func_writeStringS_2more_1eb0e830.md

本文档是Cangjie语言的API参考文档，旨在说明如何向日志输出目标写入数据。文档主要涵盖`writeString`和`writeValue`两个函数，前者用于写入String值，后者通过`LogValue`接口写入多种数据类型。关键信息包括`writeValue`已为Int64、Float64、DateTime、Array、HashMap及Option等内置类型扩展实现了接口，且两者在状态异常时均抛出`IllegalStateException`。该文档面向Cangjie开发者，适用于需要处理日志记录功能的场景，读者需具备基本的语言语法知识。