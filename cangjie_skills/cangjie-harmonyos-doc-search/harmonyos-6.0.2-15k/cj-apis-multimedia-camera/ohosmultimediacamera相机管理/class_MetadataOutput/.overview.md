# class_MetadataOutput

This directory contains API reference documentation for the `MetadataOutput` class within the Cangjie CameraKit framework. It focuses on managing metadata streams, including registering and unregistering listeners for detected metadata objects and error handling. Key topics include lifecycle management methods like start, stop, and release, as well as specific callback constraints. This documentation is intended for developers building camera applications who need to handle metadata events and manage camera output resources using the Cangjie programming language.

## Quick Navigation

**What do you want to do?**

*   Understand the class structure and unregistration methods → class_MetadataOutput_3more.md
*   Register or unregister metadata object callbacks → func_offCameraCallbackType_2more.md
*   Register error callbacks for interface failures → func_onCameraCallbackType_Callback1Argume_60b79106.md
*   Manage the output lifecycle (start, stop, release) → func_release_3more.md

## Detailed Description

### class_MetadataOutput_3more.md class_MetadataOutput_3more.md
This document provides API reference documentation for the MetadataOutput class in the Cangjie language, detailing methods to manage metadata streams within the CameraKit. It primarily describes two overloaded off functions used to unregister listeners: one for detected metadata objects and another for error handling. Key information includes parameter definitions requiring specific CameraCallbackType values, system capabilities, and version 19 compatibility. The document includes Cangjie code examples illustrating the implementation of callback classes and the unregistration process. Intended for developers building camera applications, this reference assumes familiarity with the Cangjie syntax, camera context management, and the CameraOutput parent class.

### func_offCameraCallbackType_2more.md func_offCameraCallbackType_2more.md
This API reference document details methods for managing camera event callbacks in the Cangjie programming language, specifically for handling metadata objects. It covers the `off` function for unregistering all listeners for a specific event and the `on` function for registering callbacks to receive arrays of detected metadata. Key information includes the requirement that the `on` method must use the `metadataObjectsAvailable` event type and a custom callback handler. A critical constraint is that `off` cannot be called within the `on` callback method. This documentation targets developers building multimedia applications using the CameraKit, requiring knowledge of Cangjie syntax and camera context management.

### func_onCameraCallbackType_Callback1Argume_60b79106.md func_onCameraCallbackType_Callback1Argume_60b79106.md
This API documentation details the `on` function for registering error callbacks in the Camera module's metadata stream. The document outlines the function signature, system capabilities, and parameter requirements, specifically noting that the event type must be `CameraError` to catch interface failures like `start` or `release`. Key takeaways include the restriction against calling `off` inside the callback and the use of `Callback1Argument<BusinessException>` to handle error details. A Cangjie code example demonstrates implementation. Targeted at developers using the Multimedia Camera Core system capability, this reference guide requires knowledge of camera output creation and event handling.

### func_release_3more.md func_release_3more.md
This document provides API reference documentation for managing the lifecycle of metadata output in the Cangjie CameraKit. It details three core functions: release() for freeing resources, start() for initiating metadata output, and stop() for halting the process. Key information includes function signatures, system capabilities, specific error codes such as 7400201, and practical Cangjie code examples demonstrating usage within a camera context. Targeted at developers working with multimedia camera features, this reference requires prior knowledge of the Cangjie language and camera management setup.