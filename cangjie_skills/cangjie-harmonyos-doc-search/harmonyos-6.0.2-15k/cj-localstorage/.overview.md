# cj-localstorage

This directory serves as a comprehensive resource for `cj-localstorage`, a specialized page-level UI state storage solution built for the Cangjie framework. It is designed to assist developers in utilizing LocalStorage as an efficient in-memory database to manage and synchronize state across complex component trees. The documentation covers essential concepts such as reactive state management, the application of `@LocalStorageLink` and `@LocalStorageProp` decorators, and the handling of complex data types. It is particularly suitable for HarmonyOS application developers seeking to implement robust state sharing between views and UIAbility instances while adhering to specific lifecycle constraints and usage best practices. By leveraging these tools, developers can ensure that their applications maintain consistent UI states and respond dynamically to user interactions, ultimately improving the user experience and application reliability. This guide is essential for anyone looking to master state persistence and data flow within the Cangjie ecosystem, providing the necessary theoretical background and practical implementation details to build responsive, high-performance applications.

## Quick Navigation

- **What do you want to learn?**
    - Core concepts of LocalStorage as an in-memory database → `LocalStorage页面级UI状态存储/`
    - How to use `@LocalStorageLink` and `@LocalStorageProp` for synchronization → `LocalStorage页面级UI状态存储/`
    - Lifecycle management and specific usage constraints → `LocalStorage页面级UI状态存储/`
    - Best practices for reactive state management in HarmonyOS → `LocalStorage页面级UI状态存储/`
- **What do you want to do?**
    - Implement state sharing between views and UIAbility instances → `LocalStorage页面级UI状态存储/`
    - Handle complex data types within the component tree → `LocalStorage页面级UI状态存储/`
    - Manage UI state updates efficiently using decorators → `LocalStorage页面级UI状态存储/`

## Detailed Description

### LocalStorage页面级UI状态存储/
This directory provides comprehensive documentation and guides for implementing LocalStorage, a page-level UI state storage mechanism within the Cangjie framework. It covers the core concepts of using LocalStorage as an in-memory database for managing state across component trees. Key topics include the `@LocalStorageLink` and `@LocalStorageProp` decorators for data synchronization, lifecycle management, and specific usage constraints. The content is designed for developers working with HarmonyOS applications who need to handle reactive state management, complex data types, and state sharing between views and UIAbility instances.