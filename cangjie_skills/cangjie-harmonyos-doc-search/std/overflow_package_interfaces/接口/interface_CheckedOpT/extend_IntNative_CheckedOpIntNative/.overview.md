# extend_IntNative_CheckedOpIntNative

This directory contains essential API reference documentation for the `extend_IntNative_CheckedOpIntNative` module within the Cangjie programming language ecosystem. It focuses on the implementation of the `CheckedOp` interface for the `IntNative` type, providing a robust framework for safe arithmetic and bitwise operations. The primary objective is to mitigate risks associated with integer overflow by replacing standard wrapping behavior with explicit error handling. By utilizing an `Option` return strategy, functions return `None` when operations exceed safe limits, thereby preventing undefined behavior. This resource is specifically designed for Cangjie developers who require high reliability in their applications, particularly those involving systems programming or complex mathematical logic where precise control over integer calculations is paramount. By integrating these checked operations, developers can build more resilient software that handles edge cases gracefully without crashing or producing incorrect results. Key concepts include CheckedOp, IntNative, Option types, overflow prevention, and bitwise operations.

## Quick Navigation

**What do you want to learn?**

*   About the general `CheckedOp` interface implementation for arithmetic operations (add, sub, mul, etc.)? → extend_IntNative_CheckedOp_7more.md
*   About specific functions for negation, bitwise shifts, and subtraction? → func_checkedNeg_4more.md

## Detailed Description

### extend_IntNative_CheckedOp_7more.md extend_IntNative_CheckedOp_7more.md
This API reference document provides an in-depth look at the implementation of the `CheckedOp` interface specifically for the `IntNative` type. It is designed to facilitate safe arithmetic operations, addressing the common pitfalls of integer overflow. The documentation covers a comprehensive list of specific functions, including checked addition, subtraction, division, multiplication, modulo, increment, and decrement. A critical aspect of this implementation is the `Option` return strategy. Unlike standard operations that might wrap values on overflow, these methods return `None`, allowing developers to catch and handle errors explicitly. This document is vital for Cangjie developers who need to ensure data integrity and prevent undefined behavior in their native integer manipulations.

### func_checkedNeg_4more.md func_checkedNeg_4more.md
This document serves as a crucial piece of API reference documentation for developers working with safe arithmetic and bitwise operations in the Cangjie programming language. It details the implementation and usage of four specific functions: `checkedNeg`, `checkedShl`, `checkedShr`, and `checkedSub`. The key information provided focuses on the error handling mechanism: these functions utilize an `Option` return strategy to manage potential issues gracefully. They return `None` upon detecting arithmetic overflow or when shift amounts exceed the bit width of the operand, effectively preventing undefined behavior. This documentation is targeted at Cangjie developers who require robust and predictable error handling for integer operations, ensuring that edge cases in bitwise and arithmetic logic are managed safely.