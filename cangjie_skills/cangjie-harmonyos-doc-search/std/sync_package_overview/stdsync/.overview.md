# stdsync

The `stdsync` directory serves as the central documentation hub for the synchronization package in the Cangjie programming language. It is designed to assist developers in mastering concurrent programming by providing both high-level conceptual guides and low-level API references. Key topics include the M:N threading model, lightweight user-state threads, atomic operations for various data types, and synchronization primitives like mutexes and condition variables. This directory is suitable for Cangjie developers and software engineers who need to build robust, multi-threaded applications. It ensures users can effectively manage memory ordering, prevent deadlocks, and handle mutual exclusion in complex environments. By combining feature introductions with a comprehensive API index, `stdsync` offers the necessary tools to implement efficient thread-safe operations.

## Quick Navigation

**What do you want to learn?**
*   Understand the core features, M:N threading model, and synchronization concepts → 功能介绍.md 功能介绍.md
*   Look up specific API definitions, constants, enumerations, and class structures → API_列表/

**What do you want to do?**
*   Implement thread creation using `spawn`, atomic operations, or automatic lock management → 功能介绍.md 功能介绍.md
*   Reference detailed locking mechanisms, condition variables, timers, and exception handling → API_列表/

## Detailed Description

### 功能介绍.md 功能介绍.md
This document serves as a feature introduction and guide for the sync package in the Cangjie programming language, focusing on concurrent programming capabilities. It covers the M:N threading model, thread creation using the spawn keyword, atomic operations for integers, booleans, and references, and thread synchronization using mutexes and the synchronized keyword. Key information includes that Cangjie threads are lightweight, user-state, and preemptive, while the package provides atomic read/write/swap/arithmetic operations and ensures automatic lock management to prevent deadlocks or resource leaks during exceptions. This guide targets Cangjie developers looking to implement multi-threaded applications efficiently.

### API_列表/
This directory serves as a comprehensive API reference index for a synchronization package, detailing the essential components required for multi-threaded application development. It covers a wide range of concurrency primitives, including constants, variables, enumerations, and class definitions. Key topics include atomic operations for various data types, locking mechanisms such as mutexes and read-write locks, condition variables, and utility timers. The documentation also addresses deprecated legacy elements and exception handling for synchronization states. This resource is designed for software developers and engineers seeking to implement thread-safe operations, manage memory ordering, and handle mutual exclusion in complex concurrent environments.