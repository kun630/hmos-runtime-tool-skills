# 连接server端读取和写入信息

This directory provides technical guides and code examples for implementing Bluetooth Low Energy (BLE) GATT client functionality, specifically focusing on connecting to a server to read and write data. It covers the complete workflow from establishing connections and discovering services to performing read/write operations and managing disconnections. The content includes implementations in both Cangjie and ArkTS languages within the HarmonyOS ecosystem. It is suitable for developers looking to integrate BLE communication into their applications, offering detailed insights into GATT protocols, UUID management, and asynchronous callback handling.

## Quick Navigation

**What do you want to learn or do?**

*   Learn the 6-step BLE connection process using Cangjie → 连接server端读取和写入信息_1.md
*   Implement the full GATT workflow (connection, discovery, read/write) in ArkTS → 连接server端读取和写入信息_2.md
*   Handle specific write operations and manage disconnections → 连接server端读取和写入信息_3.md

## Detailed Description

### 连接server端读取和写入信息_1.md 连接server端读取和写入信息_1.md
This document provides a technical tutorial and code example for establishing a Bluetooth Low Energy (BLE) GATT client connection to read and write data on a server. It outlines a six-step process including module imports, client instantiation, server connection, data read/write operations, and resource cleanup, followed by a Cangjie code implementation. The code demonstrates the GattClientManager class, defining specific UUIDs for services and characteristics, initializing descriptors, and handling connection state changes via callbacks. Targeted at developers using the Cangjie language and ConnectivityKit to implement BLE communication in HarmonyOS applications.

### 连接server端读取和写入信息_2.md 连接server端读取和写入信息_2.md
This technical guide demonstrates how to implement a BLE GATT client to connect to a server and perform data operations. It covers the connection process, service discovery, reading and writing characteristic values, and reading descriptor values. The code illustrates the sequential workflow: creating a GATT client, connecting, discovering services to ensure compatibility, and then executing read/write operations with error handling. It targets developers working on Bluetooth connectivity, specifically those using the ArkTS language or HarmonyOS framework, who need to manage client-side GATT interactions.

### 连接server端读取和写入信息_3.md 连接server端读取和写入信息_3.md
This technical reference document illustrates how to perform write operations and manage disconnections for a Bluetooth Low Energy (BLE) GATT client. It covers the writeDescriptorValue method for writing data to server descriptors, the stopConnect method for disconnecting and closing the client, and the ChangeStateCb callback for tracking connection states. The code emphasizes pre-flight checks for connection status, error handling via try-catch blocks, and resource cleanup by calling close(). It also references a specific error code documentation. Targeted at developers building BLE applications, this guide assumes familiarity with GATT concepts and asynchronous programming patterns. Keywords: BLE, GATT, writeDescriptor, disconnect, callback, error handling.