# interface_Formattable

This directory contains API documentation for the `Formattable` interface in the Cangjie programming language, focusing on string conversion capabilities for various numeric and primitive types. It covers the core interface definition and its extensions across signed integers (Int8, Int16, Int32, Int64), unsigned integers (UInt8, UInt16, UInt32, UInt64), floating-point numbers (Float16, Float32, Float64), and the Rune type. The documents detail the `format` method, which accepts format strings to generate string representations, handling potential exceptions. This resource is suitable for Cangjie developers looking to implement or utilize type-to-string formatting functionality within their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and Float16/Float32 implementation → interface_Formattable_4more.md
*   Format Float64, Int16, or Int32 types → extend_Float64_Formattable_3more.md
*   Format Int64, Int8, or Rune types → extend_Int64_Formattable_3more.md
*   Format unsigned integers (UInt16, UInt32, UInt64) → extend_UInt16_Formattable_3more.md
*   Format UInt8 types → extend_UInt8_Formattable.md

## Detailed Description

### extend_Float64_Formattable_3more.md extend_Float64_Formattable_3more.md
This API reference document details the implementation of the Formattable interface for the Float64, Int16, and Int32 numeric types within the Cangjie language. The content is organized into three distinct sections, each describing an extension for a specific data type and its associated format function. Key information includes the public format method, which accepts a format string parameter to convert numeric instances into strings, returning the result or throwing an IllegalArgumentException if the format specification is invalid. This documentation targets developers working with type conversions and string formatting who need to programmatically manipulate numeric representations.

### extend_Int64_Formattable_3more.md extend_Int64_Formattable_3more.md
This API reference document details the implementation of the Formattable interface for Int64, Int8, and Rune types within the Cangjie language. The content is organized into three distinct sections, each describing an extension that enables specific data types to be converted into formatted strings. Key information includes the public format function, which accepts a format string parameter and returns a String representation, while throwing an IllegalArgumentException if the format is invalid. This documentation serves Cangjie developers who need to handle string formatting for primitive integer and character types.

### extend_UInt16_Formattable_3more.md extend_UInt16_Formattable_3more.md
This API documentation file details the implementation of the Formattable interface for the unsigned integer types UInt16, UInt32, and UInt64 in the Cangjie programming language. The document is structured into three distinct sections, each mirroring the others to describe the extension of a specific unsigned integer type. Key information includes the definition of the public `format` function, which accepts a format string parameter and returns a formatted string representation of the instance. It also specifies that an `IllegalArgumentException` is thrown if the format string is invalid. This reference material is intended for Cangjie developers who need to convert unsigned integer values into formatted strings for output or display.

### extend_UInt8_Formattable.md extend_UInt8_Formattable.md
This API documentation details the extension of the UInt8 type to implement the Formattable interface in the Cangjie language, enabling string conversion capabilities. The document covers the inheritance relationship from the Formattable interface and specifically documents the public format function. Key information includes the function's signature, which accepts a format string parameter and returns a formatted string representation of the UInt8 instance. It also notes that an IllegalArgumentException is thrown if the format string is invalid. This reference guide targets Cangjie developers who need to format UInt8 values for output or string manipulation, requiring knowledge of basic type interfaces and exception handling.

### interface_Formattable_4more.md interface_Formattable_4more.md
This API documentation defines the Formattable interface in the Cangjie language, which standardizes how type instances are converted into formatted strings. The document details the interface definition, the core format method signature, and specific implementations for Float16 and Float32 types. Key information includes the format(fmt: String): String function, which accepts a format string and returns the converted result. For floating-point types, the method throws IllegalArgumentException if the provided format is invalid. This reference material is intended for developers seeking to implement or utilize formatting functionality within the convert package.