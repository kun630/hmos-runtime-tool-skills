# ohoseventhub

This directory contains API reference documentation for the `ohos.eventhub` module within the Cangjie programming language and HarmonyOS AbilityKit framework. It covers the implementation of event-driven architecture, focusing on the `EventHub` class and a series of generic `Event` and `EventCallBack` classes designed to handle events with zero to five parameters. Key topics include event subscription, unsubscription, emission, and lifecycle management. This resource is suitable for Cangjie developers and system engineers building robust, type-safe event communication mechanisms in applications using SystemCapability.Ability.AbilityRuntime.Core.

## Quick Navigation

**What do you want to learn?**
*   Understand module basics and import instructions → ohoseventhub_3more.md
*   Learn about the main event manager class → `class_EventHub/`
*   Understand the base class and validation logic → `class_EventBase/`

**What do you want to do?**
*   Handle events with specific parameter counts:
    *   Zero parameters → class_Event0.md
    *   One parameter → class_Event1.md
    *   Two parameters → class_Event2.md
    *   Three parameters → `class_Event3/`
    *   Four parameters → `class_Event4/`
    *   Five parameters → `class_Event5/`
*   Implement callback interfaces:
    *   For 0 to 3 parameters → class_EventCallBack0_4more.md
    *   For 4 to 5 parameters → class_EventCallBack4_2more.md

## Detailed Description

### class_Event0.md class_Event0.md
This document serves as API reference for the Event0 class in the Cangjie language, designed to handle events with zero parameters. It details the class structure, which inherits from EventBase, and outlines four primary methods: emit for triggering events, on for subscribing callbacks, and two off methods for unsubscribing specific or all callbacks. Key information includes system capabilities, version requirements (starting from version 12), and parameter descriptions. The documentation includes Cangjie code examples demonstrating how to implement EventCallBack0, register listeners, and manage event lifecycles within an EventHub. This resource is intended for developers using the AbilityKit framework to implement event-driven logic in their applications.

### class_Event1.md class_Event1.md
This document provides API reference details for the Event1<A> class, a generic event center designed to handle events with a single parameter in the Cangjie programming language. It covers the class definition, inheritance from EventBase, and four primary methods: emit for triggering events, on for subscribing, and two overloaded off methods for unsubscribing specific callbacks or all callbacks. The documentation specifies system capabilities, version requirements starting from version 12, and includes practical code examples demonstrating event subscription, emission, and unsubscription within an EventHub context. Targeted at application developers using the AbilityKit framework, this guide explains how to implement event-driven logic with single-argument callbacks using EventCallBack1 interfaces.

### class_Event2.md class_Event2.md
This document provides API reference documentation for the Event2 class, a generic event center designed to handle events with two parameters in the Cangjie language. It covers the class definition, which inherits from EventBase, and details four primary methods: emit, on, and two overloads of off. Key functionality includes triggering events with two generic arguments via emit, subscribing to events using on, and unsubscribing specific callbacks or all listeners via off. The documentation specifies system capabilities and version requirements, offering code examples for implementation. This reference targets developers using the AbilityKit who need to manage event-driven logic involving dual-parameter callbacks.

### class_EventCallBack0_4more.md class_EventCallBack0_4more.md
This document serves as API reference documentation for abstract callback event classes in the Cangjie programming language. It details four classes: EventCallBack0, EventCallBack1, EventCallBack2, and EventCallBack3, designed to handle events with zero to three generic parameters respectively. Key information includes the abstract `invoke` method for each class, which triggers the callback by accepting arguments corresponding to the specific parameter count. The document provides Cangjie code examples demonstrating how to inherit from these classes and override the invoke method. This content is intended for developers using the AbilityKit framework to implement event-driven logic within system capabilities.

### class_EventCallBack4_2more.md class_EventCallBack4_2more.md
This API reference document defines abstract classes for handling callback events with four and five generic parameters in the Cangjie programming language. It details the `EventCallBack4` and `EventCallBack5` classes, specifically their `invoke` methods, parameter definitions, and system capabilities. Key information includes that both classes require overriding the `invoke` function to execute logic when events are triggered, utilizing generic types A1 through A5. These components belong to the SystemCapability.Ability.AbilityRuntime.Core module starting from version 12. This guide targets Cangjie developers implementing event-driven logic within the AbilityKit framework.

### ohoseventhub_3more.md ohoseventhub_3more.md
This document serves as an API reference for the ohos.eventhub module, which enables event management through subscription, unsubscription, and triggering capabilities. It covers the module import statement using kit.AbilityKit and provides specific usage instructions for running example code within Cangjie template projects. Key takeaways include the requirement to configure Context in main_ability.cj for certain examples and the distinction between running code in index.cj. The target audience is HarmonyOS developers using the Cangjie language who need to implement event handling in their applications.

### class_Event3/
This directory contains comprehensive documentation for the `Event3` class, a core component of the Cangjie programming language's AbilityKit framework. It specifically addresses the implementation of a generic event center capable of managing events that require three distinct parameters. The documentation details the class architecture, including its inheritance from `EventBase`, and provides a complete API reference for essential lifecycle methods such as `emit`, `on`, and `off`. Furthermore, it specifies necessary interfaces like `EventCallBack3` and relevant system capabilities. This resource is primarily suitable for application developers and system engineers who need to build robust, event-driven communication channels within an `EventHub` using the `SystemCapability.Ability.AbilityRuntime.Core`. It serves as a practical guide for ensuring proper event handling and callback management in complex applications.

### class_Event4/
This directory provides comprehensive API reference documentation for the Cangjie `Event4` class and its associated subscription functionality within the AbilityKit framework. It specifically addresses event handling scenarios that require four parameters, offering detailed insights into class definitions, inheritance structures, and event management methods. The documentation covers essential operations such as triggering events via the `emit` method, removing subscriptions using `off` methods, and registering callbacks through the `on` function. Key technical aspects include generic type parameters A1 through A4 and dependencies on the `AbilityRuntime.Core` system capability. This resource is designed for Cangjie developers and system architects who need to implement robust, multi-argument event-driven logic in their applications using the EventHub context.

### class_Event5/
This directory contains comprehensive API reference documentation for the Cangjie `Event5` class and its associated subscription function within the AbilityKit framework. It specifically covers the management of events that require five generic parameters (A1 through A5). The documentation details the class definition, inheritance from `EventBase`, and methods for triggering (`emit`) and unsubscribing (`off`) from events. Additionally, it explains the subscription mechanism using the `on` function and `EventCallBack5`. Key topics include event-driven programming, generic type support, and integration with `EventHub`. This resource is suitable for Cangjie developers implementing complex event handling logic in applications requiring SystemCapability.Ability.AbilityRuntime.Core (version 12 or later). It serves as a technical guide for building robust, event-based architectures in Cangjie applications.

### class_EventBase/
This directory contains API reference documentation for the `EventBase` abstract class within the Cangjie AbilityKit, focusing on event handling and type validation mechanisms. It details methods for validating event parameter counts (from 0 to 5 arguments) and returning corresponding Event objects like `Event0` through `Event5`. The documentation includes syntax details, return types, potential `BusinessException` errors, and practical Cangjie code examples for registering callbacks and emitting events via `EventHub`. It is intended for developers building applications using the HarmonyOS AbilityRuntime Core who require knowledge of generic types and event-driven programming.

### class_EventHub/
This directory contains API reference documentation for the `EventHub` class within the Cangjie programming language and the AbilityKit framework. It focuses on event-driven architecture, providing developers with tools to manage, retrieve, and emit callback events. The documentation covers methods for obtaining event centers and handling events with varying numbers of generic parameters (from zero to five). Key topics include type-safe event handling, generic programming, and the `EventCallBack` interface. This resource is suitable for software developers building HarmonyOS applications who need to implement robust event communication mechanisms using the AbilityRuntime Core system.