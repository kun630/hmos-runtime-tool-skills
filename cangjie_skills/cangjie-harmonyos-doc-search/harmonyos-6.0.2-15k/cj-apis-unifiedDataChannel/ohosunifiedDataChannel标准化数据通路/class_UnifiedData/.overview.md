# class_UnifiedData

This directory contains essential API reference documentation for the `UnifiedData` class within the Unified Data Management Framework (UDMF), specifically tailored for HarmonyOS developers using the Cangjie language. It covers the complete lifecycle of the `UnifiedData` object, including its definition, metadata management (such as timestamps), and initialization processes. The documentation details functional methods for inserting data via `addRecord` and retrieving `UnifiedRecord` objects via `getRecords`. Additionally, it includes references for inspecting data types using `getTypes` and `hasType`. This resource is suitable for developers who need to implement or interact with the UDMF Core system capability, requiring a solid understanding of basic data types and the UDMF architecture to effectively manage unified data objects.

## Quick Navigation

**What do you want to learn?**

*   **Understand the Core Class:** View the class definition, properties, and initialization methods. → class_UnifiedData_6more.md
*   **Manipulate Data Records:** Learn how to insert new records or retrieve existing ones from the object. → class_UnifiedData_6more.md
*   **Inspect Data Types:** Find out how to retrieve all data types or check for a specific type existence. → func_getTypes_2more.md

## Detailed Description

### class_UnifiedData_6more.md class_UnifiedData_6more.md
This document provides API reference documentation for the UnifiedData class in the Cangjie language, designed for the UDMF Core system capability. It covers the class definition, the properties attribute for managing metadata like timestamps, and initialization methods. Key sections detail functional methods including addRecord for inserting data and getRecords for retrieving an array of UnifiedRecord objects. The class encapsulates data records, allowing developers to manage metadata and manipulate records within a unified object. Targeted at HarmonyOS developers using Cangjie, this guide requires understanding of basic data types and the UDMF architecture.

### func_getTypes_2more.md func_getTypes_2more.md
This API reference document details methods for inspecting data types within Unified Data Management Framework (UDMF) objects using the Cangjie language. It covers two primary functions: getTypes(), which retrieves an array of all data record types, and hasType(String), which checks for the existence of a specific data type. Key information includes return types Array<String> and Bool, the requirement for SystemCapability.DistributedDataManager.UDMF.Core, and availability starting from API version 19. The document targets HarmonyOS developers managing unified data, providing practical code examples for implementation.