# 手势响应控制

This directory focuses on advanced gesture response control mechanisms within the ArkUI framework, specifically addressing complex scenarios like nested scrolling and gesture conflicts. It covers technical implementations using ArkTS and Cangjie, detailing APIs such as `shouldBuiltInRecognizerParallelWith` and `onGestureRecognizerJudgeBegin` to manage gesture arbitration, parallelism, and dynamic state enabling. The content is designed for developers building sophisticated user interfaces on HarmonyOS or OpenHarmony who need to resolve interaction issues between overlapping scrollable components and customize gesture behaviors.

## Quick Navigation

**What do you want to learn?**

*   Understand the core API and workflow for gesture control → 手势响应控制_1.md
*   Implement nested scrolling logic in Cangjie → 手势响应控制_2.md
*   Resolve gesture conflicts using ArkTS parallel gestures → 手势响应控制_3.md
*   Handle dynamic switching between outer and inner containers → 手势响应控制_4.md
*   Manage state and offsets for child-to-parent control passing → 手势响应控制_5.md
*   Configure basic UI layout and styling for gesture components → 手势响应控制_6.md

## Detailed Description

### 手势响应控制_1.md 手势响应控制_1.md
This technical guide explains how to control gesture callback responses via API after successful recognition, specifically for complex scenarios like nested scrolling. It covers business and monitoring gesture workflows, setting parallel gestures, and dynamic enabling/disabling of gestures. Key APIs include `shouldBuiltInRecognizerParallelWith` for parallelism and `onGestureRecognizerJudgeBegin` for interception and state control. A Cangjie code example demonstrates managing nested Scroll components by synchronizing outer and inner PanGestures based on boundary conditions. This is intended for developers using the ArkUI framework who need advanced gesture handling capabilities.

### 手势响应控制_2.md 手势响应控制_2.md
This document provides a technical code example in Cangjie demonstrating how to resolve gesture conflicts in nested Scroll components. It details the implementation of onGestureRecognizerJudgeBegin and parallelGesture to dynamically manage gesture recognizer states. Key takeaways include using scroll position (begin/end) and pan direction (offsetY) to toggle the setEnabled status of child and current recognizers, thereby controlling which component scrolls. This guide targets developers building complex UIs who need to implement custom logic for nested scrolling and gesture priority handling.

### 手势响应控制_3.md 手势响应控制_3.md
This document provides a code example demonstrating how to manage gesture conflicts in nested scrollable components using ArkTS. The content focuses on implementing the `shouldBuiltInRecognizerParallelWith` method to control gesture arbitration between an outer and inner scroll container. Key information includes logic to identify Pan gestures on the inner scroll target, enabling parallel execution with the built-in recognizer. The code also illustrates how to store references to current and child recognizers for further processing. This technical reference is intended for developers building complex user interfaces on HarmonyOS or OpenHarmony who need to resolve nested scrolling interaction issues. Keywords: ArkTS, nested scrolling, gesture recognizer, parallel gestures, event arbitration.

### 手势响应控制_4.md 手势响应控制_4.md
The document provides a code implementation for managing gesture response conflicts, specifically focusing on nested scrolling scenarios. It covers the logic for dynamically switching control between an outer container and an inner scrollable target based on scroll position and pan direction. Key takeaways include using GestureJudgeResult.CONTINUE and conditionally setting setEnabled on recognizers to handle boundary conditions like isEnd and isBegin. This technical reference is intended for developers using ArkTS to build complex user interfaces requiring precise gesture coordination.

### 手势响应控制_5.md 手势响应控制_5.md
This document provides a code example for managing gesture conflicts between nested scrollable components. It covers the logic for dynamically enabling or disabling gesture recognizers based on scroll direction and boundary states. Key information includes checking GestureRecognizerState and ScrollableTargetInfo to determine if a scroll target is at the beginning or end. The code calculates vertical offsets to decide whether to pass control from a child to a parent scroll view, ensuring smooth user interaction. This technical reference is intended for developers implementing complex scrolling behaviors in the ArkUI framework, specifically dealing with parallel gestures and nested scroll views.

### 手势响应控制_6.md 手势响应控制_6.md
This document functions as a technical guide or tutorial segment focused on gesture response control within a user interface framework. It covers the implementation of UI component properties, specifically focusing on layout dimensions and visual styling. The key information includes a code example demonstrating how to set a component's height to 100 percent and apply a background color (0xDCDCDC) using declarative syntax. This material is intended for developers familiar with declarative UI programming who need to configure component appearance and layout behaviors, likely within the context of handling touch or gesture events. Keywords: gesture response, UI layout, declarative syntax, component styling, ArkUI.