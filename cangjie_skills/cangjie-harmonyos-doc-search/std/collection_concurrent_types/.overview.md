# collection_concurrent_types

This directory serves as a specialized documentation resource for the Cangjie programming language, specifically addressing the `collection_concurrent_types` module. It focuses on the API reference for deprecated type aliases found within the concurrent collection package. The content primarily covers the definitions, current status, and future removal plans for `BlockingQueue<E>` and `NonBlockingQueue<E>`, clarifying their relationship to `LinkedBlockingQueue<E>` and `ConcurrentLinkedQueue<E>`. By highlighting the deprecation warnings, this guide aims to assist developers in refactoring code to use direct class implementations, thereby preventing future compilation errors. This directory is essential for Cangjie developers, particularly those maintaining legacy systems or modernizing concurrent codebases, who need a solid grasp of generic types and concurrent programming patterns to ensure long-term code stability and compliance with the latest API standards.

## Quick Navigation

*   **What do you want to learn?**
    *   Understand the specific deprecation status of concurrent type aliases → collection_concurrent_types.md
    *   Identify the correct replacement classes for legacy queue types → collection_concurrent_types.md
    *   Review the complete API reference for `BlockingQueue` and `NonBlockingQueue` → collection_concurrent_types.md
*   **What do you want to do?**
    *   Update legacy codebases to remove deprecated type usage → collection_concurrent_types.md
    *   Verify the exact mapping between old aliases and new implementations → collection_concurrent_types.md
    *   Prepare your project for future versions by eliminating deprecated dependencies → collection_concurrent_types.md

## Detailed Description

### collection_concurrent_types.md collection_concurrent_types.md

This file acts as the central API reference for the directory, documenting the deprecated type aliases within the Cangjie concurrent collection package. It provides a detailed technical breakdown of two specific type aliases: `BlockingQueue<E>` and `NonBlockingQueue<E>`. The document explicitly clarifies that these aliases are merely shorthand references for the concrete classes `LinkedBlockingQueue<E>` and `ConcurrentLinkedQueue<E>`, respectively.

A critical focus of this file is the deprecation notice. It warns developers that these aliases are obsolete and scheduled for removal in upcoming versions of the language or library. Consequently, the document urges immediate action to refactor existing code by replacing the aliases with their direct class implementations. This resource is specifically tailored for Cangjie developers who are currently maintaining legacy codebases or performing updates to concurrent collection usage, requiring them to possess knowledge of generic types and concurrent programming concepts to successfully apply the necessary changes and ensure their projects remain future-proof.