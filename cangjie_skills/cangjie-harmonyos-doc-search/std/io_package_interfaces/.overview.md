# io_package_interfaces

This directory serves as the central repository for API reference documentation concerning input/output stream interfaces within the Cangjie language's `io` package. It comprehensively covers four primary interfaces: `InputStream`, `OutputStream`, `IOStream`, and `Seekable`. The documentation details essential operations such as the `read` method for input streams, `write` and `flush` methods for output streams, and `seek` functionality for managing cursors via properties like `length` and `position`. Additionally, it defines `IOStream` as a composite interface that merges input and output capabilities. This documentation is tailored for software developers responsible for implementing or consuming stream-based IO operations, necessitating a strong grasp of Cangjie syntax and type systems. Core keywords for this section include Cangjie, API, interfaces, InputStream, OutputStream, Seekable, IOStream, byte array, and cursor.

## Quick Navigation

What do you want to learn?

*   Review the complete API reference for IO interfaces → io_package_interfaces.md
*   Learn about input stream definitions and the read method → io_package_interfaces.md
*   Understand output stream definitions, write, and flush methods → io_package_interfaces.md
*   Explore seekable streams, cursor management, and position properties → io_package_interfaces.md
*   Investigate the composite IOStream interface structure → io_package_interfaces.md

## Detailed Description

### io_package_interfaces.md io_package_interfaces.md
This document provides API reference documentation for input/output stream interfaces within the Cangjie language's io package. It covers four main interfaces: InputStream, OutputStream, IOStream, and Seekable. Key definitions include the read method for input, write and flush methods for output, and seek functionality with properties like length and position for cursor management. IOStream is defined as a composite of input and output capabilities. The target audience is software developers implementing or consuming stream-based IO operations, requiring knowledge of Cangjie syntax and type systems. Keywords: Cangjie, API, interfaces, InputStream, OutputStream, Seekable, IOStream, byte array, cursor.