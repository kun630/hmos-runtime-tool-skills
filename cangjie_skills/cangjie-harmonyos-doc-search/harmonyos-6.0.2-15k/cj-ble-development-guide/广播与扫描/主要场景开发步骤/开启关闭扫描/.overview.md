# 开启关闭扫描

This directory focuses on implementing and managing Bluetooth Low Energy (BLE) scanning functionality using the Cangjie language within the HarmonyOS development framework. It covers the complete lifecycle of device discovery, including enabling Bluetooth, configuring scan filters and options, and managing the start and stop processes. Additionally, it provides technical references for parsing raw BLE advertising data packets to extract specific information like service UUIDs, local names, and manufacturer data. This content is suitable for HarmonyOS developers and software engineers looking to integrate robust device discovery and data interpretation capabilities into their applications.

## Quick Navigation

**What do you want to do?**

*   Implement the core BLE scanning lifecycle and configuration → 开启关闭扫描_1.md
*   Parse raw advertising data packets to extract specific fields → 开启关闭扫描_2.md
*   Handle scan result callbacks and convert byte arrays to UUIDs → 开启关闭扫描_3.md

## Detailed Description

### 开启关闭扫描_1.md 开启关闭扫描_1.md
This document serves as a technical tutorial for implementing Bluetooth Low Energy (BLE) scanning functionality using the Cangjie language. It outlines the necessary prerequisites, such as enabling Bluetooth and specific system capabilities, before presenting a step-by-step code example. The core content features a BleScanManager class that demonstrates how to subscribe to scan results, configure scan filters and options, and manage the scanning lifecycle by starting and stopping the process. Key takeaways include the use of ScanFilter for manufacturer data matching and ScanOptions for setting scan modes. This guide is intended for HarmonyOS developers seeking to integrate device discovery capabilities into their applications.

### 开启关闭扫描_2.md 开启关闭扫描_2.md
This document provides a code implementation for parsing Bluetooth Low Energy (BLE) advertising data packets. The main content details a parsing loop that iterates through raw byte arrays to identify and extract specific data types, including flags, local names, TX power levels, and various service UUIDs. Key takeaways include the use of helper functions to handle 16-bit, 32-bit, and 128-bit UUIDs, as well as manufacturer-specific data. This technical reference is designed for developers working on BLE connectivity who need to interpret scan results, requiring a solid understanding of data structures and the BLE advertising format.

### 开启关闭扫描_3.md 开启关闭扫描_3.md
This document serves as a technical reference for parsing Bluetooth Low Energy (BLE) scan data within a development framework. It details helper functions for converting byte arrays to standard UUID strings (16, 32, and 128-bit) and extracting service or manufacturer-specific data from raw packets. The code includes a callback class, ScanResultCb, which demonstrates how to handle incoming scan results, log device IDs, and trigger data parsing. Additionally, it directs developers to the Bluetooth service subsystem error codes for troubleshooting. This content is intended for software developers implementing BLE scanning functionality who need to process and interpret raw advertising data.