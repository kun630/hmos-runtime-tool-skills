# 拉起快递类应用startAbilityByType

This directory provides comprehensive technical guidance for integrating express delivery capabilities into HarmonyOS applications using the `startAbilityByType` API. It covers the complete interaction workflow, addressing both the caller side and the target side of the process. Key topics include invoking the system-level express delivery extension panel, constructing JSON parameters (such as expressNo and sceneType), and handling callbacks to display app selection. Additionally, it details how to configure a HarmonyOS app to receive and process these feature-based requests via `module.json5` and URI schemes. This content is specifically suitable for HarmonyOS developers using the Cangjie language and the AbilityKit framework who need to implement cross-app tracking features or deep linking functionality.

## Quick Navigation

**What do you want to learn?**

*   **Invoke the express delivery panel** → 拉起快递类应用startAbilityByType_3more.md startAbilityByType API usage, JSON parameter construction, and Cangjie code examples
*   **Configure the target application** → 目标方开发步骤.md module.json5 setup, linkFeature definition, and parsing incoming Want parameters

## Detailed Description

The following documents provide in-depth technical instructions for implementing the express delivery invocation flow.

### 拉起快递类应用startAbilityByType_3more.md 拉起快递类应用startAbilityByType_3more.md
This technical guide explains how to invoke the express delivery application extension panel using the startAbilityByType API. It covers parameter specifications for the "express" type, detailing required fields like expressNo and optional sceneType, and provides a step-by-step development tutorial with Cangjie code examples. The document demonstrates how to construct the JSON parameters and handle callbacks to display a selection panel of available express apps for tracking numbers. It targets application developers working with the AbilityKit framework who need to integrate express tracking capabilities into their apps.

### 目标方开发步骤.md 目标方开发步骤.md
This guide outlines the steps for configuring a HarmonyOS application to act as a target for specific feature-based URI requests. It covers configuring the module.json5 file with linkFeature and URI attributes, and parsing parameters within the UIAbility lifecycle methods. Key takeaways include defining the linkFeature (e.g., QueryExpress) and URI scheme to match incoming Want objects, then extracting parameters like expressNo to dynamically load specific pages. This tutorial targets HarmonyOS developers using the Cangjie language who need to implement deep linking or feature-based app invocation.