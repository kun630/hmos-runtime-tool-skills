# class_VerifyStatement

This directory contains essential API documentation for the `VerifyStatement` class, a core component of the Cangjie unit testing framework focused on mocking and verification. It provides detailed guidance on verifying the execution frequency of stub signatures within mock objects. The documentation covers the class definition, creation constraints enforced by the `@Called` macro, and various member functions such as `atLeastOnce`, `atLeastTimes`, `once`, and `times`. It also explains critical exception handling for `MockFrameworkException` and `IllegalArgumentException`. This resource is specifically designed for developers and software engineers who are writing unit tests and need to rigorously validate mock behavior and ensure specific functions are called the correct number of times.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition, creation constraints via the @Called macro, and basic verification methods like `atLeastOnce` and `once` → class_VerifyStatement_5more.md
*   Learn about advanced frequency checks, range validation using `times`, the `never` assertion, and internal static constructors → func_timesInt64_Int64_3more.md

## Detailed Description

### class_VerifyStatement_5more.md class_VerifyStatement_5more.md
This API documentation defines the VerifyStatement class, used in unit testing to verify the execution frequency of stub signatures within a mock framework. The document details the class definition, creation constraints via the @Called macro, and four key member functions: atLeastOnce, atLeastTimes, once, and times. It explains parameters, return values, and specific exceptions like MockFrameworkException and IllegalArgumentException. Key takeaways include that execution counts can only be specified once per object; subsequent calls trigger exceptions. If no count is specified, the framework applies defaults based on the verification action context. This reference guide targets developers writing unit tests using this specific mock framework, requiring knowledge of mocking concepts and the @Called macro.

### func_timesInt64_Int64_3more.md func_timesInt64_Int64_3more.md
This document serves as API reference documentation for the Cangjie unit testing framework, specifically detailing methods within the VerifyStatement class used for mocking verification. It covers three primary functions: times, which validates that a stub signature executes within a specified minimum and maximum range; never, a shorthand to assert a statement is never executed; and fromStub, an internal static constructor intended for framework use only. Key information includes parameter definitions, return types, and potential exceptions like MockFrameworkException. The content targets developers implementing unit tests who need to verify mock object behavior and execution frequencies.