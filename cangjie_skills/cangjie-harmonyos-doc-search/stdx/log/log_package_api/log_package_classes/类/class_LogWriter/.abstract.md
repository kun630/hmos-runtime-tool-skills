本目录包含仓颉语言中 `LogWriter` 抽象类及相关函数的 API 参考文档，主要涉及将对象序列化至日志输出目标的核心能力。内容涵盖了用于构建日志结构的 `start`/`end` 方法，以及写入 `Bool`、`Int64`、`Float64`、`String` 等多种数据类型的 `write` 方法。文档详细说明了 `LogWriter` 与 `LogValue` 接口的配合使用，以及在状态异常时抛出 `IllegalStateException` 的错误处理机制。核心关键词包括仓颉语言、日...