# struct_TupleWrapper2T0_T1

This directory provides comprehensive API documentation for the Cangjie generic struct `TupleWrapper2<T0, T1>`, designed to encapsulate and manage two-element tuples effectively within the Cangjie programming environment. It covers the fundamental struct definition, including its constructor and the `apply` method for executing functions on stored data. Furthermore, the documentation details essential extensions that implement standard interfaces like `ToString` for string conversion, `Equatable` for equality checks, and `IndexAccess` for element retrieval. Additionally, it includes specialized extensions for property-based testing frameworks, specifically the `Arbitrary` and `Shrink` interfaces, which enable random value generation and test case simplification. This resource is ideal for Cangjie developers seeking to implement structured tuple manipulation, ensure interface compliance, or build robust property-based testing frameworks involving generic types.

## Quick Navigation

**What do you want to learn?**

*   Explore the core struct definition, constructor, and the `apply` method for tuple manipulation → struct_TupleWrapper2T0_T1_7more.md
*   Understand how to implement standard interfaces like `ToString`, `Equatable`, and `IndexAccess` → struct_TupleWrapper2T0_T1_7more.md
*   Learn about property-based testing extensions for random generation and shrinking → extendT0_T1_TupleWrapper2T_2more.md

## Detailed Description

### extendT0_T1_TupleWrapper2T_2more.md extendT0_T1_TupleWrapper2T_2more.md
This API documentation details the implementation of the Arbitrary and Shrink interfaces for the TupleWrapper2 struct within a property-based testing framework. It covers two primary extensions: one enabling random value generation and another facilitating value reduction for testing purposes. Key information includes the static arbitrary function, which returns a Generator for creating random TupleWrapper2 instances, and the shrink function, which returns an Iterable for simplifying test cases. Both implementations require generic types T0 and T1 to satisfy their respective interface constraints. This reference is intended for developers using the Cangjie language to implement property-based tests involving two-element tuples.

### struct_TupleWrapper2T0_T1_7more.md struct_TupleWrapper2T0_T1_7more.md
This document serves as API reference for the Cangjie generic struct TupleWrapper2<T0, T1>, which encapsulates a two-element tuple. It covers the struct definition, constructor, and the apply method for executing functions on the stored tuple. Additionally, it details extensions implementing standard interfaces: ToString for string representation, Equatable for equality comparisons, and IndexAccess for retrieving elements by index. Key takeaways include the ability to apply closures to the tuple and perform dynamic access. This guide is intended for Cangjie developers requiring structured tuple manipulation and interface compliance.