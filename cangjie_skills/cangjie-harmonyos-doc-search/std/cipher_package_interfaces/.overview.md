# cipher_package_interfaces

This directory serves as the definitive source for API reference documentation concerning the `BlockCipher` interface within the Cangjie programming language. It is designed to provide a comprehensive specification of the standard contract that governs block encryption and decryption algorithms, ensuring interoperability and correctness across various cryptographic implementations. The documentation meticulously details the interface's mandatory properties, such as `algorithm` identification and `blockSize` parameters, which are crucial for proper cipher configuration. Additionally, it outlines the required method signatures for data transformation, specifically highlighting two distinct overloads for cryptographic operations: one optimized for ease of use by returning a new byte array, and another designed for high-performance scenarios by writing directly to a provided output buffer and returning the data length. This resource is indispensable for developers who are implementing low-level cryptographic primitives or integrating high-level security libraries, requiring a strong understanding of Cangjie syntax, array handling, and memory management. It is suitable for systems programmers and security engineers looking to build robust, standards-compliant encryption tools.

## Quick Navigation

*   **What do you want to learn?**
    *   Understand the foundational `BlockCipher` interface contract → cipher_package_interfaces.md
    *   Review specific interface properties like `algorithm` and `blockSize` → cipher_package_interfaces.md
    *   Examine the required method signatures for `encrypt` and `decrypt` → cipher_package_interfaces.md
    *   Compare the two overloads for cryptographic operations (new array vs. buffer) → cipher_package_interfaces.md
    *   Learn about the target audience and prerequisites for this documentation → cipher_package_interfaces.md

## Detailed Description

### cipher_package_interfaces.md cipher_package_interfaces.md
This document provides the complete API reference documentation for the BlockCipher interface within the Cangjie programming language. Its primary purpose is defining the standard contract for block encryption and decryption algorithms. The content details the interface's properties, specifically `algorithm` and `blockSize`, and outlines the required method signatures for `encrypt` and `decrypt`. Key information includes two distinct overloads for cryptographic operations: one that returns a new byte array and another that writes to a provided output buffer, returning the data length. This reference is intended for developers implementing or utilizing cryptographic primitives, assuming familiarity with Cangjie syntax and array handling. By adhering to this specification, developers ensure their implementations are compatible with the broader Cangjie cryptographic ecosystem, facilitating secure and efficient data processing in various applications.