# class_Zip

This directory contains API reference documentation for the Zip class and related functions within the Cangjie language's Zlib subsystem. It covers data compression and decompression capabilities, including stream-based operations, buffer management, and resource allocation. Key topics include functions like `compress`, `uncompress`, `deflate`, and `inflate`, along with initialization and termination routines for compression streams. The documentation details parameters, return values, and exception handling for various error codes. This resource is suitable for Cangjie developers implementing file or data compression features who need precise technical specifications and code examples for the SystemCapability.BundleManager.Zlib framework.

## Quick Navigation

**What do you want to do?**

*   Perform basic buffer compression or decompression?
    *   Use the Zip class or `compress2` → class_Zip_2more.md, func_compress2ArrayByte_ArrayByte_Compres_d7edafae.md
    *   Decompress data to a destination buffer → func_uncompressArrayByte_ArrayByte_Int64.md
*   Manage stream-based compression workflows?
    *   Initialize compression streams → func_deflateInit2ZStream_CompressLevel_Co_95f3ce75.md
    *   Perform stream compression → func_compressBoundInt32_2more.md
    *   Free compression resources → func_deflateEndZStream.md
*   Manage stream-based decompression workflows?
    *   Retrieve or initialize streams → func_getZStream_2more.md, func_inflateInit2ZStream_Int32.md, func_inflateInitZStream.md
    *   Perform stream decompression → func_getZStream_2more.md
    *   Free decompression resources → func_inflateEndZStream.md
*   Calculate compression size limits?
    *   Estimate buffer size requirements → func_compressBoundInt32_2more.md

## Detailed Description

### class_Zip_2more.md class_Zip_2more.md
This document serves as API reference documentation for the Zip class in the Cangjie language, focusing on data compression and decompression capabilities. It details the `compress` function, which transfers data from a source buffer to a destination buffer and returns a `ZipOutputInfo` object containing the operation status and output size. Key sections include parameter definitions, return value specifications, and exception handling for errors like buffer issues or invalid arguments. The document also provides a practical code example demonstrating buffer initialization and compression execution. This technical guide is intended for developers utilizing the SystemCapability.BundleManager.Zlib to implement file or data compression features in their applications.

### func_compress2ArrayByte_ArrayByte_Compres_d7edafae.md func_compress2ArrayByte_ArrayByte_Compres_d7edafae.md
This document provides API reference documentation for the `compress2` function in the Cangjie Zlib subsystem, detailing how to compress source data into a destination buffer. It covers the function signature, system capabilities, parameter descriptions including the optional sourceLen, return values of type `ZipOutputInfo`, and specific error codes for `BusinessException` and `IllegalArgumentException`. Key information includes the requirement for destination and source byte arrays and a compression level, returning status and size. It handles errors like ZStream and Buffer errors and includes a practical code example demonstrating compression and subsequent uncompression.

### func_compressBoundInt32_2more.md func_compressBoundInt32_2more.md
This API reference document details two specific functions within the Zlib subsystem for the Cangjie programming language, focusing on data compression operations. It covers the `compressBound` function for calculating compression size limits and the `deflate` function for stream-based compression, including syntax, parameters, return values, and exception handling. Key takeaways include the requirement for SystemCapability.BundleManager.Zlib, specific error codes like internal structure errors, and the use of structures like `ZStream` and enums like `CompressFlushMode`. Targeted at Cangjie developers, the document provides code examples to demonstrate practical implementation of these compression utilities.

### func_deflateEndZStream.md func_deflateEndZStream.md
This document serves as API reference documentation for the Cangjie function `deflateEnd`, which frees dynamically allocated data structures for a compression stream. The content details the function signature, system capabilities, parameters, and return values. It also lists specific exceptions, such as `BusinessException` with error codes for stream or internal structure errors, and `IllegalArgumentException` for parameter validation. A practical code example demonstrates the function's integration with other Zlib operations like `deflateInit2` and `deflate`. This guide targets Cangjie developers implementing compression workflows who need to ensure proper resource deallocation.

### func_deflateInit2ZStream_CompressLevel_Co_95f3ce75.md func_deflateInit2ZStream_CompressLevel_Co_95f3ce75.md
This API reference document details the `deflateInit2` function used to initialize internal stream states for compression within the Cangjie Zlib subsystem. The content covers the function signature, system capabilities, detailed parameter descriptions including compression levels and strategies, return status codes, and potential exceptions like `BusinessException`. Key takeaways include specific error codes such as 17800004 and the necessity of validating input parameters to avoid `IllegalArgumentException`. A practical Cangjie code example demonstrates the initialization workflow alongside `deflate` and `deflateEnd`. This guide targets Cangjie developers implementing data compression features who require precise parameter configuration and error handling details.

### func_getZStream_2more.md func_getZStream_2more.md
This API reference document details two specific functions within the Zlib subsystem for the Cangjie language: retrieving a zlib stream and decompressing data. The file is divided into documentation for `getZStream()` and `inflate()`, each containing function signatures, descriptions, system capabilities, parameters, return values, and potential exceptions. Key information includes that `getZStream()` returns a `ZStream` object for output, while `inflate()` accepts a stream and flush mode to decompress data, returning a `ReturnStatus`. Both functions list specific error codes, such as internal structure errors, and include practical Cangjie code examples demonstrating compression and decompression workflows.

### func_inflateEndZStream.md func_inflateEndZStream.md
This API reference document describes the `inflateEnd` function in the Cangjie language, designed to free all dynamically allocated data structures for a decompression stream. The documentation outlines the function signature, system capabilities, input parameters, and return status codes. It details potential exceptions, specifically `BusinessException` for ZStream or internal errors and `IllegalArgumentException` for parameter validation failures. A practical code example illustrates the function's usage within a complete compression and decompression workflow. This guide is intended for developers working with the Zlib subsystem who need to manage memory resources during data decompression tasks.

### func_inflateInit2ZStream_Int32.md func_inflateInit2ZStream_Int32.md
This document provides API reference documentation for the `inflateInit2` function in the Cangjie language, designed to initialize internal stream state for decompression. The content covers the function signature, system capabilities, parameters including `ZStream` and windowBits, and return values. It details potential exceptions like `BusinessException` and `IllegalArgumentException` with specific error codes. A practical code example demonstrates the function's usage within a compression and decompression workflow. Key takeaways include the importance of parameter validation and error status handling. This guide targets Cangjie developers working with the Zlib subsystem who need to implement data decompression, requiring prior knowledge of stream processing and error management.

### func_inflateInitZStream.md func_inflateInitZStream.md
This API reference document details the Cangjie function `inflateInit`, which initializes internal stream states for decompression operations. The content covers the function signature, system capabilities, required parameters (specifically the `ZStream` object), return values, and potential exceptions such as `BusinessException` and `IllegalArgumentException`. Key takeaways include the specific error codes for troubleshooting and the requirement for a valid `ZStream` input. A practical code example demonstrates the function's integration within a broader compression and decompression workflow. This documentation is intended for Cangjie developers utilizing the Zlib subsystem to implement data decompression features in their applications.

### func_uncompressArrayByte_ArrayByte_Int64.md func_uncompressArrayByte_ArrayByte_Int64.md
This document provides API reference documentation for the `uncompress` function in the Cangjie language, designed to decompress data from a source buffer into a destination buffer. It details the function signature, system capabilities, and parameters including the destination array, source array, and optional source length. The documentation explains the return type `ZipOutputInfo`, which contains status and size, and lists potential exceptions such as `BusinessException` for zlib errors and `IllegalArgumentException` for invalid parameters. A practical code example illustrates how to use the function in conjunction with compression. This guide targets Cangjie developers utilizing the Zlib subsystem for data manipulation, requiring familiarity with byte arrays and error handling.