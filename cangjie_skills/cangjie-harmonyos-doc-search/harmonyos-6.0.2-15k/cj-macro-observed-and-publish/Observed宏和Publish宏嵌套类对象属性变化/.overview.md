# Observed宏和Publish宏嵌套类对象属性变化

This directory provides a comprehensive technical guide on using the `@Observed` and `@Publish` macros in the Cangjie programming language to manage state within nested class objects and arrays. It addresses the complexities of two-way data binding and reactive UI updates that go beyond simple state management. The content covers implementation specifications, behavioral mechanics, common troubleshooting issues, and specific usage constraints. It is designed for Cangjie developers and ArkUI framework users who need to build responsive user interfaces with complex data models and deep object hierarchies.

## Quick Navigation

- **What do you want to do?**
    - Learn how to implement @Observed and @Publish for nested objects → Observed宏和Publish宏嵌套类对象属性变_4more.md
    - Understand the internal rendering behavior and dependency tracking → 观察变化和行为表现.md
    - Troubleshoot issues with constructors or UI refresh triggers → 常见问题.md
    - Check the specific limitations regarding inheritance and variable types → 限制条件_2more.md

## Detailed Description

### Observed宏和Publish宏嵌套类对象属性变_4more.md Observed宏和Publish宏嵌套类对象属性变_4more.md
This technical guide explains the usage of @Observed and @Publish macros in the Cangjie language to enable observation of property changes within nested class objects and arrays, overcoming the limitations of simple state management macros. The document covers an overview of the two-way data binding mechanism, detailed specifications for the @Observed class macro and @Publish variable macro, and rules for variable initialization and transfer between parent and child components. Key takeaways include that @Observed marks classes for internal observation, while @Publish decorates variables in child components to receive instances for binding, with specific constraints on inheritance and initialization. This guide targets Cangjie developers familiar with basic state management macros like @State who need to manage complex data models.

### 常见问题.md 常见问题.md
This troubleshooting guide addresses common issues regarding state management and UI updates in Cangjie development. It covers restrictions on @Observed class constructors, conditions for @Publish to trigger UI refreshes, and handling nested custom type changes. Key takeaways include that @Observed classes cannot define custom constructors and must use generated ones. Furthermore, to ensure UI updates propagate through nested objects, both the parent and child classes require @Observed decoration, and specific member variables need @Publish. This document targets Cangjie developers building reactive user interfaces.

### 观察变化和行为表现.md 观察变化和行为表现.md
This technical guide explains how to implement and manage state observation in Cangjie-based UI development using decorators. It covers observing changes in nested objects and arrays, handling DateTime updates, and the framework's internal rendering behavior. Key takeaways include that nested classes require the @Observed decorator to track property changes, while arrays should use ObservedArray. The document details how the framework tracks dependencies via getter and setter functions to trigger UI updates automatically. Intended for developers building reactive user interfaces, this reference material clarifies the mechanics behind state management and component re-rendering.

### 限制条件_2more.md 限制条件_2more.md
This reference document outlines specific constraints and usage scenarios for the @Observed and @Publish decorators in the Cangjie programming language within the ArkUI framework. The content covers five main limitations regarding class inheritance, constructor definitions, variable types, and initialization requirements, followed by a practical code example demonstrating nested custom types. Key takeaways include that @Observed classes cannot inherit or define custom constructors, and @Publish only works on var members of @Observed classes to trigger UI updates. Targeted at Cangjie developers, this guide ensures proper implementation of reactive state management in nested object structures.