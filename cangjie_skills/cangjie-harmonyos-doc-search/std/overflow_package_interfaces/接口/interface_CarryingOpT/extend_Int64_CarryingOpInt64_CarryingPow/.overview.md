# extend_Int64_CarryingOpInt64_CarryingPow

This directory contains comprehensive API documentation for extending the Int64 type within the Cangjie programming language to support advanced carrying arithmetic operations. It specifically addresses the implementation of the `CarryingOp` and `CarryingPow` interfaces, which are designed to handle numerical computations where overflow detection is critical. The documentation covers a wide range of functions, including basic arithmetic like addition, subtraction, multiplication, and division, as well as bitwise operations and power calculations. All operations detailed here utilize a wrapping strategy, returning a tuple consisting of a boolean overflow flag and the resulting Int64 value. This resource is primarily suitable for Cangjie developers and systems programmers who require precise control over integer overflow behavior in performance-critical or low-level applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core extension implementation for Int64 carrying operations (Add, Dec, Div, Inc, Mod) → extend_Int64_CarryingOpInt_6more.md
*   Learn about multiplication, negation, power, and bitwise shifting functions → func_carryingMulInt64_5more.md
*   Review the specific implementation of the subtraction function → func_carryingSubInt64.md

## Detailed Description

### extend_Int64_CarryingOpInt_6more.md extend_Int64_CarryingOpInt_6more.md
This API documentation file details the extension of the Int64 type in the Cangjie language to implement the CarryingOp and CarryingPow interfaces. The document covers specific carrying arithmetic functions including carryingAdd, carryingDec, carryingDiv, carryingInc, and carryingMod. Key information includes that all operations utilize a wrapping strategy, returning a tuple where the first element is a boolean indicating if overflow occurred and the second is the Int64 result. This reference is intended for Cangjie developers who require precise overflow detection and handling in their numerical computations.

### func_carryingMulInt64_5more.md func_carryingMulInt64_5more.md
This document provides API reference documentation for specific wrapping arithmetic and bitwise operations on Int64 values in the Cangjie programming language. It details five functions: carryingMul, carryingNeg, carryingPow, carryingShl, and carryingShr, each utilizing the wrapping strategy to handle potential overflows. Key takeaways include that every function returns a tuple containing a Boolean flag indicating if truncation occurred and the resulting Int64 value, allowing developers to detect overflow without errors. This reference is intended for Cangjie developers who need precise control over integer overflow behavior in low-level or performance-critical code.

### func_carryingSubInt64.md func_carryingSubInt64.md
This document serves as API reference material for the carryingSub function within the Cangjie programming language, specifically handling Int64 integer subtraction. The content details the function's signature, which accepts a subtrahend y and returns a tuple consisting of a boolean and an Int64 result. Key information highlights that the operation uses a wrapping strategy, where the boolean flag indicates whether arithmetic overflow or truncation occurred during the calculation. This documentation is intended for software developers working with the Cangjie standard library who require explicit overflow detection in mathematical operations.