# 指定密钥参数生成RSA公钥

This directory serves as a technical resource for generating RSA public keys using specified parameters within the CryptoArchitectureKit framework. It thoroughly covers the implementation details required to construct key specifications such as `RSACommonParamsSpec` and `RSAPubKeySpec`, initialize asymmetric key generators, and handle BigInt inputs in big-endian mode. The documentation includes practical Cangjie code examples demonstrating how to parse hexadecimal strings for modulus and exponent, alongside validation logic to ensure key consistency. This content is specifically tailored for software developers and security engineers who need to instantiate RSA public keys from existing parameters for cryptographic operations, offering a robust alternative to generating new key pairs.

## Quick Navigation

**What do you want to learn or do?**

*   Learn the step-by-step process to generate an RSA public key from specified parameters (modulus and exponent) → 指定密钥参数生成RSA公钥_1.md
*   View a code example for creating a key specification and validating the generated key components → 指定密钥参数生成RSA公钥_2.md

## Detailed Description

### 指定密钥参数生成RSA公钥_1.md
This technical guide explains how to generate an RSA public key using specified parameters like modulus and exponent within the CryptoArchitectureKit framework. The document details the step-by-step process involving constructing RSACommonParamsSpec and RSAPubKeySpec objects, creating an AsyKeyGeneratorBySpec, and calling generatePubKey. Key information includes the requirement that BigInt inputs must be in big-endian mode and positive. The provided Cangjie code example demonstrates parsing hexadecimal strings for the modulus and exponent to create the key specification. This tutorial targets software developers implementing cryptographic operations who need to instantiate RSA public keys from existing parameters rather than generating new pairs.

### 指定密钥参数生成RSA公钥_2.md
This document provides a code example demonstrating how to generate an RSA public key using a specific key specification. The main topics include creating an RSA 2048 public key specification, initializing an asymmetric key generator, and validating the generated key components. Key information highlights the process of extracting the modulus (N) and exponent (E) from the generated public key and comparing them against the original specification to ensure consistency. The example also includes error handling for business exceptions. This technical reference is intended for developers implementing cryptographic key generation and validation logic, requiring familiarity with RSA algorithms and exception handling.