# argopt_package_struct

This directory serves as a comprehensive technical resource for developers working with the `argopt` package within the Cangjie programming language ecosystem. It focuses specifically on the structural definition of the `ParsedArguments` struct, which is the fundamental component for handling command-line argument parsing in Cangjie applications. The documentation provides a detailed breakdown of the data structure used to encapsulate parsing results, distinguishing between different types of inputs. It covers the `nonOptions` property, designed to store an array of string arguments that are not classified as options, and the `options` property, a read-only map utilized for storing key-value pairs. By defining these collection types—Array and ReadOnlyMap—the guide offers the necessary context for integrating robust command-line logic into software projects. This material is highly suitable for Cangjie developers, API designers, and systems programmers who require a thorough understanding of input handling mechanisms. It ensures that users have the knowledge required to effectively utilize the argopt library for managing command-line inputs, assuming a prerequisite familiarity with Cangjie syntax and standard data structures. Furthermore, this documentation acts as a vital reference point for maintaining code quality and ensuring correct implementation of parsing logic in complex software environments.

## Quick Navigation

What do you want to learn?

*   Understand the core architecture of the `ParsedArguments` struct → argopt_package_struct.md
*   Learn how the `nonOptions` property handles string arrays → argopt_package_struct.md
*   Discover how the `options` property manages read-only key-value maps → argopt_package_struct.md
*   Review the complete API reference for command-line parsing → argopt_package_struct.md
*   Access technical details for Cangjie collection types → argopt_package_struct.md

## Detailed Description

The following section provides a granular look at the specific files contained within this directory, offering detailed insights into the API specifications and data structures available for the argopt package.

### argopt_package_struct.md argopt_package_struct.md

This file is the cornerstone of the package documentation, outlining the primary data structure used throughout the library.

This document provides API reference documentation for the argopt package in the Cangjie programming language, specifically defining the ParsedArguments struct. The main topic is the data structure used to encapsulate the results of command-line argument parsing. It details two key properties: nonOptions, which stores an array of string arguments that are not options, and options, a read-only map containing key-value pairs for parsed options. This reference is intended for developers utilizing the argopt library to handle command-line inputs, requiring knowledge of Cangjie syntax and collection types like Array and ReadOnlyMap.