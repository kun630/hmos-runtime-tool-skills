# class_StringReaderT_where_T_InputStream

This directory contains comprehensive API reference documentation for the Cangjie `StringReader` class, specifically designed to read and convert data from `InputStream` objects into characters or strings. It covers the class initialization, core methods like `read` and `readToEnd`, internal buffering, and UTF-8 encoding support. Additionally, it details extensions for resource management and seekable stream navigation, alongside specific parsing functions. This resource is suitable for Cangjie developers implementing text processing, I/O operations, and stream parsing who require efficient data handling, error management, and random access capabilities.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class structure, initialization, and basic reading methods → class_StringReaderT_where__6more.md
*   Learn about resource management (Resource interface) and stream navigation (Seekable interface) → extendT_StringReaderT_Reso_2more.md
*   Explore specific parsing functions like `readUntil` and character iteration (`runes`) → func_readUntilRune-Bool_3more.md

## Detailed Description

### class_StringReaderT_where__6more.md class_StringReaderT_where__6more.md
This document provides API reference documentation for the Cangjie StringReader class, designed to read and convert data from InputStream objects into characters or strings. The text covers the class initialization and core methods such as read, readln, lines, and readToEnd, explaining their return types and behaviors. Key information includes the class's internal 4096-byte buffer, exclusive UTF-8 encoding support, and the throwing of ContentFormatException upon encountering illegal characters. This guide is intended for developers implementing text processing and I/O operations in Cangjie who require efficient stream reading capabilities.

### extendT_StringReaderT_Reso_2more.md extendT_StringReaderT_Reso_2more.md
This API documentation details the implementation of the Resource and Seekable interfaces for the generic StringReader class in the Cangjie language. It covers two primary extensions: one for resource management and another for stream navigation. The Resource extension enables automatic resource release via try-with-resources syntax, featuring close and isClosed methods. The Seekable extension allows cursor manipulation through the position property and seek function, which handles offset calculations and boundary checks. This reference guide targets developers working with IO streams who require manual resource control or random access capabilities within string data.

### func_readUntilRune-Bool_3more.md func_readUntilRune-Bool_3more.md
This document serves as API reference documentation for stream reading functions in the Cangjie programming language. It details three specific methods: two overloaded readUntil functions that read data until a predicate returns true or a specific character is found, and a runes function that provides a character iterator. Key information includes that reading includes the matching character, returns Option<String> for success or failure states, and throws ContentFormatException for illegal characters. The runes method facilitates iteration over StringReader content. This guide targets Cangjie developers implementing text parsing or stream processing, requiring knowledge of the language's type system and error handling.