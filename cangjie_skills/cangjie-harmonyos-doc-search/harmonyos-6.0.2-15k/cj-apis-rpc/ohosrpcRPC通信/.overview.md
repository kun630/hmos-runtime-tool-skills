# ohosrpcRPC通信

This directory contains comprehensive API reference documentation for the `ohos.rpc` module in the Cangjie language, focusing on Inter-Process Communication (IPC) and Remote Procedure Call (RPC) mechanisms. It covers essential components for building robust communication between processes, including remote object management, data serialization, shared memory, and error handling. Key topics include `IRemoteObject`, `MessageSequence`, `Parcelable`, and `Ashmem`. This documentation is suitable for Cangjie developers and system engineers working on HarmonyOS applications that require secure and efficient data exchange across processes or devices.

## Quick Navigation

**What do you want to learn?**

*   Understand the module overview and core interfaces → ohosrpcRPC通信_4more.md
*   Handle data serialization and type definitions → enum_TypeCode.md, interface_Parcelable.md, `class_MessageSequence/`
*   Implement remote objects and proxies → `class_RemoteObject/`, `class_RemoteProxy/`, `interface_IRemoteObject/`
*   Manage IPC context and caller identity → class_IPCSkeleton.md
*   Configure message transmission options → `class_MessageOption/`
*   Handle connection lifecycle and errors → class_DeathRecipient.md, `class_ErrorCode/`, struct_RequestResult.md
*   Manage shared memory for high performance → `class_Ashmem/`

## Detailed Description

### class_DeathRecipient.md class_DeathRecipient.md
This document serves as API reference documentation for the Cangjie DeathRecipient class, which enables applications to subscribe to death notifications of remote objects within an Inter-Process Communication (IPC) context. The content covers the abstract class definition and the specific onRemoteDied method, detailing its automatic invocation upon remote object failure due to process death, device shutdown, or network disconnection. Key takeaways include the requirement to inherit this class and override the onRemoteDied function to handle cleanup or logic when a remote connection is lost. Targeted at Cangjie developers implementing IPC mechanisms, this guide requires knowledge of system capabilities and object-oriented programming.

### class_IPCSkeleton.md class_IPCSkeleton.md
This document serves as API reference documentation for the IPCSkeleton class in the Cangjie language, designed to manage Inter-Process Communication (IPC) context and system capabilities. It details static methods for retrieving caller identity (PID, UID, TokenId), device identification (local and calling device IDs), and checking call locality. Additionally, it covers command buffer flushing and accessing system capability managers. Key information includes that methods like getCallingPid return local process data if called outside the IPC context, and flushCmdBuffer is recommended before sensitive operations. This guide is intended for Cangjie developers implementing IPC mechanisms, requiring knowledge of remote objects and system communication capabilities.

### enum_TypeCode.md enum_TypeCode.md
This API reference document defines the TypeCode enum in the Cangjie language, designed to distinguish specific TypedArray types during Inter-Process Communication (IPC) data transmission. The documentation details various enum members representing numeric arrays, such as INT8_ARRAY, UINT32_ARRAY, and FLOAT64_ARRAY, alongside inherited methods including equality operators (==, !=) and toString(). Key takeaways include the enum's inheritance from Equatable and ToString, its association with the SystemCapability.Communication.IPC.Core system capability, and its availability starting from version 19. This resource targets developers working on IPC mechanisms who require precise type identification for reading and writing data.

### interface_Parcelable.md interface_Parcelable.md
This API reference document defines the Cangjie Parcelable interface, designed to serialize and deserialize objects for Inter-Process Communication (IPC). The documentation covers the interface structure, system capabilities, and details two primary methods: marshalling for writing objects to a MessageSequence and unmarshalling for recovering them. Key takeaways include the requirement to implement both methods to handle data transfer successfully, supported by a code example demonstrating a custom class implementation. Intended for Cangjie developers, this guide assumes familiarity with IPC concepts and requires version 19 system capabilities.

### ohosrpcRPC通信_4more.md ohosrpcRPC通信_4more.md
This document serves as API reference for the ohos.rpc module, providing Inter-Process Communication (IPC) and Remote Procedure Call (RPC) capabilities in the Cangjie language. It covers module import instructions, usage guidelines for example code execution, and detailed specifications for the IRemoteBroker interface. The core component is IRemoteBroker, which holds remote object proxies. Its key method, asObject(), retrieves the IRemoteObject, returning the object itself for RemoteObject instances or the holder for RemoteProxy instances. This guide targets HarmonyOS developers using Cangjie who need to implement device-internal or cross-device communication.

### struct_RequestResult.md struct_RequestResult.md
This document serves as API reference documentation for the Cangjie struct RequestResult, which encapsulates the response results of inter-process communication (IPC) requests. The content details the struct's constructor and its four read-only properties: errCode, code, data, and reply. Key information includes the data types for error handling and message passing using MessageSequence objects for both sent and received data. Part of the SystemCapability.Communication.IPC.Core capability, this documentation targets developers working on IPC functionality who need to structure and interpret request responses in Cangjie applications.

### class_Ashmem/
This directory provides comprehensive API reference documentation for the `Ashmem` class within the Cangjie programming language, specifically designed to facilitate Anonymous Shared Memory management for Inter-Process Communication (IPC). It encompasses the full lifecycle of shared memory operations, including object creation, memory mapping with specific protection levels, data read/write operations, and resource cleanup. Key technical aspects include memory protection constants (PROT_READ, PROT_WRITE), file descriptor handling, and robust error management for parameter validation. This documentation is primarily intended for system developers and software engineers building high-performance IPC mechanisms that require secure and efficient data exchange between processes on a local device.

### class_ErrorCode/
This directory provides comprehensive API documentation for the `ErrorCode` class within the Cangjie programming language. It focuses on defining specific integer constants utilized for reporting errors in Inter-Process Communication (IPC) and various system-level operations. The content covers parameter validation failures, shared memory read/write issues, and system call errors such as mmap and ioctl. This documentation is essential for developers engaged in IPC or system-level programming in Cangjie who require precise mechanisms to handle runtime exceptions and debug specific error states associated with the SystemCapability.Communication.IPC.Core capability.

### class_MessageOption/
This directory provides comprehensive API reference documentation for the Cangjie `MessageOption` class, a critical component for configuring message transmission options in Inter-Process Communication (IPC) and Remote Procedure Call (RPC) environments. The documentation covers static constants that define flags for synchronous and asynchronous calls, file descriptor acceptance, and wait time parameters. It details the class constructor and various instance methods, including `getFlags`, `getWaitTime`, `isAsync`, and `setAsync`. Key takeaways include the ability to toggle between sync and async modes and manage RPC timeout settings. This technical reference is intended for Cangjie developers implementing inter-process communication who require precise control over message request behaviors and system capabilities.

### class_MessageSequence/
This directory contains API reference documentation for the Cangjie `MessageSequence` class, a core component of the IPCKit designed for data serialization and deserialization in Inter-Process Communication (IPC) and Remote Procedure Calls (RPC). It covers methods for managing file descriptors, reading and writing various data types (integers, floats, strings, arrays), and handling memory buffers. The documentation is essential for Cangjie developers implementing efficient data transmission and resource management in IPC processes, providing detailed error codes and usage examples for robust application development.

### class_RemoteObject/
This directory provides comprehensive API reference documentation for the `RemoteObject` class and associated Inter-Process Communication (IPC) functions within the Cangjie programming language ecosystem. It focuses on the architecture required to implement remote objects, manage their lifecycles, and facilitate communication between processes. Key topics include retrieving peer information (PID/UID), managing object descriptors, handling death notifications, and sending synchronous or asynchronous messages. The documentation is specifically designed for system developers and engineers responsible for constructing robust IPC services, offering detailed specifications on parameters, return values, and exception handling for error codes like `BusinessException`.

### class_RemoteProxy/
This directory contains API reference documentation for the `RemoteProxy` class and related Inter-Process Communication (IPC) functions in the Cangjie language. It covers the implementation of the `IRemoteObject` proxy, detailing transaction codes, interface descriptors, and lifecycle management methods like death notification registration and unregistration. Key topics include synchronous and asynchronous message transmission, remote object status checks, and error handling. This documentation is suitable for system developers and engineers working on RPC and Binder architectures who need to manage IPC interactions, handle remote object states, and ensure robust communication protocols between processes.

### interface_IRemoteObject/
This directory provides comprehensive API reference documentation for the `IRemoteObject` interface within the Cangjie programming language, specifically tailored for Inter-Process Communication (IPC) scenarios. It encompasses essential functionalities such as sending MessageSequence data via synchronous or asynchronous methods, retrieving interface descriptors, and managing remote object lifecycles through death notifications. The documentation includes detailed specifications on function signatures, parameter definitions, system capabilities, and error handling mechanisms, including specific codes for invalid proxy states and parameter errors. Furthermore, it clarifies the behavioral differences in reply data and callbacks based on the selected communication mode. This resource is primarily suitable for developers and system architects who are implementing or maintaining IPC services and require precise technical guidance to ensure robust communication and object management across processes.