# interface_SortByExtensionT_supdeprecatedsup

This directory contains API documentation for a deprecated sorting interface and its specific implementation for the Array type within the Cangjie language. It covers the `SortByExtension` interface and the `extendT_ArrayT_SortByExtensionT_supdeprecatedsup` extension, detailing custom sorting logic, comparator functions, and sort stability mechanisms. Key topics include the `sortBy` function overloads, the `Ordering` return type, and the distinction between stable and unstable sorting. The material is specifically intended for Cangjie developers who are maintaining legacy codebases or actively migrating to modern sorting APIs. Since these components are marked as obsolete and will be removed in future versions, this documentation serves as a critical reference for understanding old implementations before replacing them with the recommended `sort` function.

## Quick Navigation

**What do you want to learn?**

*   Understand the deprecated interface definition and its generic sorting functions → interface_SortByExtensionT_3more.md
*   View the specific implementation for the Array type and its overloaded sortBy functions → extendT_ArrayT_SortByExtensionT_supdeprecatedsup.md

## Detailed Description

### extendT_ArrayT_SortByExtensionT_supdeprecatedsup.md extendT_ArrayT_SortByExtensionT_supdeprecatedsup.md
This API documentation defines a deprecated extension for the Array type in the Cangjie language, implementing custom sorting capabilities via the SortByExtension interface. The content details two overloaded sortBy functions: one accepting a comparator and another accepting a stability boolean alongside a comparator. Key information includes the logic for how the Ordering return type determines element placement and sort stability. Crucially, the document warns that this extension is deprecated and explicitly recommends replacing it with the sort function. This reference material targets Cangjie developers maintaining legacy code or migrating to newer sorting APIs.

### interface_SortByExtensionT_3more.md interface_SortByExtensionT_3more.md
This API reference document outlines the deprecated SortByExtension interface for the Cangjie language, designed to facilitate custom sorting of generic types. The main sections cover the interface definition and two overloaded sortBy functions: one standard and another supporting stable sorting via a boolean flag. Key information includes the requirement for a comparator function returning an Ordering type to determine sort order. Crucially, the document repeatedly warns that this interface is obsolete and will be removed in future versions. It targets developers maintaining legacy code, providing parameter details for comparator and stable arguments.