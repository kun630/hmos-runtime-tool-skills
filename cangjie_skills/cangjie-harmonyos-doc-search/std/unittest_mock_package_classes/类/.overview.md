# 类

This directory provides API reference documentation for the Cangjie `unittest.mock` framework, detailing classes essential for unit testing. It covers core functionalities such as defining stub behaviors, matching arguments, managing mock sessions, and verifying interactions. Key components include action selectors, matchers, and verifiers. Designed for developers and QA engineers, these resources enable precise control over mock objects, ensuring robust validation of logic and dependencies within test suites.

## Quick Navigation

What do you want to do?

-   **Configure mock behaviors and stubs** → class_ContinuationA.md, class_SetterActionSelectorTRet.md, [class_ConfigureMock/], [class_GetterActionSelectorTRet/], [class_MethodActionSelectorTRet/]
-   **Match arguments and types** → class_ActionSelector_3more.md, class_SyntheticFieldT_3more.md, [class_Matchers/]
-   **Verify execution and frequency** → class_MockFramework_3more.md, class_SyntheticFieldT_3more.md, [class_Verify/], [class_VerifyStatement/], [class_CardinalitySelectorA/]
-   **Manage framework sessions** → class_MockFramework_3more.md

## Detailed Description

### class_ActionSelector_3more.md class_ActionSelector_3more.md
This document serves as API reference documentation for a mocking framework in the Cangjie language, detailing classes used to define stub behaviors and match arguments in unit tests. It covers the ActionSelector class for chaining action APIs, the AnyMatcher class for matching arbitrary arguments, and the abstract ArgumentMatcher base class. Key functionalities include the fails method to trigger test failures, matchesAny for universal argument matching, and configuration methods like withDescription and forParameter to customize matcher behavior. The target audience is developers writing unit tests using mock objects, requiring knowledge of the Cangjie language and testing concepts like stubbing and spies.

### class_ContinuationA.md class_ContinuationA.md
This document provides API reference documentation for the Continuation<A> class within the Cangjie unittest mock framework, designed to facilitate the chaining of stub signature behaviors. The content details the class functionality, specifically focusing on the then() method which returns an ActionSelector subclass upon successful completion of prior operations. Key information includes the requirement that Continuation instances must be followed by a behavior definition and the throwing of MockFrameworkException when previous conditions are not met. Intended for developers writing unit tests, this guide explains how to sequentially define mock actions and handle validation errors during the mocking process.

### class_MockFramework_3more.md class_MockFramework_3more.md
This document serves as API reference documentation for a unit testing framework, specifically detailing three classes used for mocking and verification. It covers the MockFramework class, which manages stack-based sessions to scope mock objects and validate expectations; the NoneMatcher class, designed to match arguments with None values; and the OrderedVerifier class, used to collect and verify statements in a specific sequence. Key information includes session lifecycle management, exception handling for failed expectations, and argument matching logic. This guide is intended for developers writing unit tests who require precise control over mock object behavior and interaction verification.

### class_SetterActionSelectorTRet.md class_SetterActionSelectorTRet.md
This API reference document details the SetterActionSelector<TRet> class, which enables developers to define stub actions for property setters within mock or spy objects. The documentation outlines the class inheritance from ActionSelector and describes key methods such as doesNothing, setsOriginal, setsField, and throws. These methods allow users to specify behaviors like ignoring calls, invoking original logic, modifying synthetic fields, or triggering exceptions. Crucially, all functions return a CardinalitySelector to facilitate method chaining. This resource is intended for developers using the Cangjie unit testing framework to configure specific behaviors for property setters during testing.

### class_SyntheticFieldT_3more.md class_SyntheticFieldT_3more.md
This document serves as API reference documentation for a testing framework, detailing three specific classes used in mocking and verification. The main sections cover SyntheticField, which handles mutable properties via a static create method; TypedMatcher, an abstract class for verifying argument types with methods like matches and matchesAny; and UnorderedVerifier, which collects verification statements for dynamic checking. Key information includes the inheritance hierarchy of TypedMatcher and the fluent interface design of UnorderedVerifier. Intended for developers implementing unit tests or mock objects, this guide requires knowledge of the Cangjie programming language and testing concepts.

### class_CardinalitySelectorA/
This directory provides comprehensive API documentation for the `CardinalitySelector<A>` class and associated functions within the Cangjie `unittest.mock` framework. It is dedicated to the mechanisms of managing and verifying the execution frequency of stub behaviors, a core requirement for robust unit testing. The documentation details specific methods for setting call constraints, including `anyTimes`, `once`, `times`, `atLeastOnce`, and `atLeastTimes`, while also explaining exception handling for scenarios like verification failures. It is highly suitable for software developers and testers utilizing the Cangjie language who need to enforce precise behavior expectations and validate interaction counts. By using these tools, developers can ensure their mock objects behave exactly as required, leading to more reliable and maintainable test suites. This reference is essential for anyone looking to master the mocking capabilities of the Cangjie standard library.

### class_ConfigureMock/
This directory provides comprehensive API reference documentation for the `ConfigureMock` class, a core component of a testing framework designed for the Cangjie programming language. It covers the essential utilities required to configure mock objects, enabling developers to simulate specific behaviors during unit testing. The documentation details static functions for intercepting property getters, stubbing member methods, and inserting stub code into property setters. Key technical elements include argument matchers, reference tokens, and specific action selectors like `GetterActionSelector` and `MethodActionSelector`. Furthermore, it explains the parameters required for these methods, such as stub call expressions and line numbers. This resource is specifically tailored for developers and QA engineers who need to implement precise mocking strategies to validate object interactions and dependencies within their test suites.

### class_GetterActionSelectorTRet/
This directory contains API documentation for the `GetterActionSelector` class and associated functions within a mock testing framework, specifically tailored for the Cangjie language. It provides comprehensive details on defining stub behaviors for property getter functions, allowing developers to control field access, return values, and exception handling with precision. The documentation covers essential methods for synthetic field access, original value retrieval, and consecutive return sequences, as well as extensions for void methods and error simulation. This resource is suitable for developers utilizing the `@On` macro for unit testing who require advanced mocking capabilities to spy on object properties and simulate complex execution scenarios effectively.

### class_Matchers/
This directory provides comprehensive API documentation for the `Matchers` class, a fundamental component of the Cangjie unit testing framework dedicated to advanced mocking and stubbing capabilities. It serves as a guide for developers to generate argument matchers that control mock object behavior with high precision. The content covers essential functionalities such as accepting arbitrary values, filtering arguments through custom predicates, and capturing argument values for later verification. Key concepts include the distinction between mock and spy behaviors, the use of the `@On` macro for conditional stubbing, and the nuances of structural versus reference equality. This documentation is specifically tailored for developers implementing unit tests who require granular control over argument matching to ensure their test logic is both robust and reliable.

### class_MethodActionSelectorTRet/
This directory contains API reference documentation for the Cangjie `MethodActionSelector` class, a crucial component for configuring stub behaviors in unit testing and mocking frameworks. It covers methods for defining return values, executing original logic, and handling exceptions or sequential return lists. Key topics include fluent chaining, mock object setup, and test flow control. This documentation is intended for software developers using the Cangjie language who need to implement advanced mocking strategies, manage test execution flows, and handle error scenarios within their unit tests.

### class_Verify/
This directory contains API documentation for the `Verify` class within the Cangjie unit testing mock framework. It covers the mechanisms for defining and validating mock object behaviors, focusing on execution frequency, argument validation, and call order. Key components include static methods for ordered and unordered verification, the `@Called` macro, and exception handling for failed verifications. This documentation is suitable for developers writing unit tests who need to ensure stub functions are executed as expected, providing tools for precise behavioral testing and interaction validation.

### class_VerifyStatement/
This directory contains essential API documentation for the `VerifyStatement` class, a core component of the Cangjie unit testing framework focused on mocking and verification. It provides detailed guidance on verifying the execution frequency of stub signatures within mock objects. The documentation covers the class definition, creation constraints enforced by the `@Called` macro, and various member functions such as `atLeastOnce`, `atLeastTimes`, `once`, and `times`. It also explains critical exception handling for `MockFrameworkException` and `IllegalArgumentException`. This resource is specifically designed for developers and software engineers who are writing unit tests and need to rigorously validate mock behavior and ensure specific functions are called the correct number of times.