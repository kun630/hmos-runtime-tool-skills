# digest_package_overview

This directory contains the essential documentation for the `std.crypto.digest` package, serving as a comprehensive API overview for developers working with cryptographic hash algorithms. It provides a unified interface designed to simplify the implementation of various hashing standards, including MD5, SHA1, SHA256, HMAC, and SM3. The documentation focuses heavily on the generic digest function, which offers flexibility by accepting byte arrays, strings, or input streams, alongside the core Digest interface required for stream processing. This reference material is specifically tailored for developers seeking to integrate secure digest operations into their applications using generic types and efficient stream handling mechanisms. It ensures that users have a clear understanding of the available tools and how to apply them correctly in a standard library context.

## Quick Navigation

*   **What do you want to learn?**
    *   Understand the overall structure and purpose of the digest package → digest_package_overview.md
    *   Identify which cryptographic algorithms are supported (e.g., SHA256, MD5) → digest_package_overview.md
    *   Learn about the generic digest function and its input parameters → digest_package_overview.md
*   **What do you want to do?**
    *   Implement a basic hash operation using the API → digest_package_overview.md
    *   Configure stream processing using the Digest interface → digest_package_overview.md
    *   Reference specific function signatures and interfaces → digest_package_overview.md

## Detailed Description

### digest_package_overview.md digest_package_overview.md
This document serves as the primary API overview for the `std.crypto.digest` package, offering a unified interface for common cryptographic hash algorithms. The content is methodically structured into a functional introduction and a detailed API list that outlines available functions and interfaces. Key information provided includes explicit support for algorithms such as MD5, SHA1, SHA256, HMAC, and SM3. The document places significant emphasis on the generic digest function, which is designed to accept diverse input types like byte arrays, strings, or input streams, as well as the core Digest interface. Intended primarily for developers, this reference material is crucial for assisting in the implementation of digest operations using generic types and stream processing, ensuring that users can effectively leverage the package's capabilities in their software projects.