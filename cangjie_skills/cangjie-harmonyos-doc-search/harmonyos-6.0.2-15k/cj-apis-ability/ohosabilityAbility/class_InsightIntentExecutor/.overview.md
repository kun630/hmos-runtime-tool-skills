# class_InsightIntentExecutor

This directory provides comprehensive API reference documentation for the Cangjie class `InsightIntentExecutor`, which acts as a critical base class for managing intent invocation execution within the on-device intent framework. The documentation details the architecture required for handling system intents, covering essential components such as read-only context properties and the static `registerCreator` method used for binding executor instances. It specifically addresses lifecycle management through callbacks like `onExecuteInUIAbilityBackgroundMode` and `onExecuteInUIAbilityForegroundMode`. Developers are guided on overriding execution methods to return `ExecuteResult` objects containing status codes and data. This resource is primarily suitable for Cangjie application developers and engineers utilizing the AbilityKit who need to implement robust business logic that responds to system intent configurations and complex user interactions.

## Quick Navigation

**What do you want to learn?**

*   Understand the base class structure, context properties, and registration methods → class_InsightIntentExecuto_4more.md
*   Implement logic for handling intent execution when the UIAbility is in the foreground → func_onExecuteInUIAbili_b8423175.md

## Detailed Description

### class_InsightIntentExecuto_4more.md class_InsightIntentExecuto_4more.md
This document provides API reference documentation for the Cangjie class InsightIntentExecutor, which serves as a base class for handling intent invocation execution within the on-device intent framework. Key sections include the read-only context property, the static registerCreator method for binding executor instances, and the onExecuteInUIAbilityBackgroundMode callback for processing background intent triggers. Developers must override execution methods to return an ExecuteResult containing status codes and data. This documentation targets application developers using the Cangjie language to implement business logic that responds to system intent configurations and user interactions.

### func_onExecuteInUIAbili_b8423175.md func_onExecuteInUIAbili_b8423175.md
This document serves as API reference documentation for the Cangjie function onExecuteInUIAbilityForegroundMode, which is triggered when an intent call brings a UIAbility to the foreground. The content details the function signature, system capabilities, and version requirements. Key sections include a parameter table defining the intent name, parameters, and WindowStage, as well as the return type ExecuteResult. A code example demonstrates how to override this method within an InsightIntentExecutor to load content and return execution results. This guide targets Cangjie developers using the AbilityKit to implement custom intent handling logic in their applications.