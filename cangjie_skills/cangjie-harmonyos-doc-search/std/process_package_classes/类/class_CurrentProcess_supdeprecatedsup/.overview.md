# class_CurrentProcess_supdeprecatedsup

This directory provides essential API reference documentation for the Cangjie programming language, specifically targeting the deprecated `CurrentProcess` class and related environment variable utilities. It encompasses the management of the currently running process, detailing how to access command-line arguments, navigate system directories, handle standard I/O streams, and implement process termination or exit callbacks. Additionally, the directory includes references for manipulating environment variables via `setEnv` and `removeEnv` functions. This content is tailored for Cangjie developers seeking technical guidance on interacting with the runtime environment, controlling execution flow, and configuring process environments, while noting the deprecated nature of the primary class. It serves as a technical resource for understanding the structural hierarchy of the `Process` class and the specific nuances of environment handling across different operating systems.

## Quick Navigation

What do you want to learn?

*   Review the API for the deprecated `CurrentProcess` class, including properties for I/O, arguments, and process control → class_CurrentProcess_supd_10more.md
*   Find instructions on how to modify environment variables (set or remove), handle exceptions, and understand platform-specific behaviors → func_removeEnvString_2more.md

## Detailed Description

### class_CurrentProcess_supd_10more.md class_CurrentProcess_supd_10more.md
This document serves as the API reference for the deprecated `CurrentProcess` class within the Cangjie programming language. It is designed to assist developers in managing operations related to the currently active process. The documentation outlines various properties that allow access to command-line arguments, system directories, and standard I/O streams. Furthermore, it describes functions responsible for process termination, managing exit callbacks, and retrieving environment variables. Important details include the class's inheritance from the `Process` base class, a clear deprecation warning, and specific behavioral notes concerning command-line arguments, particularly in C interoperability contexts. This guide is a vital resource for developers needing to control process execution flow and interact with the runtime environment.

### func_removeEnvString_2more.md func_removeEnvString_2more.md
This document offers API reference details for functions dedicated to manipulating environment variables in the Cangjie programming language. It specifically covers two main functions: `removeEnv`, which is used to delete variables by their name, and `setEnv`, which is used to assign or update variable values. The documentation provides comprehensive information on parameter definitions, return types, and exception handling, specifically noting that an `IllegalArgumentException` is raised if inputs contain null characters. Additionally, it highlights a platform-specific behavior: on Windows, using `setEnv` to assign an empty value will result in the removal of the variable. This reference is essential for developers who need to manage process environment variables programmatically and handle cross-platform differences effectively.