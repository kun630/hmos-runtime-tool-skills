### func !=(AVPlayerState)

```cangjie
public operator func !=(other: AVPlayerState): Bool
```

**功能：** 判断两个AVPlayerState是否不等。

**系统能力：** SystemCapability.Multimedia.Media.AVPlayer

**起始版本：** 19

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|other|[AVPlayerState](#enum-avplayerstate)|是|-|另一AVPlayerState。|

**返回值：**

|类型|说明|
|:----|:----|
|Bool|两个AVPlayerState不等返回true，否则返回false。|

### func ==(AVPlayerState)

```cangjie
public operator func ==(other: AVPlayerState): Bool
```

**功能：** 判断两个AVPlayerState是否相等。

**系统能力：** SystemCapability.Multimedia.Media.AVPlayer

**起始版本：** 19

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|other|[AVPlayerState](#enum-avplayerstate)|是|-|另一AVPlayerState。|

**返回值：**

|类型|说明|
|:----|:----|
|Bool|两个AVPlayerState不等返回true，否则返回false。|

### func toString()

```cangjie
public func toString(): String
```

**功能：** 返回AVPlayerState的字符串表示。

**系统能力：** SystemCapability.Multimedia.Media.AVPlayer

**起始版本：** 19

**返回值：**

|类型|说明|
|:----|:----|
|String|返回AVPlayerState的字符串表示。|