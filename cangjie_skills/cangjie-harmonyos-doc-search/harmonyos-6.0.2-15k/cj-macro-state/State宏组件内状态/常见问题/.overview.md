# 常见问题

This directory provides a comprehensive collection of technical guides designed to assist Cangjie UI and HarmonyOS developers in mastering state management and resolving common architectural issues. It focuses on critical best practices, such as the prohibition of state variable modifications within the build method to prevent infinite rendering loops and application freezes. Furthermore, it covers the intricacies of lifecycle management to avoid memory leaks, specifically regarding callback registration and unregistration. The guides also delve into the correct usage of arrow functions for state updates and the scoping rules of decorators like @State, @Observed, and @Publish. This resource is essential for developers building custom components who need to ensure UI refresh logic functions correctly and maintain optimal application performance and memory efficiency.

## Quick Navigation

**What do you want to learn?**

*   How to avoid infinite rendering loops and memory leaks in custom components? → 不允许在build里改状态变量_2more.md
*   Why arrow functions aren't updating state variables or UI isn't refreshing? → 使用箭头函数改变状态变量未生效_2more.md

## Detailed Description

### 不允许在build里改状态变量_2more.md 不允许在build里改状态变量_2more.md

This guide outlines critical state management best practices for Cangjie UI developers. It covers the prohibition of state variable modification within the build method and the lifecycle management of registered callbacks. Key takeaways include that modifying state in build triggers infinite rendering loops and app freezes, while failing to unregister callbacks in aboutToDisappear causes memory leaks by retaining component instances. Targeted at Cangjie developers building custom components, this document requires knowledge of lifecycle methods and state decorators like @State and @Observed.

### 使用箭头函数改变状态变量未生效_2more.md 使用箭头函数改变状态变量未生效_2more.md

This technical guide addresses common pitfalls in Cangjie state management for HarmonyOS development. It covers two primary topics: correctly using arrow functions to modify state variables by explicitly passing the view model context, and understanding that state changes only refresh directly bound UI components. The document illustrates how simple types use value copying and how nested object reassignments affect UI updates. Key takeaways include the necessity of passing `this.vm` to arrow functions and the scoping rules of `@State` and `@Publish` decorators. This guide targets developers troubleshooting UI refresh logic in ArkUI applications.