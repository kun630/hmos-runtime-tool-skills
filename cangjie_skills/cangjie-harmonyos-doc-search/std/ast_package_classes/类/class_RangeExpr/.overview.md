# class_RangeExpr

This directory provides comprehensive documentation for the `RangeExpr` class within the Cangjie Abstract Syntax Tree (AST) package. It focuses on the structural definition and manipulation of range expressions, supporting both half-open and closed range operators. The content covers essential API components, including class constructors, mutable properties for accessing expression boundaries, and utility functions for token conversion and tree traversal. Designed for compiler developers and language tool implementers, this resource offers the necessary specifications to programmatically construct, analyze, and interact with range expression nodes in the Cangjie language ecosystem.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure, constructors, and properties for range expressions → class_RangeExpr_8more.md
*   Learn about converting nodes to tokens or traversing the AST hierarchy → func_toTokens_2more.md

## Detailed Description

### class_RangeExpr_8more.md
This document serves as API reference for the RangeExpr class, which represents expressions containing range operators within the Cangjie AST package. It details the class structure, including constructors for initialization and mutable properties for accessing the start, end, step, and operator tokens. Key information includes support for both half-open (..) and closed (..=) range operators, as well as exception handling for invalid tokens or omitted values in specific contexts like subscripts. Intended for compiler developers and language tool implementers, this documentation provides the necessary specifications for programmatically constructing and manipulating range expression nodes.

### func_toTokens_2more.md
This document serves as API reference documentation for two specific functions within an Abstract Syntax Tree (AST) package. It details the `toTokens()` function for converting syntax tree nodes into token sequences and the `traverse(Visitor)` function for walking through node hierarchies. Key information includes the return type of `Tokens` for the former and the mechanism for early termination using `breakTraverse` within the visitor pattern for the latter. Intended for developers working with AST manipulation or analysis, this guide assumes familiarity with the Visitor pattern and the Cangjie programming language.