# interface_CheckedOpT

This directory contains API reference documentation for the Cangjie programming language's `CheckedOp` interface and its implementations across various integer types. It focuses on safe arithmetic operations that explicitly handle integer overflow and invalid inputs by returning an `Option` type instead of throwing exceptions. The documentation covers generic interface definitions as well as specific extensions for signed and unsigned integers (Int8, Int16, Int32, Int64, IntNative, and their unsigned counterparts). This resource is suitable for Cangjie developers and system architects who require robust error handling and predictable behavior in numerical computations to ensure application stability and data integrity.

## Quick Navigation

**What do you want to learn?**

*   Understand the generic interface definition → interface_CheckedOpT_11more.md
*   View implementations for Signed Integers:
    *   Int8 → `extend_Int8_CheckedOpInt8/`
    *   Int16 → `extend_Int16_CheckedOpInt16/`
    *   Int32 → `extend_Int32_CheckedOpInt32/`
    *   Int64 (with Power) → `extend_Int64_CheckedOpInt64_CheckedPow/`
    *   IntNative → `extend_IntNative_CheckedOpIntNative/`
*   View implementations for Unsigned Integers:
    *   UInt8 → `extend_UInt8_CheckedOpUInt8/`
    *   UInt16 → `extend_UInt16_CheckedOpUInt16/`
    *   UInt32 → `extend_UInt32_CheckedOpUInt32/`
    *   UInt64 → `extend_UInt64_CheckedOpUInt64/`
    *   UIntNative → `extend_UIntNative_CheckedOpUIntNative/`

## Detailed Description

### interface_CheckedOpT_11more.md interface_CheckedOpT_11more.md
This document serves as API reference documentation for the Cangjie programming language's `CheckedOp` interface, which defines safe arithmetic operations that handle integer overflow explicitly. The content covers a comprehensive set of functions including basic arithmetic (add, sub, mul, div, mod), unary operations (neg, inc, dec), and bitwise shifts (shl, shr). The key takeaway is that every method returns an Option type (`?T`); it returns `None` if an overflow occurs or if a shift amount is invalid, otherwise returning the result. This reference is intended for Cangjie developers implementing or using numeric types that require strict overflow checking and safe error handling.

### extend_Int16_CheckedOpInt16/
This directory provides API reference documentation for the Cangjie programming language, specifically targeting the implementation of safe arithmetic operations for the `Int16` data type. It focuses on the `CheckedOp` interface and related extension functions designed to prevent runtime errors. The core theme is robust error handling through an `Option` return strategy, which returns `None` in cases of overflow or invalid operations rather than throwing exceptions. This documentation covers a wide range of operations, including basic arithmetic like addition and multiplication, as well as bitwise shifts and negation. It is primarily suitable for Cangjie developers and system architects who require reliable integer manipulation.

### extend_Int32_CheckedOpInt32/
This directory contains API documentation for the Cangjie programming language, specifically focusing on the `CheckedOp` interface implementation for the `Int32` type. It covers a wide range of safe arithmetic and bitwise operations designed to prevent runtime exceptions by handling potential overflows explicitly. The core functionality includes operations like addition, subtraction, multiplication, division, negation, and bit shifting, all utilizing an `Option` return strategy to manage errors gracefully. By returning `Option<Int32>.None` instead of wrapping values or throwing errors, these functions ensure predictable behavior in critical calculations. This resource is suitable for Cangjie developers who require robust error handling and application stability.

### extend_Int64_CheckedOpInt64_CheckedPow/
This directory contains API reference documentation for the Cangjie programming language, specifically focusing on extending the `Int64` type to support safe arithmetic operations. It covers the implementation of the `CheckedOp` and `CheckedPow` interfaces, detailing functions like `checkedAdd`, `checkedMul`, `checkedNeg`, and bitwise shifts. The core theme is safety through the `Option` return strategy, which returns `None` upon overflow instead of throwing exceptions or wrapping values. This resource is suitable for Cangjie developers who need to perform robust integer arithmetic and prevent undefined behavior caused by numerical overflows.

### extend_Int8_CheckedOpInt8/
This directory contains API reference documentation for the `CheckedOp` interface implementation specifically for the `Int8` type within the Cangjie programming language. It focuses on safe integer arithmetic operations, providing mechanisms to handle overflow scenarios gracefully without throwing runtime errors. The content covers a comprehensive range of arithmetic methods, including addition, subtraction, multiplication, division, negation, and bit manipulation. All these operations utilize `Option` return types to ensure robustness. This documentation is highly suitable for Cangjie developers who need to implement error-safe mathematical computations and bit logic in their applications, effectively preventing unexpected behavior during edge cases.

### extend_IntNative_CheckedOpIntNative/
This directory contains essential API reference documentation for the `extend_IntNative_CheckedOpIntNative` module within the Cangjie programming language ecosystem. It focuses on the implementation of the `CheckedOp` interface for the `IntNative` type, providing a robust framework for safe arithmetic and bitwise operations. The primary objective is to mitigate risks associated with integer overflow by replacing standard wrapping behavior with explicit error handling. By utilizing an `Option` return strategy, functions return `None` when operations exceed safe limits, thereby preventing undefined behavior. This resource is specifically designed for Cangjie developers who require high reliability in their applications, particularly those involving systems programming.

### extend_UInt16_CheckedOpUInt16/
This directory provides API documentation for the `CheckedOp` interface implementation specifically tailored for the `UInt16` data type within the Cangjie programming language. It focuses on safe arithmetic and bitwise operations that rigorously handle potential errors, such as arithmetic overflow or invalid shift amounts, without causing runtime exceptions. The documentation details functions that return an `Option` type, ensuring developers can gracefully manage scenarios where operations fail by returning `None` rather than crashing. This resource is essential for Cangjie developers who prioritize stability and safety in mathematical calculations involving 16-bit unsigned integers, aiming to eliminate undefined behavior in their applications.

### extend_UInt32_CheckedOpUInt32/
This directory contains API documentation for the `CheckedOp` interface implementation regarding the `UInt32` type within the Cangjie programming language. It focuses on providing a robust mechanism for arithmetic and bitwise operations that are safe from runtime errors such as overflow. The documentation details how these functions utilize the `Option` return type (`?UInt32`) to gracefully handle failure states by returning `None` rather than throwing exceptions. This approach is critical for systems requiring high reliability. The content is specifically designed for Cangjie developers and software engineers who need to perform precise, error-safe mathematical operations on unsigned 32-bit integers, ensuring code stability.

### extend_UInt64_CheckedOpUInt64/
This directory serves as a comprehensive API reference for the `CheckedOp` interface implementation specifically designed for the `UInt64` type within the Cangjie programming language. It focuses on providing safe arithmetic and bitwise operations that prevent undefined behavior by handling potential errors explicitly. The documentation covers a wide range of functions, including basic arithmetic like addition, multiplication, division, and modulo, as well as increment, decrement, negation, subtraction, and bitwise shifts. A core feature of these operations is the use of the `Option` return strategy, which returns `None` upon overflow or invalid input rather than wrapping values or throwing exceptions.

### extend_UInt8_CheckedOpUInt8/
This directory provides API documentation for the Cangjie programming language, specifically focusing on the `CheckedOp` interface implementation for the `UInt8` type. It details a suite of safe arithmetic and bitwise operations designed to prevent runtime errors by explicitly handling overflow conditions. The documentation covers essential functions such as checked addition, multiplication, division, modulo, negation, and bit shifting. A core feature of these operations is the use of the `Option` return strategy, which returns `None` when an error occurs, ensuring robust error handling. This resource is ideal for Cangjie developers seeking to implement secure numeric computations and maintain application stability when working with unsigned 8-bit integers.

### extend_UIntNative_CheckedOpUIntNative/
This directory contains API documentation for the implementation of the `CheckedOp` interface tailored to the `UIntNative` type in the Cangjie programming language. It serves as a vital technical reference for developers aiming to implement safe integer arithmetic and bitwise operations. The documentation focuses on robust error handling through an `Option` return strategy, where operations return `None` in the event of an overflow or invalid input rather than wrapping or throwing exceptions. The content covers a wide range of essential operations, including fundamental arithmetic functions such as addition, multiplication, division, and modulo, as well as increment, decrement, negation, and bit shifting.