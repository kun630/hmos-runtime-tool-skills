# class_Visitor

This directory contains the API reference for the abstract `Visitor` class in the Cangjie programming language, which implements the Visitor design pattern for traversing and modifying Abstract Syntax Trees (ASTs). It covers the core class structure, control mechanisms like `breakTraverse`, and a comprehensive set of `visit` methods for various syntax nodes, including declarations, expressions, and patterns. These resources are essential for compiler developers and language engineers building code analysis or transformation tools, providing the necessary interfaces to define custom logic during AST traversal.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class structure, control mechanisms, and general usage → class_Visitor_13more.md
*   Find visit methods for ConstPattern, Constructor, Decl, and DoWhileExpr → func_visitConstPattern_13more.md
*   Find visit methods for GenericConstraint, Lambda, If, and Interface → func_visitGenericConstrai_12more.md
*   Find visit methods for MacroDecl, MatchExpr, ParenExpr, and PackageHeader → func_visitMacroDecl_13more.md
*   Find visit methods for Pattern, PrefixType, Program, and PropDecl → func_visitPattern_13more.md
*   Find visit methods for SpawnExpr, StructDecl, SubscriptExpr, and SynchronizedExpr → func_visitSpawnExpr_12more.md
*   Find visit methods for TypeConvExpr, UnaryExpr, VarDecl, and WhileExpr → func_visitTypeConvExpr_12more.md

## Detailed Description

### class_Visitor_13more.md class_Visitor_13more.md
This API reference document details the abstract Visitor class in the Cangjie language, which implements the Visitor design pattern for traversing and modifying Abstract Syntax Tree (AST) nodes. The documentation outlines the class structure, control mechanisms like breakTraverse and needBreakTraverse, and a series of visit methods for various node types including ClassDecl, BinaryExpr, and Block. Key takeaways include that the class must be inherited, with subclasses overriding specific visit functions to define custom behavior, while default implementations remain empty. This resource targets compiler developers and language engineers working on code analysis or transformation tools.

### func_visitConstPattern_13more.md func_visitConstPattern_13more.md
This document serves as API reference documentation for a series of overloaded visit functions designed to handle specific Abstract Syntax Tree (AST) node types within the Cangjie language. The content lists 13 distinct method signatures covering nodes such as ConstPattern, Constructor, Decl, DoWhileExpr, EnumDecl, and various expression and declaration types. Key information includes that all functions are protected open, returning Unit, and accept a single parameter representing the specific AST node being traversed. These methods are intended to be overridden to define custom logic during node visitation. The target audience consists of developers implementing AST traversals or visitors who need to understand the interface for handling specific syntax elements.

### func_visitGenericConstrai_12more.md func_visitGenericConstrai_12more.md
This document provides API reference documentation for a series of `visit` functions within the Cangjie language's AST visitor implementation. It details method signatures for visiting specific node types, including generic constraints, parameters, expressions (If, Lambda, Jump), and declarations (Interface). All functions are `protected open` and return `Unit`, requiring subclass overrides to define specific behavior for traversing nodes like `GenericConstraint`, `ImportList`, and `IncOrDecExpr`. This reference is intended for compiler developers or tool builders implementing custom logic for Abstract Syntax Tree (AST) traversal.

### func_visitMacroDecl_13more.md func_visitMacroDecl_13more.md
This document serves as API reference documentation for a series of visitor pattern methods designed to handle specific Abstract Syntax Tree (AST) nodes in the Cangjie language. It details 14 overloaded `visit` functions, covering node types ranging from macro declarations (`MacroDecl`) and expressions (`MatchExpr`, `ParenExpr`) to structural elements like `PackageHeader` and `MainDecl`. Key takeaways include that all functions are `protected open` and return `Unit`, indicating they are intended to be overridden in subclasses to define custom behavior during node traversal. This reference is intended for compiler developers or tool builders implementing AST walkers or analyzers who require specific handling for these node types.

### func_visitPattern_13more.md func_visitPattern_13more.md
This document serves as API reference for the Visitor pattern implementation in the Cangjie language, detailing specific `visit` methods for various Abstract Syntax Tree (AST) node types. The content lists 13 function overloads covering nodes such as Pattern, PrefixType, PrimaryCtorDecl, PrimitiveType, Program, PropDecl, QualifiedType, QuoteExpr, RangeExpr, RefExpr, RefType, and ReturnExpr. Each method is defined as `protected open func visit(_: Type): Unit`, requiring subclasses to override them to define specific operations during node traversal. This reference is intended for compiler developers or tool builders implementing custom logic for AST analysis or transformation.

### func_visitSpawnExpr_12more.md func_visitSpawnExpr_12more.md
This document serves as API reference documentation for a series of `visit` methods within a Cangjie AST visitor interface, designed to handle specific node types during tree traversal. It details method signatures for twelve distinct AST nodes, including SpawnExpr, StructDecl, SubscriptExpr, SynchronizedExpr, ThisType, ThrowExpr, TrailingClosureExpr, TryExpr, TupleLiteral, TuplePattern, TupleType, and TypeAliasDecl. All functions are defined as `protected open` and return `Unit`, requiring subclasses to override them to implement custom logic for visiting these specific nodes. This reference is intended for compiler developers and language tool engineers implementing the Visitor pattern for syntax analysis in the Cangjie language.

### func_visitTypeConvExpr_12more.md func_visitTypeConvExpr_12more.md
This API documentation file defines a series of `visit` method overloads for traversing specific Abstract Syntax Tree (AST) nodes in the Cangjie language. The document lists twelve distinct function signatures corresponding to node types such as TypeConvExpr, TypeNode, TypePattern, UnaryExpr, VArrayExpr, VArrayType, VarDecl, VarOrEnumPattern, VarPattern, WhileExpr, WildcardExpr, and WildcardPattern. Key information includes that all methods are declared as `protected open` and return `Unit`, meaning they are designed to be overridden by subclasses to define custom behavior during node traversal. This reference material is intended for compiler developers or language engineers implementing the Visitor pattern for AST processing.