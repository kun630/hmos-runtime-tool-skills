This directory serves as the primary entry point and comprehensive overview for the Cangjie language's standard library core package. It encapsulates the essential documentation required to navigate the language's foundational environment, detailing the core functionalities that are available to programmers by default. The scope of this directory includes, but is not limited to, built-in primitive types such as integers and floats, fundamental I/O operations for system interaction, and standard interfaces like ToString and Hashable which are crucial for type interoperability. Because this core package is imported automatically, developers gain immediate access to vital classes like Array and String without needing to write explicit import statements. This documentation is specifically tailored for Cangjie developers who wish to gain a deeper understanding of the language's runtime, implement custom types that integrate seamlessly with the standard library, or execute low-level system operations. It acts as a critical resource for mastering exception handling, utilizing utility functions, and referencing the core APIs necessary for building robust and efficient applications.