# class_LinkedBlockingQueueE

This directory contains API reference documentation for the `LinkedBlockingQueue` class within the Cangjie standard library. It focuses on a thread-safe, concurrent queue implementation that supports blocking mechanisms and user-defined capacity limits. The documentation covers class definitions, initialization methods, and various queue manipulation operations, including both blocking and non-blocking behaviors. Key topics include deprecated constructors, exception handling for invalid capacities, and return types like `Option` for empty queues. This resource is suitable for Cangjie developers implementing concurrent data structures and managing thread-safe operations in multi-threaded applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition, properties, and basic initialization → class_LinkedBlockingQueueE_6more.md
*   Perform blocking operations, element inspection, or removal with timeouts → func_enqueueE_supdeprecate_6more.md
*   Execute non-blocking operations like `tryAdd` or `tryRemove` → func_tryAddE_4more.md
*   Initialize the queue with specific capacities and initial elements → initInt64_CollectionE_supd_5more.md

## Detailed Description

### class_LinkedBlockingQueueE_6more.md class_LinkedBlockingQueueE_6more.md
This document serves as API reference documentation for the LinkedBlockingQueue class, detailing a concurrent queue with blocking mechanisms and user-defined capacity limits. It covers the class definition, properties including capacity and size, and various initialization methods, specifically highlighting a deprecated constructor. Key information explains that the queue blocks threads when full or empty, while noting that the size property is not atomic in concurrent scenarios. The document also details exceptions for invalid capacities. This reference is intended for developers implementing thread-safe data structures in Cangjie, requiring knowledge of concurrency and exception handling.

### func_enqueueE_supdeprecate_6more.md func_enqueueE_supdeprecate_6more.md
This document serves as API reference documentation for queue manipulation functions within the Cangjie standard library's concurrent collection module. It details several key operations including deprecated blocking enqueue methods, non-blocking element inspection via peek and head, and blocking removal operations with and without timeouts. Key information includes specific deprecation notices directing users to replace enqueue with add and head with peek, as well as explanations of return types like Option for handling empty queues. The document provides verified code examples demonstrating multi-threaded usage. Intended for Cangjie developers, this guide assists in implementing thread-safe data structures and managing blocking behaviors in concurrent applications.

### func_tryAddE_4more.md func_tryAddE_4more.md
This document serves as API reference documentation for non-blocking queue operations within the Cangjie standard library's LinkedBlockingQueue. It details four specific functions: the active tryAdd and tryRemove methods, alongside deprecated tryDequeue and tryEnqueue methods. Key information includes function signatures, parameters, return values like Bool and Option<E>, and explicit deprecation warnings directing users to preferred alternatives. The text also provides verified code examples demonstrating successful insertion and removal. This reference is intended for Cangjie developers implementing concurrent data structures who require precise details on non-blocking behavior, error handling, and current API stability.

### initInt64_CollectionE_supd_5more.md initInt64_CollectionE_supd_5more.md
This document serves as API reference documentation for the LinkedBlockingQueue class in the Cangjie standard library, detailing initialization and element manipulation methods. It covers a deprecated constructor for creating a queue with specific capacity and initial elements, alongside blocking and timed add functions for enqueuing data. Additionally, it documents deprecated dequeue methods for retrieving elements. Key information includes exception handling for invalid capacities, return types like Bool and Option, and concurrency behavior such as blocking and timeouts. Code examples demonstrate practical usage in multi-threaded scenarios. This guide targets developers implementing concurrent data structures who need to manage thread-safe queues with capacity limits.