class avPlayerDataSrcNoSeekDemoCallback <: Callback3ArgumentWithReturn<Array<UInt8>, UInt32, Int64, Int32> {
    let fd: Int32
    public init(fd: Int32) {
        this.fd = fd
    }
    public open func invoke(buf: Array<Byte>, length: UInt32, pos: Int64): Int32 {
        var num: Int64 = 0
        num = FileFs.read(fd, buf)
        if (num > 0) {
            return Int32(num)
        }
        return -1
    }
}

class StateChangeCallback <: OnAVPlayerStateChangeHandle {
    let avPlayer: AVPlayer
    var count: Int64 = 0
    var isSeek: Bool = true
    public init(avPlayer: AVPlayer, count: Int64, isSeek: Bool) {
        this.avPlayer = avPlayer
        this.count = count
        this.isSeek = isSeek
    }

    public open func invoke(state: AVPlayerState, reason: StateChangeReason): Unit {
        match (state) {
            case AVPlayerState.idle => // 成功调用reset接口后触发该状态机上报
                AppLog.info("AVPlayer state idle called.")
                avPlayer.release() // 调用release接口销毁实例对象

            case AVPlayerState.initialized => // avplayer 设置播放源后触发该状态上报
                AppLog.info("AVPlayer state initialized called.")
                avPlayer.audioRendererInfo = AudioRendererInfo(StreamUsage.STREAM_USAGE_MUSIC, 0)
                avPlayer.prepare()
            case AVPlayerState.prepared => // prepare调用成功后上报该状态机
                AppLog.info("AVPlayer state prepared called.")
                avPlayer.play() // 调用播放接口开始播放
            case AVPlayerState.playing => // play成功调用后触发该状态机上报
                AppLog.info("AVPlayer state playing called.")
                if (this.count != 0) {
                    if (this.isSeek) {
                        AppLog.info("AVPlayer start to seek.")
                        avPlayer.seek(avPlayer.duration) //seek到音频末尾
                    } else {
                        // 当播放模式不支持seek操作时继续播放到结尾
                        AppLog.info("AVPlayer wait to play end.")
                    }
                } else {
                    sleep(3 * Duration.second)
                    AppLog.info("AVPlayer playing wait to pause")
                    avPlayer.pause() // 播放3s后调用暂停接口暂停播放
                }
                this.count++
            case AVPlayerState.paused => // pause成功调用后触发该状态机上报
                AppLog.info("AVPlayer state paused called.")
                sleep(3 * Duration.second)
                AppLog.info("AVPlayer paused wait to play again")
                avPlayer.play() // 暂停3s后再次调用播放接口开始播放
            case AVPlayerState.completed => // 播放结束后触发该状态机上报
                AppLog.info("AVPlayer state completed called.")
                avPlayer.stop() //调用播放结束接口
            case AVPlayerState.stopped => // stop接口成功调用后触发该状态机上报
                AppLog.info("AVPlayer state stopped called.")
                avPlayer.reset() // 调用reset接口初始化avplayer状态
            case AVPlayerState.released => AppLog.info("AVPlayer state released called.")
            case _ => AppLog.info("AVPlayer state unknown called.")
        }
    }
}
```

获取AbilityContext的示例代码如下：

```cangjie
// main_ability.cj
import kit.AbilityKit.{LaunchReason, LaunchParam, Want, UIAbility}
import kit.ArkUI.WindowStage
import kit.UIKit.AppLog

class MainAbility <: UIAbility {
    public init() {
        super()
        registerSelf()
    }

    public override func onCreate(want: Want, launchParam: LaunchParam): Unit {
        AppLog.info("MainAbility OnCreated.${want.abilityName}")
        match (launchParam.launchReason) {
            case LaunchReason.START_ABILITY => AppLog.info("START_ABILITY")
            case _ => ()
        }
    }

    public override func onWindowStageCreate(windowStage: WindowStage): Unit {
        AppLog.info("MainAbility onWindowStageCreate.")
        windowStage.loadContent("EntryView")
        // declared in index.cj
        ctx = this.context
    }
}
```