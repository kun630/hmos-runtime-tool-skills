# 函数

This directory provides detailed documentation for the core functions within a unit testing package tailored for the Cangjie language. It acts as a central reference for essential testing utilities, categorizing them into assertion functions, expectation functions, configuration management, and test execution entry points. The content clarifies the critical distinction between "assert" functions, which trigger exceptions upon failure, and "expect" functions, which record results without stopping execution. Additionally, it identifies `entryMain` as the designated entry point for the `cjc --test` compiler command. This directory is intended for developers and testers looking to implement robust automated testing logic, requiring a solid grasp of programming concepts and the specific syntax of this testing framework.

## Quick Navigation

- **What do you want to learn?**
    - Get an overview of the testing framework's structure, including core categories and the specific entry point for the compiler → 函数_1.md
    - Dive into specific assertion logic, including how to handle failures and invoke custom user-defined assertions → 函数_2.md
- **What do you want to do?**
    - Configure test execution and understand the difference between functions that throw exceptions versus those that log errors → 函数_1.md
    - Implement advanced testing features such as custom assertions via macros or perform floating-point comparisons using expansion utilities → 函数_2.md

## Detailed Description

### 函数_1.md 函数_1.md
This document serves as a reference index for core functions within a unit testing package, likely designed for the Cangjie language. It categorizes essential testing utilities including assertion functions, expectation functions, configuration management, and test execution entry points. Key information includes the distinction between "assert" functions, which throw exceptions upon failure (e.g., assertEqual), and "expect" functions, which log results without halting execution. The document also highlights entryMain as the specific entry point for the cjc --test compiler command. This resource is intended for developers and testers seeking to implement automated testing logic, requiring familiarity with programming concepts and the specific testing framework's syntax.

### 函数_2.md 函数_2.md
This document serves as a reference guide for specific assertion and utility functions within a unit testing framework API, detailing how to perform comparisons, handle failures, and invoke custom assertions. The content covers core assertion functions like expectEqual and fail, custom assertion invocation via invokeCustomAssert and invokeCustomExpect, and floating-point comparison utilities like isNearExpansion. Key takeaways include the distinction between hard failures that throw exceptions and soft failures that merely log results, as well as the mechanism for integrating user-defined assertions within test macros. This is intended for developers writing unit tests who need to validate code behavior, handle test outcomes, and extend the testing framework with custom logic.