# cj-apis-displaySync

This directory provides the essential API documentation for the `cj-apis-displaySync` module, a specialized library designed for the Cangjie programming language within the HarmonyOS ArkUI framework. The documentation focuses on variable frame rate control, a sophisticated feature that enables developers to synchronize custom UI rendering logic with the display's refresh cycle. This synchronization is crucial for optimizing rendering performance and ensuring visual consistency. Key components covered in this documentation include the `DisplaySync` class, which manages the synchronization process; the `IntervalInfo` class, which handles timing data; and the `OnOffType` enum, which defines state configurations. These tools facilitate the precise management of frame drawing timestamps and callback events. This resource is specifically suitable for HarmonyOS developers and software engineers who aim to optimize application performance and implement self-drawn UI tasks with rigorous performance demands.

## Quick Navigation

**What do you want to learn?**
- Learn about variable frame rate control in HarmonyOS → `ohosdisplaySync可变帧率/`

## Detailed Description

The following section provides a comprehensive breakdown of the subdirectory contained within this module. It outlines the specific purpose of the documentation and details the key classes and enumerations available to developers. By understanding these components, users can effectively implement variable frame rate strategies in their applications.

### ohosdisplaySync可变帧率/
This subdirectory serves as the primary repository for API documentation regarding the `ohos.displaySync` module. It is explicitly tailored for the Cangjie programming language and functions within the HarmonyOS ArkUI framework. The core subject matter is variable frame rate control, a technical capability that allows developers to align their custom UI rendering logic with the physical refresh cycle of the display device. This alignment is essential for creating high-performance applications that require smooth, tear-free visuals and precise timing.

The documentation provides detailed structural descriptions and usage guidelines for several critical components. The `DisplaySync` class is the central element, offering the main interface for controlling the synchronization behavior. The `IntervalInfo` class complements this by providing the necessary data structures to manage and interpret timing intervals accurately. Furthermore, the `OnOffType` enum is documented to give developers control over the activation states of the synchronization features. By utilizing these components, developers can achieve precise management of frame drawing timestamps and effectively handle callback events. This documentation is an invaluable resource for HarmonyOS developers and software engineers who are focused on optimizing application performance, particularly those implementing self-drawn UI tasks that have specific and demanding performance requirements.