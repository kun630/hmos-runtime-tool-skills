# server端操作services和通知客户端信息

This directory contains technical documentation and code examples for implementing a Bluetooth Low Energy (BLE) GATT Server, focusing on service management and client notifications. It covers the complete server lifecycle, including initialization, service addition, handling client read/write requests, and resource cleanup. The content includes implementations in both Cangjie and ArkTS, detailing specific UUIDs, callback mechanisms, and response handling. It is suitable for software developers working on connectivity features in HarmonyOS applications who need to manage BLE server-side logic and interactions with GATT clients.

## Quick Navigation

**What do you want to learn?**

*   Implement the basic BLE GATT Server workflow and initialization (Cangjie) → server端操作services和通知客户端信息_1.md
*   Manage server lifecycle, connection states, and general client requests (ArkTS) → server端操作services和通知客户端信息_2.md
*   Handle specific descriptor write requests and send responses → server端操作services和通知客户端信息_3.md

## Detailed Description

### server端操作services和通知客户端信息_1.md server端操作services和通知客户端信息_1.md
This technical tutorial guides developers on implementing a BLE GATT Server using the Cangjie language to manage services and notify clients. The document outlines the operational workflow including module imports, server instantiation, service addition, client notification, and resource cleanup. It provides a detailed code example demonstrating the initialization of descriptors, characteristics, and GATT services. Key takeaways include the specific UUIDs used for services and characteristics, the method for registering the server, and the implementation of callbacks for connection state changes, read requests, and write requests. This guide is intended for software developers with knowledge of BLE protocols and Cangjie programming, specifically those working on connectivity features in HarmonyOS applications.

### server端操作services和通知客户端信息_2.md server端操作services和通知客户端信息_2.md
This code documentation illustrates how to manage Bluetooth Low Energy (BLE) GATT server operations, specifically handling client requests and server lifecycle management. The content covers subscribing to descriptor read and write events, unregistering services, and closing the server connection. Key sections include callback implementations for connection state changes, characteristic read/write requests, and descriptor read requests, demonstrating how to construct and send ServerResponse objects. The document highlights resource cleanup procedures and error handling. Intended for developers implementing BLE server-side logic using ArkTS, this guide provides practical examples for responding to GATT client interactions and managing server states effectively.

### server端操作services和通知客户端信息_3.md server端操作services和通知客户端信息_3.md
This document details the implementation of handling descriptor write requests on a Bluetooth GATT server. It focuses on the `DescriptorWriteRequestCb` class and the `invoke` method, demonstrating how to extract request parameters like `deviceId` and `transId`, check the `needRsp` flag, and construct a `ServerResponse` object. The code illustrates sending a success response (status 0) using `gattServerManager.gattServer?.sendResponse` and includes error handling logic. Additionally, it references the Bluetooth service subsystem error codes. This technical reference is intended for developers working with BLE server-side operations who need to manage client interactions and notifications.