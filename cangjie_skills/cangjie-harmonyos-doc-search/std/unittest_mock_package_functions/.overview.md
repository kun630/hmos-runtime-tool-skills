# unittest_mock_package_functions

This directory contains essential API reference documentation for the core functions within the unittest mock package, specifically tailored for the Cangjie programming language. It serves as a guide for developers on how to create mock and spy objects to facilitate robust unit testing. The documentation details three primary functions: the basic mock generator, a mock generator that accepts specific stub modes, and a spy wrapper function. It clarifies that mock objects do not possess default implementations unless explicitly specified via the `@On` decorator, whereas spy objects wrap real instances to delegate calls by default. Furthermore, it explains how to utilize an array of StubMode to configure stubbing behavior. This guide is intended for developers writing unit tests who require knowledge of the `@On` decorator and stubbing concepts to effectively simulate or monitor object behavior.

## Quick Navigation

**What do you want to learn?**

*   Access the API reference for core unittest mock package functions → unittest_mock_package_functions.md
*   Learn how to generate basic mock objects and spy wrappers → unittest_mock_package_functions.md
*   Understand how to configure stubbing behavior using StubMode arrays → unittest_mock_package_functions.md
*   Master the usage of the `@On` decorator for mock implementations → unittest_mock_package_functions.md
*   Distinguish between the behavior of mock objects versus spy objects → unittest_mock_package_functions.md

## Detailed Description

### unittest_mock_package_functions.md unittest_mock_package_functions.md

This document serves as API reference documentation for the core functions within the unittest mock package, detailing how to create mock and spy objects for testing purposes. It covers three primary functions: the basic mock generator, the mock generator with specific stub modes, and the spy wrapper function. Key information includes that mock objects lack default implementations unless specified via @On, whereas spy objects wrap real instances to delegate calls by default. The mock function also accepts an array of StubMode to configure stubbing behavior. Intended for developers writing unit tests in Cangjie, this guide requires knowledge of the @On decorator and stubbing concepts to effectively simulate or monitor object behavior.