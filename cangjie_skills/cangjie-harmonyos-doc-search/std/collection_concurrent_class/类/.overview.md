# 类

This directory contains API reference documentation for core concurrent classes within the Cangjie standard library. It focuses on thread-safe data structures essential for multi-threaded programming, including various implementations of maps and queues. The documentation covers class definitions, constructors, and methods for handling synchronization, atomic operations, and blocking behaviors. It is suitable for Cangjie developers building concurrent applications who need precise technical details to manage shared data safely and efficiently, avoiding data conflicts and deadlocks.

## Quick Navigation

- **What do you want to learn?**
    - About iterating over concurrent hash maps → class_ConcurrentHashMapIteratorK_V_where__17f0212e.md
    - About thread-safe blocking queues with fixed capacity → [2]
    - About thread-safe hash tables for key-value storage → [3]
    - About non-blocking, thread-safe linked queues → [4]
    - About blocking queues with capacity limits → [5]

## Detailed Description

### class_ConcurrentHashMapIteratorK_V_where__17f0212e.md class_ConcurrentHashMapIteratorK_V_where__17f0212e.md
This document serves as API reference documentation for the ConcurrentHashMapIterator class in the Cangjie standard library, designed to traverse ConcurrentHashMap collections. The content covers the class definition, its inheritance from the Iterator parent type, and details the init constructor and next method. Key takeaways include specific warnings about concurrency behavior: the iterator does not provide a snapshot guarantee and may not reflect concurrent modifications, suggesting usage only when the map is not being modified by other threads. The document includes a code example demonstrating iteration logic. It targets developers utilizing concurrent data structures who require precise understanding of iterator behavior and thread-safety limitations.

### class_ArrayBlockingQueueE/
This directory contains API reference documentation for the `ArrayBlockingQueue` class in the Cangjie programming language. It focuses on a thread-safe, array-based blocking queue with a fixed capacity, essential for managing concurrent data structures. The documentation covers class properties, constructors, and various member functions including blocking, non-blocking, and timed operations. It also highlights deprecated methods and provides code examples for multi-threaded environments. This resource is suitable for Cangjie developers looking to implement safe synchronization and manage queue interactions in concurrent applications.

### class_ConcurrentHashMapK_V_where_K_Hashab_a35a4b2e/
This directory contains comprehensive API reference documentation for the `ConcurrentHashMap` class in the Cangjie programming language. It details a thread-safe hash table implementation designed for concurrent environments, covering class definitions, inheritance hierarchies, and various manipulation methods. Key topics include atomic operations, entry views, and deprecated functions. The documentation is intended for developers building concurrent applications who need efficient, thread-safe key-value storage and precise control over data insertion, retrieval, and modification.

### class_ConcurrentLinkedQueueE/
This directory provides comprehensive API reference documentation for the Cangjie `ConcurrentLinkedQueue` class, a thread-safe, non-blocking queue specifically designed for multi-threaded environments. It covers the complete class definition, constructors, the `size` property, and key methods for element manipulation, including `add`, `dequeue`, `enqueue`, and `head`. The documentation emphasizes the queue's role in preventing data conflicts and deadlocks, while also noting the non-atomic nature of the `size` property and the deprecation of specific methods in favor of newer alternatives like `remove` and `peek`. This resource is essential for Cangjie developers implementing concurrent systems—such as producer-consumer models or task scheduling—who need precise technical details to manage shared data structures effectively and safely.

### class_LinkedBlockingQueueE/
This directory contains API reference documentation for the `LinkedBlockingQueue` class within the Cangjie standard library. It focuses on a thread-safe, concurrent queue implementation that supports blocking mechanisms and user-defined capacity limits. The documentation covers class definitions, initialization methods, and various queue manipulation operations, including both blocking and non-blocking behaviors. Key topics include deprecated constructors, exception handling for invalid capacities, and return types like `Option` for empty queues. This resource is suitable for Cangjie developers implementing concurrent data structures and managing thread-safe operations in multi-threaded applications.