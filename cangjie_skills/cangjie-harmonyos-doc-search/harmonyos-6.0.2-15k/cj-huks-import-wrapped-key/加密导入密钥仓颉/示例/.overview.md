# 示例

This directory contains a comprehensive set of code examples focused on cryptographic key management and secure data operations within the HarmonyOS Universal Keystore (HUKS) framework. It covers essential security protocols including ECDH key agreement, AES encryption (AES-192, AES-256-GCM), and secure key import workflows between devices. The examples demonstrate practical implementations using the Cangjie language, detailing utility functions for byte manipulation, session management, and key lifecycle operations. These resources are designed for developers working on OpenHarmony or HarmonyOS applications who need to implement robust security features, secure key storage, and encrypted data exchange mechanisms.

## Quick Navigation

**What do you want to learn or implement?**

*   Configure cryptographic parameters and HUKS options → 示例_2.md
*   Handle low-level byte array manipulation and data formatting → 示例_1.md
*   Manage key lifecycle (generation, import, deletion) and session operations → 示例_3.md
*   Perform key agreement sessions and encrypt keys with tag separation → 示例_4.md
*   Simulate secure key transfer: Wrapping data and deriving shared secrets → 示例_5.md
*   Simulate secure key transfer: Importing wrapped keys and cleanup → 示例_6.md

## Detailed Description

### 示例_1.md
This document provides a Cangjie code example demonstrating cryptographic key management and data manipulation, specifically focusing on ECDH key agreement and AES encryption using the Universal Keystore Kit. The code covers the initialization of cryptographic constants like IV and NONCE, variable declarations for key aliases and encrypted data, and utility functions for byte array manipulation such as sub-array extraction, string conversion, and data assignment. Key takeaways include the setup for importing AES192 keys, using a caller's AES256 KEK for encryption, and handling shared keys via ECDH. The utility functions facilitate low-level byte operations required for constructing cryptographic payloads. This tutorial-style example targets developers working with the Cangjie language who need to implement secure key storage, key exchange protocols, or cryptographic data formatting within the Universal Keystore framework.

### 示例_2.md
This document serves as a code example illustrating the configuration of cryptographic parameters within the HarmonyOS Universal KeyStore (HUKS) framework. It details the setup for various security operations including ECC key generation for unwrapping, ECDH key agreement, and AES encryption. Key configurations include Curve25519 for elliptic curve operations, AES-256-GCM for encryption modes, and specific unwrap suites for importing wrapped AES-192 keys. The snippet demonstrates how to structure HuksOptions objects with tags for algorithms, purposes, and padding. This technical reference is designed for developers implementing secure key management, key exchange protocols, and data encryption in HarmonyOS applications.

### 示例_3.md
1. This document provides code examples demonstrating key management and cryptographic session operations using the HUKS framework.
2. Main topics include key lifecycle functions such as generation, import, and deletion, alongside multi-stage cipher operations involving initialization, update, and finish sessions.
3. Key information highlights the `cipherfunc` which orchestrates the full encryption process, and `publicUpdateSessionfunc` which manages data chunking with a 64-byte limit.
4. Intended for developers implementing security features in HarmonyOS or OpenHarmony applications, this reference serves as a practical guide for utilizing HUKS APIs securely.

Keywords: HUKS, Key Management, Cryptography, HarmonyOS, API Usage, Session Operations, Key Generation, Data Encryption

### 示例_4.md
This code example demonstrates cryptographic key management operations, specifically focusing on key agreement and encryption workflows. The document covers four main functions: performing key agreement sessions to derive shared secrets, importing Key Encryption Keys (KEKs) and shared secrets, generating and exporting public keys, and encrypting plain keys and KEKs with tag separation. Key takeaways include the step-by-step process of initializing, updating, and finishing cryptographic sessions, followed by importing the resulting keys and using them for encryption. This reference material is intended for developers implementing secure key storage and exchange protocols, likely within the HarmonyOS ecosystem using HUKS APIs. Prerequisites include familiarity with cryptographic concepts like ECDH and AES-GCM.

### 示例_5.md
This document provides a code example demonstrating the secure import of a symmetric key using the HUKS framework, specifically simulating a key transfer between two devices. It covers the BuildWrappedDataAndImportWrappedKey function for data serialization and the ImportWrappedKey function detailing the multi-step cryptographic workflow. The process involves generating wrapping and caller key pairs, performing ECDH key agreement to derive a shared secret, and encrypting the target key and KEK. The data structure concatenates public keys, AAD, nonces, and encrypted data with length prefixes. This is a technical tutorial intended for developers implementing secure key import mechanisms in HarmonyOS applications, requiring knowledge of asymmetric cryptography and key wrapping.

### 示例_6.md
This document serves as a code example illustrating the final stages of a secure key import workflow between two devices. It covers the process of wrapping encryption materials on Device A, importing the wrapped key on Device B, and the subsequent cleanup of temporary keys. Key steps include building wrapped data using BuildWrappedDataAndImportWrappedKey, executing the import via publicImportWrappedKeyFunc, and securely deleting wrapping, caller, and KEK keys using publicDeleteKeyItemFunc. This tutorial is intended for developers implementing cryptographic key management, specifically focusing on secure key exchange and lifecycle management. Important concepts involve AES encryption, ECDH key agreement, and secure memory handling.