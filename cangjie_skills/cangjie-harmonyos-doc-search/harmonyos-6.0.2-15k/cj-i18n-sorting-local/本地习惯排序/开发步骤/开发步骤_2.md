let strokeCollator: Collator = Collator('zh-CN', options: options)
array = ['苹果', '梨', '香蕉', '石榴', '甘蔗', '葡萄', '橘子']
compare = {a: String, b: String =>
    if (strokeCollator.compare(a, b) > 0) {
        Ordering.GT
    } else if(strokeCollator.compare(a, b) == 0) {
        Ordering.EQ
    } else {
        Ordering.LT
    }
}
sort<String>(array, by: compare) // 排序后，array = ['甘蔗', '石榴', '苹果', '香蕉', '梨', '葡萄', '橘子']

// 搜索匹配的字符串
options = CollatorOptions(
    usage: 'search',
    sensitivity: 'base'
)
let searchCollator: Collator = Collator('tr', options: options)
array = ['Türkiye', 'TüRkiye', 'salt', 'bright']
let target: String = 'türkiye'
let matches = ArrayList<String>()
// matches = ['Türkiye', 'TüRkiye']
for (i in 0..array.size) {
    if(searchCollator.compare(array[i], target) == 0){
        matches.add(array[i])
    }
}
```