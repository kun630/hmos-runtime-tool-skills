# ohosdistributed_device_manager设备管理

This directory contains API reference documentation for the `ohos.distributed_device_manager` module in the Cangjie programming language, designed for HarmonyOS distributed application development. It covers essential components for managing distributed hardware, including device discovery, authentication, state monitoring, and data type definitions. Key elements include the `DeviceManager` class, device state enums, and basic device information structures. This documentation is suitable for software developers building multi-device collaboration features who need to implement device discovery, binding, and status tracking logic using Cangjie.

## Quick Navigation

**What do you want to do?**

*   Initialize or release the device manager module? → ohosdistributed_device_man_6more.md
*   Perform device operations like discovery, authentication, or querying? → class_DeviceManager/
*   Track device state changes (Online/Offline/Ready)? → enum_DeviceStateChange.md
*   Identify specific device status event types? → enum_DeviceStatusType.md
*   Retrieve basic device properties (ID, Name, Type)? → class_DeviceBasicInfo.md
*   Define data types for key-value pairs? → enum_ValueType.md

## Detailed Description

### class_DeviceBasicInfo.md class_DeviceBasicInfo.md
This API reference document details the DeviceBasicInfo class, which encapsulates basic information for distributed devices in the Cangjie programming language. The main sections cover the class definition, four read-only properties (deviceId, deviceName, deviceType, networkId), and the primary constructor. Key information includes that deviceId is a hashed value based on UDID, and all properties are initialized via the constructor. The class belongs to the SystemCapability.DistributedHardware.DeviceManager system capability starting from version 19. This documentation targets software developers building distributed applications who need to identify and manage remote devices using Cangjie.

### enum_DeviceStateChange.md enum_DeviceStateChange.md
This API reference document defines the Cangjie language enum DeviceStateChange, used to track the status of devices within a distributed hardware system. The documentation details three primary states: UNKNOWN (physically online but not ready), AVAILABLE (synced and ready for business), and UNAVAILABLE (physically offline). It also outlines inherited methods for equality comparison (==, !=) and string representation (toString). Key takeaways include understanding the distinction between physical connection and data synchronization readiness. This resource is intended for developers implementing distributed device management logic, specifically those working with the SystemCapability.DistributedHardware.DeviceManager module.

### enum_DeviceStatusType.md enum_DeviceStatusType.md
This API reference document defines the Cangjie language enum DeviceStatusType, which specifies types for device status callback functions within the DistributedHardware.DeviceManager system capability. The content details enum members like DEVICE_STATE_CHANGE, DISCOVER_SUCCESS, and UNKNOWN, alongside operator functions for equality checks and string conversion. Key information includes the enum's inheritance from Equatable and ToString, its introduction in version 19, and its role as a parameter for on or off registration functions to monitor device discovery and state changes. This documentation is intended for developers integrating distributed hardware features who need to programmatically handle device status events.

### enum_ValueType.md enum_ValueType.md
This document provides API reference documentation for the Cangjie programming language enum ValueType, which represents data value types used in key-value pairs for distributed hardware device management. The content covers the enum's variants, specifically Integer(Int64) and Str(String), and details inherited methods including equality operators == and !=, and the toString() function. Key takeaways include the enum's inheritance from Equatable and ToString, its system capability classification under SystemCapability.DistributedHardware.DeviceManager, and its availability starting from version 19. This technical reference is intended for developers implementing device management features who require standardized data type handling.

### ohosdistributed_device_man_6more.md ohosdistributed_device_man_6more.md
This document serves as API reference material for the ohos.distributed_device_manager module in the Cangjie programming language, enabling distributed device management capabilities. It covers module imports, required permissions like ohos.permission.DISTRIBUTED_DATASYNC, and usage instructions for Cangjie template projects. Key sections detail the createDeviceManager function for initializing instances and releaseDeviceManager for cleanup, including parameters, return values, and specific error codes such as 401 and 201. The document targets HarmonyOS developers using Cangjie, providing the necessary interfaces to discover, authenticate, and query device information within a distributed network.

### class_DeviceManager/
This directory contains API reference documentation for the Cangjie `DeviceManager` class within the DistributedServiceKit. It covers essential functionalities for managing distributed hardware, including device discovery, authentication (binding), querying device information, and monitoring device status changes. Key topics include `startDiscovering`, `bindTarget`, and callback registration for state events. The documentation is tailored for HarmonyOS developers using the Cangjie language who need to implement distributed application features, requiring permissions like `ohos.permission.DISTRIBUTED_DATASYNC`. It provides syntax definitions, parameters, error codes, and code examples to facilitate the integration of multi-device collaboration capabilities.