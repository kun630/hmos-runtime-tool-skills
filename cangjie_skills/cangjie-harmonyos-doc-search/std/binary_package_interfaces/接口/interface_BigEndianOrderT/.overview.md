# interface_BigEndianOrderT

This directory contains API documentation for the Cangjie programming language, specifically focusing on the `BigEndianOrder` interface and its implementations across various primitive data types. It covers extensions for signed and unsigned integers (Int8 to Int64, UInt8 to UInt64), floating-point numbers (Float16, Float32, Float64), and Boolean values. These extensions facilitate low-level binary data serialization and deserialization using big-endian byte order. The documentation details methods like `readBigEndian` and `writeBigEndian`, buffer requirements, and exception handling. This resource is suitable for Cangjie developers working on network protocols, file I/O, or systems programming requiring precise byte manipulation.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition? → interface_BigEndianOrderT_3more.md
*   Handle Integer types?
    *   Signed integers (Int8, Int16, Int32, Int64)? → extend_Int8_BigEndianOrderInt8.md, extend_Int16_BigEndianOrderInt16.md, extend_Int32_BigEndianOrderInt32.md, extend_Int64_BigEndianOrderInt64.md
    *   Unsigned integers (UInt8, UInt16, UInt32, UInt64)? → extend_UInt8_BigEndianOrderUInt8.md, extend_UInt16_BigEndianOrderUInt16.md, extend_UInt32_BigEndianOrderUInt32.md, extend_UInt64_BigEndianOrderUInt64.md
*   Handle Floating-point types (Float16, Float32, Float64)? → extend_Float16_BigEndianOrderFloat16.md, extend_Float32_BigEndianOrderFloat32.md, extend_Float64_BigEndianOrderFloat64.md
*   Handle Boolean values? → extend_Bool_BigEndianOrderBool.md

## Detailed Description

### extend_Bool_BigEndianOrderBool.md extend_Bool_BigEndianOrderBool.md
This API documentation details the extension of the Cangjie Bool type to implement the BigEndianOrder interface, facilitating conversion between Boolean values and big-endian byte sequences. The document covers two primary functions: readBigEndian, which parses a Bool from a byte array, and writeBigEndian, which serializes a Bool into a byte array. Key information includes parameter definitions, return values, and exception handling for insufficient buffer sizes. This reference guide is intended for developers performing low-level binary data manipulation in Cangjie, providing code examples to demonstrate usage and expected output.

### extend_Float16_BigEndianOrderFloat16.md extend_Float16_BigEndianOrderFloat16.md
This API documentation details the Cangjie language extension that implements the BigEndianOrder interface for the Float16 type, enabling conversion between Float16 values and big-endian byte sequences. The document covers the extension declaration, the static readBigEndian function for reading from byte arrays, and the instance writeBigEndian function for writing to byte arrays. Key information includes that both functions require an Array<UInt8> buffer and throw IllegalArgumentException if the buffer size is insufficient. readBigEndian returns a Float16, while writeBigEndian returns the number of bytes written as Int64.

### extend_Float32_BigEndianOrderFloat32.md extend_Float32_BigEndianOrderFloat32.md
This API documentation details the extension of the Float32 type in the Cangjie language to implement the BigEndianOrder interface, enabling conversion between floating-point values and big-endian byte sequences. The document covers two primary functions: the static readBigEndian method for parsing byte arrays into Float32 values, and the instance writeBigEndian method for serializing Float32 values into byte arrays. Key information includes that both functions accept an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer is too small, while the write method returns the number of bytes written as Int64.

### extend_Float64_BigEndianOrderFloat64.md extend_Float64_BigEndianOrderFloat64.md
This API documentation details the extension of the Float64 type in the Cangjie language to implement the BigEndianOrder interface, facilitating conversion between floating-point values and big-endian byte sequences. The document covers the static readBigEndian function for parsing byte arrays into Float64 values and the instance writeBigEndian function for serializing values into byte arrays. Key information includes that both functions require an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient. The write method returns the number of bytes written as Int64.

### extend_Int16_BigEndianOrderInt16.md extend_Int16_BigEndianOrderInt16.md
This API documentation details the extension of the Int16 type in the Cangjie language to implement the BigEndianOrder interface for byte sequence conversion. It covers the static method readBigEndian for converting byte arrays to Int16 values and the instance method writeBigEndian for serializing Int16 values into byte arrays. Key information includes that both methods require an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient. The write method returns the number of bytes written as Int64.

### extend_Int32_BigEndianOrderInt32.md extend_Int32_BigEndianOrderInt32.md
This API documentation details the extension of the Int32 type in the Cangjie language to implement the BigEndianOrder interface, enabling conversion between Int32 values and big-endian byte sequences. The document covers the extension declaration, a static method readBigEndian for reading values from byte arrays, and an instance method writeBigEndian for writing values to byte arrays. Key information includes that both methods require an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient. The read method returns an Int32, while the write method returns the number of bytes written as Int64.

### extend_Int64_BigEndianOrderInt64.md extend_Int64_BigEndianOrderInt64.md
This API reference document details the extension of the Int64 type in Cangjie to implement the BigEndianOrder interface, enabling conversion between Int64 values and big-endian byte sequences. The documentation covers two primary functions: the static readBigEndian method for parsing Int64 values from byte arrays, and the writeBigEndian instance method for serializing Int64 values into byte arrays. Key information includes parameter definitions for the byte buffer, return values indicating the parsed number or byte count, and exceptions thrown for insufficient buffer sizes. The text provides executable code examples to demonstrate usage.

### extend_Int8_BigEndianOrderInt8.md extend_Int8_BigEndianOrderInt8.md
This API documentation details the extension of the Int8 type to implement the BigEndianOrder interface in the Cangjie language, enabling conversion between integer values and big-endian byte sequences. The document covers the extension declaration, a static readBigEndian function for parsing byte arrays, and an instance writeBigEndian function for serializing data. Key information includes that both functions accept an Array<UInt8> buffer, with the read function returning an Int8 and the write function returning the byte count as Int64. Operations throw IllegalArgumentException if the buffer size is insufficient.

### extend_UInt16_BigEndianOrderUInt16.md extend_UInt16_BigEndianOrderUInt16.md
This API documentation details the implementation of the BigEndianOrder interface for the UInt16 type in the Cangjie standard library, enabling conversion between 16-bit unsigned integers and big-endian byte sequences. The document covers the extension declaration, a static readBigEndian function for parsing byte arrays into a UInt16 value, and an instance writeBigEndian function for serializing values into byte arrays. Key information includes that both functions accept an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient. The read function returns the parsed value, while the write function returns the count of bytes written.

### extend_UInt32_BigEndianOrderUInt32.md extend_UInt32_BigEndianOrderUInt32.md
This API documentation details the extension of the UInt32 type in the Cangjie language to implement the BigEndianOrder interface, facilitating conversion between UInt32 values and big-endian byte sequences. The document covers the extension declaration, a static readBigEndian function for parsing byte arrays, and an instance writeBigEndian function for serializing data. Key information includes that both functions accept an Array<UInt8> buffer, return either a UInt32 or byte count, and throw IllegalArgumentException if the buffer is too small.

### extend_UInt64_BigEndianOrderUInt64.md extend_UInt64_BigEndianOrderUInt64.md
This API documentation describes the extension of the Cangjie UInt64 type to implement the BigEndianOrder interface, facilitating conversion between 64-bit unsigned integers and big-endian byte sequences. The document details two core functions: the static readBigEndian method for parsing byte arrays into UInt64 values, and the instance writeBigEndian method for serializing integers into byte arrays. Key information includes the 8-byte buffer requirement, the return of the byte count, and the handling of IllegalArgumentException for buffer size errors. Intended for Cangjie developers, this reference is crucial for low-level data processing, network programming, and binary file operations requiring specific byte ordering.

### extend_UInt8_BigEndianOrderUInt8.md extend_UInt8_BigEndianOrderUInt8.md
This API documentation describes the extension of the Cangjie UInt8 type to implement the BigEndianOrder interface, facilitating conversion between numeric values and big-endian byte sequences. The document details two primary functions: the static readBigEndian method, which parses a byte array to retrieve a UInt8 value, and the instance writeBigEndian method, which serializes a UInt8 value into a byte array. Key information includes parameter requirements for the buffer, return types, and the throwing of IllegalArgumentException for insufficient buffer sizes.

### interface_BigEndianOrderT_3more.md interface_BigEndianOrderT_3more.md
This document serves as API reference documentation for the Cangjie programming language, defining the BigEndianOrder interface for big-endian byte sequence conversion. The content details the interface definition and two primary methods: the static readBigEndian function for deserialization and the instance writeBigEndian function for serialization. Key information includes that readBigEndian converts a byte array to type T, while writeBigEndian writes T into a byte array and returns the byte count. Both methods accept an Array<UInt8> buffer and throw an IllegalArgumentException if the buffer size is insufficient.