本目录是Cangjie语言HTTP包的API参考文档集合，专注于定义和处理HTTP请求、响应及协议升级的便捷函数。文档详细介绍了三个核心函数：用于回复特定错误码的`handleError`、专门处理404响应的`notFound`，以及支持协议升级和CONNECT请求的`upgrade`。特别值得注意的是，`upgrade`函数提供了发送响应头并返回`StreamingSocket`以进行底层读写的能力。该文档主要面向使用Cangjie进行网络编程的开发者，旨在提供处理HTTP上下文和连接管理的标准接...