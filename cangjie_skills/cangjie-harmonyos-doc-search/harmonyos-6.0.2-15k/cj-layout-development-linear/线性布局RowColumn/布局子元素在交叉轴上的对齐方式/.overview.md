# 布局子元素在交叉轴上的对齐方式

This directory provides comprehensive documentation on controlling the alignment of child elements along the cross axis within various layout containers. It specifically targets developers using the Cangjie programming language, offering insights into both UIKit and ArkUI frameworks. The content covers essential properties such as `alignItems` for container-level alignment and `alignSelf` for individual element overrides. It details vertical alignment strategies for Row containers and horizontal alignment for Column containers, complete with code examples and visual aids. This resource is ideal for UI developers and HarmonyOS engineers seeking to refine their layout management skills and ensure precise positioning of interface components. By mastering these alignment techniques, developers can create responsive and visually balanced user interfaces efficiently.

## Quick Navigation

**What do you want to learn?**

*   Align child elements vertically inside a Row container (Top, Center, Bottom) → Row容器内子元素在垂直方向上的排列.md
*   Align child elements horizontally inside a Column container (Start, Center, End) → 布局子元素在交叉轴上的对齐方式_2more.md
*   Understand the difference between `alignItems` and `alignSelf` properties → 布局子元素在交叉轴上的对齐方式_2more.md
*   Apply specific alignment overrides to individual child elements → 布局子元素在交叉轴上的对齐方式_2more.md

## Detailed Description

### Row容器内子元素在垂直方向上的排列.md Row容器内子元素在垂直方向上的排列.md
This technical tutorial explains how to control the vertical alignment of child elements within a Row container using the Cangjie programming language. The document covers three specific alignment modes: VerticalAlign.Top, VerticalAlign.Center, and VerticalAlign.Bottom. Each section provides a complete code snippet demonstrating the implementation of the alignItems property alongside visual screenshots to verify the layout behavior. Intended for UI developers using the Cangjie language and UIKit framework, this guide serves as a practical reference for managing interface layouts and cross-axis alignment in row-based containers.

### 布局子元素在交叉轴上的对齐方式_2more.md 布局子元素在交叉轴上的对齐方式_2more.md
This technical guide explains how to control the alignment of child elements on the cross axis within layout containers using the ArkUI framework. It covers the `alignItems` property for general container alignment and the `alignSelf` property for overriding alignment on individual elements. The document specifically details horizontal alignment options within a `Column` container, including `HorizontalAlign.Start`, `Center`, and `End`. Key takeaways include the distinction between alignment types based on axis direction and the priority of `alignSelf` over `alignItems`. Targeted at HarmonyOS developers using the Cangjie language, this guide provides practical code examples to demonstrate layout behavior.