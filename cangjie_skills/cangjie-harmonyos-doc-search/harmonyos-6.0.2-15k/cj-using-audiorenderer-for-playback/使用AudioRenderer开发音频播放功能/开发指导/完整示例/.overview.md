# 完整示例

This directory provides a comprehensive set of code examples demonstrating how to implement audio playback using the AudioRenderer class in the Cangjie programming language. It covers the full lifecycle of audio rendering, from initial application context setup and stream configuration to playback control, state management, and resource release. Key topics include configuring AudioStreamInfo and AudioRendererInfo, initializing renderers with data write callbacks, reading WAV files from sandbox paths, and managing state transitions like prepared, running, and paused. Additionally, it addresses audio concurrency and interruption handling. These examples are intended for Cangjie developers who need practical guidance on integrating robust audio features into HarmonyOS applications and require familiarity with ability lifecycles and audio stream concepts. By following these examples, developers can effectively manage audio focus and ensure smooth playback experiences.

## Quick Navigation

**What do you want to learn?**
*   Initialize the renderer and play audio files → 完整示例_1.md
*   Stop playback and manage resource release → 完整示例_2.md

## Detailed Description

### 完整示例_1.md 完整示例_1.md
This document provides a complete code example demonstrating how to render audio files using the AudioRenderer class in the Cangjie programming language. The content is divided into application context setup in main_ability.cj and core audio logic in index.cj. Key topics include configuring AudioStreamInfo and AudioRendererInfo, initializing the renderer with a data write callback, and managing playback states through start and pause functions. The example illustrates reading a WAV file from the sandbox path and handling state transitions like prepared, running, and paused. Intended for Cangjie developers, this tutorial requires familiarity with HarmonyOS ability lifecycles and audio stream concepts to implement audio playback functionality.

### 完整示例_2.md 完整示例_2.md
This document provides code examples for managing the lifecycle of an audio renderer, specifically stopping playback and releasing resources. It covers the implementation of the stop function, which validates that the renderer is running or paused, and the release function, which ensures resources are not already freed. Additionally, it explains audio concurrency, noting that current streams can be interrupted by higher priority audio. Key takeaways include the importance of state checking before operations and the need to handle audio focus events. This technical reference is designed for developers integrating audio playback who require guidance on resource management and interruption handling. Keywords: audio renderer, stop, release, state management, audio concurrency, interruption handling.