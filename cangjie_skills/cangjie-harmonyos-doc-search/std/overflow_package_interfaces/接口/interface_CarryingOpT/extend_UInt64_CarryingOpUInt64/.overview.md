# extend_UInt64_CarryingOpUInt64

This directory provides comprehensive API reference documentation for the `CarryingOp` interface implementation tailored for the `UInt64` type within the Cangjie programming language. It details a suite of arithmetic and bitwise functions designed to handle potential overflows through a specific wrapping strategy. Instead of causing runtime errors, these operations return a tuple containing the calculation result and a Boolean flag indicating if truncation occurred. This documentation is essential for Cangjie developers who need precise, low-level control over unsigned 64-bit integer arithmetic and require reliable overflow detection mechanisms to ensure numerical integrity in their applications.

## Quick Navigation

**What do you want to learn?**

*   Learn about addition, division, increment, decrement, and modulo operations? → extend_UInt64_CarryingOpUI_6more.md
*   Learn about multiplication, negation, subtraction, and bit shifting operations? → func_carryingMulUInt64_5more.md

## Detailed Description

### extend_UInt64_CarryingOpUI_6more.md extend_UInt64_CarryingOpUI_6more.md
This API reference document details the implementation of the `CarryingOp` interface for the `UInt64` type in the Cangjie programming language. The content covers specific arithmetic functions including `carryingAdd`, `carryingDec`, `carryingDiv`, `carryingInc`, and `carryingMod`. Key information includes that these operations use a wrapping strategy and return a tuple containing a Boolean indicating if truncation occurred alongside the calculation result. The target audience is Cangjie developers requiring precise overflow detection for unsigned 64-bit integer arithmetic.

### func_carryingMulUInt64_5more.md func_carryingMulUInt64_5more.md
This document serves as API reference documentation for specific arithmetic and bitwise operations on the `UInt64` type in the Cangjie programming language. It details five functions: `carryingMul`, `carryingNeg`, `carryingShl`, `carryingShr`, and `carryingSub`, all utilizing the wrapping strategy to handle potential overflows. Key takeaways include that each function returns a tuple containing a Boolean indicating if truncation occurred and the resulting `UInt64` value, allowing developers to detect overflow without program termination. This reference is intended for Cangjie developers requiring precise control over low-level numeric operations and overflow detection.