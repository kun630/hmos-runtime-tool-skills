# class_ErrorManager

This directory provides essential API reference documentation for the Cangjie `ErrorManager` class, a core component for developers utilizing the HarmonyOS AbilityKit. It focuses on the comprehensive management of observers used for tracking application health, specifically addressing crash reporting and main thread message latency. The documentation details the implementation of static methods for registering (`on`) and unregistering (`off`) observers, providing clear definitions for parameters, return values, and error handling protocols. It highlights specific `BusinessException` codes, such as 401 and 16200001, which developers must handle. This resource is tailored for Cangjie language developers building applications on API version 12 and later, offering the necessary technical specifications to integrate system-level error monitoring and performance analysis capabilities into their software solutions effectively. By understanding these APIs, developers can ensure their applications maintain high stability and responsiveness in production environments.

## Quick Navigation

**What do you want to do?**

*   **Register Observers:** If you need to set up monitoring for application crashes or track the latency of the main thread message loop, you should look at the registration methods. → See static_func_onString_Error_2more.md
*   **Unregister Observers:** If you need to remove previously registered error observers or loop observers to stop monitoring or clean up resources, refer to the unregistration methods. → See class_ErrorManager_3more.md

## Detailed Description

The following sections provide in-depth details for each file within the `class_ErrorManager` directory. These documents outline the specific function signatures, parameters, and error handling requirements for the ErrorManager class in Cangjie.

### class_ErrorManager_3more.md class_ErrorManager_3more.md
This document serves as API reference for the Cangjie ErrorManager class, detailing methods to unregister error and main thread observers. It covers the class definition and two static `off` functions: one for removing error observers by ID and another for removing loop observers. Key details include required parameters like "error" or "loopObserver" types, observer IDs, and potential BusinessException errors with specific codes like 401 or 16200001. Targeted at developers using the AbilityKit, specifically those managing system capabilities and error handling in applications starting from API version 12.

### static_func_onString_Error_2more.md static_func_onString_Error_2more.md
This API reference document details two overloaded static functions within the ErrorManager module for registering observers in Cangjie applications. It covers the on(String, ErrorObserver) method for crash handling and the on(String, Int64, LoopObserver) method for monitoring main thread message latency. Key information includes parameter definitions, return values, and potential BusinessException error codes like 401 and 16200001. The content targets HarmonyOS developers using the Cangjie language who need to implement error tracking or performance monitoring, providing code examples for both observer types.