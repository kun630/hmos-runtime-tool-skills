# 使用SoundPool播放短音频

This directory provides a comprehensive guide for developers on using the SoundPool interface to play short audio effects with low latency. It covers the complete development workflow in the Cangjie language, including instance creation, resource loading, playback configuration, and resource management. Key topics include audio focus, stream usage for mixing, and handling load callbacks. Suitable for application developers working within the HarmonyOS or OpenHarmony ecosystem who need to implement efficient sound effects like notifications or camera shutters.

## Quick Navigation

**What do you want to learn?**

*   Understand the core concepts, workflow, and constraints of SoundPool → 使用SoundPool播放短音频.md
*   Review a complete code example demonstrating the full lifecycle in Cangjie → 完整示例.md
*   Follow a step-by-step tutorial for implementation and best practices → 开发步骤及注意事项.md

## Detailed Description

### 使用SoundPool播放短音频.md 使用SoundPool播放短音频.md
This document serves as a development guide for using the SoundPool interface to achieve low-latency playback of short audio effects in applications. It covers the complete workflow including creating a SoundPool instance, loading and decoding audio resources, configuring playback parameters like loop mode and priority, managing playback controls, and releasing resources. Key points include the 1MB file size limit for audio resources and the necessity of strictly following the operational sequence and monitoring playback status to prevent exceptions. It also references audio focus management strategies. Targeted at application developers needing to implement brief sound effects like camera shutters or system notifications, this tutorial emphasizes efficient resource management and low-latency performance.

### 完整示例.md 完整示例.md
This document provides a complete code example demonstrating low-latency audio playback using the SoundPool class in the Cangjie language. The tutorial covers the full lifecycle including SoundPool creation, audio resource loading via file descriptors, event callback registration for load completion and errors, playback parameter configuration, and resource release. Key takeaways include the specific StreamUsage settings that enable audio mixing and the necessity of waiting for load callbacks before playback. Intended for developers working with the HarmonyOS or OpenHarmony ecosystem, this guide requires basic knowledge of Cangjie syntax and audio management concepts to implement short sound effects efficiently.

### 开发步骤及注意事项.md 开发步骤及注意事项.md
This document provides a step-by-step tutorial for implementing audio playback using the SoundPool class in the Cangjie programming language. It covers the complete development lifecycle including instance creation, event listener registration for load and play states, resource loading via URI, playback configuration, and resource cleanup. Key takeaways include the importance of setting specific stream usage for audio mixing, waiting for load completion callbacks before initiating playback, and using stream IDs to control parameters like volume, loop, and priority. This guide is intended for developers building multimedia applications who need to manage short audio effects efficiently.