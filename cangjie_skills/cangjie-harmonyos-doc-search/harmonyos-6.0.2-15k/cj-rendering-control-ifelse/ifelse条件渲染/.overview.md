# ifelse条件渲染

This directory provides a comprehensive guide to implementing conditional rendering using if, else, and else if statements within the Cangjie UI framework. It covers the fundamental rules for dynamic content display, variable types, and the specific update mechanisms that drive state-driven UI changes. Key topics include component lifecycle management, state preservation, and handling nested logic. This resource is suitable for UI developers using the Cangjie framework who need to master dynamic interface layouts, optimize state persistence, and understand the underlying mechanics of component updates during conditional rendering.

## Quick Navigation

What do you want to learn?

*   Master the syntax, rules, and update mechanisms of conditional rendering → ifelse条件渲染_3more.md
*   Explore practical scenarios, nested logic, and component lifecycle impacts → 使用场景/

## Detailed Description

### ifelse条件渲染_3more.md ifelse条件渲染_3more.md
This technical guide explains how to implement conditional rendering using if, else, and else if statements within the Cangjie UI framework to dynamically display content based on application state. The document covers usage rules, including variable types, parent-child component transparency, and container-specific restrictions, alongside the update mechanism for state-driven UI changes. Key takeaways include that state variables trigger real-time updates, empty branches cause syntax errors, and updates involve deleting old components and constructing new ones without altering application state during construction. This reference guide targets UI developers using the Cangjie framework who need to manage dynamic interface layouts. Keywords: conditional rendering, Cangjie, state variables, UI components, update mechanism.

### 使用场景/
This directory focuses on practical usage scenarios for conditional rendering within the Cangjie ArkUI framework. It comprehensively covers the implementation of `if` statements, nested conditional logic, and the critical impact of conditional branches on component state management and lifecycle. Key topics include state preservation using `@Link` versus `@State`, component destruction and recreation, and handling complex UI updates driven by state variables. Through specific examples like nested statements and state hoisting, it addresses common challenges in dynamic UI development. This resource is suitable for Cangjie developers looking to master dynamic UI layouts, optimize state persistence across view changes, and deeply understand the underlying mechanics of component lifecycle during conditional rendering.