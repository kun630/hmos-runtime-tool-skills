# cj-image-tool

This directory serves as a comprehensive technical resource for developers operating within the HarmonyOS environment, specifically those utilizing the Cangjie programming language. The core subject matter is the manipulation of image metadata, with a dedicated focus on reading and editing EXIF information embedded in JPEG files. As metadata plays a crucial role in photo organization, sorting, and display, this guide addresses the technical requirements for scenarios demanding manual data correction, such as rectifying inaccurate timestamps or updating manual lens parameters that may be missing from the original capture. It provides in-depth instructions on leveraging the ImageSource class and related APIs to achieve these tasks through a step-by-step approach. This documentation is ideally suited for HarmonyOS developers building gallery applications, camera interfaces, or photo-editing tools who require precise, programmatic control over image properties, while noting the current operational scope is limited to the JPEG format.

## Quick Navigation

**What do you want to learn?**

*   Understand the EXIF format definition and identify scenarios requiring manual data correction → cj-image-tool.md
*   Learn the step-by-step process for creating an ImageSource using Cangjie → cj-image-tool.md
*   Master the implementation of `getImageProperty` and `modifyImageProperty` APIs → cj-image-tool.md
*   Review the supported property ranges and analyze specific code implementation examples → cj-image-tool.md

## Detailed Description

### cj-image-tool.md cj-image-tool.md
This document functions as a detailed development guide for reading and editing JPEG image EXIF information using the Cangjie language in HarmonyOS. It begins by explaining the fundamental structure of the EXIF format and outlines practical scenarios where developers must intervene to correct data, such as fixing timestamps or adding manual lens parameters. The main sections of the guide cover the definition of EXIF, the specific ranges of properties currently supported by the framework, and a step-by-step tutorial. This tutorial demonstrates how to effectively create an ImageSource and utilize critical APIs like `getImageProperty` and `modifyImageProperty`. Key takeaways from this document include the understanding that current support is limited to JPEG files and the provision of concrete code implementations for metadata manipulation. This guide is specifically targeted at HarmonyOS developers working on gallery or camera applications who need to manage image metadata programmatically to enhance user experience and ensure data accuracy. By following this guide, developers can integrate robust metadata handling capabilities into their applications efficiently.