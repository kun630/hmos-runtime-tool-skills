# class_UnixServerSocket

This directory contains comprehensive API reference documentation for the `UnixServerSocket` class within the Cangjie programming language's net package. It covers the implementation of Unix domain socket server functionality, including class initialization via file paths or socket addresses, configuration properties like backlog size, and connection management. Key topics include the `accept` method for handling clients, socket binding, listening, and resource cleanup. The documentation also highlights critical limitations, such as the lack of Windows support, and details exception handling for `SocketException` and `IllegalArgumentException`. This resource is designed for developers building network applications who require precise control over socket lifecycles and low-level configurations.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure, initialization, and core properties → class_UnixServerSocket_8more.md
*   Learn how to accept connections, bind sockets, and manage cleanup → func_acceptDuration_5more.md
*   Configure socket options or check the socket's status → func_getSocketOptionIntNat_6more.md

## Detailed Description

### class_UnixServerSocket_8more.md class_UnixServerSocket_8more.md
This document provides API reference documentation for the UnixServerSocket class in the Cangjie programming language, detailing how to implement Unix domain socket server functionality. It covers class initialization via file paths or socket addresses, properties for configuration like backlog size and buffer sizes, and the accept method for handling client connections. Key information includes the inheritance from ServerSocket, the requirement to bind before listening, and the critical limitation that this class does not support Windows platforms. The document outlines exceptions such as SocketException and IllegalArgumentException. This reference guide targets developers building network applications who need to understand socket configuration, exception handling, and lifecycle management within the Cangjie net package.

### func_acceptDuration_5more.md func_acceptDuration_5more.md
This document serves as API reference documentation for Unix socket operations within the Cangjie programming language. It covers critical methods including accept for establishing client connections with optional timeouts, bind for creating listening queues and managing socket files, and close for resource cleanup. Additionally, it details getSocketOption functions for retrieving socket parameters. Key information includes specific exception handling for timeouts and binding failures, as well as the requirement to manage socket files using POSIX functions like unlink. This guide is intended for developers implementing network communication, requiring familiarity with Unix domain sockets and low-level socket options.

### func_getSocketOptionIntNat_6more.md func_getSocketOptionIntNat_6more.md
This document provides API reference documentation for socket management functions in the Cangjie programming language, focusing on option manipulation and status checks. It details functions for retrieving integer socket options (getSocketOptionIntNative), setting options via pointers, booleans, or integers (setSocketOption, setSocketOptionBool, setSocketOptionIntNative), and checking socket status (isClosed, toString). Key takeaways include that these functions use Int32 for levels and options, return types like IntNative or Unit, and throw SocketException on failure. The toString method specifically returns the state of a UnixServerSocket. This reference is intended for Cangjie developers implementing low-level network socket configurations and error handling.