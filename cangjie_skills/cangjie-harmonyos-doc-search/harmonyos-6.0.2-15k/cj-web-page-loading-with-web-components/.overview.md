# cj-web-page-loading-with-web-components

This directory serves as a comprehensive technical resource for HarmonyOS developers focusing on the integration and management of web content within native applications using the Web component. It provides in-depth coverage of essential loading scenarios, including fetching network pages, rendering local HTML files stored in `rawfile` or sandbox paths, and displaying HTML rich text data directly within the app interface. The guide highlights critical technical implementation details such as configuring necessary internet permissions, effectively utilizing the `WebviewController` for management, and correctly implementing `loadUrl()` or `loadData()` interfaces. This content is specifically tailored for ArkUI and ArkWeb developers working with the Cangjie language who are building sophisticated hybrid applications requiring seamless, high-performance interaction between native code and web assets.

## Quick Navigation

**What do you want to learn?**

*   Load network pages and configure internet permissions → **使用Web组件加载页面/**
*   Render local HTML files from `rawfile` or sandbox paths → **使用Web组件加载页面/**
*   Display HTML rich text data directly in the application → **使用Web组件加载页面/**
*   Master `WebviewController`, `loadUrl()`, and `loadData()` interfaces → **使用Web组件加载页面/**

## Detailed Description

### 使用Web组件加载页面/
This directory provides comprehensive technical guidance for HarmonyOS developers on integrating web content using the Web component. It covers essential scenarios including loading network pages, rendering local HTML files from `rawfile` or sandbox paths, and displaying HTML rich text data directly. Key technical points include configuring internet permissions, utilizing `WebviewController`, and implementing `loadUrl()` or `loadData()` interfaces. The content is specifically designed for ArkUI and ArkWeb developers using the Cangjie language who are building hybrid applications requiring seamless interaction between native code and web assets.