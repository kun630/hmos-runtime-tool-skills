List(space: 20, initialIndex: 0) {
                ForEach(
                    this.numbers,
                    itemGeneratorFunc: {
                        itemInfo: String, index: Int64 => ListItem() {
                            Text(itemInfo).fontSize(16.px).textAlign(TextAlign.Center).borderRadius(10.px).
                                backgroundColor(0xAFEEEE).width(100.percent).height(80)
                        }.onDragStart(
                            {
                            evt => this.dragStartInfo = index.toString() + ", onDragStart info:" + evt.extraParams + "," +
                                evt.dragEvent.x.toString() + "," + evt.dragEvent.y.toString()
                        })
                    }
                )
            }.editMode(true).height(50.percent).width(90.percent).border(width: 1.px).divider(strokeWidth: 2.px,
                color: Color.RED, startMargin: 20.px, endMargin: 20.px).onDragEnter(
                {
                evt => this.dragListInfo = "onDragEnter info:" + evt.extraParams + "," + evt.dragEvent.x.toString() + "," +
                    evt.dragEvent.y.toString()
            }).onDragMove(
                {
                evt => this.dragListInfo = "onDragMove info:" + evt.extraParams + "," + evt.dragEvent.x.toString() + "," +
                    evt.dragEvent.y.toString()
            }).onDragLeave(
                {
                evt => this.dragListInfo = "onDragLeave info:" + evt.extraParams + "," + evt.dragEvent.x.toString() + "," +
                    evt.dragEvent.y.toString()
            }).onDrop(
                {
                evt => this.dragListInfo = "onDrop info:" + evt.extraParams + "," + evt.dragEvent.x.toString() + "," +
                    evt.dragEvent.y.toString()
            })
        }
    }
}
```

![ondraginfo](figures/ondraginfo.gif)