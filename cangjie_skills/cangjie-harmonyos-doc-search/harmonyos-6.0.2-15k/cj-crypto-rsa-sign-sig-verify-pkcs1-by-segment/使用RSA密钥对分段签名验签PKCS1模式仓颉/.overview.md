# 使用RSA密钥对分段签名验签PKCS1模式仓颉

This directory focuses on implementing segmented RSA signing and verification using the PKCS1 padding mode within the Cangjie programming language. It covers the essential workflows for handling large data volumes that cannot be processed in a single pass, utilizing the CryptoArchitectureKit. Key concepts include asymmetric encryption, RSA1024 key pairs, PKCS1 padding, and SHA256 digest algorithms. The content is designed for Cangjie developers who need to perform secure digital signatures on chunked data, ensuring data integrity and authentication through step-by-step tutorials and practical code examples.

## Quick Navigation

**What do you want to learn?**
*   Understand the step-by-step workflow for segmented signing and verification → 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md
*   Review the complete Cangjie code implementation and logic → 示例.md

## Detailed Description

### 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md 使用RSA密钥对分段签名验签PKCS1模式仓颉_3more.md
This document provides a step-by-step tutorial for performing segmented RSA signing and verification using the PKCS1 mode in the Cangjie programming language. It covers two primary workflows: the signing process and the verification process. Key steps include generating an RSA1024 key pair, creating Sign and Verify instances with the parameter string 'RSA1024|PKCS1|SHA256', initializing them with the respective private and public keys, and processing data in chunks via the update method. The guide targets developers using the CryptoArchitectureKit who need to handle data segmentation for cryptographic operations. Essential concepts include asymmetric encryption, PKCS1 padding, and SHA256 digest algorithms.

### 示例.md 示例.md
This document provides a Cangjie code example demonstrating segmented digital signature generation and verification using the CryptoArchitectureKit. It covers three main functions: signMessageBySegment for signing, verifyMessagBySegment for verification, and rsaSignatureBySegment for key generation and execution flow. Key takeaways include the implementation of chunking large plaintext data using a loop, initializing signers and verifiers with RSA1024/PKCS1/SHA256, and using the update method for segmented processing before final signing or verification. This tutorial targets Cangjie developers needing to handle cryptographic operations on large data volumes where single-pass processing is insufficient.