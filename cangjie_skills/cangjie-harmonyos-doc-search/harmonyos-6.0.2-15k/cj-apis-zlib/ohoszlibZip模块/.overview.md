# ohoszlibZip模块

This directory contains API reference documentation for the `ohos.zlib` module within the Cangjie programming language, designed for the HarmonyOS ecosystem. It covers comprehensive functionalities for data compression, decompression, and integrity verification using the Zlib subsystem. Key components include classes for managing compression streams (ZStream), GZip operations, and Zip file handling, alongside enumerations for configuring compression levels, strategies, and flush modes. The documentation also details utility functions for file archiving and checksum calculations. It is intended for Cangjie developers implementing file management and data compression features in applications using the BasicServicesKit.

## Quick Navigation

**What do you want to do?**

*   Configure compression parameters → class_ZipOptions_2more.md class_ZipOptions_2more, enum_CompressLevel.md enum_CompressLevel, enum_CompressStrategy.md enum_CompressStrategy
*   Manage compression streams and states → class_ZStream.md class_ZStream, enum_CompressFlushMode.md enum_CompressFlushMode, enum_ReturnStatus.md enum_ReturnStatus
*   Perform file or data compression/decompression → func_createZip_3more.md class_ZStream.md class_ZStream, enum_CompressFlushMode.md enum_CompressFlushMode, enum_ReturnStatus.md enum_ReturnStatus
*   Perform file or data compression/decompression → func_createZip_3more.md func_createZip_3more, ohoszlibZip模块_6more.md ohoszlibZip模块_6more, class_Zip/
*   Handle GZip specific operations → class_GZip/
*   Verify data integrity → class_Checksum.md class_Checksum
*   Set memory usage or execution modes → enum_MemLevel_2more.md enum_MemLevel_2more

## Detailed Description

### class_Checksum.md class_Checksum.md
This document serves as API reference documentation for the Cangjie Checksum class, detailing CRC-32 checksum calculation functionality. It covers the class definition and the primary method `crc32`, which updates a checksum based on an initial value and a byte array buffer. Key information includes the method's parameters, return type `UInt64`, and specific error codes like 17800009 for internal structure errors. The document specifies system capabilities and version requirements. Intended for Cangjie developers using the BasicServicesKit, this guide provides the necessary syntax and examples for implementing data integrity checks using the Zlib subsystem.

### class_ZStream.md class_ZStream.md
This document provides API reference documentation for the Cangjie ZStream class, which manages the internal compression stream for Zlib operations. The content covers the class definition, including read-only properties for input and output buffers, available byte counts, total processed bytes, data type hints, and checksums, alongside constructor details. Key takeaways include that all fields are nullable and read-only, designed to track stream state for compression or decompression, relying on the SystemCapability.BundleManager.Zlib capability. This reference targets developers implementing data compression features using the Zlib library in Cangjie, requiring an understanding of stream processing and buffer management.

### class_ZipOptions_2more.md class_ZipOptions_2more.md
This API reference document defines data structures for configuring and handling compression operations in the Cangjie programming language. It details the `ZipOptions` class for setting compression parameters and the `ZipOutputInfo` class for retrieving operation results. Key information includes `ZipOptions` properties for compression level, memory level, and strategy, which are read-only and set during initialization. `ZipOutputInfo` provides the return status and destination buffer length. Both classes utilize specific enumerations for type safety. This documentation targets software developers implementing Zlib compression features within the BundleManager system capability, requiring knowledge of the Cangjie syntax and compression concepts.

### enum_CompressFlushMode.md enum_CompressFlushMode.md
This document provides API reference documentation for the `CompressFlushMode` enum in the Cangjie language, defining flush modes used to control compression and decompression processes within the Zlib system capability. The content covers the enum's definition, specific member values such as `NoFlush`, `SyncFlush`, and `Finish`, and standard operators like equality checks and `toString`. Key information includes the functional purpose of each mode, ranging from default operations to precise control and process termination, alongside the implementation of `Equatable` and `ToString` interfaces. Intended for developers working with data compression in Cangjie, this reference requires familiarity with enumerations and the Zlib library to effectively manage data stream behaviors.

### enum_CompressLevel.md enum_CompressLevel.md
This API reference document defines the `CompressLevel` enum in the Cangjie language, used to specify compression levels within the Zlib system capability. The documentation details the enum definition, four specific compression levels ranging from no compression to best compression, and inherited operators. Key information includes the enum's inheritance from `Equatable` and `ToString`, allowing for value comparison and string representation. It highlights specific members like `COMPRESS_LEVEL_BEST_SPEED` and `COMPRESS_LEVEL_DEFAULT_COMPRESSION` to help developers balance performance and file size. Intended for Cangjie developers implementing data compression, this guide provides the necessary syntax and versioning details for configuring Zlib behavior effectively.

### enum_CompressMethod.md enum_CompressMethod.md
This document serves as an API reference for the Cangjie programming language's `CompressMethod` enum, which defines compression methods within the Zlib system capability. The content details the enum definition, its inheritance from `Equatable` and `ToString`, and specific members like the Deflated compression method. Key information includes operator overloads for equality and inequality checks, as well as a `toString` function for string representation, all available starting from version 20. This reference is intended for software developers implementing compression logic using the Cangjie framework.

### enum_CompressStrategy.md enum_CompressStrategy
This API reference document defines the `CompressStrategy` enum in the C7] enum_CompressStrategy.md
This API reference document defines the `CompressStrategy`angjie language, used to specify compression algorithms within the Zlib system capability. The content enumerates specific strategy options including Default, Filtered, Huffman Only, RLE, and Fixed, each tailored for different data types. It also documents inherited and implemented methods such as equality operators and `toString`, which were introduced in version 20. Key information includes the inheritance from `Equatable` and `ToString` interfaces. This guide is intended for software developers working on data compression who need to configure or compare compression strategies in Cangjie applications.

### enum_MemLevel_2more.md enum_MemLevel_2more.md
This document provides API reference documentation for Cangjie language enumerations used within the Zlib compression framework. It primarily defines two enums: `MemLevel`, which configures memory usage during compression (MIN, DEFAULT, MAX), and `ParallelStrategy`, which determines execution modes (Sequential or Parallel Decompression). Both enums inherit from `Equatable` and `ToString`, offering standard operators for equality comparison and string representation. Key information includes system capability requirements and version availability starting from API 19 or 20. This guide is intended for developers seeking to optimize compression performance and resource allocation in their applications.

### enum_ReturnStatus.md enum_ReturnStatus.md
This document serves as an API reference for the Cangjie programming language's `ReturnStatus` enum, which defines function return states for the Zlib system capability. It details the enum definition, specific member values including Ok, StreamEnd, and NeedDict, as well as standard operators for equality comparison and a `toString` method. Key takeaways include understanding the specific status codes indicating successful operation, end of data stream, or the requirement for a preset dictionary. This documentation is intended for Cangjie developers implementing or interacting with Zlib compression and decompression functionalities, requiring knowledge of basic Cangjie syntax and system capabilities.

### func_createZip_3more.md func_createZip_3more.md
This document serves as API reference documentation for file compression and decompression utilities within the Cangjie BasicServicesKit. It covers three main functions: `createZip` for instantiating Zip objects, `deCompressFile` for extracting archives to specified sandbox paths, and `getOriginalSize` for retrieving the uncompressed size of a file. Key information includes parameter requirements, such as mandatory .zip suffixes and existing output directories, alongside specific error codes for handling invalid inputs or damaged files. This technical guide is intended for Cangjie developers implementing file management features, requiring knowledge of system capabilities and exception handling.

### ohoszlibZip模块_6more.md ohoszlibZip模块_6more.md
This document serves as API reference documentation for the `ohos.zlib` Zip module, designed for the Cangjie programming language within the HarmonyOS ecosystem. It details functions for file and folder compression, specifically `compressFile` and `compressFiles`, alongside utility functions to create `Checksum` and `GZip` objects. Key information includes parameter definitions for input and output paths, configuration options via `ZipOptions`, and specific business exception error codes. The content targets application developers requiring file compression and data integrity verification capabilities, providing syntax examples and system capability requirements.

### class_GZip/
This directory contains comprehensive API reference documentation for the GZip class within the Cangjie programming language, specifically targeting the Zlib subsystem. It covers essential interfaces for file compression and decompression, including detailed explanations of functions for opening, reading, writing, and closing gzip files. Key topics include system capabilities, version requirements, return types like `ReturnStatus` and `Int32`, and specific error codes for handling exceptions such as ZStream errors or memory allocation failures. This documentation is suitable for Cangjie developers implementing gzip functionality in applications using the BasicServicesKit, requiring knowledge of file I/O and robust error handling mechanisms.

### class_Zip/
This directory contains API reference documentation for the Zip class and related functions within the Cangjie language's Zlib subsystem. It covers data compression and decompression capabilities, including stream-based operations, buffer management, and resource allocation. Key topics include functions like `compress`, `uncompress`, `deflate`, and `inflate`, along with initialization and termination routines for compression streams. The documentation details parameters, return values, and exception handling for various error codes. This resource is suitable for Cangjie developers implementing file or data compression features who need precise technical specifications and code examples for the SystemCapability.BundleManager.Zlib framework.