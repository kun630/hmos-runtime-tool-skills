# 帧动画ohosanimator

This directory provides comprehensive documentation and tutorials for implementing frame animations within HarmonyOS applications using the Cangjie language. It specifically explores the `ohos.animator` API, offering developers the tools to handle property adjustments on a per-frame basis. Key topics include the `AnimatorResult` object, `onFrame` callbacks, and lifecycle management methods like `onPageShow` and `onPageHide`. The resources cover practical implementations ranging from mathematical trajectory calculations to real-time UI updates. This content is ideal for developers seeking to build custom physics-based animations or require granular control over animation states beyond standard property animations.

## Quick Navigation

- **What do you want to do?**
    - Create a physics-based animation (e.g., ball parabolic motion) with playback controls → 使用帧动画实现小球抛物运动.md
    - Learn the core concepts of `ohos.animator` and how it differs from property animations → 帧动画ohosanimator_2more.md

## Detailed Description

### 使用帧动画实现小球抛物运动.md 使用帧动画实现小球抛物运动.md
This tutorial demonstrates how to implement parabolic motion for a ball using frame animation in the Cangjie language. It covers importing dependencies, defining the animated component, and configuring the AnimatorResult object within the onPageShow lifecycle. Key sections include setting up the onFrame callback to calculate trajectory using mathematical formulas, implementing control buttons for playback management, and handling resource cleanup in onPageHide. The document provides a complete code example illustrating the integration of animation logic with UI state management. It targets developers seeking to create custom physics-based animations in HarmonyOS applications using the UIKit framework.

### 帧动画ohosanimator_2more.md 帧动画ohosanimator_2more.md
This document serves as a guide for implementing frame animations using the ohos.animator API in the Cangjie language, enabling developers to handle property adjustments on a per-frame basis. It covers the conceptual comparison between frame and property animations, highlighting trade-offs in performance and control, followed by a practical tutorial on creating, configuring, and managing animation objects. Key takeaways include the ability to pause animations and respond to events in real-time via the onFrame callback, contrasting with property animations that only calculate the final state. The tutorial details steps from importing dependencies to manually releasing AnimatorResult objects. The content is intended for developers familiar with Cangjie who require granular, real-time UI animation control.