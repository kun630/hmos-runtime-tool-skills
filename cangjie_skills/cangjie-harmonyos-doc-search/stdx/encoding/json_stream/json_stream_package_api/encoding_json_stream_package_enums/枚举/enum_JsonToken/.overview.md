# enum_JsonToken

本目录包含 Cangjie 编程语言中 `JsonToken` 枚举的 API 参考文档，主要涉及 JSON 数据流解析过程中的标记类型定义。内容涵盖了枚举成员（如 BeginArray、JsonBool 等）的详细说明、与 `JsonReader.peek()` 方法的配合使用，以及相等运算符的重载实现。该模块实现了 `Equatable` 和 `Hashable` 接口，适用于需要进行 JSON 结构判断和流式读取的开发者。

## 快速导航

你想了解什么？

- 查看 JsonToken 枚举的定义、成员类型及解析用途 → enum_JsonToken_12more_3a5ca728.md
- 了解如何比较两个 JsonToken 实例是否相等 → operator_func_JsonToken_f89a0a73.md

## 详细描述

### enum_JsonToken_12more_3a5ca728.md enum_JsonToken_12more_3a5ca728.md
这份文档是 Cangjie 编程语言中 `JsonToken` 枚举的 API 参考文档，旨在定义 JSON 编码字符串中的结构、名称或值类型。文档详细列出了枚举的九个成员（如 BeginArray、JsonBool、Name 等），并说明了它们与 `JsonReader.peek()` 方法的配合使用方式及对应的读取操作。关键点在于该枚举实现了 `Equatable` 和 `Hashable` 接口，用于在流式解析中判断当前 JSON 标记类型，从而指导后续的读取逻辑。文档面向使用 Cangjie 进行 JSON 数据流解析的开发者，需具备基本的 JSON 结构理解。

### operator_func_JsonToken_f89a0a73.md operator_func_JsonToken_f89a0a73.md
本文档定义了 `JsonToken` 枚举类型的相等运算符重载，用于比较两个 `JsonToken` 实例是否相等。主要内容包括函数签名、功能描述、参数说明以及返回值定义。关键信息在于该运算符接收一个 `JsonToken` 对象作为参数，并返回布尔值以指示比较结果。这是面向使用 JSON 流编码库的开发者的 API 参考文档，适用于需要进行对象相等性判断的编程场景。