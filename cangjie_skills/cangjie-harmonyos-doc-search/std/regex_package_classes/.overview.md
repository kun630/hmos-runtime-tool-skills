# regex_package_classes

This directory serves as a comprehensive technical resource for the Cangjie programming language, specifically focusing on the class structures used for regular expression (regex) processing. It is designed to provide developers with the necessary API reference documentation to implement sophisticated text processing, pattern matching, and data validation logic within their applications. The content encompasses a wide array of topics, ranging from modern, actively maintained implementations to deprecated legacy components that remain relevant for historical maintenance. By offering detailed insights into matching modes and parsing frameworks, this directory is an essential tool for Cangjie developers. It is particularly suitable for software engineers looking to build robust text validation systems, develop complex parsing algorithms, or maintain and modernize legacy codebases that still rely on older regex frameworks.

## Quick Navigation

- What do you want to learn?
    - About the core `Regex` class for modern text processing → **类/**
    - About the deprecated `Matcher` class and its historical usage → **类/**
    - About the obsolete `RegexOption` class for matching modes → **类/**
- What do you want to do?
    - Implement robust text validation in Cangjie applications → **类/**
    - Develop complex parsing logic using regex patterns → **类/**
    - Maintain or refactor legacy codebases using older regex frameworks → **类/**

## Detailed Description

### 类/
This directory functions as the central repository for API reference documentation regarding classes within the Cangjie programming language, with a primary focus on regular expression (regex) processing. The documentation is meticulously structured to assist developers in navigating the complexities of text manipulation and pattern recognition.

Key topics covered in this section include the `Regex` class, which represents the current standard for text processing and pattern matching in Cangjie. In addition to modern implementations, the directory preserves documentation for deprecated legacy components to support ongoing maintenance tasks. This includes the deprecated `Matcher` class, which was previously utilized for pattern matching operations, and the obsolete `RegexOption` class, which historically defined various matching modes.

By documenting both current and obsolete classes, this directory ensures that Cangjie developers have access to the complete context of the language's regex evolution. It is highly suitable for developers who need to implement robust text validation and parsing logic in new projects, as well as those responsible for maintaining legacy codebases that depend on older, deprecated regex frameworks.