# class_AVImageGenerator

This directory contains API reference documentation for the Cangjie `AVImageGenerator` class, a component of the MediaKit framework designed for multimedia application development. It primarily focuses on the functionality required to retrieve video thumbnails from media resources and manage the lifecycle of these media objects. Key topics covered include setting data sources using file descriptors, capturing specific video frames via timestamps, and properly releasing system resources to prevent memory leaks. The documentation details specific methods like `fetchFrameByTime` and `release`, along with their parameters, return values, and error handling. This content is intended for developers using the Cangjie language on HarmonyOS who need to integrate video thumbnail extraction into their applications.

## Quick Navigation

**What do you want to learn?**

*   How to initialize the generator, set data sources, and capture video frames? → class_AVImageGenerator_3more.md class_AVImageGenerator_3more.md
*   How to properly release resources and handle cleanup operations? → func_release.md func_release.md

## Detailed Description

### class_AVImageGenerator_3more.md class_AVImageGenerator_3more.md
This document provides comprehensive API reference documentation for the Cangjie `AVImageGenerator` class. It details the class's ability to retrieve video thumbnails from various media resources. The documentation covers the `fdSrc` property, which is used to set data sources via file descriptors, and the `fetchFrameByTime` function, which allows for capturing frames at specific timestamps. Key information includes the prerequisites for creating an instance, strict warnings against sharing file descriptors to avoid race conditions, and the method's return of a PixelMap object. Additionally, it outlines parameters, return values, and specific error codes such as 5400102. This guide is intended for multimedia developers and assumes familiarity with Cangjie syntax and the MediaKit framework.

### func_release.md func_release.md
This document serves as API reference documentation for the `release()` function in the Cangjie language. Its primary purpose is to free resources associated with an `AVImageGenerator` instance. The content covers the function signature, required system capabilities, version requirements, and potential exceptions, including specific error codes. It also provides a practical code example demonstrating resource cleanup. Key takeaways include that the function returns `Unit`, requires the system capability `SystemCapability.Multimedia.Media.AVImageGenerator`, and throws a `BusinessException` if the operation is not allowed. The example illustrates integration with MediaKit and error handling. The target audience is developers using the Cangjie language for multimedia application development on HarmonyOS who need to manage the lifecycle of media objects to prevent memory leaks.