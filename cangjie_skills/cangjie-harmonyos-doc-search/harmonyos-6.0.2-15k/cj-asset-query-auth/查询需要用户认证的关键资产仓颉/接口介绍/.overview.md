# 接口介绍

This directory provides comprehensive technical documentation for the AssetStoreKit framework, focusing on synchronous interfaces for querying and managing key assets. It covers API functions like preQuery, query, and postQuery, alongside detailed attribute parameters, data types, and security constraints. Key topics include access control settings, lifecycle options, data storage labels, and encryption configurations. Developers will find specific references for authentication tokens, custom data labels, and API versioning updates (e.g., API 12). This documentation is suitable for application developers and system integrators implementing secure asset storage solutions who need to understand API usage, data integrity, and parameter structures.

## Quick Navigation

- **What do you want to learn?**
    - Understand the main API workflow and security warnings → 接口介绍_1.md
    - Define attribute parameters for asset management → 接口介绍_2.md
    - Review attribute structures and mandatory fields → 接口介绍_3.md
    - Configure local custom labels and encryption settings → 接口介绍_4.md
    - Implement postQuery authentication parameters → 接口介绍_5.md

## Detailed Description

### 接口介绍_1.md 接口介绍_1.md
This document serves as an introduction to synchronous interfaces for querying key assets within the AssetStoreKit framework. It details three primary API functions: preQuery for pre-processing, query for retrieving key assets, and postQuery for post-processing. Key information includes a security warning that asset parameters containing "DATA_LABEL" are unencrypted and must not store personal data. The document references external API documentation for detailed parameter lists. This technical reference is intended for developers integrating asset querying capabilities who need to understand API usage and data security constraints.

### 接口介绍_2.md 接口介绍_2.md
This document serves as a reference guide defining the attribute parameters for querying or managing critical assets within the AssetStoreKit framework. The content details a comprehensive list of tags including access control settings like AUTH_TYPE and ACCESSIBILITY, lifecycle options like IS_PERSISTENT, and various data storage labels categorized by protection level and synchronization behavior. Key takeaways include the specific data types and byte limits for each attribute, noting that data label lengths increased to 2048 bytes starting from API 12, and the distinction between integrity-protected critical data and local-only normal data. Intended for application developers integrating secure asset storage, this reference requires understanding of basic data types and API versioning.

### 接口介绍_3.md 接口介绍_3.md
This API reference document defines the attribute structure and data types for managing critical assets within the AssetStoreKit framework. It details mandatory fields such as ALIAS and AUTH_TOKEN, alongside optional configuration parameters for access control, synchronization, and persistence. It also specifies custom data labels with and without integrity protection. Key takeaways include specific byte constraints for authentication tokens and challenges, the distinction between critical and normal data labels, and an API12 update increasing data label capacity to 2048 bytes. Intended for developers implementing secure storage solutions, this document requires understanding of data types like UInt32 and Array<UInt8> and secure asset management concepts.

### 接口介绍_4.md 接口介绍_4.md
This document serves as a technical reference defining specific data fields for asset attribute queries, focusing on local custom labels and encryption settings. It details the structure of `DATA_LABEL_NORMAL_LOCAL` fields (1 through 4) and the `REQUIRE_ATTR_ENCRYPTED` boolean flag. Key takeaways include that local label fields are optional byte arrays up to 2048 bytes used for custom business logic without integrity protection or synchronization. The encryption flag allows filtering data based on whether custom auxiliary information is encrypted. Intended for developers integrating with the system's API, requiring knowledge of data types like UInt8 arrays and boolean query parameters.

### 接口介绍_5.md 接口介绍_5.md
This document functions as a specific API reference guide detailing the parameter structure for the postQuery interface. The content focuses on a parameter list table that defines the necessary attributes for data transmission. Key information includes the specification of the AUTH_CHALLENGE parameter, which is identified as a mandatory 32-byte Array<UInt8> designed to handle user authentication challenges. This technical documentation is intended for developers and system integrators who need to implement secure authentication protocols within the system, ensuring correct data formatting for the postQuery operation.