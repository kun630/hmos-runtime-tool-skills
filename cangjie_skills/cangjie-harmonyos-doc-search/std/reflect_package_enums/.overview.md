# reflect_package_enums

This directory contains comprehensive API reference documentation for the enumeration types utilized within the Cangjie reflection package. It specifically focuses on the `ModifierInfo` enum, a fundamental component for representing and identifying code modifiers during runtime type inspection. The documentation thoroughly details the enum's specific values, including `Open`, `Mut`, `Static`, and `Abstract`, while providing important context regarding the exclusion of access control modifiers—explaining that reflection only exposes public types. Furthermore, it documents standard inherited methods such as `hashCode`, `toString`, and equality operators, which adhere to standard enum semantics. This resource is designed for developers and engineers working with the Cangjie language who require the ability to programmatically analyze class or function modifiers and perform deep introspection on code structures at runtime.

## Quick Navigation

What do you want to learn?

*   Understand the core definition and structural role of the ModifierInfo enum in reflection → reflect_package_enums.md
*   Learn about specific modifier values including Open, Mut, Static, and Abstract → reflect_package_enums.md
*   Understand the technical reasons why access control modifiers are excluded from reflection → reflect_package_enums.md
*   Review documentation on inherited methods such as hashCode, toString, and equality operators → reflect_package_enums.md

## Detailed Description

### reflect_package_enums.md

This document serves as API reference documentation for the ModifierInfo enum in the Cangjie reflection package, defining how code modifiers are represented. It details the enum's specific values, including Open, Mut, Static, and Abstract, while noting that access control modifiers are excluded because reflection only exposes public types. The text also documents inherited methods such as hashCode, toString, and equality operators, which rely on standard enum semantics. This resource is designed for developers working with runtime type inspection in Cangjie who need to programmatically analyze class or function modifiers.