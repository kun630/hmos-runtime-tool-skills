# 证书锁定

This directory focuses on Certificate Pinning, a vital security technique used to secure HTTPS connections by strictly limiting them to specific, developer-defined certificates or public keys. It addresses the need to prevent Man-in-the-Middle (MitM) attacks and ensures robust network communication protocols. The content covers the complete workflow, from understanding the hierarchical JSON configuration structure—encompassing global base-config and domain-specific rules—to the practical execution of pinning using OpenSSL commands for SHA256 hash generation. It highlights critical operational considerations, such as the necessity of app updates when server certificates change. This documentation is primarily intended for application developers and system administrators, particularly those operating within the HarmonyOS/OpenHarmony ecosystem, who are responsible for implementing and maintaining high-security standards in their applications.

## Quick Navigation

What do you want to learn?

- Do you need a technical reference for JSON configuration, including field definitions and hierarchy? → JSON配置文件示例.md
- Do you need a practical implementation guide with OpenSSL commands and security warnings? → 证书锁定_3more.md

## Detailed Description

The following documents provide in-depth technical details regarding the configuration and implementation of certificate pinning strategies.

### JSON配置文件示例.md JSON配置文件示例.md

This document serves as a technical reference guide for configuring network security settings via JSON, specifically focusing on certificate trust anchors and public key pinning. It covers two primary configuration examples: pre-setting application-level certificates and configuring certificate public key hash values, followed by a comprehensive field definition table. The content details the hierarchical structure of the configuration object, including base-config for global settings and domain-config for specific domain rules, while explaining parameters like digest-algorithm and expiration. This guide is intended for developers or system administrators implementing secure network communication protocols within applications.

### 证书锁定_3more.md 证书锁定_3more.md

This technical guide explains how to implement certificate pinning to secure HTTPS connections by restricting them to developer-specified certificates. The document covers configuration file setup, pre-setting application-level certificates (crt/pem), and pre-setting certificate public key hashes. Key information includes the configuration location in network_config.json and specific OpenSSL commands for fetching server certificates and calculating SHA256 public key hashes. A critical warning highlights that certificate chain matching requires app updates if the server certificate changes. This guide targets application developers seeking to enhance network security, specifically those working within the HarmonyOS/OpenHarmony ecosystem.