# class_BufferedInputStreamT_where_T_InputStream

This directory provides comprehensive API documentation for the `BufferedInputStream` class within the Cangjie standard library. It details a generic wrapper designed to enhance input stream performance through buffering. The content covers class initialization, including various constructor overloads for memory optimization, and extends functionality for resource management and seekable operations. Additionally, it documents essential reading functions and stream state management. This resource is intended for Cangjie developers seeking to implement efficient, safe, and high-performance I/O operations in their applications.

## Quick Navigation

What do you want to learn?

- Understand class definition and constructor overloads → class_BufferedInputStreamT_4more.md
- Implement automatic resource management (try-with-resources) → extendT_BufferedInputStreamT_Resource_whe_aed56789.md
- Enable random access and cursor movement (Seekable) → extendT_BufferedInputStreamT_Seekable_whe_d6560437.md
- Perform read operations and stream binding → func_readArrayByte_3more.md

## Detailed Description

### class_BufferedInputStreamT_4more.md class_BufferedInputStreamT_4more.md
This API documentation defines the BufferedInputStream class in the Cangjie standard library, designed to wrap input streams with a buffer to optimize data reading performance. The document details the class inheritance from InputStream and explains three constructor overloads: one using a default buffer size of 4096, one accepting a custom Array<Byte> buffer for memory reuse, and one specifying a custom capacity. Key takeaways include the performance benefits of buffer reuse and the requirement that custom buffers or capacities must be greater than zero, otherwise an IllegalArgumentException is thrown. This reference guide targets Cangjie developers implementing efficient I/O operations who need to understand initialization parameters and exception handling for buffered streams.

### extendT_BufferedInputStreamT_Resource_whe_aed56789.md extendT_BufferedInputStreamT_Resource_whe_aed56789.md
This API documentation describes the extension of the Cangjie BufferedInputStream class to implement the Resource interface, enabling automatic resource management via try-with-resource syntax. The document details the implementation structure, listing the parent Resource type and defining two primary methods: close(), which terminates the stream and warns against subsequent usage, and isClosed(), which returns a Boolean status. A comprehensive code example demonstrates creating a custom input stream, wrapping it in a BufferedInputStream, and utilizing the try block for automatic resource cleanup. This reference is intended for Cangjie developers seeking to manage IO resources efficiently and safely.

### extendT_BufferedInputStreamT_Seekable_whe_d6560437.md extendT_BufferedInputStreamT_Seekable_whe_d6560437.md
This API documentation defines a Cangjie language extension that implements the Seekable interface for BufferedInputStream, enabling random access operations like querying data length and moving the cursor. The document details three properties—length, position, and remainLength—and the seek function, which moves the cursor while clearing the buffer. Key takeaways include that seeking before the stream start throws an IOException, while seeking past the end is permitted. The content includes a code example demonstrating these features with a custom stream. This reference material is intended for developers utilizing the standard IO library who require buffered reading with seek capabilities.

### func_readArrayByte_3more.md func_readArrayByte_3more.md
This document serves as API reference documentation for input stream operations within the Cangjie standard library. It covers three primary functions: read(Array<Byte>) for reading data into a buffer, readByte() for retrieving single bytes, and reset(T) for binding new input streams. Key information includes parameter specifications, return types such as Int64 and optional Byte, and exception handling for invalid arguments. The document provides verified code examples demonstrating usage with BufferedInputStream and ByteBuffer. Targeted at Cangjie developers, this guide facilitates understanding of buffered data reading, error handling, and stream state management.