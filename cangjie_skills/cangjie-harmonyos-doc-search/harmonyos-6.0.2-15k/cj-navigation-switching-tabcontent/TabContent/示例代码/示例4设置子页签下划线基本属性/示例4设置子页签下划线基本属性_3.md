),
                            {
                                => this.indicatorHeight = 10
                            }
                        )
                    }
                    this.heightFlag = !this.heightFlag
                }
            )
            Button("下划线宽度变化").width(100.percent).margin(bottom: 12.vp).onClick(
                {
                    =>
                    // 对Button组件的宽高属性进行动画配置
                    if (this.widthFlag) {
                        animateTo(
                            AnimateParam(
                                duration: 1000, // 动画时长
                                curve: Curve.Linear, // 动画曲线
                                delay: 200, // 动画延迟
                                iterations: 1, // 播放次数
                                playMode: PlayMode.Normal, // 动画模式
                                onFinish: {=> AppLog.info("width change play end, colorFlag:${this.widthFlag}")}
                            ),
                            {
                                => this.indicatorWidth = 30
                            }
                        )
                    } else {
                        animateTo(
                            AnimateParam(
                                duration: 1000, // 动画时长
                                curve: Curve.Linear, // 动画曲线
                                delay: 200, // 动画延迟
                                iterations: 1, // 播放次数
                                playMode: PlayMode.Normal, // 动画模式
                                onFinish: {=> AppLog.info("width change play end, colorFlag:${this.widthFlag}")}
                            ),
                            {
                                => this.indicatorWidth = 50
                            }
                        )
                    }
                    this.widthFlag = !this.widthFlag
                }
            )
            Button("下划线圆角半径变化").width(100.percent).margin(bottom: 12.vp).onClick(
                {
                    =>
                    // 对Button组件的宽高属性进行动画配置
                    if (this.borderFlag) {
                        animateTo(
                            AnimateParam(
                                duration: 1000, // 动画时长
                                curve: Curve.Linear, // 动画曲线
                                delay: 200, // 动画延迟
                                iterations: 1, // 播放次数
                                playMode: PlayMode.Normal, // 动画模式
                                onFinish: {=> AppLog.info("border change play end, colorFlag:${this.borderFlag}")}
                            ),
                            {
                                => this.indicatorBorderRadius = 0
                            }
                        )
                    } else {
                        animateTo(
                            AnimateParam(
                                duration: 1000, // 动画时长