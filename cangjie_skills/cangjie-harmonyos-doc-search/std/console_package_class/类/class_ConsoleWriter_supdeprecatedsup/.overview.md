# class_ConsoleWriter_supdeprecatedsup

This directory contains API reference documentation for the deprecated `ConsoleWriter` class and related console output functions in the Cangjie programming language. It covers the class structure, inheritance from `OutputStream`, and thread-safe writing mechanisms to standard output or error streams. The documentation details instantiation methods, atomic write operations, and overloaded functions for various data types including integers, strings, and generic types. It is suitable for Cangjie developers maintaining legacy code or understanding console I/O operations, noting the deprecation warning directing users to the `env` package.

## Quick Navigation

**What do you want to learn?**

*   Understand the deprecated `ConsoleWriter` class structure and its methods → class_ConsoleWriter_supde_12more.md
*   Learn about `write` functions for strings, integers, and generic types → func_writeString_13more.md
*   Find specific `writeln` function signatures for integers, runes, and strings → func_writelnInt64_9more.md

## Detailed Description

### class_ConsoleWriter_supde_12more.md class_ConsoleWriter_supde_12more.md
This document provides API reference documentation for the deprecated ConsoleWriter class in the Cangjie programming language. It details the class structure, which inherits from OutputStream, and explains its thread-safe functionality for writing to standard output or error streams. The content covers instantiation methods via Console.stdOut and Console.stdErr, along with specifications for the flush function and numerous overloaded write methods supporting data types such as Byte arrays, integers, floats, Booleans, and Runes. Key information includes the deprecation warning directing users to the env package and the guarantee that write operations are atomic. This reference targets Cangjie developers implementing console I/O operations.

### func_writeString_13more.md func_writeString_13more.md
This document serves as API reference documentation for input and output operations in the Cangjie programming language, specifically detailing functions to write data to standard output or error streams. It covers overloaded `write` functions for strings, unsigned integers, and generic types implementing the ToString interface, as well as `writeln` functions for byte arrays, booleans, floating-point numbers, and signed integers. The key distinction is that `writeln` appends a newline character to the output, whereas `write` does not. This technical reference is intended for Cangjie developers looking to perform console output operations across various primitive and custom data types.

### func_writelnInt64_9more.md func_writelnInt64_9more.md
This document serves as API reference documentation for the writeln function in the Cangjie programming language, detailing its overloaded implementations for various data types. The content covers specific function signatures for signed and unsigned integers (Int64, Int8, UInt16, UInt32, UInt64, UInt8), the Rune character type, the String type, and a generic template for types implementing the ToString interface. Key takeaways include that all functions output the text representation of the input value followed by a newline to standard output or error streams, returning a Unit value. This reference is intended for Cangjie developers seeking syntax details for printing formatted output of primitive and custom types.