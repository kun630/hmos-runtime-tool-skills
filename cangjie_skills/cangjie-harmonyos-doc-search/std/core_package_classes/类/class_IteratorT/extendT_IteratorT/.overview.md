# extendT_IteratorT

This directory contains comprehensive API documentation for the Iterator type in the Cangjie programming language. It covers a wide range of utility functions and extension methods designed for efficient data sequence manipulation, including terminal operations like `reduce` and `count`, intermediate operations like `filter` and `map`, and traversal control methods like `skip` and `zip`. The documentation distinguishes between operations that consume iterators and those that produce new ones. It is suitable for Cangjie developers looking to implement functional programming patterns and handle collections effectively.

## Quick Navigation

**What do you want to do?**

*   **Check conditions or access specific elements?**
    *   Verify element conditions (`all`, `any`) or access by index (`at`) → extendT_IteratorT_5more.md
    *   Count elements or get the first one → func_count_5more.md
    *   Check emptiness or get the last element → func_isEmpty_5more.md
*   **Transform or map data?**
    *   Filter, enumerate, or map elements → func_count_5more.md, func_isEmpty_5more.md
    *   Flatten nested structures or apply side effects (`flatMap`, `forEach`) → func_flatMapRT_-_IteratorR_5more.md
*   **Control iteration flow?**
    *   Skip, step, or take specific amounts of elements → func_skipInt64_3more.md
*   **Combine iterators?**
    *   Chain iterators together (`concat`) → extendT_IteratorT_5more.md
    *   Merge two iterators into tuples (`zip`) → func_zipRIteratorR.md

## Detailed Description

### extendT_IteratorT_5more.md extendT_IteratorT_5more.md
This API documentation file details the extension methods for the Iterator type in the Cangjie programming language, specifically focusing on five additional utility functions. The document covers terminal operations like `all` and `any` for checking element conditions, the `at` method for indexed access, and the intermediate operation `concat` for chaining iterators. It distinguishes between operations that consume elements versus those producing new iterators, providing function signatures, parameter descriptions, return types, and verified code examples for each method. This reference guide targets Cangjie developers seeking to manipulate data sequences efficiently, requiring knowledge of generic types and functional programming concepts.

### func_count_5more.md func_count_5more.md
This document provides API reference documentation for iterator utility functions in the Cangjie programming language. It covers five specific methods: `count`, `enumerate`, `filter`, `filterMap`, and `first`. Each section includes the function signature, a description of functionality, parameters, return values, and executable code examples demonstrating usage. Key takeaways include understanding that methods like `count` and `first` consume the iterator, leaving it empty, while others like `filter` and `enumerate` return new iterators. This technical guide is intended for Cangjie developers who need to perform data manipulation operations on sequences, assuming familiarity with the language's syntax and type system.

### func_flatMapRT_-_IteratorR_5more.md func_flatMapRT_-_IteratorR_5more.md
This document serves as API reference documentation for five specific functional methods of the Iterator class in the Cangjie programming language. It details the syntax, functionality, parameters, and return values for `flatMap`, `fold`, `forEach`, `inspect`, and `intersperse`. Key concepts include mapping and flattening nested iterators, accumulating values via `fold`, executing side effects with `forEach` and `inspect`, and inserting separators with `intersperse`. Each function includes verified code examples demonstrating usage and output. The target audience is Cangjie developers seeking to manipulate data sequences and perform functional operations on iterators.

### func_isEmpty_5more.md func_isEmpty_5more.md
This document serves as API reference documentation for five specific methods within the Cangjie Iterator class. It details the functionality, parameters, and return values for `isEmpty()`, `last()`, `map<R>()`, `none()`, and `reduce()`. Crucially, it highlights side effects, such as `isEmpty()` consuming an element and `last()` consuming all elements, while providing verified code examples. The target audience is Cangjie developers seeking to manipulate data sequences, requiring knowledge of basic iterator concepts and syntax.

### func_skipInt64_3more.md func_skipInt64_3more.md
This document serves as API reference documentation for three iterator manipulation functions in the Cangjie programming language. It details the `skip`, `step`, and `take` functions, each accepting an `Int64` parameter to control element traversal. Key information includes that `skip` discards a specific number of leading elements, `step` skips elements between `next()` calls, and `take` retrieves a set number of leading elements. All functions return an `Iterator<T>` and throw `IllegalArgumentException` for invalid negative counts. The target audience is Cangjie developers seeking to manipulate data sequences efficiently.

### func_zipRIteratorR.md func_zipRIteratorR.md
This document provides API reference documentation for the zip function in the Cangjie programming language, designed to merge two iterators into a single iterator of tuples. The content details the function signature, parameters, and return values, specifically noting that the resulting iterator length depends on the shorter input iterator. A practical code example demonstrates how to combine two arrays and iterate through the resulting tuples. This guide targets Cangjie developers seeking to synchronize iteration over multiple collections, serving as a technical reference for understanding iterator manipulation and data pairing.