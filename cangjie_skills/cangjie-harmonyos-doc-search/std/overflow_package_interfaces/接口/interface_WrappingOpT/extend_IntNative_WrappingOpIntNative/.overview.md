# extend_IntNative_WrappingOpIntNative

This directory provides comprehensive API documentation for the `WrappingOp` interface implementation specifically tailored for the `IntNative` type within the Cangjie programming language. It focuses on a wide array of arithmetic and bitwise operations that manage integer overflow via high-order truncation, ensuring that results wrap around predictably instead of triggering exceptions. The documentation covers essential functions such as addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and various bit shifts. This resource is highly suitable for Cangjie developers and system-level engineers who need deterministic integer behavior in performance-critical code paths where error handling overhead is unacceptable.

## Quick Navigation

**What do you want to learn?**
*   Understand the comprehensive implementation of the `WrappingOp` interface for `IntNative` including all standard arithmetic and bitwise operators → extend_IntNative_WrappingO_9more.md
*   Review specific API references for advanced right shift logic and subtraction overflow handling → func_wrappingShrUInt64_2more.md

**What do you want to do?**
*   Perform standard arithmetic operations (add, sub, mul, div, mod) with automatic overflow wrapping and no error throwing → extend_IntNative_WrappingO_9more.md
*   Execute bitwise manipulations including negation, increment, decrement, and left shift with overflow protection → extend_IntNative_WrappingO_9more.md
*   Implement right shift logic using high-bit truncation for shift amounts exceeding bit width → func_wrappingShrUInt64_2more.md
*   Handle subtraction operations that require truncating high bits upon overflow to maintain data integrity → func_wrappingShrUInt64_2more.md

## Detailed Description

### extend_IntNative_WrappingO_9more.md extend_IntNative_WrappingO_9more.md
This API documentation details the implementation of the `WrappingOp` interface for the `IntNative` type in the Cangjie programming language. The document covers a comprehensive set of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and left shift. Key information highlights that all functions utilize a high-order truncation strategy to handle overflow, wrapping results rather than throwing errors. The left shift operation specifically includes logic to handle shift counts exceeding the bit width. This reference is intended for Cangjie developers requiring precise control over integer overflow behavior in system-level or performance-critical code.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md
This document serves as API reference documentation for wrapping arithmetic and bitwise operations within the Cangjie programming language. It specifically details two functions: `wrappingShr`, which performs a right shift using a high-bit truncation strategy where the shift amount is determined by low bits if it exceeds the bit width, and `wrappingSub`, which handles subtraction by truncating high bits upon overflow. Both functions accept parameters of specific integer types and return an `IntNative` value. This technical reference is intended for Cangjie developers who need precise control over bit manipulation and overflow behavior in their code.