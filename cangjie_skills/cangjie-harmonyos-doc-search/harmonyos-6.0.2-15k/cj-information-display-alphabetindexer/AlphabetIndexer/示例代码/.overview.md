# 示例代码

This directory contains Cangjie code examples for HarmonyOS development, specifically focusing on the AlphabetIndexer component. It covers essential UI customization techniques including setting popup text content, enabling adaptive collapse modes, and applying background blur materials to prompt windows. These examples demonstrate how to configure properties like `popupFont`, `autoCollapse`, and `popupBackgroundBlurStyle` to enhance user interaction in list navigation. Suitable for HarmonyOS developers using the Cangjie language who need to implement or refine alphabetical indexing features with custom styling and responsive behaviors.

## Quick Navigation

**What do you want to learn?**

*   Configure text content and styling for the index popup → 示例1设置提示弹窗显示文本内容.md
*   Enable adaptive collapse mode for responsive layouts → 示例2开启自适应折叠模式.md
*   Apply background blur effects to the popup window → 示例3设置提示弹窗背景模糊材质.md

## Detailed Description

### 示例1设置提示弹窗显示文本内容.md 示例1设置提示弹窗显示文本内容.md
This document provides a Cangjie code example demonstrating how to configure the text content and styling of the prompt popup within an AlphabetIndexer component. The content covers the implementation of the EntryView class, data array definitions, and the detailed configuration of the AlphabetIndexer properties. Key takeaways include using the onRequestPopupData callback to map index letters to specific data arrays and customizing the popup appearance using properties like popupFont, popupColor, and popupItemBorderRadius. This tutorial is intended for HarmonyOS developers using the Cangjie language who need to implement list indexing with custom popup feedback and styling.

### 示例2开启自适应折叠模式.md 示例2开启自适应折叠模式.md
This document provides a Cangjie code example demonstrating how to implement the AlphabetIndexer UI component with adaptive collapse mode enabled. The code illustrates the configuration of the indexer's layout, styling, and data sources, specifically focusing on the autoCollapse property and dynamic height adjustment. Key takeaways include the implementation of event handlers for selection and popup data requests, as well as methods to toggle the collapse state and resize the component programmatically. Intended for HarmonyOS developers using the Cangjie language, this example serves as a practical guide for creating responsive alphabetical navigation interfaces.

### 示例3设置提示弹窗背景模糊材质.md 示例3设置提示弹窗背景模糊材质.md
This document serves as a code example demonstrating how to customize the background blur material of the popup window in an AlphabetIndexer component within a HarmonyOS application using the Cangjie language. The code covers UI layout construction using Stack and Row, data population with List and ForEach, state management for blur styles, and the specific configuration of the AlphabetIndexer including popup alignment, border radius, and background blur settings. The key takeaway is the usage of the popupBackgroundBlurStyle property applied to the AlphabetIndexer. It shows how to dynamically switch between BlurStyle.COMPONENT_REGULAR and BlurStyle.BACKGROUND_THIN via button clicks, affecting the visual appearance of the index popup. This tutorial is intended for HarmonyOS developers using the Cangjie language who need to implement or customize list indexing features with specific visual effects like background blurring.