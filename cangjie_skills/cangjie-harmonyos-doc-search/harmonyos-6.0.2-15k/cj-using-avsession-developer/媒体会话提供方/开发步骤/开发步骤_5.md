session.on(AVSessionEventType.Seek, SeekCallback({
        time =>
        AppLog.info("on seek , the seek time is ${time}")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('seek')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态和播放position。
    }))

    session.on(AVSessionEventType.SetSpeed, SetSpeedCallback({
        speed =>
        AppLog.info("on setSpeed , the speed is ${speed}")
        // do some tasks ···
    }))

    session.on(AVSessionEventType.SetLoopMode, SetLoopModeCallback({
        mode =>
        AppLog.info("on setLoopMode , the loop mode is ${mode}")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('setLoopMode')取消监听。
        // 应用自定下一个模式，处理完毕后，请使用SetAVPlayState上报切换后的LoopMode。
    }))

    session.on(AVSessionEventType.ToggleFavorite, ToggleFavoriteCallback({
        assetId =>
        AppLog.info("on toggleFavorite , the target asset Id is ${assetId}")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('toggleFavorite')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报收藏结果isFavorite。
    }))
    ```

    6.2 高级播控事件的监听

    Session侧的可以注册的高级播控事件主要包括：

    - skipToQueueItem: 播放列表其中某项被选中的事件。
    - handleKeyEvent: 按键事件。
    - outputDeviceChange: 播放设备变化的事件。
    - commonCommand: 自定义控制命令变化的事件。

    ```cangjie
    import ohos.base.*
    import kit.AVSessionKit.*
    import kit.InputKit.*
    import std.collection.*

    class SkipToQueueItemCallback <: Callback1Argument<Int32> {
        public SkipToQueueItemCallback(let f: (Int32) -> Unit) {}
        public open func invoke(itemId: Int32) {
            f(itemId)
        }
    }

    class HandleKeyEventCallback <: Callback1Argument<KeyEvent> {
        public HandleKeyEventCallback(let f: (KeyEvent) -> Unit) {}
        public open func invoke(event: KeyEvent) {
            f(event)
        }
    }

    class OutputDeviceChangeCallback <: Callback2Argument<ConnectionState, OutputDeviceInfo> {
        public OutputDeviceChangeCallback(let f: (ConnectionState, OutputDeviceInfo) -> Unit) {}
        public open func invoke(arg1: ConnectionState, arg2: OutputDeviceInfo) {
            f(arg1, arg2)
        }
    }

    class CommonCommandCallback <: Callback2Argument<String, HashMap<String, ValueType>> {
        public CommonCommandCallback(let f: (String, HashMap<String, ValueType>) -> Unit) {}
        public open func invoke(arg1: String, arg2: HashMap<String, ValueType>) {
            f(arg1, arg2)
        }
    }

    let context = Global.getStageContext() // 获取Context应用上下文请参见 [API 参考] -> [API参考概述] -> [开发说明] -> [仓颉示例代码说明]
    // 假设已经创建了一个session，如何创建session可以参考之前的案例。
    let `type`: AVSessionType = AVSessionType.SESSION_TYPE_AUDIO
    let session = createAVSession(context, 'SESSION_NAME', `type`)
    // 一般在监听器中会对播放器做相应逻辑处理。
    // 不要忘记处理完后需要通过set接口同步播放相关信息，参考上面的用例。
    session.on(
        AVSessionEventType.SkipToQueueItem,
        SkipToQueueItemCallback({
            itemId => AppLog.info("on skipToQueueItem , do skip task")
        // do some tasks ···
        })
    )
    session.on(
        AVSessionEventType.HandleKeyEvent,
        HandleKeyEventCallback({
                event => AppLog.info("on handleKeyEvent")
            // do some tasks ···
            })
    )
    session.on(
        AVSessionEventType.OutputDeviceChange,
        OutputDeviceChangeCallback({
                state, device => AppLog.info('on outputDeviceChange')
            // do some tasks ···
            })
    )
    session.on(
        AVSessionEventType.CommonCommand,
        CommonCommandCallback({
                commandString, args => AppLog.info('on commonCommand')
            // do some tasks ···
            })
    )
    ```

7. 获取当前媒体会话自身的控制器，与媒体会话对应进行通信交互。

    ```cangjie
    import ohos.base.*
    import kit.AVSessionKit.*