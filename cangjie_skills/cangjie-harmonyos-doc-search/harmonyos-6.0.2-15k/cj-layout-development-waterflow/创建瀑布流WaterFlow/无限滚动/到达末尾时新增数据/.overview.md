# 到达末尾时新增数据

This directory focuses on the technical implementation and performance optimization of dynamic data loading within WaterFlow components, specifically addressing scenarios where new data is appended as the user scrolls to the end of a list. It provides comprehensive guidance on utilizing the Cangjie language to handle infinite scrolling, covering essential mechanisms such as the `onReachEnd` event callback, `LazyForEach` rendering, and custom `IDataSource` class creation. Furthermore, it delves into advanced best practices for maintaining high performance, emphasizing methods to avoid layout stuttering caused by full data reloads. Key concepts include `onDatasetChange`, `DataAddOperation`, and efficient layout recalculation. This resource is primarily suitable for ArkUI developers and engineers aiming to build smooth, responsive, and high-performance list interfaces in their applications.

## Quick Navigation

**What do you want to learn?**
*   Implement the basic logic for infinite scrolling and data appending → 到达末尾时新增数据_1.md
*   Apply best practices to optimize performance and prevent layout stuttering → 到达末尾时新增数据_2.md

## Detailed Description

The following documents provide in-depth technical guidance. The first file outlines the fundamental steps required to implement infinite scrolling functionality, while the second file focuses on critical performance optimization techniques to ensure the application remains responsive during data updates.

### 到达末尾时新增数据_1.md 到达末尾时新增数据_1.md
This tutorial explains how to implement infinite scrolling in a WaterFlow component by dynamically adding new data when the user reaches the end of the list. It covers the implementation of the onReachEnd event callback, the use of LazyForEach for efficient rendering, and the creation of a custom IDataSource class in Cangjie. Key takeaways include the correct method for appending data items and the importance of using specific data change notifications like DataAddOperation rather than full reloads. This guide is intended for ArkUI developers using the Cangjie language to build dynamic, high-performance list interfaces.

### 到达末尾时新增数据_2.md 到达末尾时新增数据_2.md
This technical guide provides best practices for optimizing waterfall flow layouts when appending new data to the end of a list. It covers the performance implications of data reloading, specifically how variable node heights trigger full layout recalculations that cause stuttering. The core takeaway is to use the `onDatasetChange()` method to notify the layout of additions, thereby avoiding redundant processing of existing data. This guide is intended for developers seeking to improve application performance in list-based interfaces. Keywords: waterfall flow, onDatasetChange, performance optimization, layout recalculation, data appending.