# class_PhotoSession

This directory contains API reference documentation for the `PhotoSession` class within the Cangjie CameraKit framework. It focuses on managing normal photo mode operations, including session initialization, event listener management, and preconfiguration of camera settings like resolution and aspect ratio. Key topics include the `on` and `off` methods for handling callbacks, `preconfig` for setting up session parameters, and error handling via `BusinessException`. This documentation is suitable for developers using the Cangjie language to implement robust camera functionality, requiring knowledge of system capabilities, asynchronous state updates, and callback management.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure and basic capabilities? → class_PhotoSession_3more.md
*   Register listeners for focus or zoom events? → func_onCameraCallbackType__2more.md
*   Register error callbacks or unregister listeners? → func_offCameraCallbackType_2more.md
*   Preconfigure camera resolution and aspect ratio? → func_preconfigPreconfig_9c44a0ad.md

## Detailed Description

### class_PhotoSession_3more.md class_PhotoSession_3more.md
This document provides API reference details for the PhotoSession class in the Cangjie CameraKit, designed for managing normal photo mode operations. It details the class inheritance hierarchy involving Session, Flash, and Focus interfaces, and documents two specific methods: canPreconfig for checking resolution support and off for unregistering error event callbacks. Key information includes method signatures, parameter types like PreconfigType and CameraCallbackType, return values, and potential BusinessException error codes. Code examples demonstrate usage within an application context. Targeted at developers using the Cangjie language to implement camera functionality, requiring knowledge of system capabilities and callback handling.

### func_offCameraCallbackType_2more.md func_offCameraCallbackType_2more.md
This API reference document details methods for managing camera event listeners within the Cangjie CameraKit framework. It covers the `off` function for unregistering listeners and the `on` function for registering error callbacks. Key information includes that `off` handles errors, focus state changes, and smooth zoom info, while `on` specifically targets error events using a `BusinessException` callback. Notably, the documentation specifies that `off` cannot be called inside the `on` callback. This guide is intended for developers implementing error handling and state monitoring in normal photo sessions.

### func_onCameraCallbackType__2more.md func_onCameraCallbackType__2more.md
This API reference document details two overloaded `on` functions within the Cangjie CameraKit for registering camera event listeners. The main sections cover monitoring focus state changes and smooth zoom information availability, providing function signatures, parameter tables, and code examples. Key information includes the requirement for specific `CameraCallbackType` values and the constraint that the `off` method cannot be invoked inside the callback. The document targets developers using the Cangjie language to handle asynchronous camera state updates in multimedia applications.

### func_preconfigPreconfig_9c44a0ad.md func_preconfigPreconfig_9c44a0ad.md
This document serves as API reference documentation for the preconfig function in the Cangjie language, designed to preconfigure a camera session. It details the function signature, system capabilities, and version requirements. The main sections cover parameter definitions, specifically the required PreconfigType for resolution and the optional PreconfigRatio for aspect ratio, alongside exception handling for camera service errors. Key information includes the default aspect ratio of 4:3 and specific error codes like 7400201. The document targets developers using the CameraKit who need to initialize session settings, providing a practical code example demonstrating context retrieval and session management.