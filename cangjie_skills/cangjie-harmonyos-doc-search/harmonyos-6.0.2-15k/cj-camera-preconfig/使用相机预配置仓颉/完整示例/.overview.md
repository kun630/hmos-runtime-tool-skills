# 完整示例

This directory contains a comprehensive collection of code examples designed to demonstrate the complete implementation of camera shooting functionality using the Cangjie language and CameraKit framework. It covers the entire development lifecycle, starting from defining callback classes for status and error handling to initializing the CameraManager and configuring PhotoSessions. The content details critical steps such as creating camera inputs and outputs, checking supported hardware modes, applying preconfigurations like 1080P resolution, and linking preview and photo streams. Additionally, it addresses advanced topics including flash and focus mode verification, zoom ratio adjustment, and the execution of photo capture commands. These examples are intended for HarmonyOS developers who possess prior knowledge of UIAbility contexts and require a practical, step-by-step guide to integrate robust camera capabilities into their mobile applications while ensuring proper resource management and lifecycle control.

## Quick Navigation

What do you want to learn?

- Set up the camera environment, initialize the CameraManager, and configure basic session inputs/outputs → 完整示例_1.md
- Perform the actual photo capture, verify hardware support for flash/focus, and manage session resources → 完整示例_2.md

## Detailed Description

### 完整示例_1.md 完整示例_1.md
This document provides a complete code example demonstrating how to implement camera shooting functionality using the Cangjie language and CameraKit. The content covers defining callback classes for status and error handling, initializing the CameraManager, creating camera inputs and outputs, and configuring a PhotoSession. Key takeaways include the step-by-step workflow of opening a camera, checking supported modes, applying preconfigurations like 1080P, and linking preview and photo streams to the session. Intended for HarmonyOS developers, this example requires prior knowledge of UIAbility contexts and serves as a practical guide for integrating camera capabilities into applications.

### 完整示例_2.md 完整示例_2.md
This document serves as a practical code example demonstrating the end-to-end workflow for capturing photos using a camera session. It covers session initialization and configuration, hardware capability checks for flash and focus modes, zoom ratio adjustment, and the execution of the photo capture command. Key takeaways include the importance of verifying device support for specific modes before setting them and the critical sequence for resource management, specifically stopping the session and releasing inputs and outputs after capture to prevent errors. Intended for mobile application developers, this snippet provides a reference for implementing robust camera features with proper lifecycle management.