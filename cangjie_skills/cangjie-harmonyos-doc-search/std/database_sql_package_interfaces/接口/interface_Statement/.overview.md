# interface_Statement

This directory contains comprehensive API documentation for the Cangjie programming language's `Statement` interface, which is essential for managing pre-executed SQL statements and database connections. It covers core database operations such as querying, updating, parameter binding, and option setting, while also addressing error handling for `SqlException`. Additionally, it provides critical information regarding deprecated functions and migration paths for legacy code. Key topics include the inheritance from the `Resource` type and the management of parameterized queries. This documentation is intended for software developers and database engineers working within the Cangjie environment who need to implement, maintain, or migrate database interaction logic. The directory includes references for both the main interface definition and specific deprecated utility functions to ensure smooth code transitions.

## Quick Navigation

**What do you want to learn?**

*   Understand the core Statement interface structure, methods like `query` and `update`, and inheritance from Resource → interface_Statement_8more.md
*   Find details on the specific deprecated `updateArraySqlDbTy` function and how to replace it with the new `update()` method → func_updateArraySqlDbTy_4e70bac6.md

## Detailed Description

### func_updateArraySqlDbTy_4e70bac6.md func_updateArraySqlDbTy_4e70bac6.md
This document serves as API reference for a deprecated function used to execute SQL statements and retrieve update results. It details the function signature, which accepts an array of SqlDbType parameters to replace placeholders within SQL queries. The content outlines the input parameters, the returned UpdateResult object, and potential SqlException errors related to network issues or incorrect argument counts. Crucially, it includes a deprecation warning advising users to adopt an alternative update() method in future versions. This information is intended for software developers maintaining or migrating database interaction code within the Cangjie programming environment.

### interface_Statement_8more.md interface_Statement_8more.md
This document provides API reference documentation for the Cangjie Statement interface, which handles pre-executed SQL statements and database connections. It details the interface structure, including the parameterColumnInfos property and methods such as query, update, set, and setOption. Key information includes the inheritance from the Resource type, the deprecation of the parameterized query method, and the handling of SqlException during execution. The document explains how to bind parameters, execute SQL commands, and retrieve results. Intended for developers working with database operations in Cangjie, it assumes familiarity with SQL and the language's type system. Keywords: Cangjie, API, SQL, Statement, database, interface, query, update, Resource.