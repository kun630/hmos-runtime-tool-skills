# 使用AVPlayer添加视频外挂字幕

This directory offers a comprehensive guide for developers looking to implement external subtitle functionality in video playback applications using the AVPlayer instance in the Cangjie language. It covers the complete development lifecycle, from setting subtitle resources via file descriptors or URLs to registering and managing subtitle update callbacks. The documentation highlights critical constraints, such as the necessity of setting subtitles prior to playback initiation. Key technical elements include code examples for `addSubtitleFromFd`, `addSubtitleFromUrl`, and handling `AVPlayerCallbackType.subtitleUpdate` events. This resource is ideal for application developers leveraging the MediaKit API to enhance user experience with multilingual or accessible video content.

## Quick Navigation

**What do you want to learn?**

*   Learn the step-by-step process and API usage for adding subtitles to AVPlayer → 使用AVPlayer添加视频外挂字幕_2more.md
*   Examine a full code implementation including class structure and error handling → 完整示例.md

## Detailed Description

The following files provide the specific implementation details and code examples required to integrate subtitles into your application.

### 使用AVPlayer添加视频外挂字幕_2more.md 使用AVPlayer添加视频外挂字幕_2more.md
This guide instructs developers on implementing external subtitle functionality for video playback using the AVPlayer instance in the Cangjie language. It covers development steps, specifically setting subtitle resources via file descriptors or URLs, registering subtitle update callbacks, and managing callback deregistration. Key information includes the constraint that subtitles must be set before playback begins. The document provides code examples for addSubtitleFromFd, addSubtitleFromUrl, and the on/off event listeners for AVPlayerCallbackType.subtitleUpdate. This tutorial targets application developers using the MediaKit API who need to enhance video players with subtitle support.

### 完整示例.md 完整示例.md
This document provides a complete Cangjie code example demonstrating how to implement subtitle functionality within an AVPlayer application. It covers the AVPlayerSubtitleDemo class for player management, methods for adding subtitles via URL or File Descriptor, callback handling for updates and errors, and the MainAbility setup for context initialization. Key operations include addSubtitleFromUrl, addSubtitleFromFd, and registering AVPlayerCallbackType listeners. The code illustrates error handling with a reset mechanism and logging subtitle metadata like text and duration. This guide targets HarmonyOS developers using the Cangjie language who need to integrate external or packaged subtitle files into media playback applications.