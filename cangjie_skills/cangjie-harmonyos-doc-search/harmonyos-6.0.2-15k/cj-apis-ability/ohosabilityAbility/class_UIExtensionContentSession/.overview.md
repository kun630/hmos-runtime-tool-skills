# class_UIExtensionContentSession

This directory contains API reference documentation for the `UIExtensionContentSession` class and related functions within the HarmonyOS AbilityKit framework, specifically for the Cangjie programming language. It covers essential lifecycle management tasks for UI extension components, including loading content, managing window privacy settings, implicitly starting abilities, and terminating sessions with result data. Key topics include `loadContent`, `setWindowPrivacyMode`, `startAbilityByType`, and `terminateSelf`. This documentation is suitable for HarmonyOS developers building UI extension components who need to handle interface rendering, security permissions, and session lifecycle events using Cangjie.

## Quick Navigation

What do you want to learn or do?

*   Manage the UI extension session lifecycle, load content, or set window privacy modes? → class_UIExtensionContentSe_3more.md
*   Implicitly start a UI extension ability from a foreground application? → func_startAbilityByTypeString_String_Abil_f807f481.md
*   Terminate the current session and return results to the caller? → func_terminateSelf_2more.md

## Detailed Description

### class_UIExtensionContentSe_3more.md class_UIExtensionContentSe_3more.md
This document provides API reference documentation for the Cangjie class UIExtensionContentSession, designed to manage interface loading and window behavior within the AbilityKit framework. It details two primary methods: loadContent, which loads specific page paths associated with LocalStorage, and setWindowPrivacyMode, which prevents screen recording when enabled. Key information includes required parameters, specific error codes like 401, and the necessary ohos.permission.PRIVACY_WINDOW permission for privacy features. The content includes code examples demonstrating usage within PhotoEditorExtensionAbility. This guide targets HarmonyOS developers building UI extension components who need to handle content rendering and security settings.

### func_startAbilityByTypeString_String_Abil_f807f481.md func_startAbilityByTypeString_String_Abil_f807f481.md
This document serves as API reference documentation for the Cangjie function startAbilityByType, which enables foreground applications to implicitly start a UIExtensionAbility using a specific type string. The content covers the function signature, system capabilities, version requirements, and detailed parameter descriptions including the ability type, extension parameters, and callback interface. It also lists potential business exceptions with specific error codes and provides practical code examples demonstrating the registration and invocation process. Key takeaways include that this function requires the app to be in the foreground and uses a callback to handle success or failure results. Targeted at HarmonyOS developers using the Cangjie language and AbilityKit, this reference requires knowledge of UIExtensionAbility components and asynchronous callback handling.

### func_terminateSelf_2more.md func_terminateSelf_2more.md
This API reference document details methods for terminating UI extension content sessions in the Cangjie programming language. It covers two primary functions: `terminateSelf()` for simple termination and `terminateSelfWithResult()` for termination with a return value. Key information includes that both functions stop the window interface object associated with `UIExtensionContentSession`. The latter accepts an `AbilityResult` parameter to pass data back to the `UIExtensionComponent`. Both functions can throw a `BusinessException` for parameter errors (code 401). The document includes code examples demonstrating usage within a photo editor context. This guide targets HarmonyOS developers working with UIExtensionAbilities who need to manage session lifecycles and return data, requiring knowledge of Cangjie syntax and the AbilityKit.