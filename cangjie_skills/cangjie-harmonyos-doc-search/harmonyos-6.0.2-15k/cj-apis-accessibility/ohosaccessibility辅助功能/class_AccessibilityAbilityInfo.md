## class AccessibilityAbilityInfo

```cangjie
public class AccessibilityAbilityInfo {}
```

**功能：** 辅助应用信息。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**起始版本：** 19

### prop abilityTypes

```cangjie
public prop abilityTypes: Array<AbilityType>
```

**功能：** 辅助应用类型。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** Array\<[AbilityType](#enum-abilitytype)>

**读写能力：** 只读

**起始版本：** 19

### prop bundleName

```cangjie
public prop bundleName: String
```

**功能：** Bundle名称。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** String

**读写能力：** 只读

**起始版本：** 19

### prop capabilities

```cangjie
public prop capabilities: Array<Capability>
```

**功能：** 辅助应用能力列表。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** Array\<[Capability](#enum-capability)>

**读写能力：** 只读

**起始版本：** 19

### prop description

```cangjie
public prop description: String
```

**功能：** 辅助应用描述。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** String

**读写能力：** 只读

**起始版本：** 19

### prop eventTypes

```cangjie
public prop eventTypes: Array<EventType>
```

**功能：** 辅助应用关注的无障碍事件列表。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** Array\<[EventType](#enum-eventtype)>

**读写能力：** 只读

**起始版本：** 19

### prop id

```cangjie
public prop id: String
```

**功能：** ability id。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** String

**读写能力：** 只读

**起始版本：** 19

### prop label

```cangjie
public prop label: String
```

**功能：** 扩展应用在扩展服务列表中的名称。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** String

**读写能力：** 只读

**起始版本：** 19

### prop name

```cangjie
public prop name: String
```

**功能：** ability名。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** String

**读写能力：** 只读

**起始版本：** 19

### prop needHide

```cangjie
public prop needHide: Bool
```

**功能：** 辅助应用是否在已安装的扩展服务列表中被隐藏，true表示隐藏服务，false表示显示服务。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** Bool

**读写能力：** 只读

**起始版本：** 19

### prop targetBundleNames

```cangjie
public prop targetBundleNames: Array<String>
```

**功能：** 关注的目标Bundle名称。

**系统能力：** SystemCapability.BarrierFree.Accessibility.Core

**类型：** Array\<String>

**读写能力：** 只读

**起始版本：** 19