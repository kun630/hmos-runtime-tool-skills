# class_ByteBuffer

This directory contains API reference documentation for the Cangjie `ByteBuffer` class, which manages in-memory byte stream read and write operations. It covers class structure, initialization, capacity management, and various methods for data manipulation like reading, writing, seeking, and cloning. Key topics include IOStream inheritance, Seekable interface, and exception handling. This resource is suitable for Cangjie developers implementing data buffering, stream manipulation, and low-level I/O operations.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure, initialization constructors, and basic properties like capacity → class_ByteBuffer_6more.md
*   Reset the buffer, create independent copies, or read data from the stream → func_clear_4more.md
*   Manage buffer capacity expansion or move the cursor position within the stream → func_reserveInt64_2more.md
*   Modify the buffer length or write data into the output stream → func_setLengthInt64_3more.md

## Detailed Description

### class_ByteBuffer_6more.md class_ByteBuffer_6more.md
This document provides API reference documentation for the Cangjie ByteBuffer class, designed for managing byte stream read and write operations. It covers the class structure, which inherits from IOStream and Seekable, and details the capacity property and bytes() method. The text explains three initialization constructors: default, specific size, and from an existing Array<Byte>, including error handling for negative capacities. Key information includes the behavior of data slicing and the mutability of returned arrays. This resource is intended for Cangjie developers implementing in-memory data buffering and stream manipulation.

### func_clear_4more.md func_clear_4more.md
This document provides API reference documentation for four specific methods of the ByteBuffer class in the Cangjie standard library. It covers the clear(), clone(), read(), and readByte() functions, detailing their syntax, parameters, return values, and exception handling. Key information includes how clear() resets data without changing capacity, clone() creates independent copies, and read methods handle data transfer with error checking. The content includes verified code examples demonstrating usage and expected output. This technical reference is intended for Cangjie developers working with IO operations and byte stream manipulation who need precise implementation details.

### func_reserveInt64_2more.md func_reserveInt64_2more.md
This API documentation details the usage and behavior of the `reserve` and `seek` functions within the Cangjie `ByteBuffer` class. It covers function signatures, parameters, return values, exception handling, and verified code examples for buffer capacity management and cursor positioning. The `reserve` function expands capacity using a max-growth strategy, while `seek` allows moving the cursor within or beyond data bounds but not before the start. Both functions handle specific exceptions like `IllegalArgumentException` and `IOException`. This reference guide targets Cangjie developers needing to manipulate byte buffers, requiring knowledge of basic I/O operations and exception handling.

### func_setLengthInt64_3more.md func_setLengthInt64_3more.md
This document serves as API reference documentation for specific functions within the Cangjie standard library's ByteBuffer class. It covers three main topics: the setLength(Int64) function for modifying buffer size without changing the seek offset, and the write functions for inserting data into the output stream. Key information includes parameter definitions, return types, and specific exception handling for setLength, such as IllegalArgumentException and OverflowException. Each function is accompanied by verified Cangjie code examples demonstrating usage and output. This resource is designed for developers working with byte stream operations who need precise functional definitions and implementation patterns.