# deriving_package_overview

This directory contains the definitive documentation for the `std.deriving` package, a robust framework designed to automate the generation of standard interface implementations for custom data types. It comprehensively covers the automatic derivation of essential behaviors such as `ToString`, `Hashable`, `Equatable`, and `Comparable` for classes, structs, and enums. The content details the specific core macros—including `Derive` for enabling generation, `DeriveExclude` and `DeriveInclude` for precise field selection, and `DeriveOrder` for sorting—that control the generation process. This resource is highly suitable for developers seeking to significantly minimize boilerplate code and streamline their workflow by leveraging automated standard behaviors in their applications. It provides a clear API summary to ensure effective implementation and consistency across projects, making it an essential tool for modern software development focused on efficiency and maintainability.

## Quick Navigation

*   **What do you want to learn?**
    *   Understand the fundamental purpose, architecture, and benefits of the std.deriving package? → deriving_package_overview.md
    *   Learn how to automatically generate interface implementations for classes, structs, and enums? → deriving_package_overview.md
    *   Identify which specific interfaces (ToString, Hashable, Equatable, Comparable) are supported? → deriving_package_overview.md
    *   Master the `Derive` macro to enable automatic code generation? → deriving_package_overview.md
    *   Control field selection precisely using `DeriveExclude` and `DeriveInclude`? → deriving_package_overview.md
    *   Configure sorting behavior and order using the `DeriveOrder` macro? → deriving_package_overview.md
    *   Find a reference guide to help reduce boilerplate code and automate standard behaviors? → deriving_package_overview.md

## Detailed Description

The following section provides a comprehensive breakdown of the documentation available in this directory. It outlines the specific capabilities of the deriving package, including the supported interfaces and the specific macros used to control the generation logic. This guide is essential for mastering the automation of standard type behaviors.

### deriving_package_overview.md deriving_package_overview.md
This document serves as an overview for the std.deriving package, explaining how to automatically generate interface implementations for classes, structs, and enums. It details supported interfaces such as ToString, Hashable, Equatable, and Comparable, and lists the core macros used to control this process. Key macros include Derive for enabling generation, DeriveExclude and DeriveInclude for field selection, and DeriveOrder for sorting. This reference guide targets developers looking to reduce boilerplate code by automating standard behaviors, providing a clear API summary for effective implementation.