# 示例3使用LengthEdgeWidthsType类型值

This directory contains a collection of code examples designed to demonstrate the practical application of the `LengthEdgeWidthsType` type value within the `borderImage` interface for Cangjie and ArkUI development. The content focuses on building dynamic user interfaces where border properties—including slice, width, and outset—are managed and manipulated through state variables. It illustrates the integration of interactive controls such as sliders, select menus, and toggle switches to enable real-time visual feedback. These tutorials are highly suitable for HarmonyOS developers seeking to master state management, component interaction, and complex styling logic within the ArkUI framework.

## Quick Navigation

What do you want to learn or do?

*   Implement dynamic border properties (slice, width, outset) using sliders and select menus → 示例3使用LengthEdgeWidthsType类型值_1.md
*   Create a toggle switch to control the borderImageFill property state → 示例3使用LengthEdgeWidthsType类型值_2.md

## Detailed Description

### 示例3使用LengthEdgeWidthsType类型值_1.md
This document serves as a code example demonstrating the application of the LengthEdgeWidthsType within the borderImage interface for Cangjie development. It details the construction of an EntryView component that dynamically manages border properties through state variables. Key sections include the build method, which implements a text component with a borderImage modifier, and interactive controls like sliders and a select menu. The example illustrates how to adjust slice, width, and outset values in real-time. This tutorial is intended for HarmonyOS developers seeking to implement dynamic UI styling and understand state management in ArkUI.

### 示例3使用LengthEdgeWidthsType类型值_2.md
This document provides a code example demonstrating how to implement a toggle control for the borderImageFill property within an ArkUI layout. The content focuses on the implementation of a Row and Flex container, a Text component displaying the current state, and a Toggle component configured as a switch. Key information includes state management using this.FillValue, event handling via onChange, and visual styling with selectedColor and switchPointColor. It references a visual output image to show the result. Intended for HarmonyOS developers learning ArkUI component interaction and state management, specifically within the context of styling border images.