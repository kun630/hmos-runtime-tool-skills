# const_函数和常量求值

本目录包含关于仓颉编程语言中 `const` 函数和常量求值机制的详细文档。内容涵盖了 `const init` 构造器的定义规则、`const` 函数的语法特性及使用限制，以及常量表达式的求值机制。这些文档旨在帮助开发者利用编译时计算来优化程序性能，并深入理解仓颉的类型系统。适合需要编写编译期常量逻辑、追求高性能或深入理解语言特性的仓颉开发者阅读。

## 快速导航

- 你想了解如何定义常量构造器？
  → 查看 `const init` 规则与限制 const_init_be076dc0.md
- 你想学习如何定义和使用 `const` 函数？
  → 查看 `const` 函数语法与特性 const_函数.md
- 你想深入理解常量求值的机制与表达式规则？
  → 查看 `const` 上下文与求值规则 const_函数和常量求值_2more_346751b0.md

## 详细描述

### const_init_be076dc0.md
本文档旨在说明仓颉语言中 `const init` 构造器的定义规则，使结构体或类实例能用于常量表达式。内容涵盖 `class` 类型的成员限制、继承调用规范、成员初始值的常量要求及构造器内赋值操作的约束。核心规则包括：`class` 禁止 `var` 成员且必须调用父类 `const init`；成员初始值需为常量表达式；构造器内仅允许简单赋值 `=`。这是一份语言特性参考文档，面向需要编写编译期常量逻辑的仓颉开发者，需具备面向对象编程基础。

### const_函数.md
本文档介绍了仓颉编程语言中 `const` 函数的定义、特性及使用规则，这类函数具备在编译时求值的能力。内容涵盖基本语法示例、变量访问限制、局部变量声明规则、结构体与类中的特殊限制，以及接口和泛型约束中的 `const` 函数行为。关键点包括必须使用 `const` 修饰、仅支持 `let` 和 `const` 局部变量、类中定义实例成员需先有 `const init`，以及接口实现时 `const` 函数的匹配规则。该参考文档面向需要利用编译期计算优化性能或深入理解类型系统的仓颉开发者。关键词：仓颉、const函数、编译时求值、泛型约束。

### const_函数和常量求值_2more_346751b0.md
本文档是一份技术参考指南，旨在阐述常量求值的机制与规则，帮助开发者利用编译时计算优化程序性能。文档核心内容定义了`const`上下文与`const`表达式，详细列出了包括字面量、数组、函数调用、运算符及控制流在内的11条构成规则。关键要点指出`const`上下文必须避免副作用，且当前编译器暂不支持`throw`作为`const`表达式。该文档适合需要深入理解编译期求值逻辑的语言使用者。