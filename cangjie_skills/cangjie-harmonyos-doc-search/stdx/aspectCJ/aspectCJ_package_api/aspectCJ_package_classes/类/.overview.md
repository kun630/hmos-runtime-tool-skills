# 类

本目录包含仓颉语言面向切面编程（AOP）包的API参考文档，主要涉及代码织入相关的类定义。文档详细介绍了 `InsertAtEntry`、`InsertAtExit` 以及 `ReplaceFuncBody` 等核心类，这些类允许开发者在指定方法的入口或退出点织入函数调用，或者直接替换方法体。内容涵盖了构造函数参数、类型字符串规范及递归织入选项等关键信息。该文档适合使用 `aspectCJ` 包进行切面逻辑配置和代码增强的开发者阅读。

## 快速导航

您想了解什么功能或如何操作？

*   想要在方法入口或退出点织入函数调用？ → 查看 **插入逻辑类** class_InsertAtEnt_2more_28ce128c.md
*   想要完全替换目标方法的方法体？ → 查看 **替换方法体类** class_ReplaceFuncBody_70f36e91.md

## 详细说明

### class_InsertAtEnt_2more_28ce128c.md class_InsertAtEnt_2more_28ce128c.md
本文档是仓颉语言面向切面编程（AOP）包的API参考文档，详细介绍了 `InsertAtEntry` 和 `InsertAtExit` 两个类的定义与构造函数。文档主要涵盖这两个类如何在指定方法的入口或退出点织入函数调用，并详细列出了构造函数的参数说明，包括包名、类名、方法名、静态属性、类型字符串及递归织入选项。关键信息指出两者参数结构相同，且需严格遵守类型字符串的格式规范。文档面向使用 aspectCJ 包进行代码织入的开发者，旨在指导其正确配置切面逻辑。

### class_ReplaceFuncBody_70f36e91.md class_ReplaceFuncBody_70f36e91.md
这是一份关于Cangjie语言中 `ReplaceFuncBody` 类的API参考文档，旨在说明如何利用该注解实现方法体的替换功能。文档主要涵盖了类的定义及其构造函数 `init` 的详细参数说明，包括目标函数的包名、类名、方法名，以及用于标识静态函数和递归织入的布尔标志。核心信息在于该类通过注解机制，将指定方法体替换为对被注解函数的调用，支持对成员函数及子类重写版本的处理。该文档面向使用 aspectCJ 包进行面向切面编程的开发者，要求其具备基本的注解和代码织入概念。