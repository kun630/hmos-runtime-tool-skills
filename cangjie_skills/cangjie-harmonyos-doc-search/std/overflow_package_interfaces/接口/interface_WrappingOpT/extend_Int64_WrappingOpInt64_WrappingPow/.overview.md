# extend_Int64_WrappingOpInt64_WrappingPow

This directory provides comprehensive API reference documentation for implementing wrapping arithmetic and bitwise operations on the `Int64` type within the Cangjie programming language. It specifically covers the `WrappingOp` and `WrappingPow` interfaces, detailing functions for essential arithmetic tasks such as addition, subtraction, multiplication, division, modulo, negation, exponentiation, and bit shifting. A primary focus is the high-order truncation strategy used to handle overflow, ensuring that results wrap around predictably rather than raising runtime errors. This resource is highly suitable for Cangjie developers and systems programmers who require precise, deterministic control over integer overflow behavior and low-level bit manipulation in their mathematical computations.

## Quick Navigation

**What do you want to learn?**
*   Understand the core interfaces for wrapping arithmetic and exponentiation on Int64 → extend_Int64_WrappingOpIn_10more.md
*   Learn about specific bitwise right shift and subtraction functions with overflow control → func_wrappingShrUInt64_2more.md

**What do you want to do?**
*   Implement addition, multiplication, division, or power operations with defined overflow behavior → extend_Int64_WrappingOpIn_10more.md
*   Perform right shifts with masked shift amounts or safe subtraction with truncation → func_wrappingShrUInt64_2more.md

## Detailed Description

The following files provide specific implementation details and function references for the wrapping operations mentioned above.

### extend_Int64_WrappingOpIn_10more.md extend_Int64_WrappingOpIn_10more.md
This API reference document details the implementation of the WrappingOp and WrappingPow interfaces for the Int64 type in the Cangjie language. The content outlines specific functions for arithmetic operations such as addition, subtraction, multiplication, division, modulo, negation, exponentiation, and bit shifting. Key information highlights that all operations employ a high-order truncation strategy to handle overflow, ensuring results wrap around instead of raising errors. Additionally, the wrappingShl function specifies logic for handling shift amounts larger than the bit width. This documentation is intended for Cangjie developers who need to perform mathematical computations with defined overflow behavior.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md
This document serves as API reference documentation for wrapping arithmetic and bitwise operations in the Cangjie programming language. It details two specific functions: `wrappingShr`, which performs a right shift using a high-bit truncation strategy where the shift amount is masked to the low 6 bits if too large, and `wrappingSub`, which performs subtraction that truncates high bits upon overflow. Both functions accept a numeric parameter and return an `Int64` result. This guide is intended for developers implementing low-level logic requiring precise control over integer overflow and bit manipulation behaviors.