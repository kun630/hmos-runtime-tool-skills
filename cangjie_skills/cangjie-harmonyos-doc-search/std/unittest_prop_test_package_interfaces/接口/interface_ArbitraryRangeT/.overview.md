# interface_ArbitraryRangeT

This directory contains API reference documentation for the `ArbitraryRange` interface in the Cangjie programming language. It focuses on generating random values within specified ranges for various numeric types, facilitating property-based testing. The documentation covers interface definitions and extensions for signed and unsigned integers (Int8, Int16, Int32, Int64, IntNative, UInt8, UInt16, UInt32, UInt64, UIntNative) and floating-point numbers (Float16, Float32, Float64). Key components include static methods for boundary retrieval and the `arbitraryRange` function for creating Generators. This resource is suitable for developers implementing property-based testing frameworks or needing constrained random data generation for unit tests.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and Float16/Float32 implementations → interface_ArbitraryRangeT_6more.md
*   Implement range generation for Float64, Int16, or Int32 → extend_Float64_ArbitraryRa_3more.md
*   Implement range generation for Int64, Int8, or IntNative → extend_Int64_ArbitraryRang_3more.md
*   Implement range generation for unsigned integers (UInt16, UInt32, UInt64) → extend_UInt16_ArbitraryRan_3more.md
*   Implement range generation for small unsigned integers (UInt8, UIntNative) → extend_UInt8_ArbitraryRang_2more.md

## Detailed Description

### extend_Float64_ArbitraryRa_3more.md extend_Float64_ArbitraryRa_3more.md
This API reference document details the implementation of the ArbitraryRange interface for Float64, Int16, and Int32 types in the Cangjie language. The content is organized into three distinct sections, each dedicated to a specific numeric type extension. Key information includes the definition of static methods for retrieving minimum and maximum values, alongside the arbitraryRange function. This function accepts a RandomSource and boundary parameters to return a Generator capable of producing random numbers within a specified interval. Targeted at developers implementing property-based testing, this documentation facilitates the generation of constrained random numeric values for testing scenarios.

### extend_Int64_ArbitraryRang_3more.md extend_Int64_ArbitraryRang_3more.md
This API reference document details the implementation of the ArbitraryRange interface for Int64, Int8, and IntNative types in the Cangjie language. The content is structured into three distinct sections, one for each integer type, describing static methods for range generation. Key functions include min() and max() for retrieving boundary values, and arbitraryRange() which accepts a RandomSource and bounds to return a Generator for random values within that specific range. This documentation targets developers implementing property-based tests who need to generate random integer data within specific constraints.

### extend_UInt16_ArbitraryRan_3more.md extend_UInt16_ArbitraryRan_3more.md
This API documentation file details the implementation of the ArbitraryRange interface for unsigned integer types (UInt16, UInt32, and UInt64) to facilitate random value generation within specific bounds. The content is structured into three distinct sections, one for each type, each defining static methods for range generation and boundary retrieval. Key information includes the `arbitraryRange` function, which accepts a RandomSource and min/max bounds to return a Generator, alongside `min` and `max` methods that return type limits. This reference guide targets developers utilizing property testing frameworks who need to generate constrained random data for test cases.

### extend_UInt8_ArbitraryRang_2more.md extend_UInt8_ArbitraryRang_2more.md
This API documentation specifies the implementation of the ArbitraryRange interface for UInt8 and UIntNative types, enabling random value generation within defined limits. The document details extensions for both types, outlining static methods to retrieve minimum and maximum values and to generate arbitrary ranges. Key functionality includes the arbitraryRange method, which accepts a RandomSource and boundary parameters to return a Generator for the specific type. This reference guide is designed for developers working with property-based testing frameworks who require tools to create constrained random unsigned integers for test cases.

### interface_ArbitraryRangeT_6more.md interface_ArbitraryRangeT_6more.md
This API documentation defines the ArbitraryRange interface for generating values within a specified range for types supporting arbitrary and comparable operations. The document details the interface definition, including static methods min(), max(), and arbitraryRange(), alongside specific implementations for Float16 and Float32 types. Key information includes the arbitraryRange function, which accepts a RandomSource and boundary values to return a Generator for the specified type. This reference material is intended for developers working with property-based testing frameworks who need to generate constrained random data for unit tests. Keywords: ArbitraryRange, Cangjie, Generator, RandomSource, Float16, Float32, property-based testing.