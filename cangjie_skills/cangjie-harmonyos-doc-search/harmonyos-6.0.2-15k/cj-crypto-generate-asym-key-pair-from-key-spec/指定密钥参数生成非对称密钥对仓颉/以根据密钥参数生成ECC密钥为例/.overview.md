# 以根据密钥参数生成ECC密钥为例

This directory provides comprehensive Cangjie code examples focused on generating Elliptic Curve Cryptography (ECC) keys based on specific key parameters within the HarmonyOS ecosystem. It covers the manual construction of `ECCCommonParamsSpec`, defining prime fields, curve coefficients, and generator points, as well as retrieving detailed cryptographic attributes from generated keys. Key topics include secp224r1 curve implementation, key pair generation workflows, and parameter inspection. These resources are suitable for Cangjie developers and security engineers implementing custom cryptographic operations or validating ECC key properties programmatically. The examples demonstrate both the setup of curve parameters and the subsequent extraction of key details for verification.

## Quick Navigation

**What do you want to learn or do?**

*   Manually define ECC parameters (prime field, coefficients, generator) and generate keys → 以根据密钥参数生成ECC密钥为例_1.md
*   Generate keys using common specifications and retrieve detailed parameter values → 以根据密钥参数生成ECC密钥为例_2.md

## Detailed Description

### 以根据密钥参数生成ECC密钥为例_1.md 以根据密钥参数生成ECC密钥为例_1.md
This document provides a Cangjie code example demonstrating how to generate Elliptic Curve Cryptography (ECC) keys by manually defining key parameters. The content covers the construction of the `ECCCommonParamsSpec` structure, which includes defining the prime field, curve coefficients, generator point, and order. It also details a utility function for retrieving and logging specific key attributes like field size, curve name, and public key coordinates. Key takeaways include the specific hex values used for the secp224r1 curve parameters and the method for inspecting generated key objects. This tutorial is intended for Cangjie developers working within the HarmonyOS ecosystem who require custom ECC key generation and parameter inspection capabilities.

### 以根据密钥参数生成ECC密钥为例_2.md 以根据密钥参数生成ECC密钥为例_2.md
This document provides a code example demonstrating how to generate an ECC key pair using a common key specification and retrieve detailed cryptographic parameters from the resulting keys. The content covers the `showEccSpecDetailInfo` function for logging key attributes and the `testEccUseCommKeySpecGetSync` function for the key generation workflow. Key information includes retrieving specific ECC parameters such as curve coefficients (p, a, b), generator point coordinates (gX, gY), order (n), cofactor (h), field type, and the private key value using the `getAsyKeySpec` method. This tutorial is intended for developers implementing cryptographic operations, specifically those needing to inspect or validate ECC key properties programmatically.