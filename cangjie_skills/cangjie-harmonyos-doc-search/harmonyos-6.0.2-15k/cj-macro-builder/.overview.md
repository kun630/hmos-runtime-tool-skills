# cj-macro-builder

This directory serves as a comprehensive technical resource for the `cj-macro-builder` project, specifically focusing on the implementation and optimization of the `@Builder` macro within the Cangjie UI framework. It is designed to assist developers in transitioning from basic UI coding to advanced, reusable component architecture. The content thoroughly explores the mechanics of declarative UI syntax, providing deep insights into how custom build functions can be structured to enhance modularity and reduce code redundancy. Key topics include the distinction between private and global implementation methods, the critical differences between pass-by-value and pass-by-reference parameters, and the integration of reactive state management using `@State` and `@Observed`. This documentation is essential for Cangjie UI developers and HarmonyOS application engineers who are looking to streamline their development workflow, ensure efficient UI refresh mechanisms, and build scalable, dynamic user interfaces with professional-grade code reusability.

## Quick Navigation

What do you want to learn or achieve?

- **Master the Basics:** Learn how to define and utilize the `@Builder` macro to create custom, reusable UI components efficiently → [Builder宏自定义构建函数/]
- **Scope & Implementation:** Understand the technical differences and specific use cases for private versus global build function implementations → [Builder宏自定义构建函数/]
- **Data Handling:** Deep dive into parameter passing rules, specifically understanding when to use pass-by-value over pass-by-reference for performance and correctness → [Builder宏自定义构建函数/]
- **State Management:** Explore how to effectively integrate `@State` and `@Observed` decorators to manage dynamic UI updates within your custom builders → [Builder宏自定义构建函数/]
- **Advanced Architecture:** Discover practical scenarios for optimizing component reusability and mastering UI refresh logic in complex, data-driven applications → [Builder宏自定义构建函数/]

## Detailed Description

This section provides an in-depth look at the specific subdirectory contained within the `cj-macro-builder` project. The documentation focuses on practical implementation strategies and theoretical underpinnings of the Cangjie UI framework's macro capabilities. By examining the resources below, developers can gain a holistic understanding of how to leverage advanced language features to build more robust and maintainable applications.

### Builder宏自定义构建函数/
This directory provides a comprehensive guide to using the @Builder macro in the Cangjie UI framework for creating reusable custom build functions. It covers implementation methods (private vs. global), parameter passing rules (pass-by-value vs. pass-by-reference), and practical usage scenarios involving dynamic UI construction and state management. Key concepts include @Builder, @State, @Observed, and UI refresh mechanisms. This resource is suitable for Cangjie UI developers and HarmonyOS application engineers looking to optimize component reusability and master declarative UI syntax.