# interface_Focus

This directory provides a comprehensive collection of API reference documents designed for developers working with the CameraKit framework in the Cangjie language. The content focuses specifically on the `interface_Focus` module, which is critical for managing and controlling camera device focus operations. It covers a wide range of functionalities, from configuring focus modes and setting specific focus points to querying current focus states and retrieving focal lengths. The documentation explains complex logic such as the 0-1 coordinate system relative to device orientation and details necessary prerequisites like verifying mode support. Furthermore, it includes guidance on error handling for specific BusinessException codes, such as session configuration failures. This resource is primarily suitable for software developers and multimedia engineers who are building camera-integrated applications and need to implement precise, reliable focus management features to enhance user experience.

## Quick Navigation

**What do you want to do?**

*   **Configure camera focus behavior** – Learn how to select focus types and define specific focus coordinates using the 0-1 coordinate system, including prerequisites and error handling → func_setFocusModeFocusMode_2more.md
*   **Query focus status and interface structure** – Understand the `Focus` interface, retrieve focal lengths, and get current focus modes or points with specific return types → interface_Focus_4more.md

## Detailed Description

### func_setFocusModeFocusMode_2more.md func_setFocusModeFocusMode_2more.md

This API reference document outlines methods for configuring camera focus behavior in the Cangjie language. It details the `setFocusMode` function for selecting focus types and `setFocusPoint` for defining specific focus coordinates. Key information includes the prerequisite to verify mode support, the 0-1 coordinate system logic based on device orientation, and error handling for BusinessException codes like Session not config. The document provides code examples and is intended for developers working with the CameraKit to manage photo sessions.

### interface_Focus_4more.md interface_Focus_4more.md

This API reference document defines the Cangjie interface `Focus` for managing camera device focus operations. It details the interface structure, which inherits from `FocusQuery`, and provides specifications for methods like `getFocalLength`, `getFocusMode`, and `getFocusPoint`. Key information includes return types such as `Float32` and `Point`, system capabilities, and error handling for session configuration failures (error code 7400103). Intended for developers using the CameraKit, it includes code examples to guide the implementation of focus controls in multimedia applications.