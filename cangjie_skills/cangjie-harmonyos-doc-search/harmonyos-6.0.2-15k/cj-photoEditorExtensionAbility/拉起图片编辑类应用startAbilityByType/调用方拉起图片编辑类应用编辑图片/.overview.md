# 调用方拉起图片编辑类应用编辑图片

This directory serves as a technical guide for developers seeking to implement image editing features by invoking a system image editing application from within their own app. It covers the end-to-end process, including selecting images from the gallery, copying them to a local sandbox, and launching the editor with specific parameters. Key technical concepts include URI conversion, permission management via flags, and the use of the `startAbilityByType` interface to interact with `PhotoEditorExtensionAbility`. The documentation explains how to handle callbacks to retrieve the edited image data. This content is specifically designed for HarmonyOS application developers using either Cangjie or ArkTS, providing the necessary instructions to seamlessly integrate system-level image editing capabilities into their software projects.

## Quick Navigation

**Which programming language are you using to implement the image editing feature?**

*   **Cangjie** → 调用方拉起图片编辑类应用编辑图片_1.md Follow this technical guide to invoke the system image editing panel, configure launch parameters, and handle result callbacks.
*   **ArkTS** → 调用方拉起图片编辑类应用编辑图片_2.md Explore this code example to learn about PhotoViewPicker usage, file I/O operations, and PixelMap result processing.

## Detailed Description

The following documents provide specific implementation guides and code examples for invoking the image editor.

### 调用方拉起图片编辑类应用编辑图片_1.md 调用方拉起图片编辑类应用编辑图片_1.md
This technical guide instructs developers on how to invoke a system image editing panel within an application using the startAbilityByType interface. It covers module imports, optional image selection from the gallery, copying images to the local sandbox, configuring launch parameters, and handling the result callback to retrieve the edited image. Key steps include converting images to URIs, setting read permissions via flags, and utilizing PhotoEditorExtensionAbility implementations. This tutorial targets application developers using the Cangjie language who need to integrate image editing capabilities into their apps.

### 调用方拉起图片编辑类应用编辑图片_2.md 调用方拉起图片编辑类应用编辑图片_2.md
This document provides a code example demonstrating how to invoke a system image editor from a calling application using ArkTS. It covers the workflow of selecting an image via PhotoViewPicker, copying it to the application sandbox, and launching the editor using startAbilityByType with specific URI permissions. Key sections include handling photo selection callbacks, file I/O operations, and processing the edited image result through a PixelMap. This guide is intended for HarmonyOS developers seeking to implement image editing features within their applications.