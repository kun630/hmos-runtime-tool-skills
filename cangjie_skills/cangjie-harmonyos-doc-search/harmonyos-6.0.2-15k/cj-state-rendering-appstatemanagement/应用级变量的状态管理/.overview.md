# 应用级变量的状态管理

This directory focuses on application-level variable state management within the Cangjie and ArkUI frameworks. It covers essential mechanisms for managing data across an application's lifecycle, including global UI state storage via AppStorage, page-level storage via LocalStorage, and persistent data handling with PersistentStorage. It also addresses device environment queries for adaptive UIs. Key topics include data synchronization, decorators like @StorageLink, and performance considerations. This resource is suitable for HarmonyOS developers and engineers seeking to implement robust, centralized state management and data persistence in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core concepts and capabilities of state management → 应用级变量的状态管理_2more.md
*   Manage global UI state across the application → AppStorage应用全局的UI状态存储/
*   Handle state within a specific page or component tree → LocalStorage页面级UI状态储存/
*   Persist UI state to local disk storage → PersistentStorage持久化存储UI状态/
*   Query and adapt to device environment settings → Environment设备环境查询/

## Detailed Description

### 应用级变量的状态管理_2more.md
This document serves as a technical guide for implementing application-level variable state management using the Cangjie programming language. It outlines the core capabilities of the state management module, specifically focusing on general data storage, persistent data management, UIAbility data storage, and environment state handling. The text provides the necessary import syntax for the UIKit module to access these features. Key takeaways include understanding the module's role in centralizing application data and ensuring data persistence across different components. This resource is intended for developers building applications that require robust data handling and state synchronization within the UIKit framework.

### AppStorage应用全局的UI状态存储/
This directory provides comprehensive documentation for AppStorage, a global UI state storage mechanism within the ArkUI framework designed for application-level data management. It covers the core concepts of AppStorage as a central hub for bridging PersistentStorage and Environment variables with the UI, distinguishing it from page-level LocalStorage. The content includes theoretical definitions, practical code examples for one-way and two-way data binding using decorators like @StorageProp and @StorageLink, and detailed API documentation. It is specifically tailored for developers using the Cangjie language and ArkUI version 12 or higher to implement robust state management solutions across multiple UIAbility instances.

### Environment设备环境查询/
This directory provides comprehensive documentation and practical examples for utilizing the ArkUI framework's Environment class within the Cangjie language ecosystem. It covers the mechanisms for querying and reacting to device environment parameters, such as system language, color modes, and layout directions. By linking system variables to AppStorage, developers can create adaptive user interfaces that respond dynamically to device settings. Key technical concepts include `envProp`, `ColorMode`, and `LayoutDirection`. This resource is essential for HarmonyOS developers aiming to implement dynamic, context-aware UI behaviors in their applications.

### LocalStorage页面级UI状态储存/
This directory focuses on page-level UI state management using the LocalStorage mechanism within the Cangjie and HarmonyOS ArkUI frameworks. It comprehensively covers the theoretical concepts of LocalStorage as an in-memory database, usage rules for component tree inheritance, and specific decorators like @LocalStorageProp and @LocalStorageLink for data synchronization. The content includes technical guides, practical code examples, and detailed API references for the LocalStorage class. It is suitable for developers building UIs who need to manage state across components within a package, ensuring robust data persistence and synchronization between the UI and the storage layer.

### PersistentStorage持久化存储UI状态/
This directory provides comprehensive documentation on the PersistentStorage singleton object, a crucial component for HarmonyOS applications designed to persist UI state by synchronizing specific AppStorage properties to local disk storage. It covers the fundamental two-way synchronization mechanism, supported data types, and critical performance constraints, such as avoiding large datasets to prevent UI lag. The resources include technical guides, detailed API references for the Cangjie language, and practical code examples. This collection is ideal for HarmonyOS developers and application engineers seeking to implement reliable local state persistence that survives application restarts while maintaining optimal rendering performance.