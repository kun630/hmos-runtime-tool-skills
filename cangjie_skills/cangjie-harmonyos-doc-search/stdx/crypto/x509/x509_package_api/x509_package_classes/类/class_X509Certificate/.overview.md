# class_X509Certificate

本目录包含仓颉语言中 X509Certificate 类的 API 参考文档，专注于处理 X.509 数字证书。内容涵盖了证书的核心属性定义（如颁发者、公钥）、证书验证逻辑（有效期、域名、链验证）以及证书的创建、编解码（DER/PEM）和系统根证书获取。该模块适用于从事公钥基础设施（PKI）和安全通信开发的程序员，提供了完整的证书管理与加密通信接口。

## 快速导航

- 想要了解证书的基本结构和属性（颁发者、有效期、公钥等） → class_X509Certif_15more_a5cf2fb1.md
- 想要进行证书验证（有效期、域名、链验证）或对象比较 → func_verifyVerify_3more_6a796aeb.md
- 想要创建证书、进行 DER/PEM 编解码或获取系统根证书 → initX509Certifica_8more_80bfa05b.md

## 详细描述

### class_X509Certif_15more_a5cf2fb1.md class_X509Certif_15more_a5cf2fb1.md
本文档是 Cangjie 语言中 X509Certificate 类的 API 参考文档，旨在定义用于加密通信的 X509 数字证书结构。文档涵盖了类的构造函数定义、继承的父类型（如 Equatable 和 Hashable），以及一系列用于解析证书信息的属性。关键属性包括颁发者、使用者、有效期、公钥、序列号、签名算法以及备选名称（如 DNS 和 IP 地址），这些属性共同构成了证书的核心数据。该文档面向需要处理公钥基础设施（PKI）和安全通信的开发者，提供了访问和操作数字证书详细信息的接口规范。

### func_verifyVerify_3more_6a796aeb.md func_verifyVerify_3more_6a796aeb.md
本文档是仓颉语言 X.509 包的 API 参考文档，详细说明了证书验证与对象比较的功能。主要内容包括 `verify` 函数，它依据 `VerifyOption` 按优先级验证证书有效期、DNS 域名及证书链，返回布尔值或抛出 `X509Exception`；以及 `!=` 和 `==` 运算符，用于比较两个 `X509Certificate` 实例的异同。关键点在于验证流程的优先级设置及异常处理。文档面向从事安全通信开发的程序员，需具备仓颉语法和 PKI 基础。关键词：X.509、证书验证、仓颉、API文档、运算符重载。

### initX509Certifica_8more_80bfa05b.md initX509Certifica_8more_80bfa05b.md
本文档是 X509Certificate 类的 API 参考文档，详细说明了数字证书对象的创建、编码、解码及管理方法。主要涵盖使用公私钥构造证书的初始化方法，DER 和 PEM 格式的编解码函数，以及获取系统根证书、哈希计算和字符串表示等功能。关键信息包括支持 RSA、ECDSA 和 DSA 密钥类型，默认使用 SHA256 签名算法，支持跨平台系统根证书获取，并定义了针对无效数据或密钥不匹配的异常处理。该文档面向需要实现加密安全功能的开发人员，要求读者具备 X.509 标准及公钥基础设施的相关知识。