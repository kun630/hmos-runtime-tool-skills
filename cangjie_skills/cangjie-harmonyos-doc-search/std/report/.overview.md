# report

This directory provides a comprehensive technical guide focused on the `diagReport` interface within the Cangjie AST package. It is an essential resource for developers aiming to implement sophisticated custom error reporting mechanisms directly inside macros. The documentation details how to generate native-style WARNING and ERROR messages, ensuring that diagnostics are both user-friendly and consistent with the compiler's output. It covers the complete lifecycle of usage, from macro definition and invocation to compilation, while highlighting critical constraints such as execution limits during macro expansion. This guide is specifically tailored for macro developers and Cangjie programmers who require precise input token validation and wish to improve the diagnostic quality of their development tools. By mastering these tools, developers can significantly enhance the robustness and usability of their language extensions.

## Quick Navigation

What do you want to learn? Use the decision tree below to quickly locate the information relevant to your specific development needs and goals.

*   How to implement custom error reporting in Cangjie macros using the AST package? → report.md
*   What are the specific parameters for the `diagReport` function, including severity levels and token ranges? → report.md
*   How to generate native-style WARNING and ERROR messages effectively during the compilation process? → report.md
*   What are the execution constraints of `diagReport` when used inside versus outside of macro expansion? → report.md
*   Where can I find a complete usage example covering macro definition, invocation, and compilation? → report.md

## Detailed Description

The detailed section below breaks down the specific documentation available in this directory, offering a deep dive into the technical specifications and practical applications of the `diagReport` interface. Understanding these concepts is vital for creating professional-grade language extensions.

### report.md report.md
This document serves as a technical guide for using the `diagReport` interface in the Cangjie AST package to implement custom error reporting within macros. It details the interface's ability to output native-style WARNING and ERROR messages, provides a complete usage example including macro definition, invocation, and compilation, and demonstrates behavior outside macro expansion. Key takeaways include the function's parameters—severity level, token range, message, and hint—and the critical constraint that it only executes during macro expansion, acting as a no-op in regular functions. This guide targets macro developers and Cangjie programmers needing to validate input tokens and provide user-friendly diagnostics.