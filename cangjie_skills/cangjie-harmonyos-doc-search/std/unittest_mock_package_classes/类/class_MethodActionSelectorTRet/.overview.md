# class_MethodActionSelectorTRet

This directory contains API reference documentation for the Cangjie `MethodActionSelector` class, a crucial component for configuring stub behaviors in unit testing and mocking frameworks. It covers methods for defining return values, executing original logic, and handling exceptions or sequential return lists. Key topics include fluent chaining, mock object setup, and test flow control. This documentation is intended for software developers using the Cangjie language who need to implement advanced mocking strategies, manage test execution flows, and handle error scenarios within their unit tests.

## Quick Navigation

**What do you want to do?**

*   Learn about the main class and basic stubbing methods like `returns` and `callsOriginal` → class_MethodActionSelector_5more.md
*   Set up sequential return lists or configure stubs to throw exceptions → func_returnsConsecutivelyA_3more.md

## Detailed Description

### class_MethodActionSelector_5more.md
This API reference document details the Cangjie class MethodActionSelector, which configures stub behaviors for member functions within a unit testing and mocking framework. The content outlines the class inheritance from ActionSelector and documents key methods such as callsOriginal, returns, and returnsConsecutively. Key information includes how these functions allow developers to specify return values via closures or direct input, execute original logic, and manage sequential return lists, all supporting fluent chaining through CardinalitySelector and Continuation return types. Intended for software developers using the Cangjie language, this guide assists in setting up mock and spy objects to control test execution flows effectively.

### func_returnsConsecutivelyA_3more.md
This document serves as API reference documentation for configuring test stub behaviors within a unit testing framework, specifically focusing on return sequences and exception handling. It covers three main functions: returnsConsecutively, which allows stubs to return values from an ArrayList in order, and two overloads of throws that define stub behavior to throw exceptions via a factory closure or a direct object. Key information includes the IllegalArgumentException for empty lists and the conceptual distinction between throws for testing recovery and fails for unexpected failures. Intended for developers writing unit tests in the Cangjie language, this guide requires an understanding of mocking concepts and exception handling.