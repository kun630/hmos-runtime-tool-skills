# 仓颉-C_互操作

本目录详细介绍了仓颉语言与 C 语言进行互操作（FFI）的完整机制。内容涵盖了核心的 `CFunc` 和 `CType` 类型定义、`unsafe` 安全上下文调用规范、以及 `foreign` 函数声明方式。此外，还深入讲解了类型映射规则、内存布局管理及具体的代码示例。该文档适合需要集成现有 C 语言生态、进行底层系统开发或跨语言调用的仓颉开发者阅读，旨在帮助用户在保证安全的前提下高效实现 C 语言交互。

## 快速导航

- **我想了解基础用法和代码示例** → 示例_bc85959f.md 示例
- **我想学习如何声明和调用 C 函数** → 仓颉-C_互操作_2more_695628de.md 仓颉-C_互操作
- **我想掌握函数指针与回调机制** → CFunc_2more_37858034.md CFunc, CType_3more_f175253b.md CType
- **我需要了解 unsafe 上下文与调用约定** → unsafe_3more_9ef482d3.md unsafe
- **我想查看详细的类型映射与内存规则** → 类型映射/

## 详细描述

### CFunc_2more_37858034.md CFunc_2more_37858034.md
本文档旨在介绍仓颉语言中用于与 C 语言交互的 CFunc 类型及其使用规范。主要涵盖 CFunc 的三种定义形式（foreign 函数、@C 修饰函数、CFunc lambda）、类型约束及 unsafe 调用上下文。文档重点讲解了 inout 参数在 CFunc 调用中的引用传值机制，详细列出了关于变量来源、生命周期及类型限制的严格规则。这是一份面向具备 C 语言互操作基础的开发者的技术参考文档，旨在指导用户安全地进行跨语言函数调用和指针操作。

### CType_3more_f175253b.md CType_3more_f175253b.md
本文档介绍了仓颉语言中与 C 语言互操作的核心机制，涵盖 `CType` 接口、`CFunc` 函数指针类型及编译选项。主要内容包括 `CType` 在泛型约束中的使用、通过 `CFunc` 实现 C 与仓颉函数互调（如 `@C` 修饰符和闭包），以及链接 C 库的编译参数。关键信息指出 `CType` 不可继承、`CFunc` 命名需避免 `CJ_` 前缀，并强调了编译 C 代码时开启栈保护的重要性。该文档面向需要集成 C 库或进行底层开发的仓颉程序员，属于技术参考文档。

### unsafe_3more_9ef482d3.md unsafe_3more_9ef482d3.md
本文档介绍了仓颉语言中用于C语言互操作的`unsafe`关键字、调用约定及相关使用约束。主要涵盖`unsafe`关键字的修饰规则与Lambda使用注意事项、`@CallingConv`定义的CDECL和STDCALL调用约定，以及操作系统线程局部变量、线程绑定、同步原语和进程fork等场景下的使用限制。关键点在于调用C函数必须在`unsafe`上下文中，默认调用约定为CDECL，且因仓颉线程调度机制，使用C语言的线程局部变量存在风险。本参考指南面向需要进行C语言互操作开发的仓颉程序员，旨在帮助其管理内存安全及系统级交互。

### 仓颉-C_互操作_2more_695628de.md 仓颉-C_互操作_2more_695628de.md
这份文档介绍了仓颉语言与C语言的互操作机制，重点讲解了如何在仓颉中声明和调用C函数。主要涵盖`foreign`关键字的使用、函数声明规范、类型映射要求、`unsafe`块调用规则以及变长参数支持。关键信息包括：调用C函数需使用`foreign`声明且必须在`unsafe`块中执行，不支持命名参数和默认值，需注意C侧栈溢出风险。这是一份技术指南，适用于需要集成现有C语言生态的仓颉开发者。

### 示例_bc85959f.md 示例_bc85959f.md
本文档是一个教程，旨在演示仓颉语言与 C 语言的互操作能力。主要涵盖了定义 `@C` 结构体、声明外部函数、以及利用 `write` 和 `read` 接口通过 `CPointer` 进行数据传递与读取。文档通过具体的代码示例，展示了如何在仓颉中分配内存、调用 C 函数修改结构体值，并验证结果。此外，还详细说明了使用 CJNative 后端的编译与运行步骤。该文档适合具备 C 语言基础并希望掌握仓颉原生互操作特性的开发者。

### 类型映射/
本目录详细介绍了仓颉语言与 C 语言进行互操作（FFI）时的类型映射规则与内存管理机制。内容涵盖了基础数据类型、结构体、指针、数组及字符串的映射方式，以及如何使用 `sizeOf`、`alignOf` 等工具函数获取内存布局信息。文档重点讲解了 `@C` 修饰符、`CPointer` 类型及 `unsafe` 块的使用，旨在帮助开发者在保证类型安全和内存一致性的前提下，高效调用 C 语言库或进行底层系统编程。适合需要深入理解仓颉与 C 交互细节的开发者阅读。