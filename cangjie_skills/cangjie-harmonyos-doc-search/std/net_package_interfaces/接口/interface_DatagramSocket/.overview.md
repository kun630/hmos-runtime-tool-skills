# interface_DatagramSocket

This directory provides essential API reference documentation for the `DatagramSocket` interface within the Cangjie programming language's standard library. It focuses on packet-based network communication, specifically detailing the implementation of connectionless protocols like UDP. The documentation covers the structural definition of the socket interface, its inheritance hierarchy from Resource and ToString, and essential properties such as local and remote addresses. Furthermore, it provides in-depth technical specifications for blocking data transmission functions, including `receiveFrom` and `sendTo`, along with parameter types, return values, and exception handling strategies for socket errors and timeouts. This resource is indispensable for Cangjie developers and network engineers looking to implement robust, low-level networking solutions, manage socket lifecycles, and handle data flow with precision.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition, inheritance structure, and socket properties (addresses, timeouts) → interface_DatagramSocket_5more.md
*   Learn about specific blocking functions for sending and receiving data packets → func_receiveFromArrayByte_2more.md

## Detailed Description

### func_receiveFromArrayByte_2more.md func_receiveFromArrayByte_2more.md
This document serves as API reference documentation for Cangjie network programming, detailing functions for blocking data transmission. It covers two primary topics: the receiveFrom function for capturing incoming packets into a byte buffer and the sendTo function for dispatching data to specific remote addresses. Key information includes parameter specifications for Array<Byte> and SocketAddress, return values containing packet size and sender info, and exception handling for socket errors and timeouts. The content targets developers building socket-based applications who require precise control over data flow and error management within the Cangjie net package.

### interface_DatagramSocket_5more.md interface_DatagramSocket_5more.md
This document provides API reference documentation for the DatagramSocket interface in the Cangjie programming language, detailing the structure and functionality of packet-based network communication. It covers the interface definition, inheritance from Resource and ToString, and detailed specifications for properties including localAddress, remoteAddress, receiveTimeout, and sendTimeout. Key information explains that datagram transmission is unreliable and unordered, with specific size limits like 64KB for UDP. It outlines methods for sending and receiving data, exception handling for socket errors, and timeout configuration behaviors. This reference guide targets Cangjie developers implementing network protocols, requiring knowledge of socket programming and the language's standard library.