# class_BufferedOutputStreamT_where_T_OutputStream

This directory contains API documentation for the Cangjie `BufferedOutputStream` class, a generic wrapper designed to enhance I/O performance by buffering data before writing to an underlying output stream. It covers class definitions, constructors with various buffer configurations, and extensions for resource management and seekable operations. Key topics include memory optimization, automatic cleanup via the Resource interface, random access capabilities, and low-level byte writing functions. This documentation is suitable for Cangjie developers looking to implement efficient, safe, and flexible data output operations in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition and constructors → class_BufferedOutputStream_3more.md
*   Implement automatic resource management (try-with-resources) → extendT_BufferedOutputStreamT_Resource_wh_c645eeb6.md
*   Enable cursor manipulation and random access (Seekable) → extendT_BufferedOutputStreamT_Seekable_wh_37040a9b.md
*   Write data (bytes or arrays) to the stream → func_writeArrayByte_2more.md
*   Initialize, flush, or reset the stream state → initT_Int64_3more.md

## Detailed Description

### class_BufferedOutputStream_3more.md class_BufferedOutputStream_3more.md
This document serves as API reference for the Cangjie BufferedOutputStream class, designed to enhance output performance by buffering data before writing to an underlying stream. It covers the class definition, its inheritance from OutputStream, and three specific constructors: one using a default buffer size, one accepting a custom capacity, and another allowing buffer reuse for optimized memory management. Key information includes parameter descriptions, the default 4096-byte capacity, and the IllegalArgumentException triggered by empty buffers. This documentation targets Cangjie developers seeking to implement efficient I/O operations and reduce memory allocation overhead.

### extendT_BufferedOutputStreamT_Resource_wh_c645eeb6.md extendT_BufferedOutputStreamT_Resource_wh_c645eeb6.md
This API documentation describes the extension of the BufferedOutputStream class to implement the Resource interface in the Cangjie language, enabling automatic resource management. The document details the implementation declaration and defines two primary methods: close(), which terminates the stream, and isClosed(), which returns a boolean status. Key takeaways include the ability to use this stream within try-with-resource syntax for automatic cleanup and a warning against accessing the stream after closure. A comprehensive code example demonstrates these features in action. This reference is designed for Cangjie developers seeking to implement safe resource handling in IO operations.

### extendT_BufferedOutputStreamT_Seekable_wh_37040a9b.md extendT_BufferedOutputStreamT_Seekable_wh_37040a9b.md
This API documentation details the Cangjie language extension that implements the Seekable interface for BufferedOutputStream, enabling cursor manipulation and data length queries. The document defines public properties for total length, current cursor position, and remaining unread data, alongside the seek function for moving the cursor. Key information includes that the seek operation flushes the buffer before moving, allows positioning past the data end, but throws an IOException if moved before the start. This reference is intended for Cangjie developers requiring random access capabilities in buffered output streams.

### func_writeArrayByte_2more.md func_writeArrayByte_2more.md
This document serves as API reference documentation for output stream writing functions in the Cangjie programming language. It details two primary functions: write, which accepts an Array<Byte> buffer, and writeByte, which writes a single Byte. Key takeaways include understanding how to bind output streams, write data in bulk or individually, and flush buffers to ensure data transmission. Both functions are demonstrated using BufferedOutputStream and ByteBuffer. The target audience is Cangjie developers seeking to perform low-level I/O operations, requiring knowledge of stream handling and byte manipulation. Keywords: Cangjie, API, I/O, BufferedOutputStream, Byte, Array.

### initT_Int64_3more.md initT_Int64_3more.md
This document serves as API reference material for the BufferedOutputStream class in the Cangjie standard library, detailing initialization and control methods. It covers the constructor init(T, Int64) which sets the output stream and buffer capacity, func flush() for writing buffered data to the underlying stream, and func reset(T) for rebinding to a new output stream. Key information includes the IllegalArgumentException thrown for non-positive capacities and the behavior of resetting state while preserving capacity. Intended for Cangjie developers, this guide provides syntax, parameters, and code examples for efficient buffered I/O operations.