# hex

本目录提供了关于仓颉语言中十六进制编码转换的详细技术文档。它主要围绕 `stdx.encoding.hex` 包展开，深入讲解了字节数组与十六进制字符串之间的双向转换机制。文档内容不仅涵盖了 `toHexString` 和 `fromHexString` 等核心函数的具体用法，还通过完整的代码示例演示了如何处理 `Option` 类型的返回值，确保开发者能够安全地进行数据解码。核心关键词包括仓颉语言、Hex 编码、字节数组转换、标准库使用。该文档非常适合需要进行数据序列化、网络传输或加密解密操作的仓颉语言开发者阅读，是一份实用的基础库使用教程。

## Quick Navigation

- **你想学习什么？**
    - 了解字节数组与十六进制字符串的转换方法 → hex.md
    - 查看 `toHexString` 和 `fromHexString` 的代码示例 → hex.md
    - 学习如何处理 Option 类型的解码返回值 → hex.md

## Detailed Description

### hex.md hex.md
这份文档旨在演示仓颉语言中字节数组与十六进制字符串的相互转换方法。主要涵盖了 `stdx.encoding.hex` 包下的 `toHexString` 和 `fromHexString` 两个核心函数的使用。文档通过完整的代码示例，展示了如何将 `Array<Byte>` 编码为字符串，以及如何将字符串解码回字节数组并处理 Option 类型的返回值。关键信息包括具体的函数调用方式及预期的运行结果。该文档适合需要处理数据编码转换的仓颉语言开发者阅读，属于基础库使用教程。