# ohosscreen_lock_file_manager锁屏敏感数据管理

This directory contains API documentation for the `ohos.screen_lock_file_manager` module, designed to manage sensitive data access permissions on HarmonyOS devices. It focuses on handling encryption key lifecycles—destroying keys when the screen is locked and restoring them upon unlocking—to ensure data security. The documentation covers the core `ScreenLockFileManager` class, status enums for access requests, and module configuration requirements. It is intended for HarmonyOS developers using the Cangjie language who need to implement secure file access controls and manage permission states in locked-screen scenarios.

## Quick Navigation

**What do you want to learn?**

*   Understand the module overview and key lifecycle management → ohosscreen_lock_file_manag_4more.md
*   Learn how to request or revoke sensitive data access permissions → class_ScreenLockFileManager.md
*   Check the status of an access request (Granted/Denied) → enum_AccessStatus.md
*   Check the status of a release request (Granted/Denied) → enum_ReleaseStatus.md

## Detailed Description

### class_ScreenLockFileManager.md class_ScreenLockFileManager.md
This API reference document details the ScreenLockFileManager class for managing sensitive data access permissions while the device screen is locked. It covers two primary static methods: acquireAccess for requesting permission and releaseAccess for revoking it, along with their return types (AccessStatus, ReleaseStatus) and potential BusinessException errors. Key takeaways include the synchronous nature of these methods, specific error codes for permission denial or system issues, and practical Cangjie code examples for implementation. Targeted at application developers using the Cangjie language who need to handle secure data access in locked-screen scenarios.

### enum_AccessStatus.md enum_AccessStatus.md
This API reference document defines the AccessStatus enum in the Cangjie language, representing the status of sensitive data access requests under lock screen conditions. The documentation details the enum's structure, including its two primary values: ACCESS_DENIED and ACCESS_GRANTED. It also explains inherited functionality such as equality operators (==, !=) and the toString method for string representation. Key takeaways include the enum's inheritance from Equatable and ToString interfaces and its association with the SystemCapability.Security.ScreenLockFileManager capability. Intended for system developers, this document provides the necessary specifications to handle permission states within security-related applications.

### enum_ReleaseStatus.md enum_ReleaseStatus.md
This API reference document defines the Cangjie enum ReleaseStatus, which manages the state of sensitive data access permissions during screen lock. The documentation details the enum's definition, its two primary values—RELEASE_DENIED and RELEASE_GRANTED—and inherited methods including equality operators and toString. Key information includes the enum's inheritance from Equatable and ToString, its association with the SystemCapability.Security.ScreenLockFileManager, and its introduction in version 19. This guide is intended for developers implementing security logic for file access permissions in locked screen scenarios.

### ohosscreen_lock_file_manag_4more.md ohosscreen_lock_file_manag_4more.md
This document details the ohos.screen_lock_file_manager module, designed to protect sensitive data by destroying encryption keys when the screen is locked and restoring them upon unlocking. The content covers module imports, the specific permission ohos.permission.PROTECT_SCREEN_LOCK_DATA, and instructions for running API examples in Cangjie template projects. Key takeaways include the automatic key lifecycle management and the requirement for specific Context configuration. This technical reference is intended for HarmonyOS developers using the Cangjie language to implement secure data access controls in their applications.