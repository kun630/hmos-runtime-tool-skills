# ohospasteboard剪贴板

This directory contains API reference documentation for the HarmonyOS `ohos.pasteboard` module, specifically designed for the Cangjie programming language. It covers the implementation of system clipboard functionality, including data representation, property configuration, and system pasteboard management. Key components include classes for data records and system operations, enums for sharing options, and functions for data construction. This documentation is suitable for Cangjie developers using the BasicServicesKit to implement copy-paste features, handle various data types like text and images, and manage data sharing scopes within HarmonyOS applications.

## Quick Navigation

**What do you want to learn?**
*   Understand the module overview and MIME type constants → ohospasteboard剪贴板_11more.md
*   Define the scope of data sharing (In-app, Local, Cross-device) → enum_ShareOption.md
*   Configure clipboard data attributes (tags, timestamps) → struct_PasteDataProperty.md
*   Understand the structure of clipboard content entries → class_PasteDataRecord.md

**What do you want to do?**
*   Construct clipboard data objects and records → func_createDataString_Arra_4more.md
*   Retrieve the system clipboard object → func_getSystemPasteboard.md
*   Manage clipboard content (add, remove, modify data) → class_PasteData/
*   Perform system operations (clear, read, write to clipboard) → class_SystemPasteboard/

## Detailed Description

### class_PasteDataRecord.md class_PasteDataRecord.md
This API documentation defines the `PasteDataRecord` class, which serves as an abstract representation of clipboard content entries in the Cangjie language. The document details read-only properties for various data formats, including `htmlText`, `plainText`, `uri`, `pixelMap`, and `mimeType`. It also describes the `toPlainText()` method for converting record data into a string. Key information includes that all fields are read-only and the class requires the `SystemCapability.MiscServices.Pasteboard` capability. This reference guide targets developers implementing clipboard functionality for system version 12, providing the necessary structure to handle diverse data types like text, HTML, and images.

### enum_ShareOption.md enum_ShareOption.md
This API reference document defines the `ShareOption` enum in the Cangjie language, specifying the scope for pasting data within the system's pasteboard service. The documentation details three primary enum members: `INAPP` for same-application pasting, `LOCALDEVICE` for any local application, and `CROSSDEVICE` for cross-device sharing. It also describes the `getValue()` method used to retrieve the integer value of the enum. Key takeaways include the system capability requirement `SystemCapability.MiscServices.Pasteboard` and the introduction of these features in version 12. This document is intended for Cangjie developers implementing clipboard functionality who need to manage data access permissions and sharing ranges.

### func_createDataString_Arra_4more.md func_createDataString_Arra_4more.md
This API reference document details functions for constructing clipboard data objects and records in the Cangjie programming language. It covers four main functions: `createData` for byte arrays, and `createRecord` overloads accepting String, PixelMap, or byte arrays. Key information includes the requirement for a MIME type string and a value parameter, with return types being either `PasteData` or `PasteDataRecord`. The document specifies system capabilities, parameter descriptions, and provides practical code examples for each function. Targeted at developers using the BasicServicesKit, this guide facilitates the implementation of pasteboard functionality by handling various data formats like text, images, and raw binary data.

### func_getSystemPasteboard.md func_getSystemPasteboard.md
This API documentation defines the Cangjie function `getSystemPasteboard`, which is designed to retrieve the system clipboard object for data manipulation. The document covers the function signature, required system capabilities, starting version 12, and the return type `SystemPasteboard`. Key takeaways include the specific system capability `SystemCapability.MiscServices.Pasteboard` and a practical code example demonstrating how to import BasicServicesKit and invoke the function. Intended for Cangjie developers, this reference serves as a guide for implementing clipboard functionality in applications requiring data sharing or copy-paste features.

### ohospasteboard剪贴板_11more.md ohospasteboard剪贴板_11more.md
This document serves as API reference documentation for the `ohos.pasteboard` module, designed to manage system clipboard capabilities in HarmonyOS applications using the Cangjie programming language. It details the necessary imports, permissions, and usage context for template projects. Key sections include definitions for MIME type constants such as `MIMETYPE_TEXT_PLAIN` and `MIMETYPE_PIXELMAP`, alongside the `createData` function overloads used to construct `PasteData` objects from strings or images. The documentation provides syntax examples and parameter descriptions, targeting developers seeking to implement copy and paste functionality for text, HTML, URIs, and PixelMap data within their apps.

### struct_PasteDataProperty.md struct_PasteDataProperty.md
This API reference document defines the `PasteDataProperty` struct used to configure attributes for clipboard content entries in the Cangjie programming language. It details the struct's constructor and its five public properties: `mimeTypes`, `tag`, `timestamp`, `localOnly`, and `shareOption`. Key information includes the management of data types, custom tags, write timestamps, and sharing scope. Notably, the `shareOption` property overrides `localOnly` for controlling cross-device data transfer. All properties are readable and writable. This documentation targets developers implementing clipboard functionality, specifically for system version 12 and above, requiring knowledge of the `SystemCapability.MiscServices.Pasteboard` service.

### class_PasteData/
This directory provides comprehensive API reference documentation for the Cangjie `PasteData` class, a core component for handling clipboard content in HarmonyOS applications. It details the structure of pasteboard data objects, including data records and properties, and covers a wide range of operations such as adding, retrieving, modifying, and removing various data types like text, HTML, PixelMap, and byte arrays. Designed for Cangjie developers utilizing the BasicServicesKit, these resources facilitate the implementation of complex pasteboard features under the `SystemCapability.MiscServices.Pasteboard` capability.

### class_SystemPasteboard/
This directory contains essential API reference documentation for the `SystemPasteboard` class in the Cangjie programming language, focusing on the management of system clipboard operations. It provides detailed insights into methods for clearing, reading, verifying, and writing data to the clipboard. Key topics include the `setData` function, permission requirements like `ohos.permission.READ_PASTEBOARD`, and system capabilities such as `SystemCapability.MiscServices.Pasteboard`. The documentation also covers error handling for business exceptions and concurrent operations. This content is specifically tailored for application developers and software engineers working with the BasicServicesKit who need to implement or troubleshoot copy-paste functionality, offering precise code examples and return value definitions to ensure effective system integration.