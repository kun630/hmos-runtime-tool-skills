# Flex布局

This directory provides comprehensive technical documentation for implementing Flex layouts within the ArkUI framework using the Cangjie programming language. It focuses on the core APIs required to control component sizing, space distribution, and alignment within parent containers such as Flex, Column, and Row. Key topics include `flexBasis`, `flexGrow`, `flexShrink`, and `alignSelf`. These resources are specifically tailored for developers building responsive user interfaces who need precise control over layout behavior and wish to understand how to override default alignment settings for individual components. The documentation clarifies constraints, default values, and prerequisites necessary for effective layout management, making it an essential resource for mastering UI mechanics in ArkUI.

## Quick Navigation

What do you want to learn or implement?

*   Understand how to control component sizing, allocate remaining space, and define shrinkage ratios within Flex containers → Flex布局_6more.md
*   Learn how to override the parent container's alignment properties for specific child components using `alignSelf` → func_alignSelfItemAlign_2more.md

## Detailed Description

### Flex布局_6more.md Flex布局_6more.md
This document serves as an API reference for Flex layout properties in the ArkUI framework using the Cangjie language, detailing how to control component sizing and space distribution within parent containers. It covers four main functions: flexBasis for setting base dimensions, flexGrow for allocating remaining space, and flexShrink for defining shrinkage ratios. Key information includes specific constraints that these properties only work within Flex, Column, Row, or GridRow parents, with distinct default values for flexShrink depending on the container type. Intended for developers building responsive UIs, it clarifies prerequisites like explicit main axis sizing for shrinkage to take effect.

### func_alignSelfItemAlign_2more.md func_alignSelfItemAlign_2more.md
This API documentation file details the `alignSelf` function in the Cangjie language, which controls the cross-axis alignment of child components within parent containers. The document covers the function signature, system capabilities, version requirements, and a parameter table defining the `ItemAlign` value. It also includes a comprehensive code example. Key takeaways are that `alignSelf` overrides the `alignItems` property of Flex, Column, Row, and GridRow containers, allowing individual child components to dictate their own alignment. The example demonstrates this override behavior alongside other Flex properties like `flexGrow` and `flexShrink`. The target audience is ArkUI developers using Cangjie who need granular control over component layout alignment.