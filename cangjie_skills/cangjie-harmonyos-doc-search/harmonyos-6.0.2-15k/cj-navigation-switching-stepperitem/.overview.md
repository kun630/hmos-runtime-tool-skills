# cj-navigation-switching-stepperitem

This directory serves as a comprehensive resource for developers working with the Cangjie ArkUI framework, specifically focusing on the `StepperItem` component. As a crucial child element of the Stepper component, `StepperItem` facilitates the creation of step-by-step navigation interfaces, often used in wizards or multi-stage forms. The documentation covers the complete API reference, including component initialization via the `init` function and the customization of navigation labels such as `nextLabel` and `prevLabel`. It provides in-depth details on state management through the `ItemState` enum, which allows developers to define behaviors for Normal, Disabled, Waiting, and Skip states. Additionally, the guide explains layout constraints, specifically how the item fills the parent's width. This material is best suited for developers who possess a solid understanding of Cangjie syntax and ArkUI component structures and are looking to implement robust navigation logic in their applications.

## Quick Navigation

**What do you want to learn?**

- If you want to understand the fundamental API reference and how to create the component using the `init` function → cj-navigation-switching-stepperitem.md
- If you need to customize the user interface by modifying navigation labels such as `nextLabel` and `prevLabel` → cj-navigation-switching-stepperitem.md
- If you want to manage the component's behavior and state using the `ItemState` enum, including Normal, Disabled, Waiting, and Skip states → cj-navigation-switching-stepperitem.md
- If you are looking for information on layout constraints and how the item fills the parent's width → cj-navigation-switching-stepperitem.md

## Detailed Description

### cj-navigation-switching-stepperitem.md

This document provides API reference documentation for the StepperItem component, a child element of the Stepper component in the Cangjie ArkUI framework. It covers the component creation using the init function and details specific attributes for customizing navigation labels, including nextLabel and prevLabel. Key information includes the status attribute and the ItemState enum, which defines states like Normal, Disabled, Waiting, and Skip to control navigation button behavior. The document also outlines layout constraints, noting that the item fills the parent's width. Intended for developers, this guide requires prior knowledge of Cangjie syntax and ArkUI component structures.