# class_IfExpr

This directory contains comprehensive documentation for the `IfExpr` class, a critical component within the Abstract Syntax Tree (AST) of the Cangjie programming language. It focuses on the structural representation and manipulation of conditional expressions, detailing class definitions, constructors, and essential properties such as `condition`, `ifBlock`, and `elseExpr`. Additionally, it covers AST utility functions for token conversion and tree traversal using the Visitor pattern. Key topics include syntax error handling via `ASTException` and programmatic access to code structure. This documentation is specifically suitable for developers building compiler tools, static analysis applications, or language parsers who need to programmatically access and manipulate conditional logic structures.

## Quick Navigation

**What do you want to learn?**

*   Understand the class structure, properties, and constructors of conditional expressions → class_IfExpr_10more.md
*   Learn about AST traversal and token conversion methods for syntax nodes → func_toTokens_2more.md

## Detailed Description

The following files provide specific API references and implementation details for the components mentioned above.

### class_IfExpr_10more.md class_IfExpr_10more.md
This document provides API reference details for the IfExpr class, which represents conditional expressions within the Abstract Syntax Tree (AST) of the Cangjie programming language. It covers the class definition, constructors, and properties such as condition, ifBlock, elseExpr, and specific token properties for syntax elements like keywords and parentheses. Key takeaways include the ability to construct IfExpr objects from token collections and the handling of ASTException for invalid token assignments or missing else branches. This technical reference is intended for developers building language tools or compilers who need to manipulate or parse conditional logic structures.

### func_toTokens_2more.md func_toTokens_2more.md
This document provides API reference documentation for Abstract Syntax Tree (AST) manipulation functions within the Cangjie language. It covers two primary methods: toTokens, which converts the current syntax tree node into a Tokens type, and traverse, which iterates over the node and its children using a Visitor pattern. Key takeaways include the specific return types for toTokens and the ability to control traversal flow by overriding the visit function and calling breakTraverse to stop early. This technical reference is intended for developers building compiler tools or static analysis applications who require programmatic access to code structure and syntax.