# interface_CarryingOpT

This directory contains API reference documentation for the `CarryingOp` interface and its implementations across various integer types in the Cangjie programming language. It focuses on arithmetic and bitwise operations designed to detect truncation and overflow explicitly. Key concepts include carrying operations, wrapping strategies, and tuple return types containing boolean flags and result values. This documentation is suitable for Cangjie developers, systems programmers, and engineers who require precise low-level control over integer arithmetic, ensuring numerical stability and safety in performance-critical applications.

## Quick Navigation

What do you want to learn?

*   Understand the core interface definition and specifications → interface_CarryingOpT_9more.md
*   Look up specific functions like carryingShr or carryingSub → func_carryingShrUInt64_2more.md
*   Explore implementations for signed integers (Int8, Int16, Int32, Int64, IntNative) → `extend_Int8_CarryingOpInt8/`, `extend_Int16_CarryingOpInt16/`, `extend_Int32_CarryingOpInt32/`, `extend_Int64_CarryingOpInt64_CarryingPow/`, `extend_IntNative_CarryingOpIntNative/`
*   Explore implementations for unsigned integers (UInt8, UInt16, UInt32, UInt64, UIntNative) → `extend_UInt8_CarryingOpUInt8/`, `extend_UInt16_CarryingOpUInt16/`, `extend_UInt32_CarryingOpUInt32/`, `extend_UInt64_CarryingOpUInt64/`, `extend_UIntNative_CarryingOpUIntNative/`

## Detailed Description

### func_carryingShrUInt64_2more.md func_carryingShrUInt64_2more.md
This document serves as API reference documentation for two specific Cangjie language functions designed to perform arithmetic and bitwise operations while detecting truncation. The main sections cover the carryingShr function for UInt64 right shifts and the generic carryingSub function for subtraction. Key information includes that both functions return a tuple containing a boolean flag indicating if truncation occurred and the resulting value of the operation. This reference is intended for Cangjie developers who need to handle low-level error checking or overflow detection in their code.

### interface_CarryingOpT_9more.md interface_CarryingOpT_9more.md
This API reference document defines the CarryingOp interface in the Cangjie programming language, designed to handle integer operations with explicit truncation detection. The documentation details the interface structure and specifications for ten methods covering arithmetic operations like addition, subtraction, multiplication, division, and modulo, as well as bitwise shifts and increment or decrement functions. A key takeaway is that every method returns a tuple containing a boolean flag indicating if truncation occurred and the resulting value. This interface is intended for developers who need to detect overflow or underflow during low-level integer calculations in Cangjie applications.

### extend_Int16_CarryingOpInt16/
This directory serves as a comprehensive repository for API reference documentation regarding the `CarryingOp` interface implementation specifically tailored for the `Int16` type within the Cangjie programming language. It functions as a critical technical guide for developers aiming to implement arithmetic operations equipped with robust overflow detection mechanisms. The content encompasses a wide array of functions, including carryingAdd, carryingMul, carryingSub, carryingDiv, carryingNeg, carryingShl, carryingShr, carryingInc, carryingDec, and carryingMod. Each function detailed herein utilizes a wrapping strategy and returns a tuple containing the calculation result paired with a Boolean flag to indicate overflow or truncation.

### extend_Int32_CarryingOpInt32/
This directory serves as a technical reference for the `CarryingOp` interface implementation regarding the `Int32` data type in the Cangjie programming language. It encompasses a wide array of arithmetic and bitwise operations that are designed to manage integer overflow using a wrapping strategy. The content is divided into specific documentation files covering functions like `carryingAdd`, `carryingMul`, `carryingSub`, `carryingDiv`, `carryingMod`, `carryingInc`, `carryingDec`, `carryingNeg`, `carryingShl`, and `carryingShr`. A core feature of these operations is their return type: a tuple containing both the result and a Boolean flag indicating if truncation took place.

### extend_Int64_CarryingOpInt64_CarryingPow/
This directory contains comprehensive API documentation for extending the Int64 type within the Cangjie programming language to support advanced carrying arithmetic operations. It specifically addresses the implementation of the `CarryingOp` and `CarryingPow` interfaces, which are designed to handle numerical computations where overflow detection is critical. The documentation covers a wide range of functions, including basic arithmetic like addition, subtraction, multiplication, and division, as well as bitwise operations and power calculations. All operations detailed here utilize a wrapping strategy, returning a tuple consisting of a boolean overflow flag and the resulting Int64 value.

### extend_Int8_CarryingOpInt8/
This directory provides a detailed collection of API documentation regarding the `CarryingOp` interface implementation specifically designed for the `Int8` type in the Cangjie programming language. It focuses on arithmetic operations that employ a wrapping strategy, a critical feature for systems programming where standard overflow behaviors must be explicitly detected and managed rather than causing runtime errors. The documentation covers a comprehensive set of functions, ranging from basic arithmetic such as addition and subtraction to more complex operations like division, modulo, and bit shifting. Each function is documented to return a tuple containing a Boolean flag indicating overflow or truncation alongside the resulting value.

### extend_IntNative_CarryingOpIntNative/
This directory provides essential API documentation for the `CarryingOp` interface as implemented for the `IntNative` type in the Cangjie programming language. It is dedicated to detailing arithmetic operations that employ a wrapping strategy to manage integer overflow effectively. The scope of this documentation includes fundamental functions such as addition, subtraction, multiplication, division, and modulo, as well as bitwise operations like shifting and negation. Crucially, all documented functions return a tuple containing a Boolean overflow flag and the computed result, allowing for precise error detection.

### extend_UInt16_CarryingOpUInt16/
This directory provides comprehensive API reference documentation for the `CarryingOp` interface implementation specifically tailored for the `UInt16` data type within the Cangjie programming language. It focuses on arithmetic and bitwise operations that require explicit overflow detection, moving beyond standard calculations to offer developers granular control over computation results. The documentation covers a wide array of functions, including addition, subtraction, multiplication, division, and bit shifting, all of which employ a wrapping strategy to handle data limits. Each operation is designed to return a tuple containing both the result and a Boolean flag indicating overflow or truncation.

### extend_UInt32_CarryingOpUInt32/
This directory serves as a specialized API reference for the `CarryingOp` interface implementation regarding the `UInt32` data type in the Cangjie programming language. It thoroughly documents a collection of arithmetic and bitwise functions that utilize a wrapping strategy to manage integer overflow. Unlike standard operations that might panic or wrap silently, these functions return a tuple comprising the calculated value and a Boolean flag that signals whether truncation occurred. This mechanism provides developers with explicit overflow detection capabilities.

### extend_UInt64_CarryingOpUInt64/
This directory provides comprehensive API reference documentation for the `CarryingOp` interface implementation tailored for the `UInt64` type within the Cangjie programming language. It details a suite of arithmetic and bitwise functions designed to handle potential overflows through a specific wrapping strategy. Instead of causing runtime errors, these operations return a tuple containing the calculation result and a Boolean flag indicating if truncation occurred. This documentation is essential for Cangjie developers who need precise, low-level control over unsigned 64-bit integer arithmetic and require reliable overflow detection mechanisms to ensure numerical integrity in their applications.

### extend_UInt8_CarryingOpUInt8/
This directory contains API reference documentation for the Cangjie language, specifically focusing on the `CarryingOp` interface implementation for the `UInt8` type. It covers a comprehensive set of arithmetic and bitwise operations designed with explicit overflow detection capabilities. All functions utilize a wrapping strategy and return tuples containing a Boolean flag indicating overflow or truncation status alongside the resulting value. This documentation is essential for developers engaged in low-level systems programming, cryptography, or any application requiring precise control over numeric operations and error handling.

### extend_UIntNative_CarryingOpUIntNative/
This directory contains comprehensive API documentation for the implementation of the `CarryingOp` interface specifically tailored for the `UIntNative` type within the Cangjie programming language. It focuses on a specialized set of arithmetic and bitwise operations that are essential for low-level system programming. The documentation explains how these functions utilize a wrapping strategy to handle overflow, ensuring that developers can detect truncation events reliably. By returning tuples that combine the computed result with a Boolean status flag, these APIs provide the necessary tools for managing integer arithmetic safely.