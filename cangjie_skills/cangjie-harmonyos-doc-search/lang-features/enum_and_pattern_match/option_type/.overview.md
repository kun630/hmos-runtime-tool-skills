# option_type

本目录包含仓颉编程语言中 `Option` 类型的核心文档，详细介绍了如何处理可能存在或不存在的值。内容涵盖 `Option` 作为泛型枚举的定义（包含 `Some` 和 `None` 构造器）、简写语法 `?Ty`、上下文类型推断时的自动封装机制，以及 `None` 的显式类型指定方法。该文档适合仓颉语言的学习者和开发者，旨在帮助掌握空值安全处理及泛型应用，建议结合错误处理相关文档阅读。

## 快速导航

- 你想了解 Option 类型的定义与基础用法？ → 查看 option_type.md Option 类型定义与构造器
- 你想掌握简写语法 `?Ty` 和自动封装机制？ → 查看 option_type.md 语法糖与类型推断
- 你想知道如何显式指定 None 的类型？ → 查看 option_type.md None 类型指定

## 详细描述

### option_type.md option_type.md

本文档介绍了仓颉编程语言中 `Option` 类型的定义、语法糖及基本用法，用于表示可能存在或不存在的值。主要内容包括 `Option` 作为泛型枚举的定义（包含 `Some` 和 `None` 构造器）、简写语法 `?Ty`、上下文类型推断时的自动封装机制，以及 `None` 的显式类型指定方法。核心要点包括 `Option<T>` 等价于 `?Ty`，在明确需要 `Option` 类型的上下文中可直接传入 `T` 类型的值（自动封装），以及在类型不明确时需使用 `None<T>` 语法。该文档属于语言基础教程或参考文档，面向仓颉语言的学习者和开发者，建议结合泛型及错误处理相关文档阅读。