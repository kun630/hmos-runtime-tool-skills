# cj-persiststorage

This directory serves as the central repository for technical documentation regarding the `cj-persiststorage` module, a critical component for Cangjie developers working within the HarmonyOS ecosystem. It focuses extensively on the `PersistentStorage` singleton object, which is designed to bridge the gap between volatile memory and persistent storage for UI states. The documentation explains how to maintain specific `AppStorage` properties on disk, thereby ensuring that user settings and interface states are preserved even after an application terminates and restarts. It delves into the technical synchronization mechanisms required to write and retrieve data efficiently. Furthermore, it outlines the supported data types, ranging from simple primitives to complex JSON-serializable objects, while also highlighting strict constraints such as the ban on nested objects and the 2kb file size limit. Additionally, it details the precise initialization sequences required to avoid common pitfalls. This resource is indispensable for HarmonyOS developers and software engineers who are proficient with `AppStorage` and are seeking to implement reliable, high-performance state management and data persistence strategies in their applications.

## Quick Navigation

**What do you want to learn?**
- Understand the core architecture of PersistentStorage and how it synchronizes with AppStorage → [1] PersistentStorage持久化存储UI状态/
- Identify supported data types and learn about strict system limitations like the 2kb size cap → [1] PersistentStorage持久化存储UI状态/
- Master the correct initialization sequence to prevent data loss in Cangjie applications → [1] PersistentStorage持久化存储UI状态/
- Explore practical usage scenarios and best practices for efficient state persistence → [1] PersistentStorage持久化存储UI状态/

## Detailed Description

The following section contains the in-depth technical documentation necessary for implementing and understanding the persistence features within this module. It covers the architectural design, constraints, and practical implementation details required for effective state management.

### PersistentStorage持久化存储UI状态/
This directory provides comprehensive technical documentation on PersistentStorage, a singleton object designed to persist specific AppStorage properties to disk, ensuring UI state retention across application restarts. It covers the synchronization mechanism between PersistentStorage and AppStorage, detailing how data is written and retrieved. Key topics include supported data types (simple types and JSON-serializable objects), strict limitations (prohibition of nested objects, 2kb size limit), and initialization sequences. The documentation also explores usage scenarios within Cangjie applications, emphasizing the critical importance of calling PersistentStorage before AppStorage to prevent data loss. It is intended for HarmonyOS developers and application developers familiar with AppStorage who need to implement robust state management and data persistence efficiently while adhering to performance best practices.