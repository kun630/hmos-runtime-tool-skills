# 示例代码

This directory provides a comprehensive collection of code examples designed for the Cangjie language and ArkUI framework, focusing specifically on the implementation and manipulation of the `borderImage` interface. It covers essential UI styling techniques, ranging from applying static gradient borders to creating highly interactive interfaces with dynamic property adjustments. Key topics include configuring `BorderImageOption`, managing state variables for real-time updates, and utilizing `LengthEdgeWidthsType` for precise control. These tutorials are ideal for HarmonyOS developers and UI engineers seeking to enhance their applications with advanced visual effects and responsive component styling.

## Quick Navigation

**What do you want to learn?**

*   Apply a static gradient color border to a UI component → 示例1设置渐变色边框.md
*   Dynamically adjust border properties (slice, width, etc.) using interactive controls → 示例2动态调整属性值.md
*   Manage complex border configurations using `LengthEdgeWidthsType` and state variables → [3]

## Detailed Description

### 示例1设置渐变色边框.md 示例1设置渐变色边框.md
This document provides a practical code example demonstrating how to apply a gradient color border to UI components using the borderImage interface in the Cangjie language. The content focuses on the implementation of the EntryView class, specifically detailing the configuration of BorderImageOption and LinearGradientOption within a Text component. Key takeaways include the syntax for defining gradient colors, angles, slice dimensions, border widths, outset values, and repeat modes. This tutorial is intended for developers using the Cangjie framework for UI development who need to implement advanced border styling and visual effects.

### 示例2动态调整属性值.md 示例2动态调整属性值.md
This document provides a code example demonstrating how to dynamically adjust borderImage properties using the Slider interface in the Cangjie language. The main topics include state management for attributes like slice, width, outset, repeat mode, and fill, linking them to interactive UI controls such as Slider, Select, and Toggle. Key information highlights the use of @State decorators and BorderImageOption to create real-time visual updates. This tutorial is intended for developers familiar with Cangjie and ArkUI syntax who are learning to implement dynamic styling and user interaction.

### [3] 示例3使用LengthEdgeWidthsType类型值/
This directory contains a collection of code examples designed to demonstrate the practical application of the `LengthEdgeWidthsType` type value within the `borderImage` interface for Cangjie and ArkUI development. The content focuses on building dynamic user interfaces where border properties—including slice, width, and outset—are managed and manipulated through state variables. It illustrates the integration of interactive controls such as sliders, select menus, and toggle switches to enable real-time visual feedback. These tutorials are highly suitable for HarmonyOS developers seeking to master state management, component interaction, and complex styling logic within the ArkUI framework.