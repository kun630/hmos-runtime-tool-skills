# class_Regex

This directory contains comprehensive API reference documentation for the Regex class within the Cangjie programming language. It covers essential functionalities for text processing, including creating regular expression instances, performing pattern matching, finding multiple matches, and string manipulation operations like replacement and splitting. The documentation details constructors, exception handling, and specific methods for managing capture groups. Suitable for Cangjie developers, this resource provides the technical details needed to implement robust text validation and parsing logic in applications.

## Quick Navigation

**What do you want to do?**

*   Initialize a Regex instance or find the first match → class_Regex_4more.md
*   Retrieve all matches or manage named capture groups → func_findAllString_Bool_4more.md
*   Check for pattern existence or perform basic string replacement → func_matchesString_4more.md
*   Split strings or perform advanced replacement with limits → func_replaceAllString_Stri_4more.md

## Detailed Description

### class_Regex_4more.md class_Regex_4more.md
This document provides API reference documentation for the Regex class in the Cangjie programming language, detailing how to create regular expression instances and perform pattern matching. It covers two constructors, specifically the recommended init method accepting a pattern string and an array of RegexFlag, and a deprecated alternative. Additionally, it documents the find function, which locates the first matching subsequence and returns an Option<MatchData>. Key information includes exception handling for initialization failures and the ability to toggle capture group extraction. The text includes a code example demonstrating case-sensitive matching. This technical reference targets Cangjie developers seeking to implement text processing and validation logic.

### func_findAllString_Bool_4more.md func_findAllString_Bool_4more.md
This document serves as API reference documentation for the Cangjie standard library's Regex package, detailing functions for finding all matches and managing capture groups. It covers the `findAll` function for retrieving all matches as an array, `lazyFindAll` for iterator-based lazy evaluation, `getNamedGroups` for mapping named capture groups, and the deprecated `matcher` function. Key functions include `findAll` and `lazyFindAll`, both accepting an input string and a boolean flag to enable group extraction. The `getNamedGroups` method provides a mapping of group names to indices. The `matcher` function is marked for future deprecation. This reference is intended for Cangjie developers implementing regular expression matching, specifically those needing to extract multiple matches or handle named groups efficiently.

### func_matchesString_4more.md func_matchesString_4more.md
This document provides API reference documentation for string manipulation methods in the Cangjie programming language's Regex module. It covers four primary functions: matches to check for pattern existence, replace to substitute the first occurrence from the start or a specific index, and replaceAll to substitute all occurrences. Key information includes detailed parameter definitions, return types, exception handling for index errors, and verified code examples demonstrating date format matching and replacement. This technical resource is intended for Cangjie developers seeking to implement regular expression pattern matching and text substitution logic in their applications.

### func_replaceAllString_Stri_4more.md func_replaceAllString_Stri_4more.md
This document serves as API reference documentation for specific string manipulation methods within the Cangjie language's Regex module. It details four key functions: `replaceAll` for substituting matched patterns with a limit, two overloads of `split` for dividing input sequences, and `string` for retrieving the original input. Key information includes the behavior of the `limit` parameter, which controls the number of operations, and the return types such as `Array<String>`. The content provides function signatures, parameter descriptions, and verified code examples. Targeted at Cangjie developers, this reference requires prior knowledge of regular expressions and basic string operations.