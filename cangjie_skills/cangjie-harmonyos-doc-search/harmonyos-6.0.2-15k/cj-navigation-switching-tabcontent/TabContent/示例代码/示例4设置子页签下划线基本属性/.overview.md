# 示例4设置子页签下划线基本属性

This directory contains code examples focused on configuring and animating the underline indicator for sub-tabs in HarmonyOS applications. It covers setting basic visual properties like color, height, width, border radius, and spacing using languages such as Cangjie and ArkTS. The examples demonstrate how to use `SubTabBarStyle`, `IndicatorStyle`, and the `animateTo` function to create static and dynamic UI effects. These tutorials are suitable for developers working on HarmonyOS UI development who need to customize tab navigation components and implement smooth property animations.

## Quick Navigation

**What do you want to learn?**

*   Configure basic static properties (color, size, spacing) in Cangjie → 示例4设置子页签下划线基本属性_1.md
*   Configure basic static properties and lifecycle events in ArkTS → 示例4设置子页签下划线基本属性_5.md
*   Animate color and height dynamically → 示例4设置子页签下划线基本属性_2.md
*   Animate height, width, and border radius → 示例4设置子页签下划线基本属性_3.md
*   Animate border radius and spacing → 示例4设置子页签下划线基本属性_4.md

## Detailed Description

### 示例4设置子页签下划线基本属性_1.md 示例4设置子页签下划线基本属性_1.md
This document serves as a code example demonstrating how to configure the basic visual properties of the underline indicator for sub-tabs in a Cangjie-based application. It covers the implementation of the SubTabBarStyle class, specifically focusing on the indicator property. The code defines state variables for color, dimensions, border radius, and spacing. Key takeaways include the use of @State decorators for reactive UI updates and the specific parameters like indicatorColor, indicatorWidth, and indicatorHeight to customize the tab bar's appearance. The SelectedMode is set to INDICATOR. This tutorial is intended for developers using the Cangjie language for HarmonyOS UI development who need to customize tab navigation components.

### 示例4设置子页签下划线基本属性_2.md 示例4设置子页签下划线基本属性_2.md
This code example demonstrates how to dynamically animate the color and height of a sub-tab underline indicator in a HarmonyOS application using ArkTS. The document focuses on implementing property animations triggered by user interactions, specifically button clicks. Key sections include the configuration of the animateTo function with AnimateParam, defining parameters like duration, curve, delay, and play mode. The code illustrates toggling state variables to switch between specific color values and height dimensions. This tutorial is designed for developers seeking to enhance UI interactivity through smooth transitions and requires basic knowledge of ArkTS syntax and declarative UI paradigms.

### 示例4设置子页签下划线基本属性_3.md 示例4设置子页签下划线基本属性_3.md
This code example demonstrates how to dynamically animate the basic properties of a sub-tab underline indicator in an ArkUI application. The content covers three specific interactive scenarios: changing the underline's height, width, and border radius. Key information includes the implementation of the `animateTo` function to handle state transitions, utilizing parameters like duration, curve, and delay to control the animation behavior for variables such as `indicatorHeight` and `indicatorWidth`. This tutorial is designed for developers seeking to implement interactive UI animations and state management within the ArkTS framework.

### 示例4设置子页签下划线基本属性_4.md 示例4设置子页签下划线基本属性_4.md
This code example demonstrates how to animate the basic properties of a sub-tab underline indicator in a HarmonyOS application. The document covers two specific animation scenarios: changing the underline's border radius and adjusting the spacing between the underline and the text. Key information includes the use of the animateTo function with AnimateParam to configure duration, curve, delay, and play mode, alongside state toggling logic for properties like indicatorBorderRadius and indicatorSpace. This tutorial is intended for developers familiar with ArkTS who want to implement dynamic UI animations for tab components.

### 示例4设置子页签下划线基本属性_5.md 示例4设置子页签下划线基本属性_5.md
This document serves as a code example demonstrating how to configure the basic properties of the underline indicator for sub-tabs in a Tabs component using ArkTS. The code illustrates the implementation of SubTabBarStyle and IndicatorStyle within a Tabs container. It covers setting visual attributes like color, height, width, border radius, and spacing for the indicator. It also shows lifecycle events like onWillShow and onWillHide. The primary takeaway is the syntax for customizing the tab indicator via IndicatorStyle parameters. The example also highlights the use of BarMode.Scrollable and dynamic content rendering. This is a technical tutorial aimed at developers building UI applications, likely on the HarmonyOS platform. Prerequisites include familiarity with ArkTS syntax and basic UI component concepts.