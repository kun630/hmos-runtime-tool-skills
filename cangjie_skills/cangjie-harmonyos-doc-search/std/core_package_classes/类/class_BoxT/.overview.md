# class_BoxT

This directory contains the API documentation for the generic `Box` class in the Cangjie programming language. It covers the core class definition that wraps values to provide class-based reference semantics for non-reference types like structs. Additionally, it details interface extensions for `Hashable`, `ToString`, and `Comparable`, enabling Box instances to support hashing, string representation, and ordered comparisons. This resource is suitable for Cangjie developers, library maintainers, and software engineers who need to manage object lifetimes, enable reference behavior for value types, or utilize Box objects in collections and comparisons.

## Quick Navigation

**What do you want to learn?**

*   Understand the core definition, constructor, and value property of the Box class → class_BoxT_3more.md
*   Learn how to use Box with hash-based collections or string formatting → extendT_BoxT_Hashable_wher_2more.md
*   Implement or understand comparison logic and operator overloads for Box instances → `extendT_BoxT_ComparableBoxT_where_T_ComparableT/`

## Detailed Description

### class_BoxT_3more.md class_BoxT_3more.md
This API documentation file defines the generic class Box in the Cangjie language, which wraps values to provide class-based reference semantics. The document covers the class definition, the public property value for accessing the wrapped data, and the constructor init for creating instances. The key takeaway is that Box enables non-reference types like structs to be treated as references by adding a class layer. It exposes a mutable property value of type T and requires an initial value v for instantiation. This reference guide targets Cangjie developers needing to manage object lifetimes or enable reference behavior for value types. Keywords: Cangjie, Box, generic class, reference semantics, wrapper, API documentation.

### extendT_BoxT_Hashable_wher_2more.md extendT_BoxT_Hashable_wher_2more.md
This API reference document details interface extensions for the generic Box class in the Cangjie programming language. It covers two specific conditional extensions: implementing the Hashable interface and the ToString interface. Both extensions are constrained by generic type T, requiring T to also implement the respective interface. Key information includes the hashCode() method, which returns the hash of the encapsulated value, and the toString() method, which returns the string representation of the encapsulated value. This documentation is intended for Cangjie developers looking to utilize Box objects in hash-based collections or string formatting contexts.

### extendT_BoxT_ComparableBoxT_where_T_ComparableT/
This directory serves as a comprehensive technical resource for the Cangjie language, detailing the specific implementation of comparison features for the generic `Box` class. It addresses the extension of `Box` to conform to the `Comparable` interface, a critical feature for enabling ordered comparisons on generic containers. The content explains the constraints requiring the inner type `T` to be comparable and how the logic is delegated to the encapsulated value. Furthermore, it documents the specific operator overloads—such as equality, greater than, and less than—that allow developers to treat `Box` instances like primitive types during comparisons. This documentation is vital for Cangjie developers, library maintainers, and software engineers who are building generic systems or working with the standard library's type system, ensuring they understand how to implement and utilize comparison logic effectively. The files provided offer both high-level implementation details and specific API references for the operator functions involved.