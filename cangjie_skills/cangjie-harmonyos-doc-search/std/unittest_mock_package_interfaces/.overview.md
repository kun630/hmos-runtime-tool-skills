# unittest_mock_package_interfaces

This directory provides essential API reference documentation for the `unittest_mock_package_interfaces`, specifically focusing on the `ValueListener` interface within the Cangjie programming language's testing ecosystem. It is designed to assist developers in implementing robust unit tests by providing tools to monitor, capture, and validate arguments passed to mocked function stubs. The documentation covers core concepts such as argument capturing, callback execution, and parameter verification using matchers like `argThat` or `capture`. This resource is ideal for developers and QA engineers working in Cangjie who require precise control over mock object inputs to ensure code reliability. Users should have a foundational understanding of mocking frameworks and Cangjie syntax to effectively utilize these interfaces.

## Quick Navigation

What do you want to learn?

*   Understand the core definition and functional purpose of the ValueListener interface → unittest_mock_package_interfaces.md
*   Learn how to capture and validate arguments passed to mocked function stubs → unittest_mock_package_interfaces.md
*   Explore specific member functions such as `lastValue`, `allValues`, `addCallback`, `new`, and `onEach` → unittest_mock_package_interfaces.md
*   Review practical code examples demonstrating integration with parameter matchers like `capture` → unittest_mock_package_interfaces.md
*   Discover how to execute callbacks on every argument or retrieve processed values → unittest_mock_package_interfaces.md

## Detailed Description

The following file provides comprehensive documentation for the interfaces available in this package, offering deep insights into their usage and implementation details.

### unittest_mock_package_interfaces.md unittest_mock_package_interfaces.md

This document serves as the primary API reference documentation for the `ValueListener` interface within the unittest mock package. It details how to monitor and validate arguments passed to mocked function stubs in the Cangjie programming language. The documentation covers the interface definition, its functional purpose in argument capturing, and specific member functions including `lastValue`, `allValues`, `addCallback`, `new`, and `onEach`. A practical code example demonstrates integration with parameter matchers like `capture`. Key takeaways include the ability to create listeners that execute callbacks on every argument, retrieve all processed values, or fetch the last value. The interface is primarily used alongside `argThat` or `capture` to define stub behaviors and verify input parameters during testing. Targeted at developers writing unit tests in Cangjie who need to inspect or assert arguments passed to mock objects. Prerequisites include familiarity with mocking frameworks and the Cangjie language syntax.