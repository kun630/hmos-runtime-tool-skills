## enum KeyOfMetaData

```cangjie
public enum KeyOfMetaData <: Equatable<KeyOfMetaData> {
    | META_KEY_ASSET_ID
    | META_KEY_TITLE
    | META_KEY_ARTIST
    | META_KEY_AUTHOR
    | META_KEY_AVQUEUE_NAME
    | META_KEY_AVQUEUE_ID
    | META_KEY_AVQUEUE_IMAGE
    | META_KEY_AVQUEUE_IMAGE_URI
    | META_KEY_ALBUM
    | META_KEY_WRITER
    | META_KEY_COMPOSER
    | META_KEY_DURATION
    | META_KEY_MEDIA_IMAGE
    | META_KEY_MEDIA_IMAGE_URI
    | META_KEY_PUBLISH_DATE
    | META_KEY_SUBTITLE
    | META_KEY_DESCRIPTION
    | META_KEY_LYRIC
    | META_KEY_PREVIOUS_ASSET_ID
    | META_KEY_NEXT_ASSET_ID
    | META_KEY_SKIP_INTERVALS
    | META_KEY_FILTER
    | META_KEY_DISPLAY_TAGS
    | META_KEY_DRM_SCHEMES
    | ...
}
```

**功能：** 媒体元数据的相关属性。

**系统能力：** SystemCapability.Multimedia.AVSession.Core

**起始版本：** 19

**父类型：**

- Equatable\<[KeyOfMetaData](#enum-keyofmetadata)>

### META_KEY_ALBUM

```cangjie
META_KEY_ALBUM
```

**功能：** 专辑名称。

**起始版本：** 19

### META_KEY_ARTIST

```cangjie
META_KEY_ARTIST
```

**功能：** 艺术家。

**起始版本：** 19

### META_KEY_ASSET_ID

```cangjie
META_KEY_ASSET_ID
```

**功能：** 媒体ID。

**起始版本：** 19

### META_KEY_AUTHOR

```cangjie
META_KEY_AUTHOR
```

**功能：** 专辑作者。

**起始版本：** 19

### META_KEY_AVQUEUE_ID

```cangjie
META_KEY_AVQUEUE_ID
```

**功能：** 歌单（歌曲列表）唯一标识Id。

**起始版本：** 19

### META_KEY_AVQUEUE_IMAGE

```cangjie
META_KEY_AVQUEUE_IMAGE
```

**功能：** 歌单（歌曲列表）封面图的像素数据。

**起始版本：** 19

### META_KEY_AVQUEUE_IMAGE_URI

```cangjie
META_KEY_AVQUEUE_IMAGE_URI
```

**功能：** 歌单（歌曲列表）封面图的路径地址。

**起始版本：** 19

### META_KEY_AVQUEUE_NAME

```cangjie
META_KEY_AVQUEUE_NAME
```

**功能：** 歌单（歌曲列表）名称。

**起始版本：** 19

### META_KEY_COMPOSER

```cangjie
META_KEY_COMPOSER
```

**功能：** 作曲者。

**起始版本：** 19

### META_KEY_DESCRIPTION

```cangjie
META_KEY_DESCRIPTION
```

**功能：** 媒体描述。

**起始版本：** 19

### META_KEY_DISPLAY_TAGS

```cangjie
META_KEY_DISPLAY_TAGS
```

**功能：** 媒体资源的金标类型。

**起始版本：** 19

### META_KEY_DRM_SCHEMES

```cangjie
META_KEY_DRM_SCHEMES
```

**功能：** 当前session支持的DRM方案。

**起始版本：** 19

### META_KEY_DURATION

```cangjie
META_KEY_DURATION
```

**功能：** 媒体时长。

**起始版本：** 19

### META_KEY_FILTER

```cangjie
META_KEY_FILTER
```

**功能：** 当前session支持的协议。

**起始版本：** 19

### META_KEY_LYRIC

```cangjie
META_KEY_LYRIC
```

**功能：** 媒体歌词内容。

**起始版本：** 19

### META_KEY_MEDIA_IMAGE

```cangjie
META_KEY_MEDIA_IMAGE
```

**功能：** 图片的像素数据。

**起始版本：** 19

### META_KEY_MEDIA_IMAGE_URI

```cangjie
META_KEY_MEDIA_IMAGE_URI
```

**功能：** 图片的路径地址。

**起始版本：** 19

### META_KEY_NEXT_ASSET_ID

```cangjie
META_KEY_NEXT_ASSET_ID
```

**功能：** 下一首媒体ID。

**起始版本：** 19

### META_KEY_PREVIOUS_ASSET_ID

```cangjie
META_KEY_PREVIOUS_ASSET_ID
```

**功能：** 上一首媒体ID。

**起始版本：** 19

### META_KEY_PUBLISH_DATE

```cangjie
META_KEY_PUBLISH_DATE
```

**功能：** 发行日期。

**起始版本：** 19

### META_KEY_SKIP_INTERVALS

```cangjie
META_KEY_SKIP_INTERVALS
```

**功能：** 快进快退支持的时间间隔。

**起始版本：** 19

### META_KEY_SUBTITLE

```cangjie
META_KEY_SUBTITLE
```

**功能：** 子标题。

**起始版本：** 19

### META_KEY_TITLE

```cangjie
META_KEY_TITLE
```

**功能：** 标题。

**起始版本：** 19

### META_KEY_WRITER

```cangjie
META_KEY_WRITER
```

**功能：** 词作者。

**起始版本：** 19