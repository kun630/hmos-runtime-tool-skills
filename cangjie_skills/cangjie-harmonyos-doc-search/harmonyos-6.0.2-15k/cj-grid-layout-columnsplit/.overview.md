# cj-grid-layout-columnsplit

This directory contains the API reference documentation for the `ColumnSplit` component within the ArkUI framework. It focuses on the technical implementation of arranging child components vertically using horizontal dividers. The documentation covers essential aspects such as component initialization, constraints applied to child components, support for universal attributes, and specific properties like `divider` and `resizeable`. It clarifies the behavior of divider positions, which are calculated initially and remain static unless manually dragged, while also noting limitations regarding dynamic resizing. This resource is suitable for developers using the Cangjie language to build user interfaces, providing the necessary syntax definitions and code examples to create flexible, resizable layouts.

## Quick Navigation

**What do you want to learn?**

*   Understand the core functionality of the ColumnSplit component → cj-grid-layout-columnsplit.md
*   Learn how to arrange child components vertically with dividers → cj-grid-layout-columnsplit.md
*   Explore properties like `divider` and `resizeable` → cj-grid-layout-columnsplit.md
*   Review syntax definitions and Cangjie code examples → cj-grid-layout-columnsplit.md
*   Understand constraints on dynamic resizing and initialization → cj-grid-layout-columnsplit.md

## Detailed Description

### cj-grid-layout-columnsplit.md cj-grid-layout-columnsplit.md

This document serves as a comprehensive API reference for the ColumnSplit component within the ArkUI framework. Its primary focus is detailing how developers can arrange child components vertically with the inclusion of horizontal dividers. The text covers the full lifecycle of component usage, including initialization, constraints applied to child components, and the support for universal attributes.

A significant portion of the document is dedicated to specific properties such as `divider` and `resizeable`. It provides key technical insights, explaining that divider positions are calculated initially and remain static unless dragged by the user, while also outlining specific limitations on dynamic resizing capabilities. To facilitate practical application, the document provides syntax definitions and a Cangjie code example that demonstrates how to implement a resizable layout. This guide is intended for developers building UI layouts in Cangjie and requires prior knowledge of basic ArkUI components and layout principles.