# 类

This directory contains API reference documentation for synchronization and concurrency classes in the Cangjie programming language. It covers high-level coordination primitives like Mutexes, Barriers, Semaphores, and Timers, as well as low-level atomic operations for various integer types and references. The documentation includes both active classes and deprecated legacy components, guiding developers on thread-safe programming, resource management, and lock-free algorithms. It is suitable for software developers and systems programmers building multi-threaded applications who need to manage shared state, coordinate execution, or implement efficient synchronization mechanisms.

## Quick Navigation

**What do you want to learn?**

*   Coordinate threads using locks or barriers → class_Barrier.md Barrier, class_Mutex.md Mutex, class_ReadWriteLock.md ReadWriteLock, class_Semaphore.md Semaphore, class_SyncCounter.md Timer
*   Perform low-level atomic operations → `class_AtomicBool/`, `class_AtomicInt8/`, `class_AtomicInt16/`, `class_AtomicInt32/`, `class_AtomicInt64/`, `class_AtomicUInt8/`, `class_AtomicUInt16/`, `class_AtomicUInt32/`, `class_AtomicUInt64/`, `class_AtomicReferenceT_where_T_Object/`, `class_AtomicOptionReferenceT_where_T_Object/`
*   Review deprecated synchronization classes → class_Monitor_supdeprecatedsup.md Monitor, class_ReentrantMutex_supde_2more.md ReentrantMutex, class_ReentrantReadWriteMutex_supdeprecatedsup.md ReentrantReadWriteMutex, class_ReentrantWriteMut_babfcb58.md ReentrantWriteMutex, `class_MultiConditionMonitor_supdeprecatedsup/`
*   Manage countdown operations → class_SyncCounter.md SyncCounter

## Detailed Description

### class_Barrier.md class_Barrier.md
This document provides API reference documentation for the Barrier class in the Cangjie programming language, which facilitates thread synchronization by coordinating multiple threads to reach a common execution point. The documentation details the class constructor `init(Int64)`, which sets the required number of threads and validates against negative inputs, and the `wait(Duration)` method, which handles the blocking and waking logic based on thread arrival counts. Key takeaways include the exception handling for invalid counts and the timeout mechanism for the wait function. Intended for software developers working on concurrent applications, this guide explains how to use barriers to pause threads until all participants are ready, ensuring synchronized execution.

### class_Monitor_supdeprecatedsup.md class_Monitor_supdeprecatedsup.md
This API reference documents the deprecated Monitor class in the Cangjie language, designed for thread synchronization using shared variables to block and resume execution. The content covers the class definition, its inheritance from ReentrantMutex, and core methods: the constructor, notify, notifyAll, and wait. Key information includes the specific behavior of the wait function, which releases the mutex during suspension and reacquires it upon waking, returning a boolean status. The document explicitly warns that this class is deprecated and advises migrating to the Condition interface. It targets developers managing concurrency who need to understand legacy synchronization logic or handle exceptions related to mutex ownership.

### class_Mutex.md class_Mutex.md
This document serves as API reference documentation for the Cangjie Mutex class, detailing the implementation of reentrant mutex locks for thread synchronization. It covers the class definition, inheritance from UniqueLock, and methods including initialization, locking mechanisms, and condition variable creation. Key information includes that the Mutex ensures only one thread accesses a critical section at a time, supporting reentrancy where a thread can reacquire the lock. It requires explicit unlocking matching the number of locks and handles system errors via IllegalSynchronizationStateException. This guide targets Cangjie developers managing concurrent operations who need to protect shared data safely using synchronization primitives.

### class_ReadWriteLock.md class_ReadWriteLock.md
This document provides API reference for the Cangjie ReadWriteLock class, a synchronization tool enabling concurrent reads or exclusive writes. It details the class constructor, properties for accessing read and write locks, and the fairness check method. Key concepts explained include write mutual exclusion, read concurrency, reentrancy, lock downgrading, and the distinction between fair and non-fair modes. The documentation highlights that while multiple threads can hold read locks, only one can hold a write lock, and specific rules govern lock acquisition order. Intended for Cangjie developers, this guide assists in implementing efficient thread-safe concurrency control in multi-threaded applications.

### class_ReentrantMutex_supde_2more.md class_ReentrantMutex_supde_2more.md
This API reference document outlines the deprecated ReentrantMutex and ReentrantReadMutex classes used for thread synchronization in the Cangjie language. It details the constructors and methods for locking, attempting to lock, and unlocking, emphasizing reentrant capabilities where a thread can acquire a lock multiple times. Key information includes the deprecation status advising migration to Mutex or Lock, exception handling for illegal states, and specific behaviors for read locks regarding fair and non-fair modes. The document targets developers requiring precise control over critical sections and shared data access, providing the necessary interface definitions and functional descriptions for these synchronization primitives.

### class_ReentrantReadWriteMutex_supdeprecatedsup.md class_ReentrantReadWriteMutex_supdeprecatedsup.md
This API reference document details the deprecated Cangjie class ReentrantReadWriteMutex, which provides reentrant read-write locking functionality for concurrent programming. It covers the class definition, properties for accessing read and write locks, and the constructor for specifying fairness modes. The text explains core concepts like write exclusivity, read concurrency, reentrancy rules, lock downgrading, and fairness strategies. Crucially, it warns that this class is deprecated and recommends migrating to ReadWriteLock. This guide targets Cangjie developers managing thread synchronization who need to understand legacy concurrency primitives.

### class_ReentrantWriteMut_babfcb58.md class_ReentrantWriteMut_babfcb58.md
This document provides API reference documentation for the deprecated ReentrantWriteMutex class in the Cangjie language, designed for thread synchronization via reentrant write locks. It details the class inheritance from ReentrantMutex and outlines three core methods: lock, tryLock, and unlock. Key information includes the exclusive nature of the write lock, reentrancy support, and specific wake-up logic for fair and non-fair modes upon unlocking. The documentation highlights exceptions like IllegalSynchronizationStateException and strongly advises users to migrate to the UniqueLock interface. Intended for developers managing concurrent access to shared resources, this reference requires an understanding of multithreading concepts and lock mechanisms.

### class_Semaphore.md class_Semaphore.md
This document serves as API reference documentation for the Cangjie language's Semaphore class, detailing its use in controlling concurrent access to shared resources via an internal counter. Main sections include the class constructor, the count property, and core methods: acquire, release, and tryAcquire. Key information highlights that acquire blocks threads until permits are available, while release increments the counter up to the initial value and wakes waiting threads. The tryAcquire method provides a non-blocking alternative. All methods enforce parameter validation to prevent negative values or amounts exceeding the initial count. Intended for Cangjie developers, this reference assists in implementing thread synchronization and managing resource limits in multi-threaded applications.

### class_SyncCounter.md class_SyncCounter.md
This document serves as API reference documentation for the SyncCounter class in the Cangjie programming language, detailing a synchronization mechanism for managing countdown operations across multiple threads. The content covers the class constructor, the count property for retrieving the current value, the dec method for decrementing the counter, and the waitUntilZero method for blocking threads until the counter reaches zero. Key information includes that the constructor throws an IllegalArgumentException for negative initial values, and the dec method wakes all waiting threads when the count hits zero. This reference is intended for developers implementing concurrent programming patterns who need to coordinate thread execution based on a shared counter state.

### class_AtomicBool/
This directory contains essential API reference documentation for the `AtomicBool` class in the Cangjie programming language, focusing on atomic operations for boolean values. It is a critical resource for developers building concurrent or multithreaded applications, providing the necessary low-level synchronization primitives to ensure thread safety. The documentation thoroughly covers the class constructor, Compare-And-Swap (CAS) operations, and atomic load/store functions. It also clarifies the distinction between current standard methods that use default memory ordering and deprecated versions that require explicit memory order parameters. This guide is specifically suitable for software engineers and systems programmers who need to manage shared boolean states safely and efficiently in a multi-threaded environment.

### class_AtomicInt16/
This directory contains API reference documentation for the `AtomicInt16` class in the Cangjie programming language, focusing on thread-safe operations for 16-bit integers. It covers essential atomic mechanisms such as compare-and-swap, arithmetic operations (fetchAdd, fetchSub), bitwise manipulations (fetchAnd, fetchOr, fetchXor), and swap/load/store functions. The documentation highlights the deprecation of variants requiring explicit `MemoryOrder` parameters, guiding developers toward default memory ordering implementations. This resource is suitable for developers and systems engineers working on concurrent applications, low-level synchronization, and lock-free data structures requiring precise atomic state management.

### class_AtomicInt32/
This directory contains API reference documentation for the `AtomicInt32` class in the Cangjie programming language, designed to facilitate thread-safe operations on 32-bit integers. It covers essential concurrency primitives such as atomic arithmetic, bitwise manipulations, compare-and-swap (CAS), and memory loading/storing. A significant focus is placed on the distinction between current implementations using default memory ordering and deprecated versions that required explicit memory order arguments. This resource is suitable for developers working on concurrent systems who require low-level synchronization primitives and atomic data types to ensure data integrity in multi-threaded environments.

### class_AtomicInt64/
This directory contains API reference documentation for the `AtomicInt64` class in the Cangjie programming language, focusing on thread-safe operations for 64-bit integers. It covers essential concurrency mechanisms such as compare-and-swap, atomic arithmetic (add, subtract), bitwise operations, and value swapping. The documentation highlights the distinction between standard implementations using default memory ordering and deprecated versions that allowed explicit memory order specifications. This resource is suitable for systems programmers and experienced developers implementing synchronization and concurrent algorithms in multi-threaded applications.

### class_AtomicInt8/
This directory contains API reference documentation for the `AtomicInt8` class in the Cangjie programming language, focusing on thread-safe operations for 8-bit integers. It covers essential atomic functionalities such as constructors, compare-and-swap (CAS) mechanisms, arithmetic operations like addition and subtraction, bitwise manipulations, and swap operations. The documentation highlights the distinction between current implementations using default memory ordering and deprecated versions requiring explicit memory order parameters. This resource is suitable for developers implementing concurrent systems who require precise control over low-level integer synchronization and memory consistency.

### class_AtomicOptionReferenceT_where_T_Object/
This directory contains API reference documentation for the `AtomicOptionReference` class within the Cangjie programming language. It focuses on facilitating atomic operations on optional reference types, enabling developers to manage shared state safely without relying on explicit locks. The documentation covers class constructors, atomic reading, conditional updates, and memory synchronization functions. Key topics include atomic operations, memory ordering, concurrency, and the `Option` type. This resource is suitable for systems programmers and developers working on concurrent applications who need to implement thread-safe value exchanges and understand the nuances of deprecated memory order specifications.

### class_AtomicReferenceT_where_T_Object/
This directory contains comprehensive API reference documentation for the `AtomicReference` class within the Cangjie programming language, specifically focusing on instances where the generic type `T` is constrained to be a subclass of `Object`. It provides essential details for performing thread-safe, lock-free atomic operations such as reading, writing, swapping, and compare-and-swap (CAS). The content differentiates between modern implementations using default memory ordering and legacy overloads involving explicit `MemoryOrder` parameters. This resource is designed for systems programmers and developers building high-performance concurrent data structures who require precise control over reference updates in multi-threaded environments. By understanding these atomic primitives, developers can ensure data consistency and avoid race conditions in complex applications.

### class_AtomicUInt16/
This directory contains API reference documentation for the `AtomicUInt16` class in the Cangjie programming language. It focuses on thread-safe atomic operations for the `UInt16` data type, essential for concurrent programming. Key topics include atomic arithmetic (add, subtract), bitwise operations (AND, OR, XOR), memory synchronization (load, store, swap), and compare-and-swap (CAS) mechanisms. The documentation highlights deprecated functions involving explicit memory ordering versus default ordering versions. It is suitable for systems programmers and developers building multi-threaded applications requiring low-level synchronization primitives to manage shared data safely.

### class_AtomicUInt32/
This directory contains API reference documentation for the `AtomicUInt32` class in the Cangjie programming language. It focuses on thread-safe operations for unsigned 32-bit integers, covering arithmetic, bitwise, and memory manipulation functions. Key topics include atomic addition, subtraction, swapping, and memory ordering semantics, with specific notes on deprecated methods requiring explicit memory order parameters. This documentation is suitable for developers and systems programmers implementing concurrent logic, synchronization mechanisms, or low-level data structures where race conditions must be avoided.

### class_AtomicUInt64/
This directory contains API reference documentation for the `AtomicUInt64` class in the Cangjie programming language. It covers atomic operations for 64-bit unsigned integers, including arithmetic, bitwise, and memory access functions. Key topics include thread-safe synchronization, compare-and-swap (CAS), fetch-and-modify operations, and memory ordering deprecations. The documentation is intended for systems programmers and developers implementing low-level concurrency primitives who require efficient, thread-safe manipulation of shared data.

### class_AtomicUInt8/
This directory contains API reference documentation for the `AtomicUInt8` class in the Cangjie programming language. It focuses on atomic operations for unsigned 8-bit integers, essential for thread-safe concurrent programming. Key topics include constructors, compare-and-swap mechanisms, arithmetic operations (add, subtract), bitwise operations (AND, OR, XOR), and memory management (load, store, swap). The documentation highlights deprecated methods involving explicit memory ordering versus current default implementations. It is suitable for developers implementing low-level concurrency, synchronization mechanisms, and precise atomic state control.

### class_MultiConditionMonitor_supdeprecatedsup/
This directory contains technical API reference documentation for the deprecated `MultiConditionMonitor` class and related synchronization utilities in the Cangjie programming language. It addresses advanced concurrency control patterns, specifically focusing on the architecture that allows multiple condition variables to be bound to a single reentrant mutex. The content covers the class definition, inheritance from ReentrantMutex, and critical methods such as constructors, condition creation, and notification mechanisms. Additionally, it details the `wait` function for suspending threads, including exception handling for illegal states. This documentation is intended for developers working on complex multi-threaded applications who require granular control over synchronization states, although it highlights the class's deprecated status and advises migration to the Mutex class.

### class_Timer/
This directory contains the API reference documentation for the Cangjie programming language's Timer class, located within the standard library's synchronization package. It covers the comprehensive functionality required to execute tasks at specific times or intervals, including one-shot and repeating schedules. Key topics include thread management, task lifecycle, exception handling, and catch-up strategies. The documentation details static initialization methods like `once`, `repeat`, and `after`, alongside instance control methods such as `cancel`. This resource is suitable for Cangjie developers implementing asynchronous logic, concurrent programming, and precise time-based operations in their applications.