# 形状裁剪

This directory focuses on shape clipping and masking functionalities within the ArkUI framework using the Cangjie language. It covers API references for geometric shapes (circles, ellipses, rectangles), path definitions, and dynamic masking effects like progress masks. It includes practical code examples for implementing visual effects such as rounded corners and image overlays. This documentation is suitable for UI developers and system architects working with HarmonyOS applications who need to manipulate component visibility and create custom graphical layouts.

## Quick Navigation

**What do you want to learn?**

*   Implement dynamic progress-based masking effects → func_maskProgressMask.md
*   Learn API references for clipping and masking functions → 形状裁剪_10more.md
*   View practical code examples for image clipping and masking → 示例代码.md
*   Understand fundamental shape definitions (Circle, Rect, Path) → [基础类型定义/]

## Detailed Description

### func_maskProgressMask.md func_maskProgressMask.md
This document serves as API reference documentation for the mask function, which applies a progress-based mask to UI components in the ArkUI framework. The content covers the function signature, system capabilities, version requirements, and a detailed parameter table explaining the ProgressMask input. Key takeaways include that the function accepts a required ProgressMask object to dynamically control mask properties like progress, maximum value, and color. It returns the component instance for chaining and is available from API version 19. This reference guide targets developers using the ArkUI framework who need to implement dynamic masking effects on UI components, requiring knowledge of Cangjie programming syntax and UI layout principles.

### 形状裁剪_10more.md 形状裁剪_10more.md
This document serves as an API reference for shape clipping and masking functionalities within the ArkUI framework using the Cangjie language. It details the `clip` and `mask` functions, specifically their overloads accepting `Bool`, `CircleShape`, `EllipseShape`, `RectShape`, and `PathShape` parameters. Key information includes that the `clip(Bool)` function controls whether child components outside the parent's bounds are rendered and respond to gestures, while shape-based overloads define specific geometric boundaries. The `mask` functions apply overlays to alter component visibility. This technical reference is intended for UI developers working with ArkUI system capabilities starting from version 12.

### 示例代码.md 示例代码.md
This document provides a Cangjie code example demonstrating image clipping and masking techniques within a UI component. The primary focus is on utilizing the clip and mask modifiers to alter the visual presentation of images. Key sections include applying rounded corners to a Row container, clipping an Image directly using CircleShape, and masking images with RectShape and CircleShape filled with specific colors. The code illustrates the practical implementation of these modifiers within a Column layout. This example serves as a reference for HarmonyOS developers using the Cangjie language to create custom UI shapes and effects.

### 基础类型定义/
This directory contains API reference documentation for fundamental shape classes in the Cangjie language within the ArkUI framework. It covers abstract base classes and specific geometric shapes like circles, ellipses, rectangles, and custom paths, used primarily for clipping, masking, and visual effects. Key concepts include ShapeAbstract, PathShape, ProgressMask, and various shape constructors. It is suitable for UI developers and system architects working with ArkUI version 12 who need to implement graphical manipulations and custom visual components in their applications.