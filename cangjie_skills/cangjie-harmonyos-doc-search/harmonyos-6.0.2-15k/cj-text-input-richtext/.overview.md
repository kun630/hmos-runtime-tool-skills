# cj-text-input-richtext

This directory serves as a specialized technical repository for HarmonyOS developers who are leveraging the Cangjie programming language to construct dynamic user interfaces using the ArkUI framework. The primary focus of this collection is the `RichText` component, a crucial UI element designed to interpret, parse, and visually render HTML strings within a mobile application environment. The directory provides an in-depth exploration of the component's architecture, covering everything from the fundamental API reference and the `init` constructor to more complex universal attributes and lifecycle event handling, such as `onStart` and `onComplete`. It is an essential resource for developers aiming to present formatted content, manage asynchronous loading states, and optimize layout distribution using weight-based strategies. Additionally, the documentation offers vital performance guidelines, explicitly warning about the memory-intensive nature of the component and advising against its use in heavy customization or repetitive list rendering scenarios, while pointing toward alternatives like Web or RichEditor for more demanding requirements.

## Quick Navigation

**What do you want to learn?**
*   Understand the RichText component API and initialization → See RichText/
*   Learn about lifecycle events like `onStart` and `onComplete` → See RichText/
*   Explore code examples for HTML/CSS rendering → See RichText/
*   Identify performance constraints and memory usage limits → See RichText/
*   Find alternatives for complex or heavy customization scenarios → See RichText/

## Detailed Description

### RichText/
This directory provides comprehensive documentation and practical code examples for the RichText component within the ArkUI framework, specifically tailored for developers using the Cangjie language. It covers the complete API reference for creating components that parse and display HTML strings, detailing the `init` constructor, universal attributes, and lifecycle events such as `onStart` and `onComplete`. The content also includes implementation examples for rendering HTML/CSS content and managing UI layouts in HarmonyOS applications. Key information highlights critical usage constraints, noting that the component is memory-intensive and unsuitable for heavy customization or repetitive list rendering, while suggesting alternatives like Web or RichEditor for complex scenarios. Included files offer both theoretical background and hands-on code samples. This resource is suitable for HarmonyOS developers seeking to integrate basic rich text display functionality, handle loading states, and manage complex UI layouts efficiently using weight-based distribution.