# interface_RadixConvertibleT

This directory contains API documentation for the Cangjie programming language's `RadixConvertible` interface and its implementations across standard integer types. It covers the core interface definition and extensions for signed (Int8, Int16, Int32, Int64) and unsigned (UInt8, UInt16, UInt32, UInt64) integers. The documentation details methods for parsing strings to integers and converting integers to strings across various numerical bases (radices). Key features include strict parsing with exceptions, safe parsing using the Option type, and string formatting. This resource is suitable for Cangjie developers who need to perform base conversions, handle numeric string parsing, or implement robust error handling for numeric data types.

## Quick Navigation

**What do you want to learn?**
*   Understand the core interface definition and rules → interface_RadixConvertible_4more.md
*   Implement conversions for **Signed Integers**:
    *   8-bit (Int8) → extend_Int8_RadixConvertibleInt8.md
    *   16-bit (Int16) → extend_Int16_RadixConvertibleInt16.md
    *   32-bit (Int32) → extend_Int32_RadixConvertibleInt32.md
    *   64-bit (Int64) → extend_Int64_RadixConvertibleInt64.md
*   Implement conversions for **Unsigned Integers**:
    *   8-bit (UInt8) → extend_UInt8_RadixConvertibleUInt8.md
    *   16-bit (UInt16) → extend_UInt16_RadixConvertibleUInt16.md
    *   32-bit (UInt32) → extend_UInt32_RadixConvertibleUInt32.md
    *   64-bit (UInt64) → extend_UInt64_RadixConvertibleUInt64.md

## Detailed Description

### extend_Int16_RadixConvertibleInt16.md extend_Int16_RadixConvertibleInt16.md
This document details the Cangjie extension implementing the RadixConvertible interface for the Int16 type. It covers the extension declaration and three primary functions: static `parse` for strict string-to-integer conversion (throwing exceptions), static `tryParse` for safe conversion returning an Option type, and instance `toString` for formatting Int16 values in a specified radix.

### extend_Int32_RadixConvertibleInt32.md extend_Int32_RadixConvertibleInt32.md
This reference describes the RadixConvertible implementation for Int32. It explains the static `parse` method for strict conversion, `tryParse` for safe error handling using Option<Int32>, and `toString` for integer formatting across different bases. It highlights exception handling for invalid inputs and the use of Option types for non-exception based failure handling.

### extend_Int64_RadixConvertibleInt64.md extend_Int64_RadixConvertibleInt64.md
This file covers the RadixConvertible extension for Int64. It details `parse` (throws IllegalArgumentException), `tryParse` (returns Option), and `toString` for converting Int64 values to strings based on a specified radix. It is intended for developers handling numerical parsing and formatting with the Cangjie type system.

### extend_Int8_RadixConvertibleInt8.md extend_Int8_RadixConvertibleInt8.md
This documentation outlines the RadixConvertible interface implementation for Int8. It includes `parse` (throws on overflow/invalid), `tryParse` (safe, Option type), and `toString` for formatting 8-bit signed integers in various bases. It targets developers requiring precise control over parsing small signed integers.

### extend_UInt16_RadixConvertibleUInt16.md extend_UInt16_RadixConvertibleUInt16.md
This guide details the RadixConvertible extension for UInt16. It covers `parse` (throws on invalid inputs like empty strings or overflow), `tryParse` (returns None on failure), and `toString` for converting 16-bit unsigned integers to strings. Both parsing methods accept a radix parameter to define the base.

### extend_UInt32_RadixConvertibleUInt32.md extend_UInt32_RadixConvertibleUInt32.md
This document describes the RadixConvertible implementation for UInt32. It outlines `parse` (throws IllegalArgumentException), `tryParse` (Option type), and `toString` for generating string representations. It includes parameter definitions, return types, and specific exception conditions such as overflow or invalid characters.

### extend_UInt64_RadixConvertibleUInt64.md extend_UInt64_RadixConvertibleUInt64.md
This file details the RadixConvertible extension for UInt64. It explains `parse` for strict conversion with exceptions, `tryParse` for safe handling using Option<UInt64>, and `toString` for string representation in different bases. All methods accept a radix parameter, targeting developers needing to parse large unsigned numeric strings.

### extend_UInt8_RadixConvertibleUInt8.md extend_UInt8_RadixConvertibleUInt8.md
This reference details the RadixConvertible implementation for UInt8. It covers `parse` (throws on invalid/overflow), `tryParse` (Option type), and `toString` for string-to-integer conversions for unsigned 8-bit integers. It is intended for developers handling base conversions for small unsigned numbers.

### interface_RadixConvertible_4more.md interface_RadixConvertible_4more.md
This document defines the RadixConvertible interface, standardizing string parsing and type conversion based on numerical radix. It specifies rules for radix ranges (2-36), sign handling, and the API for `parse`, `tryParse`, and `toString`. It distinguishes between exception-throwing and Option-based failure handling for types like Int64 and UInt64.