# enum_AVCastControllerEventType

This directory provides comprehensive API reference documentation for the `AVCastControllerEventType` enum within the Cangjie language environment. It is designed to assist developers in defining and handling events related to an AV cast controller. The content covers a wide range of asynchronous events, including playback state transitions, media item updates, playback control actions (such as next and previous), seek completions, valid command changes, error reporting, and DRM key requests. Key technical details include required system capabilities like `SystemCapability.Multimedia.AVSession.AVCast` and the specific data structures provided upon event triggering, such as `AVPlaybackState`. Additionally, the documentation covers utility methods for comparing these events and converting them to string representations. This resource is essential for developers working on multimedia casting applications who need to implement robust event handling, state management, and debugging capabilities within the AVSession framework.

## Quick Navigation

What do you want to learn?

*   View the definition of specific event types (playback, media updates, errors, DRM) → enum_AVCastControllerEven_11more.md
*   Understand operator overloading (equality, inequality) and string conversion methods → func_AVCastControllerEvent_3more.md

## Detailed Description

### enum_AVCastControllerEven_11more.md enum_AVCastControllerEven_11more.md
This document serves as the primary API reference for the `AVCastControllerEventType` enum in the Cangjie language. It defines the various events that an AV cast controller can trigger. The documentation details specific event types, such as changes in playback state, updates to media items, playback control triggers (next/previous), completion of seek operations, changes in valid commands, error occurrences, and DRM key requests. Each enum entry specifies the necessary system capability, typically `SystemCapability.Multimedia.AVSession.AVCast`, and describes the data payload provided when the event is triggered, such as `AVPlaybackState` objects or specific error codes. The enum is noted to inherit from `ToString` and `Equatable`. This reference is intended for developers building multimedia casting applications who need to handle asynchronous events and manage state changes effectively within the AVSession framework.

### func_AVCastControllerEvent_3more.md func_AVCastControllerEvent_3more.md
This document provides API reference documentation for the utility methods associated with the `AVCastControllerEventType` enum, specifically focusing on operator overloading and string conversion in the Cangjie language. It details the implementation of the inequality operator (`!=`), the equality operator (`==`), and the `toString()` method. Key information includes that the comparison operators return a `Bool` value indicating whether two instances are equal, while the `toString()` method returns a string representation in lower camel case format. All functions documented here require the system capability `SystemCapability.Multimedia.AVSession.Core`, starting from API version 19. This reference is intended for developers implementing multimedia casting features who need to compare event types for logic flow or convert them to strings for logging and debugging purposes.