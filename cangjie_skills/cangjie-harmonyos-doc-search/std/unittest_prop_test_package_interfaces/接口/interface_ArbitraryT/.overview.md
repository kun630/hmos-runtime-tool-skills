# interface_ArbitraryT

This directory contains API reference documentation for the `Arbitrary` interface within the Cangjie programming language's `unittest_prop_test` package. It details implementations for generating random values across various data types, including numeric primitives, strings, and generic collections like Arrays and HashMaps. Each document explains the static `arbitrary` function required to produce `Generator` instances from a `RandomSource`. This resource is essential for developers performing property-based testing who need to understand how specific types conform to the `Arbitrary` interface to automate random data generation in their unit tests.

## Quick Navigation

**What do you want to learn?**

*   Understand the core interface definition and basic primitives (Bool, Float16, Float32) → interface_ArbitraryT_5more.md
*   Generate random numeric values (Integers and Floats) → extend_Float64_ArbitraryFl_4more.md, extend_Int8_ArbitraryInt8_4more.md
*   Generate random unsigned integers and strings → extend_String_ArbitraryStr_4more.md, extend_UInt8_ArbitraryUInt_3more.md
*   Generate random generic collections (Array, ArrayList, Option) → extendT_ArrayT_ArbitraryAr_3more.md
*   Generate random hash-based collections (HashSet, HashMap) → extendT_HashSetT_Arbitrary_2more.md

## Detailed Description

### extendT_ArrayT_ArbitraryAr_3more.md extendT_ArrayT_ArbitraryAr_3more.md
This API reference document details the implementation of the Arbitrary interface for generic collection and optional types in the Cangjie programming language. It covers three specific extensions: Array, Option, and ArrayList, each requiring the generic type T to also implement the Arbitrary interface. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator to produce random instances for property-based testing. This documentation targets developers utilizing the unittest_prop_test package who need to generate random data structures for automated testing scenarios.

### extendT_HashSetT_Arbitrary_2more.md extendT_HashSetT_Arbitrary_2more.md
This document serves as API reference documentation for implementing the Arbitrary interface on HashSet and HashMap collections within the Cangjie language. It details two primary extensions that enable these collection types to generate random values for property-based testing, provided their generic types also implement Arbitrary. The key sections define the static arbitrary function for both HashSet and HashMap, which accepts a RandomSource parameter and returns a corresponding Generator. This documentation is intended for developers working with the unittest_prop_test framework who require automated random data generation for complex collection types in their unit tests.

### extend_Float64_ArbitraryFl_4more.md extend_Float64_ArbitraryFl_4more.md
This API documentation file details the implementation of the Arbitrary interface for four fundamental numeric types in the Cangjie language. The document covers extensions for Float64, Int16, Int32, and Int64, each defining a static arbitrary function. Key information includes the function signature taking a RandomSource parameter and returning a Generator specific to the type, enabling the creation of random value generators for property-based testing. This reference guide targets developers utilizing the unittest_prop_test package who need to generate random numeric data for testing purposes.

### extend_Int8_ArbitraryInt8_4more.md extend_Int8_ArbitraryInt8_4more.md
This API documentation file details the implementation of the Arbitrary interface for four specific data types in the Cangjie language. The document covers extensions for Int8, IntNative, Ordering, and Rune, explaining how each type conforms to the Arbitrary parent type. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator to produce random values for property-based testing. Intended for developers utilizing the unittest_prop_test package, this reference requires understanding of interfaces and random generation concepts.

### extend_String_ArbitraryStr_4more.md extend_String_ArbitraryStr_4more.md
This API reference document details the implementation of the Arbitrary interface for the String, UInt16, UInt32, and UInt64 types in the Cangjie programming language. The content is divided into four distinct sections, each extending a specific data type to satisfy the Arbitrary interface. Key information includes the static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator capable of producing random values for that specific type. This documentation targets developers working with property-based testing frameworks who need to generate random test data for these fundamental types.

### extend_UInt8_ArbitraryUInt_3more.md extend_UInt8_ArbitraryUInt_3more.md
This API documentation file details the implementation of the Arbitrary interface for the UInt8, UIntNative, and Unit data types within a property-based testing framework. The document is divided into three distinct sections, each extending a specific type to conform to the Arbitrary interface. Key information includes the definition of a static arbitrary function for each type, which accepts a RandomSource parameter and returns a Generator capable of producing random values for the respective type. This reference material is intended for developers utilizing property-based testing who need to generate random instances of these fundamental types for their test cases.

### interface_ArbitraryT_5more.md interface_ArbitraryT_5more.md
This API reference document defines the Arbitrary interface in the Cangjie programming language, designed for generating random values in property-based testing. The content details the interface's static arbitrary method and its implementations for the Bool, Float16, and Float32 types. Each extension allows these types to return a specific Generator when provided with a RandomSource. Key takeaways include the method signatures and the inheritance structure showing these primitives conform to the Arbitrary interface. This documentation is intended for developers using the unittest_prop_test package who require random data generation for unit testing.