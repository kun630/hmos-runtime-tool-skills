# extend_Int8_CheckedOpInt8

This directory contains API reference documentation for the `CheckedOp` interface implementation specifically for the `Int8` type within the Cangjie programming language. It focuses on safe integer arithmetic operations, providing mechanisms to handle overflow scenarios gracefully without throwing runtime errors. The content covers a comprehensive range of arithmetic methods, including addition, subtraction, multiplication, division, negation, and bit manipulation. All these operations utilize `Option` return types to ensure robustness. This documentation is highly suitable for Cangjie developers who need to implement error-safe mathematical computations and bit logic in their applications, effectively preventing unexpected behavior during edge cases such as arithmetic overflow or invalid shift amounts. By utilizing these checked operations, developers can write more predictable and stable code.

## Quick Navigation

**What do you want to learn?**
*   Understand the core implementation of the CheckedOp interface for Int8, including arithmetic and increment/decrement operations → extend_Int8_CheckedOpInt8_8more.md
*   Explore specific functions for bit manipulation (shifting) and subtraction, focusing on their error-handling return types → func_checkedShlUInt64_3more.md

**What do you want to do?**
*   Implement safe addition, multiplication, division, or negation that returns `None` on overflow → extend_Int8_CheckedOpInt8_8more.md
*   Perform safe bit shifts (left/right) or subtraction with explicit overflow checks and `Option` returns → func_checkedShlUInt64_3more.md

## Detailed Description

This section provides in-depth API references for the files contained within the directory, detailing the specific functions and safety mechanisms available for Int8 operations.

### extend_Int8_CheckedOpInt8_8more.md extend_Int8_CheckedOpInt8_8more.md
This API reference document details the implementation of the CheckedOp interface for the Int8 type in the Cangjie programming language. The content covers specific arithmetic methods such as checkedAdd, checkedDiv, checkedMul, and checkedNeg, alongside increment and decrement operations. Key information highlights that these functions return an Option type to safely handle overflow scenarios, returning None if an overflow occurs instead of throwing an error. This documentation is intended for Cangjie developers requiring safe integer arithmetic to prevent unexpected runtime behavior during mathematical computations.

### func_checkedShlUInt64_3more.md func_checkedShlUInt64_3more.md
This document provides API reference documentation for safe integer operations in the Cangjie programming language. It covers three specific functions: checkedShl, checkedShr, and checkedSub. Each function uses the Option return strategy to handle potential errors gracefully. Key takeaways include that checkedShl and checkedShr return None if the shift amount is greater than or equal to the operand's bit width, while checkedSub returns None if an overflow occurs. All functions return a ?Int8 type. This documentation is designed for Cangjie developers implementing error-safe arithmetic and bit manipulation logic, ensuring robust code by explicitly handling edge cases like overflow and invalid shifts.