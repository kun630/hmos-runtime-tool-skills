# class_QualifiedType

This directory provides API reference documentation for the `QualifiedType` class in the Cangjie programming language's Abstract Syntax Tree (AST). It details the representation of user-defined member types, such as `T.a`, covering class definitions, inheritance from `TypeNode`, and initialization methods. Key topics include mutable properties for base types, identifiers, dot tokens, and generic type arguments, including specific handling for angle brackets and comma separators. The documentation also addresses serialization, traversal via the Visitor pattern, and `ASTException` error handling. This resource is designed for compiler developers and language tool implementers who need to parse and manipulate qualified type nodes programmatically.

## Quick Navigation

What do you want to learn?

*   Understand the class structure, properties, and inheritance? → class_QualifiedType_9more.md
*   Learn about initialization, serialization, or traversal methods? → initTokens_3more.md

## Detailed Description

### class_QualifiedType_9more.md class_QualifiedType_9more.md
This document serves as API reference documentation for the Cangjie programming language's QualifiedType class, which represents user-defined member types like T.a within an Abstract Syntax Tree. The content covers the class definition, its inheritance from TypeNode, and two initialization methods. Key sections detail mutable properties for accessing the base type, identifier, dot token, and generic type arguments, including specific handling for angle brackets and comma separators. The documentation highlights that certain properties throw ASTException upon invalid token assignment. Intended for compiler developers and language tool implementers, this reference provides the structural details necessary to parse and manipulate qualified type nodes programmatically.

### initTokens_3more.md initTokens_3more.md
This document serves as API reference documentation for specific methods within an Abstract Syntax Tree (AST) package, likely for the Cangjie language. It details the constructor init for creating a QualifiedType object from Tokens, the toTokens method for serialization, and the traverse function for visiting child nodes using the Visitor pattern. Key information includes parameter definitions, return types, and potential ASTException errors during construction. The text also explains how to customize traversal behavior by overriding visit functions. This reference is intended for compiler developers or software engineers building tools that manipulate syntax trees, requiring knowledge of the AST structure and visitor patterns. Keywords: AST, Cangjie, QualifiedType, Tokens, Visitor pattern, API reference.