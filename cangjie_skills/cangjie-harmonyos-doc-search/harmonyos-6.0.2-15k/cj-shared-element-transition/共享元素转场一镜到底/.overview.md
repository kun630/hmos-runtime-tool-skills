# 共享元素转场一镜到底

This directory focuses on "Shared Element Transitions," also known as "one shot to the end" effects, which create seamless animations by matching element positions and sizes during interface switches. It covers core concepts, visual comparisons, and implementation strategies within the ArkUI framework, including direct component modification and the `geometryTransition` API. Suitable for UI developers and engineers working with HarmonyOS, Cangjie, or ArkTS who aim to optimize rendering performance and achieve high-quality, fluid user experiences in their applications.

## Quick Navigation

**What do you want to learn?**

- Understand the core concepts and comparison of implementation methods → 共享元素转场一镜到底.md
- Learn how to modify existing components directly for performance → [2]
- Master the `geometryTransition` API for complex modal transitions → [3]

## Detailed Description

### 共享元素转场一镜到底.md 共享元素转场一镜到底.md
This document defines shared element transitions, also known as "one shot to the end" effects, which create fluid animations by matching the position and size of elements during interface switching. It covers the core concept, visual examples comparing standard versus animated transitions, and a comparison of two implementation methods: direct container modification and using the system's geometryTransition API. Key takeaways include that direct modification suits simple scenarios without route jumps, while geometryTransition binds IDs within an animateTo closure to automatically handle size, position, and opacity adjustments for smoother results. This guide targets developers seeking to enhance UI fluidity, specifically those working on interface transitions and animation logic.

### [2] 不新建组件并直接变化原组件/
This directory serves as an essential resource for developers working on ArkUI and HarmonyOS, focusing on the sophisticated technique of modifying existing component instances to achieve fluid animations instead of creating new components. This approach is vital for optimizing rendering performance and ensuring smooth user experiences during complex layout changes. The guides delve into the integration of component transitions with attribute animations, the utilization of `animateTo` for state interpolation, and the application of `TransitionEffect` for managing opacity and visibility. Targeted at UI developers proficient in Cangjie or ArkTS, these documents provide practical, code-based examples for handling intricate scenarios such as expandable lists, dynamic image sizing, and container expansion. By following these strategies, developers can ensure their applications remain responsive and visually polished during every state transition.

### [3] 使用geometryTransition共享元素转场/
This directory provides a comprehensive guide to implementing shared element transitions using the `geometryTransition` interface within the ArkUI framework. It covers the core concepts of binding components with shared IDs to ensure smooth context inheritance during view changes. The content ranges from basic state-switching animations to complex implementations involving modal page transitions. Key topics include `geometryTransition`, shared element transitions, ArkUI, Cangjie, ArkTS, and UI animation. This resource is specifically designed for UI developers and engineers who aim to create polished, fluid user experiences and high-performance navigation animations in their applications.