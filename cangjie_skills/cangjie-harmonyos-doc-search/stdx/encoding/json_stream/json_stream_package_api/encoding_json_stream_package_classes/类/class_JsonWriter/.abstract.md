本目录包含仓颉语言中 `JsonWriter` 类的 API 参考文档，专注于基于流的 JSON 序列化，详细说明了如何构建 JSON 结构并将其写入输出流。核心内容包括类的初始化、配置、对象与数组的结构化方法以及具体的值写入函数。文档涵盖了缓冲机制、刷新操作以及 `JsonSerializable` 接口的使用等关键概念。该文档适合需要使用 `json.stream` 包进行高效、底层 JSON 编码和数据流操作的开发者阅读。