# class_AtomicReferenceT_where_T_Object

This directory contains comprehensive API reference documentation for the `AtomicReference` class within the Cangjie programming language, specifically focusing on instances where the generic type `T` is constrained to be a subclass of `Object`. It provides essential details for performing thread-safe, lock-free atomic operations such as reading, writing, swapping, and compare-and-swap (CAS). The content differentiates between modern implementations using default memory ordering and legacy overloads involving explicit `MemoryOrder` parameters. This resource is designed for systems programmers and developers building high-performance concurrent data structures who require precise control over reference updates in multi-threaded environments. By understanding these atomic primitives, developers can ensure data consistency and avoid race conditions in complex applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core `AtomicReference` class, including its constructor, generic type requirements, and the standard atomic methods available for thread-safe operations → class_AtomicReferenceT_whe_9more.md
*   Review the technical details of the deprecated swap function, including its signature, parameters, and migration advice for legacy codebases → func_swapT_MemoryOrder__14ecd038.md

## Detailed Description

The following section provides detailed descriptions of the files contained in this directory. These documents serve as the primary reference for understanding the implementation, usage, and deprecation status of atomic reference operations in Cangjie.

### class_AtomicReferenceT_whe_9more.md class_AtomicReferenceT_whe_9more.md

This document provides API reference documentation for the AtomicReference class in the Cangjie programming language, detailing atomic operations for reference types. It covers the class constructor and methods for atomic read, write, swap, and compare-and-swap (CAS) operations. Key information includes the requirement that the generic type T must be a subclass of Object. The documentation distinguishes between current methods using default memory ordering and deprecated overloads that accepted explicit MemoryOrder parameters. This reference is intended for developers implementing thread-safe code and concurrent data structures who need to perform lock-free atomic updates on object references.

### func_swapT_MemoryOrder__14ecd038.md func_swapT_MemoryOrder__14ecd038.md

This document serves as API reference for a deprecated atomic swap function within the Cangjie language. It details the function signature, functionality, parameters, and return values. The key information highlights that this operation writes a value to an atomic type using a specific memory order while returning the original value. Most importantly, the documentation explicitly marks this function as deprecated, advising developers to migrate to the standard swap(T) method. The intended audience includes Cangjie developers working with low-level concurrency and atomic operations who need to update legacy code or understand deprecated synchronization behaviors.