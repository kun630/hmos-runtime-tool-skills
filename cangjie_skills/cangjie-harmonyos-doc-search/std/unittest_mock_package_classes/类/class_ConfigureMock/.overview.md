# class_ConfigureMock

This directory provides comprehensive API reference documentation for the `ConfigureMock` class, a core component of a testing framework designed for the Cangjie programming language. It covers the essential utilities required to configure mock objects, enabling developers to simulate specific behaviors during unit testing. The documentation details static functions for intercepting property getters, stubbing member methods, and inserting stub code into property setters. Key technical elements include argument matchers, reference tokens, and specific action selectors like `GetterActionSelector` and `MethodActionSelector`. Furthermore, it explains the parameters required for these methods, such as stub call expressions and line numbers. This resource is specifically tailored for developers and QA engineers who need to implement precise mocking strategies to validate object interactions and dependencies within their test suites.

## Quick Navigation

**What do you want to do?**

*   Configure mock objects for property getters and member methods → class_ConfigureMock_3more.md
*   Insert stub code into property Setter methods → static_func_stubSetterT_fae19e1e.md

## Detailed Description

### class_ConfigureMock_3more.md class_ConfigureMock_3more.md
This document serves as API reference documentation for the ConfigureMock class, which is designed to configure mock objects within a testing framework. The content details two primary static functions: stubGetter for intercepting property getters and stubFunction for stubbing member methods. Key information includes the specific parameters required for these methods, such as the stub call expression, argument matchers, reference tokens, names, descriptions, and line numbers. Both functions return specific selector objects, GetterActionSelector and MethodActionSelector, to further define mock behavior. This reference is intended for developers implementing unit tests who need to simulate object behavior and interactions using the Cangjie language.

### static_func_stubSetterT_fae19e1e.md static_func_stubSetterT_fae19e1e.md
This document serves as API reference documentation for the static function stubSetter, which creates an operator object to insert stub code into property Setter methods. The main sections include the function signature, a functional description, detailed parameter definitions, and return value specifications. Key information highlights the use of generic type TArg for type capturing, an ArgumentMatcher for parameter validation, and specific inputs for handling references and line numbers. The function returns a SetterActionSelector to configure the stubbing behavior. Intended for developers working with testing or mocking frameworks, this document requires knowledge of the specific programming language's type system and stubbing concepts. It provides the necessary parameters to mock static, class, or top-level property setters effectively.