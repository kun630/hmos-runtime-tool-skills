# interface_IntegerT

This directory contains comprehensive API documentation for the `Integer` interface within the Cangjie programming language. It serves as a central reference for understanding the generic structure, inheritance hierarchy, and specific implementations of various integer types. The content covers the core interface definition, including essential methods, operator overloads for arithmetic and bitwise operations, and the static `isSigned` function. Furthermore, it details extensions for a wide range of numeric types, including signed types like Int8, Int16, Int32, Int64, and IntNative, as well as unsigned types such as UInt8, UInt16, UInt32, UInt64, and UIntNative. This documentation is primarily suitable for Cangjie developers who need to verify type properties, ensure interface compliance, and implement robust numeric operations in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core Integer interface definition, including its inheritance from the Number type and available operators → interface_IntegerT_11more.md
*   Verify the implementation and sign status of specific signed integer types (Int64, Int8, IntNative, UInt16, UInt32) → extend_Int64_IntegerInt64_5more.md
*   Verify the implementation and sign status of specific unsigned integer types (UInt64, UInt8, UIntNative) → extend_UInt64_IntegerUInt6_3more.md

## Detailed Description

### extend_Int64_IntegerInt64_5more.md extend_Int64_IntegerInt64_5more.md
This API reference document details the implementation of the generic Integer interface for specific Cangjie integer types. It covers extensions for Int64, Int8, IntNative, UInt16, and UInt32, explicitly defining their inheritance hierarchy. The key information provided is the static isSigned() function, which returns true for signed integer types and false for unsigned ones. This documentation serves Cangjie developers who need to verify type properties and ensure interface compliance for numeric operations, offering a quick lookup for type sign characteristics.

### extend_UInt64_IntegerUInt6_3more.md extend_UInt64_IntegerUInt6_3more.md
This API documentation file details the implementation of the generic Integer interface for unsigned integer types in the Cangjie language. The document covers three specific extensions: UInt64, UInt8, and UIntNative, each explicitly inheriting from the Integer interface. Key information includes the static function isSigned(), which consistently returns false across all three types, confirming their unsigned nature. This reference is intended for developers working with core numeric types who need to verify interface implementations and type properties. Keywords: Cangjie, Integer interface, UInt64, UInt8, UIntNative, isSigned, API documentation.

### interface_IntegerT_11more.md interface_IntegerT_11more.md
This API reference document defines the Integer interface in the Cangjie language, outlining methods and operators for integer types. The main sections cover the interface definition, a static isSigned function, and operator overloads for arithmetic (modulo) and bitwise operations (AND, OR, XOR, NOT, left/right shifts). Additionally, it documents extensions for Int16 and Int32 types, which implement this interface. Key information includes the inheritance from the Number parent type and the specific return values for signed checks. This documentation is intended for Cangjie developers implementing or utilizing integer functionality.