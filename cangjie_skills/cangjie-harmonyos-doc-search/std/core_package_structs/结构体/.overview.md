# 结构体

This directory contains API reference documentation for core data structures in the Cangjie programming language. It covers fundamental types such as Array, String, Duration, and Range, alongside specialized structures for C language interoperability and low-level memory management. Key topics include generic constraints, resource lifecycle management, hashing algorithms, and time-based calculations. This documentation is suitable for Cangjie developers seeking detailed technical specifications for implementing efficient data handling, performing system-level operations, or integrating with native C libraries.

## Quick Navigation

**What do you want to do?**

*   **Perform C interoperability or memory management?**
    *   Manage C pointers and resources safely → struct_CPointerResourceT_w_2more.md
    *   Handle raw C pointers and arrays → struct_CPointerHandleT_where_T_CType.md
    *   Use LibC functions (malloc/free) → struct_LibC.md
*   **Work with standard data types?**
    *   Manipulate arrays and sequences → `struct_ArrayT/`
    *   Handle text and strings → `struct_String/`
*   **Implement specific logic?**
    *   Calculate hash values → `struct_DefaultHasher/`
    *   Manage time intervals → `struct_Duration/`
    *   Define numeric ranges → `struct_RangeT_where_T_CountableT_Comparab_0ec58319/`

## Detailed Description

### struct_CPointerHandleT_where_T_CType.md struct_CPointerHandleT_where_T_CType.md
This API documentation defines the Cangjie struct CPointerHandle, which encapsulates an Array and its corresponding raw CPointer for interoperability. The document outlines the struct's properties, specifically the array instance and the raw pointer. It details two initialization methods, both marked as deprecated, advising developers to use the acquireArrayRawData function instead for constructing instances. Key information includes the constraint that the generic type T must implement CType. This reference material is intended for developers performing low-level memory operations or C language integration within the Cangjie ecosystem.

### struct_CPointerResourceT_w_2more.md struct_CPointerResourceT_w_2more.md
This API reference document details resource management structures for C interoperability in the Cangjie language. It covers the generic CPointerResource struct and the specific CStringResource struct, both implementing the Resource interface. Key information includes the value property for accessing underlying C types and the close and isClosed methods for managing memory lifecycle. These structures ensure safe handling of C pointers and strings by wrapping them in resource objects. The document targets developers requiring explicit memory management for foreign function interfaces, providing definitions for properties and return types like Bool and Unit.

### struct_LibC.md struct_LibC.md
This document serves as API reference documentation for the LibC struct in the Cangjie programming language, detailing high-frequency C interfaces for memory management. The main sections cover static functions for allocating and releasing heap memory, specifically malloc, mallocCString, and free. Key information includes the requirement for unsafe execution contexts, handling of CType and CString pointers, and specific exceptions like IllegalArgumentException for invalid counts or IllegalMemoryException for allocation failures. The document provides syntax definitions, parameter descriptions, and verified code examples demonstrating heap allocation and string conversion. This reference is intended for Cangjie developers performing low-level memory operations or C interoperability who need to manually manage resources.

### struct_ArrayT/
This directory contains comprehensive API reference documentation for the `Array` struct in the Cangjie programming language. It covers the core structure, including constructors, properties, and cloning mechanisms, alongside a wide range of manipulation functions such as concatenation, mapping, and slicing. The documentation details operator overloading for element access and range operations, as well as interface extensions for `ToString` and `Equatable` functionalities. Designed for Cangjie developers, this resource provides the necessary technical details for efficient generic array handling, data transformation, and standard library implementation.

### struct_DefaultHasher/
This directory contains comprehensive API reference documentation for the `DefaultHasher` struct within the Cangjie programming language. It focuses on the default implementation used for calculating combined hash values, specifically detailing initialization, result retrieval, and reset functionality. The documentation thoroughly covers various overloaded `write` functions designed to input different data types—such as unsigned integers (UInt8, UInt16, UInt32, UInt64), strings, and floating-point numbers—into hash combination operations. This resource is highly suitable for developers implementing custom hashing logic or utilizing hash-based collections who need to integrate specific data types into their hashing workflows efficiently and accurately.

### struct_Duration/
This directory contains API reference documentation for the `Duration` struct in the Cangjie programming language. It covers the representation and manipulation of time intervals with nanosecond precision. The documentation details arithmetic operators, comparison logic, unit conversion methods, and static constants for time units. It is suitable for Cangjie developers who need to perform precise time-based calculations, handle time spans, or format duration data in their applications. Key topics include operator overloading, exception handling for overflow, and interface implementations like `Comparable` and `Hashable`.

### struct_RangeT_where_T_CountableT_Comparab_0ec58319/
This directory contains essential API documentation for the generic `Range` struct in the Cangjie programming language. It details a sequence type characterized by fixed bounds and a configurable step size, requiring the generic type `T` to adhere to `Countable` and `Comparable` constraints. The content encompasses the struct's core definition, literal syntax, and iteration methods, alongside extensions that enable `Equatable` and `Hashable` functionality. This documentation is specifically designed for Cangjie developers seeking to implement sequential data ranges, perform precise equality comparisons, or integrate ranges into hash-based collections efficiently.

### struct_String/
This directory contains comprehensive API reference documentation for the `String` struct in the Cangjie programming language. It covers a wide range of string manipulation capabilities, including basic operations like comparison, searching, and splitting, as well as advanced features such as UTF-8 byte access, rune iteration, and operator overloading. The documentation details constructors, properties, static methods, and exception handling for string processing. It is designed for Cangjie developers seeking precise syntax, parameter definitions, and behavioral details for implementing efficient text handling and data manipulation in their applications.