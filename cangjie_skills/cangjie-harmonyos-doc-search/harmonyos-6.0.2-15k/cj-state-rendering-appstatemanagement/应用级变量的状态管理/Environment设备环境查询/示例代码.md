### 示例代码

#### 从UI中访问Environment参数

- 使用`Environment.envProp<T>`将设备运行的环境变量存入AppStorage中：

```cangjie
// 将设备的语言code存入AppStorage，默认值为en
let e = Environment.envProp<String>("languageCode", "en");
```

- 可以使用`@StorageProp`链接到Component中

```cangjie
@StorageProp["languageCode"]
let code: String = "en"
```

设备环境到Component的更新链：Environment --> AppStorage -->Component。

> 说明：
>
> @StorageProp关联的环境参数可以在本地更改，但不能同步回AppStorage中，因为应用对环境变量参数是不可写的，只能在Environment中查询。

<!-- run -->

```cangjie
package ohos_app_cangjie_entry

import kit.UIKit.*
import ohos.state_macro_manage.*

func parseColor(color: ColorMode): String {
    return match (color) {
        case ColorMode.Light => "Light"
        case _ => "Dark"
    }
}

func parseLayout(layout: LayoutDirection): String {
    return match (layout) {
        case LayoutDirection.Ltr => "Ltr"
        case _ => "Rtl"
    }
}

@Entry
@Component
class EntryView {
    let e = Environment.envProp<String>("languageCode", "en")
    let e1 = Environment.envProp<ColorMode>("colorMode", ColorMode.Light)
    let e2 = Environment.envProp<Bool>("accessibilityEnabled", false)
    let e3 = Environment.envProp<Float32>("fontScale", 1.0)
    let e4 = Environment.envProp<Float32>("fontWeightScale", 1.0)
    let e5 = Environment.envProp<LayoutDirection>("layoutDirection", LayoutDirection.Ltr)

    @StorageProp["languageCode"]
    let code: String = "en"
    @StorageProp["colorMode"]
    let color: ColorMode = ColorMode.Light
    @StorageProp["accessibilityEnabled"]
    let acc: Bool = false
    @StorageProp["fontScale"]
    let fontScale: Float32 = 1.0
    @StorageProp["fontWeightScale"]
    let fontWeightScale: Float32 = 1.0
    @StorageProp["layoutDirection"]
    let layout: LayoutDirection = LayoutDirection.Ltr

    func build() {
        Column {
            Text("languageCode ${code}")
            Text("colorMode ${parseColor(color)}")
            Text("accessibilityEnabled ${acc}")
            Text("fontScale ${fontScale}")
            Text("fontWeightScale ${fontWeightScale}")
            Text("layoutDirection ${parseLayout(layout)}")
        }
    }
}
```

![environment](figures/environment.png)