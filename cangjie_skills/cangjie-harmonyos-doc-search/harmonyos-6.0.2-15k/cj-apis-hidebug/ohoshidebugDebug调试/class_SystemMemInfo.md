## class SystemMemInfo

```cangjie
public class SystemMemInfo {
    public let availableMem: UInt64
    public let freeMem: UInt64
    public let totalMem: UInt64
}
```

**功能：** 描述系统内存信息。

**系统能力：** SystemCapability.HiviewDFX.HiProfiler.HiDebug

**起始版本：** 19

### let availableMem

```cangjie
public let availableMem: UInt64
```

**功能：** 系统可用的内存，以KB为单位。

**系统能力：** SystemCapability.HiviewDFX.HiProfiler.HiDebug

**类型：** UInt64

**读写能力：** 只读

**起始版本：** 19

### let freeMem

```cangjie
public let freeMem: UInt64
```

**功能：** 系统空闲的内存，以KB为单位。

**系统能力：** SystemCapability.HiviewDFX.HiProfiler.HiDebug

**类型：** UInt64

**读写能力：** 只读

**起始版本：** 19

### let totalMem

```cangjie
public let totalMem: UInt64
```

**功能：** 系统总的内存，以KB为单位。

**系统能力：** SystemCapability.HiviewDFX.HiProfiler.HiDebug

**类型：** UInt64

**读写能力：** 只读

**起始版本：** 19