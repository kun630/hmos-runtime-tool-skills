This directory serves as the definitive guide for interface definitions within the Cangjie language's logging infrastructure. It is primarily concerned with the `LogValue` interface, which establishes the essential protocols required for serializing a w...