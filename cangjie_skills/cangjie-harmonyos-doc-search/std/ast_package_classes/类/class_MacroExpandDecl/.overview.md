# class_MacroExpandDecl

This directory contains comprehensive API reference documentation for the `MacroExpandDecl` class and associated utility functions within the Cangjie programming language's Abstract Syntax Tree (AST) framework. It focuses on the representation and manipulation of macro call nodes, detailing how to manage syntax components such as identifiers, brackets, and input parameters. Additionally, it covers essential operations for converting syntax tree nodes into token sequences and traversing the AST structure using visitor patterns. Key topics include macro expansion handling, ASTException management, and custom traversal logic. This documentation is specifically designed for developers working on compiler tools, language parsers, or those performing low-level AST operations and transformations in the Cangjie ecosystem. It serves as a technical guide for understanding the internal structure of macro declarations and the mechanisms used to process and navigate the syntax tree effectively.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition, properties, and initialization for macro call nodes → class_MacroExpandDecl_11more.md
*   Explore utility functions for token conversion and tree traversal with visitor patterns → func_toTokens_2more.md

## Detailed Description

### class_MacroExpandDecl_11more.md
This document provides API reference documentation for the MacroExpandDecl class, which represents macro call nodes within an Abstract Syntax Tree (AST). It covers the class definition, properties for managing macro syntax components like identifiers, brackets, and inputs, and constructors for object initialization. Key information includes handling both function-like and attribute-like macros through specific token properties, with ASTException handling for invalid tokens or missing declarations. This technical reference is intended for developers working on compiler tools or AST manipulation in the Cangjie programming language.

### func_toTokens_2more.md
This document provides API reference documentation for functions within an Abstract Syntax Tree (AST) package in the Cangjie language. It covers two main functionalities: the toTokens function, which converts a syntax tree node into a Tokens type, and the traverse function, which iterates over the node and its children using a Visitor instance. Key information includes the return types and parameters for these functions, specifically how traverse supports early termination via breakTraverse for custom visit logic. This guide is intended for developers working with AST parsing and manipulation who require precise method signatures and behavioral descriptions for node conversion and tree traversal.