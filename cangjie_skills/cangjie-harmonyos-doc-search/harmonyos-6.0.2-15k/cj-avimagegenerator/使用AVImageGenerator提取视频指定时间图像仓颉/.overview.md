# 使用AVImageGenerator提取视频指定时间图像仓颉

This directory serves as a comprehensive technical resource for Cangjie developers operating within the HarmonyOS ecosystem, specifically targeting media processing and image manipulation tasks. It provides an in-depth look at extracting video thumbnails at precise timestamps using the `AVImageGenerator` class. The documentation covers the complete development workflow, including instance creation, source configuration via file descriptors, `PixelMap` retrieval, and resource management. It emphasizes best practices for file access using `ResourceManager` and sandbox paths. This material is highly suitable for developers seeking to integrate video snapshot functionality into their applications, offering both theoretical guidelines and practical code implementations leveraging the MediaKit and ImageKit frameworks.

## Quick Navigation

**What do you want to learn?**

*   Understand the theoretical workflow and API usage of `AVImageGenerator` → 使用AVImageGenerator提取视频指定时间_2more.md
*   Examine a full code implementation including UI setup and application lifecycle → 完整示例.md

## Detailed Description

### 使用AVImageGenerator提取视频指定时间_2more.md 使用AVImageGenerator提取视频指定时间_2more.md

This file serves as the primary instructional guide for Cangjie developers. It provides a structured approach to utilizing the `AVImageGenerator` class for the purpose of extracting video thumbnails at specific times. The document outlines a clear, four-step workflow: creating an instance, configuring the source via the `fdSrc` property, retrieving a `PixelMap` using `fetchFrameByTime`, and finally releasing resources. Additionally, it highlights critical technical considerations such as using `ResourceManager` or sandbox paths for secure file access and ensuring unique file descriptors for different media objects. This tutorial serves as a practical reference for developers implementing video snapshot functionality within the MediaKit framework.

### 完整示例.md 完整示例.md

This file serves as the practical implementation guide, providing a complete code example demonstrating how to extract a video thumbnail at a specific timestamp using the Cangjie language. The content is divided into two main files: `index.cj`, which handles the UI and core logic, and `main_ability.cj`, which manages the application lifecycle and context. Key takeaways include the usage of `AVImageGenerator` to set file descriptors, configure `PixelMapParams` for size, and invoke `fetchFrameByTime` to retrieve the image. This tutorial is intended for Cangjie developers building HarmonyOS applications who need to implement video thumbnail extraction features using MediaKit and ImageKit.