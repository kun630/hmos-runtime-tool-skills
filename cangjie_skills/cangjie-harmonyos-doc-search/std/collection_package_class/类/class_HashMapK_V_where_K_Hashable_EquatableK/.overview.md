# class_HashMapK_V_where_K_Hashable_EquatableK

This directory contains comprehensive API reference documentation for the `HashMap` class in the Cangjie standard library. It details the implementation of a hash table data structure where keys must be `Hashable` and `Equatable`. The documentation covers class definitions, initialization methods, core operations like insertion and deletion, and extensions for equality and string conversion. It is designed for Cangjie developers seeking efficient key-value storage solutions, providing technical insights into time complexity, operator overloading, and exception handling for generic types.

## Quick Navigation

**What do you want to learn?**
*   Understand the core class definition and basic properties → class_HashMapK_V_where_K_H_6more.md
*   Learn how to initialize a HashMap with specific capacity or functions → initInt64_5more.md

**What do you want to do?**
*   Compare two HashMaps for equality → extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md
*   Convert a HashMap to a string representation → extendK_V_HashMapK_V_To_d1bae6de.md
*   Access or modify elements using subscripts `[]` → operator_func_K_2more.md
*   Perform utility operations (clone, check existence, iterate) → func_clone_9more.md
*   Manage data (remove entries, reserve capacity, extract values) → func_removeK_5more.md

## Detailed Description

### class_HashMapK_V_where_K_H_6more.md class_HashMapK_V_where_K_H_6more.md
This document provides API reference documentation for the HashMap class in the Cangjie standard library, detailing its implementation as a hash table. It covers the class definition, inheritance from the Map interface, properties like capacity and size, and multiple initialization methods including default, array-based, and collection-based constructors. Key information includes the requirement for keys to be Hashable and Equatable, O(1) time complexity for operations, and the handling of duplicate keys where later entries overwrite earlier ones. This technical reference is intended for Cangjie developers seeking to implement efficient key-value storage.

### extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md extendK_V_HashMapK_V_EquatableHashMapK_V__deb7d8ef.md
This API reference document details the extension of the Cangjie HashMap type to implement the Equatable interface, enabling equality comparisons. The content covers the generic extension definition and two operator overloads: the inequality operator != and the equality operator ==. Key information includes the constraint that the value type V must be Equatable and the definition that two HashMaps are equal only if they contain identical key-value pairs. This documentation is intended for Cangjie developers who need to perform equality checks on HashMap instances within their applications.

### extendK_V_HashMapK_V_To_d1bae6de.md extendK_V_HashMapK_V_To_d1bae6de.md
This API documentation describes a Cangjie language extension that enables string conversion for HashMap instances. The document details the implementation of the ToString interface for the HashMap class, specifically requiring that both keys and values also support string conversion. Key sections include the extension declaration and the definition of the public toString() function. This function transforms the map instance into a formatted string representation, displaying key-value pairs as a list of tuples like "[(k1, v1), (k2, v2)]". The primary audience is Cangjie developers seeking to serialize or debug collection data by converting HashMap contents into readable strings.

### func_clone_9more.md func_clone_9more.md
This document serves as API reference documentation for various utility and manipulation functions within the Cangjie HashMap class. It details nine specific methods including clone, contains (overloaded for single keys and collections), entryView, get, isEmpty, iterator, keys, and remove. Each entry provides the function signature, a description of its functionality, input parameters, and return types, often linking to related interfaces like Option and Collection. This technical reference is intended for Cangjie developers seeking to understand and implement HashMap operations such as data retrieval, existence checks, and iteration.

### func_removeK_5more.md func_removeK_5more.md
This document serves as API reference documentation for specific manipulation and query functions within the Cangjie HashMap class. It details five key functions: remove, removeIf, reserve, toArray, and values. Key information includes that remove deletes a key and returns the value as an Option, while removeIf conditionally deletes entries but risks ConcurrentModificationException if the map is modified during iteration. The reserve function handles capacity expansion logic, whereas toArray and values facilitate data extraction into arrays or collections. This reference guide targets Cangjie developers who need to manage HashMap data structures, requiring knowledge of lambda expressions and exception handling.

### initInt64_5more.md initInt64_5more.md
This document provides API reference details for specific initialization and manipulation methods of the HashMap class in the Cangjie programming language. It covers two constructors, init(Int64) and init(Int64, (Int64) -> (K, V)), alongside methods add(Collection<(K, V)>), add(K, V), and clear(). Key information includes that constructors allow setting capacity or generating elements via a function, throwing exceptions for negative sizes. The add methods insert key-value pairs, overwriting existing keys and returning previous values via Option<V>, while clear removes all entries. This reference guide targets Cangjie developers implementing hash maps, requiring knowledge of generic types and exception handling.

### operator_func_K_2more.md operator_func_K_2more.md
This document provides API reference documentation for the subscript operator overloads within the Cangjie language's collection package. It covers two primary function signatures: a getter method that retrieves a value by key and a setter method that inserts or updates a key-value pair. Key information includes the getter's behavior of throwing a NoneValueException if a key is missing, and the setter's logic to overwrite existing values or add new ones to a HashMap. This technical reference is intended for developers working with Cangjie collections who need to understand operator behavior, parameter requirements, and exception handling for generic types.