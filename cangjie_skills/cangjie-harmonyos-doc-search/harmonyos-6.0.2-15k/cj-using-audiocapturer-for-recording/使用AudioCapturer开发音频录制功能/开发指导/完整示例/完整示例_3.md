func build() {
            Row {
                Column {
                    Button("requestPermission").fontSize(30).margin(10).fontWeight(FontWeight.Bold).onClick {
                        evt => requestPermission()
                    }
                    Button("initCapturer").fontSize(30).margin(10).fontWeight(FontWeight.Bold).onClick {
                        evt => initAudioCapturer()
                    }
                    Button("start").fontSize(30).margin(10).fontWeight(FontWeight.Bold).onClick {
                        evt => start()
                    }
                    Button("stop").fontSize(30).margin(10).fontWeight(FontWeight.Bold).onClick {
                        evt => stop()
                    }
                    Button("release").fontSize(30).margin(10).fontWeight(FontWeight.Bold).onClick {
                        evt => release()
                    }
                }.width(100.percent)
            }.height(100.percent)
        }
    }
    ```