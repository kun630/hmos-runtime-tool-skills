# encoding_json_package_enums

本目录包含Cangjie语言`encoding_json`包中关于枚举类型的API参考文档，核心内容是`JsonKind`枚举的定义与使用。该文档详细阐述了JSON标准中的各种数据类型在Cangjie语言中的映射，涵盖了从基础的空值、布尔值、数值类型到复杂的字符串、数组和对象结构。通过定义`JsNull`、`JsBool`、`JsInt`等枚举成员，文档为开发者提供了识别和处理JSON数据结构的类型标识机制。这对于确保JSON解析的类型安全性和数据完整性至关重要。该文档主要面向使用Cangjie语言进行JSON数据序列化、反序列化及处理的开发者，要求读者具备基本的Cangjie语法基础和对JSON数据格式的理解。

## Quick Navigation

- 你想了解 `JsonKind` 枚举的整体定义及其在 `encoding_json` 包中的作用吗？ → encoding_json_package_enums.md
- 你想查看 `JsonValue` 支持的具体类型成员（如 `JsNull`, `JsBool`, `JsArray` 等）及其含义吗？ → encoding_json_package_enums.md
- 你需要确认 JSON 标准类型与 Cangjie 枚举值之间的对应关系吗？ → encoding_json_package_enums.md

## Detailed Description

### encoding_json_package_enums.md encoding_json_package_enums.md

这份文档是Cangjie语言encoding_json包中JsonKind枚举的API参考文档，旨在定义JsonValue的具体类型。文档详细列出了JsNull、JsBool、JsInt、JsFloat、JsString、JsArray和JsObject等枚举成员，并逐一解释了它们在JSON标准中对应的类型（如空值、布尔值、整数、浮点数、字符串、数组和对象）。关键信息在于这些枚举值提供了对JSON数据结构的类型标识。文档面向使用该库进行JSON解析和处理的开发者，要求读者具备基本的Cangjie语法和JSON格式知识。