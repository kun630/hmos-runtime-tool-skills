# class_JsonReader

本目录包含仓颉编程语言中 `JsonReader` 类的 API 参考文档，专注于提供 JSON 数据流到仓颉对象的反序列化能力。内容涵盖了类的构造函数、核心解析方法（如 `endArray`、`peek`、`readName`）以及流式读取函数（如 `readValue`、`skip`）。该文档详细阐述了如何通过输入流高效读取、跳过及导航 JSON 数据，并处理各种异常情况。适合需要进行底层流式 JSON 解析、精细控制数据读取过程的仓颉开发者参考。

## Quick Navigation

**你想了解类的构造函数及基础解析方法？**
- 查看 `init`、`endArray`、`peek` 等核心功能 → class_JsonReader_6more_d9033371.md

**你想掌握数据读取、跳过及导航的具体函数？**
- 了解 `readValue`、`readValueBytes`、`skip` 等流操作 → func_readValueT_w_5more_812914eb.md

## Detailed Description

### class_JsonReader_6more_d9033371.md class_JsonReader_6more_d9033371.md
这份文档是仓颉编程语言中 `JsonReader` 类的 API 参考文档，旨在提供 JSON 数据流到仓颉对象的反序列化能力。文档详细描述了构造函数 `init` 以及 `endArray`、`endObject`、`peek` 和 `readName` 等核心方法的功能、参数及异常处理。重点说明了如何通过输入流读取数据、跳过空白字符、预览令牌类型及读取名称。该文档面向需要进行流式 JSON 解析的仓颉开发者，要求读者具备 JSON 格式基础和流处理概念。

### func_readValueT_w_5more_812914eb.md func_readValueT_w_5more_812914eb.md
本文档是 Cangjie 语言 JSON 流处理的 API 参考文档，详细说明了从输入流读取、跳过及导航 JSON 数据的函数。主要内容包括泛型函数 `readValue` 的反序列化逻辑（特别是 String 类型的特殊处理）、`readValueBytes` 读取原始字节数据的规则、`skip` 跳过数据结构的功能，以及 `startArray` 和 `startObject` 对起始符号的消费。关键点在于各函数根据下一个 `JsonToken` 的不同行为及异常处理机制。文档面向需要精细控制 JSON 解析过程的开发者，提供了底层流操作接口。