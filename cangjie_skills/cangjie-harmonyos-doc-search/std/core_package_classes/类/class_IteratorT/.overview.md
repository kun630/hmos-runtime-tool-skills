# class_IteratorT

This directory provides comprehensive API reference documentation for the `Iterator` class within the Cangjie programming language. It serves as a central resource for understanding the abstract class used for traversing container elements, detailing its inheritance from `Iterable` and core traversal methods. Beyond the basics, it covers extensive extensions that enable comparison operations, equality checks, and advanced functional programming utilities. Developers will find guidance on terminal operations like `reduce`, intermediate operations like `map` and `filter`, and traversal control mechanisms. This documentation is essential for Cangjie developers aiming to master sequential data access, implement efficient collection handling, and utilize functional programming patterns in their applications.

## Quick Navigation

**What do you want to learn?**

*   Understand the core Iterator class structure and basic traversal? → class_IteratorT_4more.md
*   Perform comparisons (max/min) or check element existence? → extendT_IteratorT_where_T__2more.md
*   Use advanced functional operations like map, filter, or reduce? → extendT_IteratorT/

## Detailed Description

### class_IteratorT_4more.md class_IteratorT_4more.md
This document provides API reference for the abstract Iterator class in the Cangjie programming language, designed for traversing container elements. It details the class inheritance from Iterable and documents key methods: the default constructor init, iterator which returns the instance itself, and next which retrieves the next element as an Option type. The text includes a code example demonstrating how to iterate over an Array using a while loop and pattern matching to handle Some and None cases. Targeted at Cangjie developers, this reference explains the fundamental mechanism for sequential data access.

### extendT_IteratorT_where_T__2more.md extendT_IteratorT_where_T__2more.md
This document serves as API reference documentation for extending the Iterator class in the Cangjie programming language to support comparison and equality operations. It details two specific extensions: one for types implementing Comparable and another for types implementing Equatable. Key functions include max() and min() for finding extreme values, and contains() for checking element existence. All methods consume the iterator elements during execution and return Option or Bool types. This guide targets Cangjie developers seeking to perform aggregate operations or searches on iterators.

### extendT_IteratorT/
This directory contains comprehensive API documentation for the Iterator type in the Cangjie programming language. It covers a wide range of utility functions and extension methods designed for efficient data sequence manipulation, including terminal operations like `reduce` and `count`, intermediate operations like `filter` and `map`, and traversal control methods like `skip` and `zip`. The documentation distinguishes between operations that consume iterators and those that produce new ones. It is suitable for Cangjie developers looking to implement functional programming patterns and handle collections effectively.