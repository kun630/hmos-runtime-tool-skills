func stopVibrationExample() {
        try {
            // 按照VIBRATOR_STOP_MODE_PRESET模式停止振动
            stopVibration(VibratorStopMode.VIBRATOR_STOP_MODE_PRESET)
        } catch (e: Exception) {
            AppLog.error("stopVibration error: ${e.message.toString()}")
        }
    }
    ```

    **方式二** 停止所有模式的马达振动，包括自定义振动：

    ```cangjie
    import kit.SensorServiceKit.*

    func stopVibrationExample() {
        try {
            stopVibration(Option<VibratorStopMode>.None)
        } catch (e: Exception) {
            AppLog.error("stopVibration error: ${e.message.toString()}")
        }
    }
    ```