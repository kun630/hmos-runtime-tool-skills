# extend_UInt8_CarryingOpUInt8

This directory contains API reference documentation for the Cangjie language, specifically focusing on the `CarryingOp` interface implementation for the `UInt8` type. It covers a comprehensive set of arithmetic and bitwise operations designed with explicit overflow detection capabilities. All functions utilize a wrapping strategy and return tuples containing a Boolean flag indicating overflow or truncation status alongside the resulting value. This documentation is essential for developers engaged in low-level systems programming, cryptography, or any application requiring precise control over numeric operations and error handling. By providing detailed specifications for functions like addition, division, and bit shifting, this resource ensures developers can safely manage edge cases in 8-bit unsigned integer arithmetic.

## Quick Navigation

**What do you want to learn?**

*   **Basic arithmetic operations with carry** (Addition, Increment, Decrement, Division, Modulo) → extend_UInt8_CarryingOpUIn_6more.md
*   **Advanced arithmetic and bitwise operations** (Multiplication, Negation, Subtraction, Bit Shifting) → func_carryingMulUInt8_5more.md

## Detailed Description

### extend_UInt8_CarryingOpUIn_6more.md extend_UInt8_CarryingOpUIn_6more.md
This API reference document details the implementation of the `CarryingOp` interface specifically for the `UInt8` type within the Cangjie language. The content covers specific arithmetic functions including `carryingAdd`, `carryingDec`, `carryingDiv`, `carryingInc`, and `carryingMod`. Key information provided is that all operations defined here use a wrapping strategy to handle value limits. Furthermore, these functions return a tuple containing a Boolean indicating overflow status and the resulting `UInt8` value. This documentation is intended for developers performing low-level arithmetic operations that require explicit overflow detection to ensure system reliability and correctness.

### func_carryingMulUInt8_5more.md func_carryingMulUInt8_5more.md
This document serves as API reference documentation for Cangjie language functions that perform arithmetic and bitwise operations on `UInt8` integers with overflow detection. It details five specific functions: `carryingMul`, `carryingNeg`, `carryingShl`, `carryingShr`, and `carryingSub`, covering multiplication, negation, left shift, right shift, and subtraction respectively. Key takeaways include that all functions utilize a wrapping strategy and return a tuple containing a Boolean indicating if truncation occurred and the resulting `UInt8` value. This reference is intended for Cangjie developers who need precise control over numeric operations and overflow handling in their applications.