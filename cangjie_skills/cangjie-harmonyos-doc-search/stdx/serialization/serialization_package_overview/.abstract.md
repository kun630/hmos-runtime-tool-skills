本目录提供了 `stdx.serialization.serialization` 包的全面 API 概览，核心聚焦于数据的序列化与反序列化机制。文档深入解析了如何通过实现 `Serializable` 接口来支持对象状态的保存与恢复，并详细列出了用于数据封装的 `field` 函数。内容涵盖了作为中间数据表示层的 `DataModel` 类及其针对基本类型（如 `Bool`、`Int`、`String`）的子类实现，同时也介绍了处理结构体元素的 `Field` 类以及相关的异常处理机制 `DataM...