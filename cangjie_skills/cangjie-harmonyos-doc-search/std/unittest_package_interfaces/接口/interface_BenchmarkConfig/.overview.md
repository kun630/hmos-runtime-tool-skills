# interface_BenchmarkConfig

This directory contains essential API documentation for the `BenchmarkConfig` interface within the Cangjie unittest framework, specifically designed to facilitate high-precision performance testing. It thoroughly documents the extension of the Configuration class, enabling developers to implement specific benchmarking configurations. Key topics covered include methods to control execution parameters, such as defining batch sizes via fixed counts or ranges, managing garbage collection behavior, and setting constraints on minimum batches and duration. The documentation also details warmup configurations to ensure test stability. This resource is ideal for developers and software engineers using the Cangjie framework who need to customize and fine-tune benchmark test scenarios for accurate, reliable performance measurement and analysis.

## Quick Navigation

**What do you want to learn?**

*   Understand how to extend the Configuration class to implement benchmark settings → extend_Configuration_BenchmarkConfig.md
*   Review specific function signatures, parameter types, and overloads for the interface → interface_BenchmarkConfig_8more.md

## Detailed Description

### extend_Configuration_BenchmarkConfig.md

This API documentation details the extension of the Configuration class to implement the BenchmarkConfig interface, enabling specific configuration for performance testing. The document outlines methods to control benchmark execution parameters, including batch size settings via fixed counts or ranges, garbage collection behavior, and constraints on minimum batches and duration. It also covers warmup configurations to ensure stable test results. Key functions include batchSize, explicitGC, minBatches, minDuration, and warmup. This reference guide is intended for developers using the Cangjie unittest framework who need to customize and fine-tune benchmark test scenarios for accurate performance measurement.

### interface_BenchmarkConfig_8more.md

This API reference document defines the Cangjie BenchmarkConfig interface, which provides function signatures for configuring performance test parameters within the @Configuration macro. The documentation details specific configuration methods including batchSize, warmup, minDuration, explicitGC, and minBatches. Key information includes overloaded functions for setting batch sizes via integers or ranges, defining warmup periods by iteration count or duration, and specifying garbage collection types. All methods return Unit and accept parameters like Int64, Duration, or Range. This guide targets developers implementing performance benchmarks who need to customize test execution parameters and behavior.