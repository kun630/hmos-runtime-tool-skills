## 创建组件

### init(Length, Length)

```cangjie
public init(width!: Length, height!: Length)
```

**功能：** 在宽度为width、高度为height的填充区域内绘制一条折线。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 19

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|width|[Length](./cj-common-types.md#interface-length)|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>默认单位：vp。<br>异常值按照取0处理。|
|height|[Length](./cj-common-types.md#interface-length)|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>默认单位：vp。<br>异常值按照取0处理。|

### init(Int64, Int64)

```cangjie
public init(width!: Int64, height!: Int64)
```

**功能：** 在宽度为width、高度为height的填充区域内绘制一条折线。需要设置[width](./cj-universal-attribute-size.md#func-widthlength)或[height](./cj-universal-attribute-size.md#func-heightlength)属性参数不为0才能显示出来。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 19

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|width|Int64|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>单位：vp。<br>异常值按照取0处理。|
|height|Int64|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>单位：vp。<br>异常值按照取0处理。|

### init(Float64, Float64)

```cangjie
public init(width!: Float64, height!: Float64)
```

**功能：** 在宽度为width、高度为height的填充区域内绘制一条折线。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 19

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|width|Float64|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>单位：vp。<br>异常值按照取0处理。|
|height|Float64|是|-| **命名参数。** 填充区域宽度，取值范围≥0。<br>单位：vp。<br>异常值按照取0处理。|

### init()

```cangjie
public init()
```

**功能：** 在宽度为0、高度为0的填充区域内绘制一条折线。需要设置[width](./cj-universal-attribute-size.md#func-widthlength)或[height](./cj-universal-attribute-size.md#func-heightlength)属性参数不为0才能显示出来。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 19