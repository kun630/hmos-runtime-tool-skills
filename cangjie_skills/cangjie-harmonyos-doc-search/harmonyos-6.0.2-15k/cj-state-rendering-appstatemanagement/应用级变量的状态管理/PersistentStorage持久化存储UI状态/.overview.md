# PersistentStorage持久化存储UI状态

This directory provides comprehensive documentation on the PersistentStorage singleton object, a crucial component for HarmonyOS applications designed to persist UI state by synchronizing specific AppStorage properties to local disk storage. It covers the fundamental two-way synchronization mechanism, supported data types, and critical performance constraints, such as avoiding large datasets to prevent UI lag. The resources include technical guides, detailed API references for the Cangjie language, and practical code examples. This collection is ideal for HarmonyOS developers and application engineers seeking to implement reliable local state persistence that survives application restarts while maintaining optimal rendering performance.

## Quick Navigation

- Understand the overview, constraints, and performance warnings? → PersistentStorage持久化存储UI状态_2more.md
- Reference specific API methods and initialization priority logic? → class_PersistentStorage.md
- View code examples for implementation and UI binding? → 示例代码.md

## Detailed Description

### PersistentStorage持久化存储UI状态_2more.md PersistentStorage持久化存储UI状态_2more.md
This technical guide explains the PersistentStorage singleton object used to persist UI state by synchronizing selected AppStorage properties to disk. Main sections include an overview of the two-way synchronization mechanism and specific usage constraints. Key information highlights supported data types (Int64, Float64, String, Bool), performance warnings to avoid large datasets (recommended under 2kb) or frequent changes due to synchronous disk writes, and the requirement that initialization must occur after the UI instance is ready. This reference is intended for application developers managing local state persistence who need to ensure data survives application restarts without impacting UI rendering performance.

### class_PersistentStorage.md class_PersistentStorage.md
This document serves as API reference documentation for the PersistentStorage class in the Cangjie language, enabling state variable persistence for ArkUI applications. It covers static methods for managing properties, including deleteProp to remove attributes, keys to retrieve all property names, and persistProp or persistProps to save data to files. A critical takeaway is the initialization priority logic, which warns that existing AppStorage values can override file-stored data, potentially compromising persistence. This guide targets HarmonyOS developers requiring robust data storage mechanisms within their applications.

### 示例代码.md 示例代码.md
This document provides a Cangjie code example demonstrating how to access and manage properties initialized by PersistentStorage through AppStorage in a HarmonyOS application. It covers the initialization of persistent properties using persistProp and persistProps, binding these properties to UI components using the @StorageLink decorator, and implementing user interactions to modify or clear stored data. The code illustrates the bidirectional synchronization between the UI and persistent storage, showing how data persists across sessions and can be dynamically updated or deleted via button clicks. This tutorial is intended for HarmonyOS developers using the Cangjie language who need to implement state management and data persistence in their applications.