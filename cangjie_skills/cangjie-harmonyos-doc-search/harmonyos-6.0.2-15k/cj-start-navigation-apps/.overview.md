# cj-start-navigation-apps

This directory is a specialized technical resource designed for HarmonyOS developers utilizing the Cangjie language to build or integrate navigation capabilities. It centers on the `startAbilityByType` interface, a critical component for invoking navigation application extension panels. The documentation provides an in-depth look at the entire implementation lifecycle, covering both the calling application (which requests map services) and the target application (which provides them). Key technical areas include route planning logic, navigation execution, location search functionalities, and the precise configuration of parameters using the GCJ-02 coordinate system. Additionally, it details the necessary `module.json5` setup. With practical Cangjie code examples for constructing JSON parameters, managing callbacks, and parsing incoming requests, this guide is essential for developers seeking to enable robust map interactions or allow their apps to serve as map providers within the HarmonyOS ecosystem.

## Quick Navigation

**What do you want to learn or achieve?**

-   **Invoke Navigation Panels:** Learn how to use the `startAbilityByType` interface to open map extensions from your app → `拉起导航类应用startAbilityByType/`
-   **Develop a Map Provider:** Understand how to configure your app to receive and handle external navigation requests → `拉起导航类应用startAbilityByType/`
-   **Configure Coordinates and Routes:** Master the setup of GCJ-02 coordinates and route planning parameters → `拉起导航类应用startAbilityByType/`
-   **Implement Cangjie Code:** Find examples for JSON construction, callback handling, and request parsing → `拉起导航类应用startAbilityByType/`

## Detailed Description

### 拉起导航类应用startAbilityByType/
This directory provides comprehensive technical documentation for invoking navigation application extension panels on HarmonyOS using the `startAbilityByType` interface. It covers the complete implementation lifecycle for both the calling application (requesting map services) and the target application (providing map services). Key topics include route planning, navigation, location search, parameter configuration using the GCJ-02 coordinate system, and `module.json5` setup. The guides include Cangjie code examples for constructing JSON parameters, handling callbacks, and parsing incoming requests. This resource is suitable for HarmonyOS application developers looking to integrate robust map functionalities or enable their apps to respond to external map requests effectively.