# extend_UIntNative_WrappingOpUIntNative

This directory contains essential API documentation for the Cangjie language, focusing on the implementation of the `WrappingOp` interface for the `UIntNative` type. It provides a detailed overview of a wide array of arithmetic and bitwise operations, such as addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and bit shifting. The core theme of this documentation is the management of numeric overflow through high-bit truncation strategies. By ensuring that results wrap around instead of raising errors, these functions offer predictable behavior critical for system-level programming. This collection is specifically designed for Cangjie developers who need granular control over integer operations and require reliable overflow handling mechanisms in their applications.

## Quick Navigation

**What do you want to learn?**

*   Explore the full implementation of the `WrappingOp` interface, covering arithmetic operations like addition and multiplication, as well as left shift logic → extend_UIntNative_Wrapping_9more.md
*   Investigate specific functions for right shift (`wrappingShr`) and subtraction (`wrappingSub`), focusing on their unique overflow and shift count handling → func_wrappingShrUInt64_2more.md

## Detailed Description

### extend_UIntNative_Wrapping_9more.md extend_UIntNative_Wrapping_9more.md
This API documentation details the implementation of the WrappingOp interface for the UIntNative type in the Cangjie language. It covers a comprehensive set of arithmetic and bitwise operations, including addition, subtraction, multiplication, division, modulo, negation, increment, decrement, and left shift. Key information highlights that all functions utilize a high-bit truncation strategy to handle overflow, ensuring results wrap around instead of raising errors. Specifically, the left shift operation handles large shift counts by masking to the type's bit width. This reference guide is intended for Cangjie developers who need precise control over numeric overflow behavior in system-level programming.

### func_wrappingShrUInt64_2more.md func_wrappingShrUInt64_2more.md
This document provides API reference documentation for two Cangjie language functions that utilize high-bit truncation strategies for arithmetic and bitwise operations. The main sections detail the wrappingShr function, which performs a right shift using the low bits of the shift count, and the wrappingSub function, which handles subtraction by truncating high bits upon overflow. Key takeaways include the specific behavior of wrappingShr when the shift amount exceeds the bit width and the overflow handling mechanism of wrappingSub. Both functions return UIntNative values. This reference is intended for Cangjie developers requiring precise control over integer operations and overflow behavior.