# struct_MatchData

This directory contains comprehensive API documentation for the `MatchData` struct within the Cangjie standard library's regex module. It focuses on the mechanisms for storing and querying regular expression match results, specifically detailing methods for retrieving match positions, substrings, and capture group information. Key topics include `matchPosition`, `matchString`, `groupCount`, and exception handling for invalid inputs. This documentation is suitable for Cangjie developers implementing text processing logic who need to programmatically inspect regex matches and their locations, providing verified code examples to facilitate practical implementation.

## Quick Navigation

**What do you want to learn?**

*   Retrieve specific match substrings or position data using named capture groups or indices? → func_matchPositionString_4more.md
*   Understand the core `MatchData` struct, its properties like `groupCount`, and general match inspection methods? → struct_MatchData_5more.md

## Detailed Description

### func_matchPositionString_4more.md func_matchPositionString_4more.md

This API documentation file details specific functions for retrieving match results and position data within the Cangjie standard library's regex module. It covers four main functions: `matchPosition(String)` for locating named capture groups, and three overloads of `matchString` for retrieving substrings by no argument, integer index, or string name. Key information includes parameter definitions, return types like `Position` and `String`, and potential `IllegalArgumentException` errors for invalid inputs or disabled capture groups. Targeted at Cangjie developers, this reference requires knowledge of regular expressions and includes verified code examples demonstrating practical usage.

### struct_MatchData_5more.md struct_MatchData_5more.md

This API reference document details the `MatchData` struct in the Cangjie standard library, which is designed to store regular expression match results and facilitate result queries. It covers methods for retrieving capture group counts via `groupCount` and obtaining position data through `matchPosition`, including both the overall match and specific capture groups. Key information includes the deprecation of `groupNumber` and the exception handling for invalid group indices in `matchPosition`. The document provides verified code examples demonstrating usage with `Regex` and `lazyFindAll`. This resource targets Cangjie developers implementing text processing logic who need to programmatically inspect regex matches and their locations.