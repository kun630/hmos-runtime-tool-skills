# interface_LittleEndianOrderT

This directory provides API documentation for the Cangjie language's `LittleEndianOrder` interface and its type extensions. It enables binary serialization and deserialization for Booleans, Integers (Int8-64, UInt8-64), and Floating-point numbers (Float16-64) using little-endian byte order. Key functionalities include `readLittleEndian` and `writeLittleEndian` methods for converting between byte arrays and native types, with strict buffer size validation. This resource is designed for Cangjie developers working on low-level data processing, network protocols, or file I/O requiring precise binary manipulation.

## Quick Navigation

**What do you want to learn?**

*   Understand the generic interface definition? → interface_LittleEndianOrde_3more.md
*   Handle Boolean data? → extend_Bool_LittleEndianOrderBool.md
*   Handle Signed Integers?
    *   8-bit (Int8) → extend_Int8_LittleEndianOrderInt8.md
    *   16-bit (Int16) → extend_Int16_LittleEndianOrderInt16.md
    *   32-bit (Int32) → extend_Int32_LittleEndianOrderInt32.md
    *   64-bit (Int64) → extend_Int64_LittleEndianOrderInt64.md
*   Handle Unsigned Integers?
    *   8-bit (UInt8) → extend_UInt8_LittleEndianOrderUInt8.md
    *   16-bit (UInt16) → extend_UInt16_LittleEndianOrderUInt16.md
    *   32-bit (UInt32) → extend_UInt32_LittleEndianOrderUInt32.md
    *   64-bit (UInt64) → extend_UInt64_LittleEndianOrderUInt64.md
*   Handle Floating-point numbers?
    *   Float16 → extend_Float16_LittleEndianOrderFloat16.md
    *   Float32 → extend_Float32_LittleEndianOrderFloat32.md
    *   Float64 → extend_Float64_LittleEndianOrderFloat64.md

## Detailed Description

### extend_Bool_LittleEndianOrderBool.md
Details the Bool type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing Booleans from byte arrays and instance `writeLittleEndian` for serialization. Includes parameter requirements for UInt8 buffers, return types, and exception handling for insufficient buffer sizes via IllegalArgumentException. Targets Cangjie developers in binary data serialization.

### extend_Float16_LittleEndianOrderFloat16.md
Details the Float16 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Includes parameter definitions, return types, and exception handling for insufficient buffer sizes, with executable code examples. Targets developers using the std.binary package.

### extend_Float32_LittleEndianOrderFloat32.md
Details the Float32 type extension implementing LittleEndianOrder. Covers the extension declaration, static `readLittleEndian` for parsing bytes, and instance `writeLittleEndian` for serialization. Both methods handle buffer size validation and throw IllegalArgumentException for insufficient buffers. Targets Cangjie developers in binary data manipulation.

### extend_Float64_LittleEndianOrderFloat64.md
Describes the Float64 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for deserialization and instance `writeLittleEndian` for serialization. Requires an 8-byte buffer, throwing IllegalArgumentException otherwise, strictly following Little-Endian byte order. Targets developers in binary processing and network protocols.

### extend_Int16_LittleEndianOrderInt16.md
Details the Int16 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Includes parameter requirements for Array<UInt8> buffers, return types, and IllegalArgumentException for insufficient buffer sizes. Targets Cangjie developers in binary serialization.

### extend_Int32_LittleEndianOrderInt32.md
Details the Int32 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Includes parameter definitions, return types, and specific exceptions for insufficient buffers. Code examples demonstrate byte order reversal. Targets Cangjie developers in low-level binary manipulation.

### extend_Int64_LittleEndianOrderInt64.md
Details the Int64 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Requires a byte array buffer and throws IllegalArgumentException if the buffer is too small for 64-bit data. Intended for Cangjie developers in binary serialization.

### extend_Int8_LittleEndianOrderInt8.md
Details the Int8 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing Int8 from byte arrays and instance `writeLittleEndian` for serialization. Includes parameter definitions, return values, and exception handling for insufficient buffer sizes. Intended for developers in low-level binary manipulation.

### extend_UInt16_LittleEndianOrderUInt16.md
Details the UInt16 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Both operate on Array<UInt8> buffers, return specific types, and throw IllegalArgumentException for insufficient buffers. Includes practical code examples. Targets developers in binary data processing.

### extend_UInt32_LittleEndianOrderUInt32.md
Details the UInt32 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Both functions operate on little-endian byte order, require Array<UInt8> buffers, and throw IllegalArgumentException for insufficient sizes. The write method returns the byte count. Intended for low-level binary manipulation.

### extend_UInt64_LittleEndianOrderUInt64.md
Details the UInt64 type extension implementing LittleEndianOrder. Covers the interface implementation, static `readLittleEndian` for parsing, and instance `writeLittleEndian` for serialization. Both require Array<UInt8> buffers and throw IllegalArgumentException for insufficient sizes. Read returns UInt64; write returns Int64 byte count. Targets developers in binary manipulation.

### extend_UInt8_LittleEndianOrderUInt8.md
Details the UInt8 type extension implementing LittleEndianOrder. Covers static `readLittleEndian` for parsing byte arrays and instance `writeLittleEndian` for serialization. Includes parameter definitions, return values, and exceptions for insufficient buffer sizes. Features executable code examples. Designed for developers in binary data manipulation.

### interface_LittleEndianOrde_3more.md
Provides API reference for the generic LittleEndianOrder interface. Details the interface definition, static `readLittleEndian` for parsing data from byte arrays, and instance `writeLittleEndian` for serialization. Includes Array<UInt8> buffer usage, return types, and IllegalArgumentException for insufficient sizes. Intended for developers in data serialization and network protocols.