# enum_Month

This directory provides essential API documentation for the `Month` enum in the Cangjie programming language, specifically designed to handle the representation and manipulation of months within the time package. It encompasses the complete definition of the enum, listing all twelve months, and details critical methods for type conversion, such as converting between integers and month objects. The documentation also highlights the implementation of standard interfaces like `ToString` and `Equatable`, ensuring developers can easily integrate month logic into their applications. Furthermore, it covers operator overloading for arithmetic operations and notes on deprecated functions. This resource is ideal for Cangjie developers, backend engineers, and system architects who require precise, type-safe date and time functionality in their software projects.

## Quick Navigation

**What do you want to learn?**

*   Explore the core definition, constructors, and standard methods like `toInteger` and `toString` → enum_Month_16more.md
*   Review deprecated functions and understand arithmetic operators for month calculations → func_value_supdeprecatedsu_5more.md

## Detailed Description

### enum_Month_16more.md enum_Month_16more.md
This document serves as an API reference for the `Month` enum in the Cangjie programming language, designed to represent months of the year. It details the enum definition listing all twelve months, individual constructors, and key methods including the static `of` function for integer conversion, `toInteger`, and `toString`. Key information includes the enum's implementation of `ToString` and `Equatable` interfaces, support for bidirectional conversion with `Int64`, and exception handling for invalid inputs. This reference is intended for Cangjie developers working with date and time logic who require precise month manipulation and type conversion capabilities.

### func_value_supdeprecatedsu_5more.md func_value_supdeprecatedsu_5more.md
This document provides API reference details for methods and operators associated with the Month enum in the Cangjie time package. It covers the deprecated value() function for integer conversion and several operator overloads including inequality, addition, subtraction, and equality. Key information includes the deprecation notice for value() in favor of toInteger(), and the behavior of arithmetic operators for calculating future or past months. This reference is intended for developers working with date and time logic who need to manipulate or compare Month instances.