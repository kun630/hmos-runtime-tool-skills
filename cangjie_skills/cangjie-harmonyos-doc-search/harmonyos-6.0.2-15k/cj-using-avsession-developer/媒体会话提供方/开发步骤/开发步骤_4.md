class StopCallback <: Callback0Argument {
        public StopCallback (let f: () -> Unit) {}
        public open func invoke() {
            f()
        }
    }

    class PlayNextCallback <: Callback0Argument {
        public PlayNextCallback(let f: () -> Unit) {}
        public open func invoke() {
            f()
        }
    }

    class PlayPreviousCallback <: Callback0Argument {
        public PlayPreviousCallback(let f: () -> Unit) {}
        public open func invoke() {
            f()
        }
    }

    class FastForwardCallback <: Callback0Argument {
        public FastForwardCallback(let f: () -> Unit) {}
        public open func invoke() {
            f()
        }
    }

    class RewindCallback <: Callback0Argument {
        public RewindCallback(let f: () -> Unit) {}
        public open func invoke() {
            f()
        }
    }

    class SeekCallback <: Callback1Argument<Int32> {
        public SeekCallback(let f: (Int32) -> Unit) {}
        public open func invoke(time: Int32) {
            f(time)
        }
    }

    class SetSpeedCallback <: Callback1Argument<Float64> {
        public SetSpeedCallback(let f:(Float64) -> Unit) {}
        public open func invoke(speed: Float64) {
            f(speed)
        }
    }

    class SetLoopModeCallback <: Callback1Argument<LoopMode> {
        public SetLoopModeCallback(let f:(LoopMode) -> Unit) {}
        public open func invoke(mode: LoopMode) {
            f(mode)
        }
    }

    class ToggleFavoriteCallback <: Callback1Argument<String> {
        public ToggleFavoriteCallback(let f: (String) -> Unit) {}
        public open func invoke(assetId: String) {
            f(assetId)
        }
    }

    let context = Global.getStageContext() // 获取Context应用上下文请参见 [API 参考] -> [API参考概述] -> [开发说明] -> [仓颉示例代码说明]
    let `type`: AVSessionType = AVSessionType.SESSION_TYPE_AUDIO
    // 假设已经创建了一个session，如何创建session可以参考之前的案例。
    let session = createAVSession(context, 'SESSION_NAME', `type`)
    // 一般在监听器中会对播放器做相应逻辑处理。
    // 不要忘记处理完后需要通过set接口同步播放相关信息，参考上面的用例。
    session.on(AVSessionEventType.Play, PlayCallback({
        =>
        AppLog.info("on play , do play task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('play')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态。
    }))

    session.on(AVSessionEventType.Pause, PauseCallback({
        =>
        AppLog.info("on pause , do pause task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('pause')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态。
    }))

    session.on(AVSessionEventType.Stop, StopCallback({
        =>
        AppLog.info("on stop , do stop task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('stop')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态。
    }))

    session.on(AVSessionEventType.PlayNext, PlayNextCallback({
        =>
        AppLog.info("on playNext , do playNext task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('playNext')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态，使用SetAVMetadata上报媒体信息。
    }))

    session.on(AVSessionEventType.PlayPrevious, PlayPreviousCallback({
        =>
        AppLog.info("on playPrevious , do playPrevious task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('playPrevious')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态，使用SetAVMetadata上报媒体信息。
    }))

    session.on(AVSessionEventType.FastForward, FastForwardCallback({
        =>
        AppLog.info("on fastForward , do fastForward task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('fastForward')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态和播放position。
    }))

    session.on(AVSessionEventType.Rewind, RewindCallback({
        =>
        AppLog.info("on rewind , do rewind task")
        // 如暂不支持该指令，请勿注册；或在注册后但暂不使用时，通过session.off('rewind')取消监听。
        // 处理完毕后，请使用SetAVPlayState上报播放状态和播放position。
    }))