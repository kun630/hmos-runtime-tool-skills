# class_UdpSocket

This directory contains API reference documentation for the `UdpSocket` class within the Cangjie standard library, designed to facilitate User Datagram Protocol (UDP) network communication. It covers essential aspects of socket management, including class constructors, data transmission and reception, configuration of socket options, and lifecycle control. Key topics include binding sockets to local ports, managing buffer sizes, handling operation timeouts, and error handling for platform-specific limitations. This documentation is suitable for developers implementing network protocols in Cangjie who require precise control over low-level datagram operations, socket configuration, and resource management.

## Quick Navigation

**What do you want to do?**

*   **Initialize a socket or manage its lifecycle?**
    *   Review class_UdpSocket_8more.md for class constructors and core properties.
    *   See prop_sendTimeout_8more.md for specific lifecycle methods like `bind`, `close`, and `connect`.
*   **Send data or configure socket parameters?**
    *   Go to func_sendArrayByte_5more.md for `send`, `sendTo`, and `setSocketOption` functions.
*   **Receive data or check socket status?**
    *   Go to func_getSocketOptionBoolIn_5more.md for `receive`, `receiveFrom`, and `isClosed` functions.
*   **Inspect the socket state for debugging?**
    *   Go to func_toString.md for the `toString` method.

## Detailed Description

### class_UdpSocket_8more.md
This document serves as API reference documentation for the UdpSocket class within the Cangjie standard library, designed to facilitate UDP datagram communication. It details class constructors for binding sockets and outlines key properties for managing socket behavior, such as local and remote addresses, send and receive buffer sizes, and operation timeouts. The documentation also explains socket options like address and port reuse, noting platform-specific limitations on Windows. Key takeaways include the requirement for explicit closing, the 64KB packet size limit, and the inheritance from the DatagramSocket parent type. This guide is intended for developers implementing network protocols in Cangjie who require precise control over UDP socket configuration and lifecycle management.

### func_getSocketOptionBoolIn_5more.md
This document serves as API reference documentation for socket manipulation functions in the Cangjie programming language. It details five specific functions: getSocketOptionBool, getSocketOptionIntNative, isClosed, receive, and receiveFrom. Key information includes the retrieval of socket parameters as boolean or integer types, checking socket closure status, and receiving data packets. The documentation specifies parameter types like Int32 and Array<Byte>, return values, and exceptions such as SocketException and SocketTimeoutException. This reference is intended for developers implementing network communication, requiring knowledge of socket programming concepts and the Cangjie standard library.

### func_sendArrayByte_5more.md
This document provides API reference documentation for socket communication functions in the Cangjie programming language, specifically focusing on data transmission and configuration. It covers five main functions: send and sendTo for transmitting byte arrays to connected or specific addresses, and three overloaded setSocketOption methods for configuring socket parameters using different data types (raw pointers, booleans, and integers). Key details include parameter definitions for payload and recipient addresses, potential blocking behavior, and specific SocketException conditions such as system limits, ICMP errors, or platform-specific failures on Windows and macOS. This reference guide targets developers building network applications who need to implement low-level socket operations and error handling in Cangjie.

### func_toString.md
This document serves as an API reference entry for the toString() method within the Cangjie UdpSocket class. It details the function's signature, its specific functionality, and return value specifications. The key information provided is that this public override function returns a String object containing the current state information of the UdpSocket instance. This documentation is intended for software developers working with the Cangjie network package who need to inspect or debug socket states programmatically. Keywords include Cangjie, UdpSocket, toString, API reference, and network programming.

### prop_sendTimeout_8more.md
This document serves as API reference documentation for the UdpSocket class in the Cangjie programming language, detailing properties and methods for socket management. Key sections include the sendTimeout property for configuring send operation limits, constructors for initializing instances with addresses or ports, and lifecycle methods like bind, close, connect, and disconnect. It also covers the low-level getSocketOption function. Important takeaways include automatic clamping of timeout values, the requirement to call close after a failed bind, and the packet filtering behavior of connect. This guide is intended for developers building network applications who need precise control over UDP socket behavior, error handling, and connection states.