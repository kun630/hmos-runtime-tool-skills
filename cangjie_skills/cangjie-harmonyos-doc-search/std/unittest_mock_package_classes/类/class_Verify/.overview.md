# class_Verify

This directory contains API documentation for the `Verify` class within the Cangjie unit testing mock framework. It covers the mechanisms for defining and validating mock object behaviors, focusing on execution frequency, argument validation, and call order. Key components include static methods for ordered and unordered verification, the `@Called` macro, and exception handling for failed verifications. This documentation is suitable for developers writing unit tests who need to ensure stub functions are executed as expected, providing tools for precise behavioral testing and interaction validation.

## Quick Navigation

**What do you want to learn?**

*   Understand the class overview, the `@Called` macro, and basic verification methods? → class_Verify_4more.md
*   Verify execution sequence and frequency using static functions like `ordered` and `that`? → static_func_orderedArrayVe_3more.md
*   Verify execution counts without enforcing order using `unordered` functions? → static_func_unorderedArray_3more.md

## Detailed Description

### class_Verify_4more.md class_Verify_4more.md
This API documentation details the Verify class within a Cangjie unit testing mock framework, providing static methods to define and validate mock object behaviors. The document outlines the class overview, the two-step verification process utilizing the @Called macro, and specific method references for clearInvocationLog, noInteractions, and ordered. Key information includes verifying execution frequency, arguments, and call order, with ordered requiring disjoint statements and exhaustive matching. It also notes exceptions like VerificationFailedException. Intended for developers writing unit tests, this guide helps ensure stub functions are executed as expected.

### static_func_orderedArrayVe_3more.md static_func_orderedArrayVe_3more.md
This document provides API reference documentation for static verification functions in a Cangjie unit testing framework, detailing how to validate mock object interactions. It covers three primary functions: ordered, which verifies execution sequence and frequency; that, for validating a single statement; and unordered, which checks execution counts without enforcing order via a dynamic closure. Key takeaways include that all verifications operate in exhaustive mode, requiring disjoint statements to prevent ambiguity, and throw VerificationFailedException upon failure. The ordered function defaults to one execution, whereas unordered defaults to at least one. This guide is intended for developers using the Cangjie unittest mock package to ensure precise behavioral testing of their code.

### static_func_unorderedArray_3more.md static_func_unorderedArray_3more.md
This API documentation details the overloaded `unordered` static functions used for verifying mock method executions without enforcing order in the Cangjie testing framework. It covers three function signatures accepting arrays of `VerifyStatement` or a closure with `UnorderedVerifier`, alongside an `Exhaustiveness` parameter. The functions verify execution counts (defaulting to at least once) and require statements to be disjoint to prevent ambiguous matches. The default mode is exhaustive, meaning all executions must be accounted for. Examples illustrate valid usage and exception scenarios. This reference targets developers implementing unit tests who need flexible, order-independent verification of mock interactions.