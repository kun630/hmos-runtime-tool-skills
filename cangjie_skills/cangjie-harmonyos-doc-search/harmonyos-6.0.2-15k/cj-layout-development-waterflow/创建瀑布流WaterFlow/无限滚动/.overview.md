# 无限滚动

This directory focuses on implementing and optimizing infinite scrolling mechanisms in ArkUI applications using the Cangjie language. It covers techniques for dynamic data loading within WaterFlow components, specifically addressing performance issues like stuttering when appending new data. Key topics include pre-loading strategies, `onScrollIndex` callbacks, `onReachEnd` events, and custom `IDataSource` implementations. This resource is suitable for ArkUI developers and engineers aiming to build smooth, responsive, and high-performance list interfaces.

## Quick Navigation

What do you want to learn?

-   Optimize performance by pre-loading data before reaching the end → 提前新增数据.md
-   Implement data loading exactly when the user reaches the bottom → 到达末尾时新增数据/

## Detailed Description

### 提前新增数据.md 提前新增数据.md

This document serves as a technical guide for optimizing infinite scrolling performance in ArkUI applications using the Cangjie language. It addresses the issue of visible stuttering when loading data at the very end of a list. The main topic involves implementing a pre-loading mechanism within the WaterFlow component. Key information includes using the onScrollIndex callback to monitor the last visible item's position relative to the total dataset size. By triggering data addition when the remaining items fall below a threshold, developers can ensure seamless scrolling. The document provides a complete code example featuring a custom MyDataSource and LazyForEach. This guide targets application developers seeking to enhance user experience through fluid UI interactions.

### 到达末尾时新增数据/

This directory focuses on the technical implementation and performance optimization of dynamic data loading within WaterFlow components, specifically addressing scenarios where new data is appended as the user scrolls to the end of a list. It provides comprehensive guidance on utilizing the Cangjie language to handle infinite scrolling, covering essential mechanisms such as the `onReachEnd` event callback, `LazyForEach` rendering, and custom `IDataSource` class creation. Furthermore, it delves into advanced best practices for maintaining high performance, emphasizing methods to avoid layout stuttering caused by full data reloads. Key concepts include `onDatasetChange`, `DataAddOperation`, and efficient layout recalculation. This resource is primarily suitable for ArkUI developers and engineers aiming to build smooth, responsive, and high-performance list interfaces in their applications.