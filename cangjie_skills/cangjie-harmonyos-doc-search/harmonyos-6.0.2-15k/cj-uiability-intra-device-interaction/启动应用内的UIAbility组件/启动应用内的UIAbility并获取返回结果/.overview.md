# 启动应用内的UIAbility并获取返回结果

This directory serves as a technical resource for HarmonyOS developers utilizing the Cangjie programming language within the Stage model. It focuses on the specific workflow of initiating an in-app UIAbility and subsequently retrieving return results, a critical pattern for applications requiring data exchange between distinct user interfaces, such as login screens or multi-step forms. The content covers the full lifecycle of this interaction, from the caller perspective using `startAbilityForResult` to the callee perspective using `terminateSelfWithResult`. Key technical concepts include the construction of `Want` objects for parameter passing, the management of asynchronous callbacks, the parsing of JSON data from results, and the proper handling of result codes. This guide is best suited for developers who have a foundational understanding of the AbilityKit framework and Cangjie syntax and are looking to implement complex, data-driven navigation flows in their applications.

## Quick Navigation

**What do you want to learn?**

*   Learn the complete workflow for the caller (startAbilityForResult) and callee (terminateSelfWithResult) → 启动应用内的UIAbility并获取返回结果_1.md
*   Focus on constructing Want objects and parsing JSON parameters in the result → 启动应用内的UIAbility并获取返回结果_1.md
*   Understand the Stage model implementation details and result code matching → 启动应用内的UIAbility并获取返回结果_2.md
*   Handle success and error states in result callbacks → 启动应用内的UIAbility并获取返回结果_2.md

## Detailed Description

### 启动应用内的UIAbility并获取返回结果_1.md 启动应用内的UIAbility并获取返回结果_1.md
This technical tutorial guides developers on initiating an in-app UIAbility and retrieving return results using the Cangjie language. The document is divided into two main sections: the caller implementation using startAbilityForResult to launch the target ability and handle asynchronous callbacks, and the callee implementation using terminateSelfWithResult to return data upon termination. Key information includes constructing Want objects, parsing JSON parameters from the result, and managing result codes. This guide targets HarmonyOS application developers familiar with the AbilityKit framework and Cangjie syntax, specifically those implementing workflows like login screens that require data return to the main entry point.

### 启动应用内的UIAbility并获取返回结果_2.md 启动应用内的UIAbility并获取返回结果_2.md
This technical tutorial demonstrates how to start an in-app UIAbility and handle its return result using the Cangjie language in the Stage model. The content covers initiating an ability with startAbilityForResult, passing parameters via Want, and terminating the target ability with terminateSelfWithResult. Key takeaways include constructing a Want with custom JSON parameters, defining a result callback to handle success or error states, and ensuring result codes match between the caller and callee. This guide targets HarmonyOS developers familiar with Cangjie syntax and UIAbility lifecycle management.