# 完整示例

This directory provides comprehensive code examples for implementing video playback using the AVPlayer class within the MediaKit framework. It covers essential functionalities such as streaming media playback (HLS VOD, HLS Live, DASH), lifecycle management, state change handling, and custom HTTP header configuration. Key topics include track selection, surface ID management, seek operations, buffering updates, and error recovery. These examples are designed for developers using ArkTS or Cangjie who need to integrate robust multimedia capabilities into their applications, serving as practical references for both basic playback logic and advanced network configurations. By exploring these files, developers will learn how to manage complex player states, handle rendering events, and ensure smooth media delivery across different streaming protocols.

## Quick Navigation

**What do you want to learn?**

*   **Implement streaming media playback in Cangjie:** Learn how to handle HLS VOD, HLS Live, and DASH streams, including track selection and surface ID management → 完整示例_1.md
*   **Manage player lifecycle in ArkTS:** Understand the state machine logic, custom HTTP headers, and event handling for robust playback control → 完整示例_2.md

## Detailed Description

### 完整示例_1.md
This document provides a complete Cangjie code example demonstrating how to implement streaming media playback using the AVPlayer class. It covers callback registration for state changes and errors, specific implementations for HLS Video on Demand (VOD), HLS Live streams, and DASH video playback with track selection. Key information includes handling surface IDs for display, managing seek capabilities based on stream type, and programmatically selecting specific audio or video tracks. This tutorial targets developers using the MediaKit framework who need to integrate video playback functionality into their applications.

### 完整示例_2.md
This document provides a complete code example demonstrating the implementation and lifecycle management of an AVPlayer in ArkTS. It covers media source configuration with custom HTTP headers, playback strategy settings, and the definition of various event handling callbacks. Key takeaways include the detailed state machine logic within `StateChangeCallback`, which manages transitions from initialization to release, including surface setup, playback control, and error recovery. The example also illustrates handling seek operations, buffering updates, and rendering events. Intended for developers building multimedia applications, this code serves as a practical reference for integrating robust video playback capabilities with custom network configurations.