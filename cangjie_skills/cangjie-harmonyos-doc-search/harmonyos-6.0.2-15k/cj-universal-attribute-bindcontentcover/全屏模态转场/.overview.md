# 全屏模态转场

This directory focuses on full-screen modal transitions within the HarmonyOS ArkUI framework using the Cangjie language. It covers the implementation of overlay interfaces, specifically detailing the `bindContentCover` function and its configuration options. Key topics include transition animations (vertical sliding, fading), modal lifecycle management, and state-driven UI interactions. The documentation is designed for HarmonyOS and Cangjie developers looking to create custom full-screen overlays with dynamic visual effects and precise control over presentation behaviors.

## Quick Navigation

**What do you want to learn?**
*   Understand the core API for binding full-screen modals → 全屏模态转场_2more.md
*   Configure transition modes, callbacks, and styles → 基础类型定义.md
*   View a practical code example with nested modals → 示例代码.md

## Detailed Description

### 全屏模态转场_2more.md 全屏模态转场_2more.md
This document serves as API reference documentation for the `bindContentCover` function in the Cangjie language, detailing how to bind full-screen modal pages to components with transition effects. It covers the function's signature, system capabilities, version requirements, and a detailed parameter table explaining the visibility flag, content builder, and configuration options. Key information includes support for transition animations like vertical sliding and fading, as well as constraints prohibiting screen rotation and routing jumps. Intended for ArkUI developers, this guide facilitates the implementation of custom full-screen overlays with dynamic visual transitions.

### 基础类型定义.md 基础类型定义.md
This API documentation defines the `ContentCoverOptions` class used for configuring full-screen modal page transitions within the ArkUI framework using the Cangjie language. The document details the class structure, which inherits from `BindOptions`, and provides a comprehensive specification of its constructor parameters. Key configuration options include transition modes, interactive dismissal callbacks, custom transition effects, background colors, and lifecycle event handlers for appearance and disappearance. It is intended for HarmonyOS developers requiring precise control over modal presentation behaviors and animations.

### 示例代码.md 示例代码.md
This document provides a practical Cangjie code example demonstrating the implementation of modal transitions using the `bindContentCover` method within a HarmonyOS application. The code illustrates how to manage UI state using `@State` variables and define custom layouts with `@Builder` functions. Key concepts include configuring `ContentCoverOptions` for modal transitions, background colors, and lifecycle callbacks like `onAppear` and `onDisappear`. The example specifically showcases a nested modal scenario where one modal triggers another. This tutorial-style snippet is designed for Cangjie developers seeking to implement overlay interfaces and understand state-driven UI interactions in the HarmonyOS ecosystem.