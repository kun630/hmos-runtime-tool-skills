func sample() {
        // 证书链二进制数据，需业务自行赋值。
        let encodingBlob = EncodingBlob(
            certChainData.toArray(),
            // 根据encodingData的格式进行赋值，支持FORMAT_PEM、FORMAT_DER和FORMAT_PKCS7。
            EncodingFormat.FORMAT_PEM
        )
        var x509CertChain: X509CertChain
        try {
            x509CertChain = createX509CertChain(encodingBlob)
        } catch (e: BusinessException) {
            AppLog.error('createX509CertChain failed, errCode: ${e.code}, errMsg: ${e.message}')
            return
        }
        try {
            let certList = x509CertChain.getCertList()
        } catch (e: BusinessException) {
            AppLog.error('X509CertChain getCertList failed, errCode: ${e.code}, errMsg: ${e.message}')
        }

        // 证书链校验数据，需业务自行赋值。
        var anchor = X509TrustAnchor()
        let param = CertChainValidationParameters(trustAnchors: [anchor, anchor])
        try {
            let validationRes = x509CertChain.validate(param)
            AppLog.info('X509CertChain validate success')
        } catch (e: BusinessException) {
            AppLog.error('X509CertChain validate failed')
        }
    }
    ```