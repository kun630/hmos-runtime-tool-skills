# 函数

This directory contains API documentation for file system utility functions in the Cangjie programming language. It covers essential operations such as path normalization, file copying, existence checking, removal, and renaming. The documents detail function signatures, parameters, return types, and exception handling for both `Path` and `String` inputs. Key topics include recursive deletion, overwrite controls, and symbolic link resolution. This resource is suitable for Cangjie developers who need precise technical references to implement robust file management logic and handle file system errors effectively.

## Quick Navigation

**What do you want to do?**

*   Resolve relative paths or copy files with type matching → func_canonicalizePath_3more.md
*   Perform file replication, check existence, or delete content → func_copyString_String_Boo_5more.md
*   Conditionally remove items or rename with overwrite options → func_removeIfExistsPath_Bo_3more.md
*   Learn specific safety implications of the rename function → func_renameString_String_Bool.md

## Detailed Description

### func_canonicalizePath_3more.md
This API documentation file details file system utility functions in the Cangjie programming language, specifically focusing on path normalization and file copying operations. The document is divided into three main sections describing the `canonicalize(Path)`, `canonicalize(String)`, and `copy(Path, Path, Bool)` functions. Key information includes that `canonicalize` resolves relative paths and symbolic links to absolute paths, while `copy` handles file and directory replication with overwrite controls and type matching requirements. Both functions define specific parameters, return types, and potential exceptions like `FSException` and `IllegalArgumentException`. This reference guide targets Cangjie developers implementing file system logic who need to handle path resolution and file manipulation safely.

### func_copyString_String_Boo_5more.md
This document serves as API reference documentation for file system operations in the Cangjie programming language. It details the `copy` function for replicating files or directories, `exists` functions for verifying path presence, and `remove` functions for deleting content. Key features include support for overwriting existing files, recursive deletion, and type consistency checks. The functions handle exceptions such as `FSException` and `IllegalArgumentException` for invalid paths or operations. Overloaded versions accept both `String` and `Path` types. This guide targets developers implementing file management logic who need specific syntax and error handling details. Keywords: Cangjie, file system, API reference, copy, remove, exists, exception handling.

### func_removeIfExistsPath_Bo_3more.md
This document serves as API reference documentation for file system manipulation functions in the Cangjie language. It details three specific functions: two overloaded versions of removeIfExists (accepting Path or String) and the rename function. The removeIfExists functions check for a target's existence and delete it if found, returning a boolean status, with an optional recursive parameter for directories. The rename function moves or renames files and directories, featuring an overwrite flag that triggers recursive deletion of existing targets. Key takeaways include the handling of FSException and IllegalArgumentException for errors like invalid paths or failed operations. This reference is intended for developers implementing file management logic who need precise parameter definitions and exception handling details.

### func_renameString_String_Bool.md
This document provides API reference documentation for the rename function in the Cangjie programming language, detailing how to move or rename files and directories. The content covers the function signature, functionality description, parameter definitions, and potential exceptions. Key information includes the handling of the overwrite parameter, which defaults to false; when set to true, the function recursively deletes the target directory before renaming, a behavior that requires caution. The document also lists exceptions such as FSException and IllegalArgumentException. This reference is intended for Cangjie developers performing file system operations who need to understand the specific behavior and safety implications of file renaming.