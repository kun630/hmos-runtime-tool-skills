# class_InstancePropertyInfo

This directory contains API reference documentation for the `InstancePropertyInfo` class within the Cangjie programming language's reflection package. It provides comprehensive details on metadata for instance member properties, covering class definitions, inheritance hierarchies, and core properties like annotations and modifiers. The content also explains methods for runtime manipulation, including retrieving and setting property values, as well as introspection capabilities. This documentation is suitable for Cangjie developers who need to programmatically inspect program structure and dynamically interact with object properties using reflection.

## Quick Navigation

**What do you want to learn?**

*   Understand the class definition, inheritance, and core properties (annotations, modifiers, name) → class_InstancePropertyInfo_6more.md
*   Retrieve property values or check introspection flags (isAbstract, isMutable) → func_getValueAny_5more.md
*   Modify property values or compare property info objects → func_setValueAny_Any_4more.md

## Detailed Description

### class_InstancePropertyInfo_6more.md class_InstancePropertyInfo_6more.md
This API reference document details the `InstancePropertyInfo` class, which provides metadata for instance member properties in the Cangjie programming language. The document outlines the class definition, its inheritance from standard interfaces like `Equatable` and `Hashable`, and its core properties. Key sections include annotations for retrieving applied notes, modifiers for property access details, name for identification, and `typeInfo` for type data. It also describes the `findAnnotation` function for searching specific annotations. Important notes indicate that the modifiers collection includes semantic modifiers even if not explicitly declared. This documentation is intended for developers utilizing reflection to inspect and manipulate program structure programmatically.

### func_getValueAny_5more.md func_getValueAny_5more.md
This API reference document details specific methods of the `InstancePropertyInfo` class within the Cangjie language's reflection package. It covers the `getValue` function for retrieving property values from instances, alongside utility methods like `hashCode`, `isAbstract`, `isMutable`, and `isOpen` for introspection. Key information includes the requirement for strict type matching in `getValue`, which throws `IllegalTypeException` on failure, and specific checks for property modifiers like `mut` and `open`. The document includes a code example demonstrating dynamic property access. This content is intended for Cangjie developers utilizing reflection to inspect and manipulate object properties at runtime.

### func_setValueAny_Any_4more.md func_setValueAny_Any_4more.md
This document serves as API reference documentation for methods within the `InstancePropertyInfo` class in the Cangjie reflection package. It details the `setValue` function for modifying instance properties, the `toString` method for string representation, and equality operators (`==` and `!=`) for comparing property info objects. Key takeaways include the specific exceptions thrown by `setValue`, such as `IllegalSetException` for immutable properties and `IllegalTypeException` for type mismatches, ensuring strict type safety during runtime reflection. The target audience is developers working with runtime reflection in Cangjie who need to dynamically inspect and modify object properties.