# 流媒体播放场景下设置URL

This directory provides comprehensive technical documentation for configuring media URLs within the Cangjie development environment, specifically focusing on the AVPlayer component. It covers a wide range of streaming scenarios essential for modern application development, including the playback of standard HTTP/HTTPS resources, the handling of HLS (Video on Demand and Live) streams, and the management of local file descriptors. Key technical concepts explored include the use of `createMediaSourceWithUrl` for advanced configurations, the implementation of custom HTTP request headers for secure authentication, and the configuration of `PlaybackStrategy` to control buffering behavior. These resources are specifically designed for Cangjie developers who are implementing robust media playback features and require efficient methods to handle diverse network protocols and resource access mechanisms.

## Quick Navigation

**What do you want to do?**

*   Play standard HTTP/HTTPS resources or handle HLS (VOD/Live) streams → 流媒体播放场景下设置URL_1.md
*   Set custom HTTP request headers for authentication and control buffering → 流媒体播放场景下设置URL_1.md
*   Utilize local RAW file descriptors to trigger online stream playback → 流媒体播放场景下设置URL_1.md
*   Parse a local m3u8 file within the application sandbox to play online media → 流媒体播放场景下设置URL_2.md

## Detailed Description

The following files provide in-depth technical instructions and code examples for implementing specific media playback scenarios in Cangjie.

#### 流媒体播放场景下设置URL_1.md 流媒体播放场景下设置URL_1.md
This technical guide demonstrates how to configure media URLs for the AVPlayer component in Cangjie across various streaming scenarios. It details four specific use cases: playing standard HTTP/HTTPS resources, handling HLS (VOD/Live) streams, setting custom HTTP request headers for authentication, and utilizing local RAW file descriptors to trigger online stream playback. Key takeaways include using createMediaSourceWithUrl for advanced configurations like headers and PlaybackStrategy for buffering control, as well as constructing fd:// URLs using resourceManager.getRawFd. This guide targets Cangjie developers implementing media playback features who need to handle different network protocols and resource access methods.

#### 流媒体播放场景下设置URL_2.md 流媒体播放场景下设置URL_2.md
This technical tutorial explains how to play online streaming media by parsing a local m3u8 file located within the application sandbox. The document details the process of obtaining a file descriptor using fs.open, constructing a specific fdUrl string, and setting HTTP request headers. Key takeaways include the code for creating a MediaSource with custom headers and configuring a PlaybackStrategy to manage buffer duration. Intended for Cangjie developers, this guide requires knowledge of the MediaKit and CoreFileKit APIs to implement advanced media playback scenarios involving local playlist files.