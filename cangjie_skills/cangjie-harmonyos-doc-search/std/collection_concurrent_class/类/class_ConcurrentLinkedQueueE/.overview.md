# class_ConcurrentLinkedQueueE

This directory provides comprehensive API reference documentation for the Cangjie `ConcurrentLinkedQueue` class, a thread-safe, non-blocking queue specifically designed for multi-threaded environments. It covers the complete class definition, constructors, the `size` property, and key methods for element manipulation, including `add`, `dequeue`, `enqueue`, and `head`. The documentation emphasizes the queue's role in preventing data conflicts and deadlocks, while also noting the non-atomic nature of the `size` property and the deprecation of specific methods in favor of newer alternatives like `remove` and `peek`. This resource is essential for Cangjie developers implementing concurrent systems—such as producer-consumer models or task scheduling—who need precise technical details to manage shared data structures effectively and safely.

## Quick Navigation

**What do you want to learn?**

*   Understand the core class definition, constructors, and the non-atomic size property? → class_ConcurrentLinkedQueu_8more.md
*   Learn about element manipulation methods like add, enqueue, and deprecated methods? → class_ConcurrentLinkedQueu_8more.md
*   Explore essential queue operations such as isEmpty, iterator, and toArray? → func_isEmpty_5more.md
*   Understand the Option type usage in peek and remove for handling empty states? → func_isEmpty_5more.md
*   Review specific warnings regarding thread-safety and atomicity during modifications? → func_isEmpty_5more.md

## Detailed Description

### class_ConcurrentLinkedQueu_8more.md class_ConcurrentLinkedQueu_8more.md
This document provides API reference documentation for the Cangjie ConcurrentLinkedQueue class, a thread-safe, non-blocking queue designed for multi-threaded environments. It details the class definition, constructors, the size property, and key methods for element manipulation including add, dequeue, enqueue, and head. Key information includes the queue's purpose in preventing data conflicts and deadlocks, the non-atomic nature of the size property, and the deprecation of several methods like dequeue and head in favor of remove and peek. This guide targets Cangjie developers implementing concurrent systems such as producer-consumer models or task scheduling.

### func_isEmpty_5more.md func_isEmpty_5more.md
This document provides API reference documentation for essential queue operations in the Cangjie programming language. It details five key functions: isEmpty, iterator, peek, remove, and toArray, explaining their syntax, functionality, and return types. Key information includes the use of the Option type for handling empty states in peek and remove, and specific warnings that iterator and toArray operations are non-atomic and not thread-safe during concurrent modifications. This reference is intended for Cangjie developers who need precise behavioral details and return value specifications for managing queue data structures.