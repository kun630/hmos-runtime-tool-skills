## struct NativeOptionFloat64

```cangjie
public struct NativeOptionFloat64 {
    public NativeOptionFloat64(
        public let hasValue: Bool,
        public let value: Float64
    )
}
```

**功能：** 框架内使用结构体。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

### let hasValue

```cangjie
public let hasValue: Bool
```

**功能：** UI框架使用。

**类型：** Bool

**读写能力：** 只读

**起始版本：** 12

### let value

```cangjie
public let value: Float64
```

**功能：** UI框架使用。

**类型：** Float64

**读写能力：** 只读

**起始版本：** 12

### NativeOptionFloat64(Bool, Float64)

```cangjie
public NativeOptionFloat64(
    public let hasValue: Bool,
    public let value: Float64
)
```

**功能：** 创建NativeOptionFloat64类型对象。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|hasValue|Bool|是|-|标记是否为空值。|
|value|Float64|是|-|值数据。|

## struct NativeOptionInt32

```cangjie
public struct NativeOptionInt32 {
    public NativeOptionInt32(
        public let hasValue: Bool,
        public let value: Int32
    )
}
```

**功能：** 框架内使用结构体。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

### let hasValue

```cangjie
public let hasValue: Bool
```

**功能：** UI框架使用。

**类型：** Bool

**读写能力：** 只读

**起始版本：** 12

### let value

```cangjie
public let value: Int32
```

**功能：** UI框架使用。

**类型：** Int32

**读写能力：** 只读

**起始版本：** 12

### NativeOptionInt32(Bool, Int32)

```cangjie
public NativeOptionInt32(
    public let hasValue: Bool,
    public let value: Int32
)
```

**功能：** 创建NativeOptionInt32类型对象。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|hasValue|Bool|是|-|标记是否为空值。|
|value|Int32|是|-|值数据。|

## struct NativeOptionInt64

```cangjie
public struct NativeOptionInt64 {
    public NativeOptionInt64(
        public let hasValue: Bool,
        public let value: Int64
    )
}
```

**功能：** 框架内使用结构体。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

### let hasValue

```cangjie
public let hasValue: Bool
```

**功能：** UI框架使用。

**类型：** Bool

**读写能力：** 只读

**起始版本：** 12

### let value

```cangjie
public let value: Int64
```

**功能：** UI框架使用。

**类型：** Int64

**读写能力：** 只读

**起始版本：** 12

### NativeOptionInt64(Bool, Int64)

```cangjie
public NativeOptionInt64(
    public let hasValue: Bool,
    public let value: Int64
)
```

**功能：** 创建NativeOptionInt64类型对象。

**系统能力：** SystemCapability.ArkUI.ArkUI.Full

**起始版本：** 12

**参数：**

|参数名|类型|必填|默认值|说明|
|:---|:---|:---|:---|:---|
|hasValue|Bool|是|-|标记是否为空值。|
|value|Int64|是|-|值数据。|