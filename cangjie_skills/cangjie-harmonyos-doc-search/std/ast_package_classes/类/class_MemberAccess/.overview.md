# class_MemberAccess

This directory provides comprehensive documentation for the `MemberAccess` class within the Cangjie language's Abstract Syntax Tree (AST). It focuses on modeling member access expressions, such as `T.a`, detailing the class architecture, inheritance from the `Expr` type, and specific mutable properties like `baseExpr`, `field`, `dot`, and `typeArguments`. Additionally, it covers essential utility functions for converting AST nodes to tokens and traversing the tree structure using the Visitor pattern. This resource is designed for developers and engineers involved in building compiler tools, static analyzers, or any program requiring syntactic analysis and manipulation of Cangjie code. It provides the necessary API references to programmatically handle expression parsing and tree walking.

## Quick Navigation

What do you want to learn?

*   Understand the core structure, properties, and initialization of the `MemberAccess` class → class_MemberAccess_10more.md
*   Learn how to convert AST nodes to tokens or traverse the tree structure with a Visitor → func_toTokens_2more.md

## Detailed Description

### class_MemberAccess_10more.md
This document serves as API reference for the MemberAccess class, which models member access expressions like T.a in the Cangjie language's Abstract Syntax Tree. It details the class structure, its inheritance from the Expr parent type, and specific mutable properties such as baseExpr, field, dot, and typeArguments. The documentation explains constructors for creating nodes, including one that parses Tokens, and highlights exception handling for invalid token types. This guide is intended for developers building compiler tools or static analyzers who need to manipulate AST nodes programmatically.

### func_toTokens_2more.md
This document serves as API reference documentation for two specific functions within an Abstract Syntax Tree (AST) package, detailing how to convert nodes to tokens and traverse the tree structure. It covers the `toTokens()` function for type conversion and the `traverse(Visitor)` function for node iteration. Both sections include function signatures, descriptions, parameters, and return values. The `toTokens()` function transforms a syntax tree node into a `Tokens` object. The `traverse()` function accepts a `Visitor` instance to walk through nodes and their children, allowing for early termination of the traversal process by overriding the `visit` method. Targeted at developers working with AST manipulation in the Cangjie language who need to analyze or transform syntax tree structures programmatically.