### SYNC_TYPE(UInt32)

```cangjie
SYNC_TYPE(UInt32)
```

**功能：** 关键资产支持的同步类型。

**系统能力：** SystemCapability.Security.Asset

**起始版本：** 19

### UPDATE_TIME(Array\<UInt8>)

```cangjie
UPDATE_TIME(Array<UInt8>)
```

**功能：** 数据的更新时间（时间戳形式）。

**系统能力：** SystemCapability.Security.Asset

**起始版本：** 19

### func getTag()

```cangjie
public func getTag(): UInt32
```

**功能：** 获取关键资产支持的属性名称类型对应的AssetTag值。

**系统能力：** SystemCapability.Security.Asset

**起始版本：** 19

**返回值：**

|类型|说明|
|:----|:----|
|UInt32|返回关键资产支持的属性名称类型对应的AssetTag值。|