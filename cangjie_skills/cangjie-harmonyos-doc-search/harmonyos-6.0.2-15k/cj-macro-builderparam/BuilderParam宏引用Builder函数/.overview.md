# BuilderParam宏引用Builder函数

This directory provides a comprehensive technical resource on the `@BuilderParam` macro within the ArkUI framework and Cangjie language. It focuses on how this macro references `@Builder` functions to dynamically inject UI functionality into custom component instances. The content covers core concepts such as slot placeholders, type matching, and initialization methods, while also addressing strict usage limitations and common compilation errors. This documentation is suitable for ArkUI developers and Cangjie programmers who possess prior knowledge of `@Builder` and are seeking to implement flexible, dynamic UI designs and component communication.

## Quick Navigation

**What do you want to learn?**

*   Understand the technical definition, syntax, and strict constraints of `@BuilderParam` → BuilderParam宏引用Builder函数_3more.md
*   Explore practical usage scenarios for parameter initialization and UI logic passing → 使用场景.md
*   Troubleshoot compilation errors related to `@State` variable initialization → 常见问题.md

## Detailed Description

### BuilderParam宏引用Builder函数_3more.md BuilderParam宏引用Builder函数_3more.md
This technical guide explains the `@BuilderParam` macro in ArkUI, which allows developers to dynamically inject specific UI functionality into custom component instances by referencing `@Builder` functions. The document covers initialization methods using global `@Builder` functions and outlines strict usage limitations, including type requirements, visibility restrictions, and variable declaration rules. Key takeaways include that `@BuilderParam` acts as a slot placeholder and must be initialized by a `@Builder` function returning Unit. Intended for developers with prior knowledge of `@Builder`, this guide provides the syntax and constraints for implementing flexible component designs in the Cangjie language.

### 使用场景.md 使用场景.md
This document serves as a practical guide for using the `@BuilderParam` decorator in Cangjie to initialize custom components. It details two primary usage scenarios: parameter initialization, emphasizing type matching between the parameter and the target `@Builder` method, and initialization via global or parent component `@Builder` methods. Key takeaways include the ability to pass UI logic from parent to child components and the distinction between local and global variable scopes within these builders. This tutorial is designed for ArkUI developers seeking to implement dynamic UI composition and component communication.

### 常见问题.md 常见问题.md
This FAQ document addresses a specific compilation constraint regarding the `@BuilderParam` macro in Cangjie development. It explains that variables decorated with `@BuilderParam` cannot be initialized using `@State` variables, as this causes compilation errors. The content contrasts a counter-example showing the error with a correct example demonstrating the proper initialization using global `@Builder` functions. Key takeaways include understanding the strict type requirements for macro initialization and avoiding state variable misuse in component parameters. Intended for Cangjie developers building HarmonyOS UIs, this guide helps resolve common build failures related to component parameter passing and macro usage.