本目录是仓颉（Cangjie）语言 HTTP 包中核心枚举类型的 API 参考文档。它详细定义了网络编程中处理文件传输、HTTP 协议协商以及 WebSocket 通信所需的基础数据结构。文档重点涵盖了 `FileHandlerType`（用于配置文件上传或下载模式）、`Protocol`（定义 HTTP 1.0/1.1/2.0 版本）以及 `WebSocketFrameType`（描述文本、二进制及控制帧）三个核心枚举。这些枚举均继承了 `Equatable` 和 `ToString` 接口，并实现...