# extendT_BoxT_ComparableBoxT_where_T_ComparableT

This directory serves as a comprehensive technical resource for the Cangjie language, detailing the specific implementation of comparison features for the generic `Box` class. It addresses the extension of `Box` to conform to the `Comparable` interface, a critical feature for enabling ordered comparisons on generic containers. The content explains the constraints requiring the inner type `T` to be comparable and how the logic is delegated to the encapsulated value. Furthermore, it documents the specific operator overloads—such as equality, greater than, and less than—that allow developers to treat `Box` instances like primitive types during comparisons. This documentation is vital for Cangjie developers, library maintainers, and software engineers who are building generic systems or working with the standard library's type system, ensuring they understand how to implement and utilize comparison logic effectively. The files provided offer both high-level implementation details and specific API references for the operator functions involved.

## Quick Navigation

**What do you want to learn?**

*   **Understand the Comparable Interface Extension:** If you want to learn how the `Box` class implements the `Comparable` interface, including the core `compare` function, the `Ordering` enum, and relational operators like not equal (!=), less than (<), and less than or equal to (<=), refer to this file. → extendT_BoxT_ComparableBox_5more.md
*   **Review Operator Overloads:** If you need specific API reference details for comparison operator overloads within the `Box` class, specifically looking at equality (==), greater than (>), and greater than or equal (>=) functions, consult this document. → operator_func_BoxT_3more.md

## Detailed Description

### extendT_BoxT_ComparableBox_5more.md extendT_BoxT_ComparableBox_5more.md
This API documentation details a Cangjie language extension that implements the Comparable interface for the generic Box class, enabling size comparisons. It covers the extension declaration, the core compare function returning an Ordering enum, and relational operators including not equal, less than, and less than or equal to. The key takeaway is that the comparison logic delegates to the encapsulated value T, requiring T to also implement Comparable. A code example demonstrates usage with a custom struct. This reference is intended for Cangjie developers working with generic types and custom comparison logic.

### operator_func_BoxT_3more.md operator_func_BoxT_3more.md
This document serves as API reference documentation for comparison operator overloads within the Box class in the Cangjie language. It details three specific operator functions: equality (==), greater than (>), and greater than or equal (>=). Each function accepts a Box object as a parameter and returns a Bool indicating the result of the comparison. The documentation specifies the functionality, parameters, and return values for each operator. This is intended for developers working with the Cangjie standard library who need to compare Box instances.