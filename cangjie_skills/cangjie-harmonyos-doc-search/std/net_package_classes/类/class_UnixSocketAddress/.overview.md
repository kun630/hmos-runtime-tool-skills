# class_UnixSocketAddress

This directory contains API reference documentation for the `UnixSocketAddress` class within the Cangjie standard library. It covers the implementation of Unix Domain Socket addressing, including class properties, constructors for handling file system paths, and methods for address manipulation and comparison. Key topics include socket address encapsulation, byte-level data structures, and operator overloading for equality checks. This documentation is suitable for Cangjie developers working on network communication who need precise control over socket addressing, path validation, and data conversion.

## Quick Navigation

- **What do you want to learn?**
    - Understand the class structure, constructors, and properties → class_UnixSocketAddress_7more.md
    - Learn how to convert the address to a readable string → func_toString.md
    - Understand how to compare two socket addresses → operator_func_UnixSocketAd_2more.md

## Detailed Description

### class_UnixSocketAddress_7more.md class_UnixSocketAddress_7more.md
This document provides API reference documentation for the UnixSocketAddress class in the Cangjie standard library, designed to encapsulate file system paths for Unix Domain Socket communication. It covers class inheritance, properties like family and size, and constructors accepting String or Array<Byte>. Key information includes support for standard, unnamed, and abstract addresses with a maximum path length of 108 bytes. The class validates inputs, throwing IllegalArgumentException for errors, and provides methods to retrieve raw byte representations compatible with sockaddr_un. This reference is intended for Cangjie developers implementing network communication who require precise control over socket addressing and byte-level data structures.

### func_toString.md func_toString.md
This document provides API reference details for the toString() method belonging to the UnixSocketAddress class in the Cangjie standard library. It covers the function signature, a description of its functionality, the return value type, and a comprehensive code example. The method returns a String representing the socket path, and the example demonstrates handling various input formats, including byte arrays and strings with null terminators, showing how they map to specific string outputs. This is intended for Cangjie developers implementing or debugging Unix domain socket connections who need to convert address objects to readable strings.

### operator_func_UnixSocketAd_2more.md operator_func_UnixSocketAd_2more.md
This document provides API reference details for the equality and inequality operator functions of the UnixSocketAddress class within the Cangjie net package. It specifically covers the `!=` and `==` operator functions, defining their syntax, parameters, and return values. Both functions accept a `rhs` parameter of type UnixSocketAddress and return a `Bool` indicating the comparison result. This documentation is intended for developers implementing network communication in Cangjie who need to validate or compare Unix socket addresses. Key concepts include operator overloading, boolean logic, and network address comparison.