# extend_String_UnicodeStringExtension

This directory provides comprehensive API reference documentation for the `UnicodeStringExtension` interface within the Cangjie standard library. It details the extension of the `String` type to facilitate advanced Unicode character set operations, such as case conversion (lower, upper, title), whitespace detection, and string trimming. Key technical elements include the `CasingOption` enum for locale-specific transformations, handling of `IllegalArgumentException` for invalid UTF-8 encoding, and the management of deprecated methods. This resource is essential for Cangjie developers seeking to implement precise text manipulation, validation logic, and input sanitization while adhering to Unicode standards.

## Quick Navigation

**What do you want to do?**

*   Understand the core extension interface and basic case conversion functions → extend_String_UnicodeStrin_5more.md
*   Perform specific casing transformations (Title/Upper) or end trimming operations → func_toTitleCasingOption_5more.md
*   Handle whitespace trimming using legacy methods versus recommended replacements → func_trimLeft_supdeprecate_3more.md

## Detailed Description

### extend_String_UnicodeStrin_5more.md extend_String_UnicodeStrin_5more.md
This API reference document details the extension of the Cangjie String type with the `UnicodeStringExtension` interface to enable advanced character set operations. It covers specific member functions including `isBlank` for whitespace detection, `toLower` for case conversion (with and without `CasingOption`), and `toTitle` for title casing. Each function entry provides syntax, descriptions, return types, and potential exceptions like `IllegalArgumentException` for invalid UTF-8 encoding, alongside verified code examples. This technical guide targets Cangjie developers seeking to implement precise Unicode string manipulation and validation logic.

### func_toTitleCasingOption_5more.md func_toTitleCasingOption_5more.md
This document serves as API reference documentation for string manipulation functions within the Cangjie standard library's unicode package. It details five specific functions: `toTitle`, `toUpper` (with and without options), `trim`, and `trimEnd`. Key takeaways include the use of the `CasingOption` enum for locale-specific casing transformations and the definition of whitespace via `Rune.isWhiteSpace`. All functions return a new String and throw `IllegalArgumentException` for invalid UTF-8 encoding. The target audience is Cangjie developers seeking to perform text casing transformations and whitespace trimming, requiring knowledge of basic string handling and Unicode concepts.

### func_trimLeft_supdeprecate_3more.md func_trimLeft_supdeprecate_3more.md
This document serves as API reference documentation for string manipulation functions within the Cangjie standard library's unicode package. It details three specific functions: the deprecated `trimLeft` and `trimRight`, and the recommended `trimStart`. Key information includes the deprecation status of older methods, their designated replacements (`trimStart`, `trimEnd`), and the functional behavior of removing leading or trailing whitespace as defined by the `isWhiteSpace` extension. All functions return a modified String and throw `IllegalArgumentException` if invalid UTF-8 encoding is detected. The target audience is Cangjie developers seeking to sanitize string inputs, requiring an understanding of UTF-8 encoding and exception handling.