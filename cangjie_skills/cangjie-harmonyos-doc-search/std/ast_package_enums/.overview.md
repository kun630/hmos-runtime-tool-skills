# ast_package_enums

The `ast_package_enums` directory functions as a pivotal component within the Cangjie programming language's compiler architecture, specifically dedicated to housing enumeration definitions. This directory acts as a foundational module that underpins the Abstract Syntax Tree (AST) implementation by providing standardized type definitions essential for the system's operation. It encompasses a wide array of critical data structures, including error reporting severities, various import statement configurations, and the comprehensive list of lexical tokens required by the parser. By centralizing these enumerations, the directory ensures type safety and consistency across the compiler's codebase, which is vital for robust parsing and accurate error handling. This resource is indispensable for compiler developers, language engineers, and contributors who are actively involved in syntax analysis, tooling development, or the maintenance of the compiler's internal logic. It serves as the definitive reference for understanding the grammar and structural constraints of the Cangjie language, ensuring that all components of the compiler speak the same language regarding data types. It is a key resource for anyone looking to modify or extend the compiler's capabilities.

## Quick Navigation

- **What do you want to learn?**
    - Explore the centralized API reference for enumeration types used in the compiler infrastructure → `枚举/`
    - Understand the specific data structures defining error reporting severities and import statement variations → `枚举/`
    - Review the exhaustive set of lexical tokens, including keywords, operators, and punctuation → `枚举/`
- **What do you want to do?**
    - Ensure type safety and standardization in your compiler contributions and codebase → `枚举/`
    - Facilitate robust parsing and accurate error reporting in the AST implementation → `枚举/`
    - Gain foundational knowledge of the language's grammar and internal logic for tooling → `枚举/`

## Detailed Description

### 枚举/
This directory serves as a centralized API reference for all enumeration types utilized within the Cangjie programming language's compiler infrastructure. It comprehensively documents critical data structures that define error reporting severities, import statement variations, and the exhaustive set of lexical tokens recognized by the parser. By covering components ranging from diagnostic levels to specific syntax tokens like keywords, operators, and punctuation, this collection provides the foundational type definitions necessary for understanding the language's grammar and internal logic. It is primarily suitable for compiler developers, language engineers, and contributors working on syntax analysis, tooling, or the Abstract Syntax Tree (AST) implementation. These enumerations ensure type safety and standardization across the compiler's codebase, facilitating robust parsing, accurate error reporting, and consistent handling of language constructs. By defining these core types, the directory allows for a more maintainable and error-resistant codebase, where the specific states and categories of language elements are strictly defined and enforced.